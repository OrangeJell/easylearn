import{follow,md}from'../practice-answers/shared.mjs'

const f=follow

export default{
  'elasticsearch/inverted-index':[
    f('商品标题支持分词搜索后，订单号这种字段也能用同样的 text Mapping 吗？',md(
      '不能。订单号、SKU、品牌 ID 需要完整精确匹配和聚合，应使用 `keyword`；标题用 `text` 建倒排词项，必要时再配一个 keyword 子字段做精确排序或聚合。',
      '',
      '我会从查询语义反推 Mapping。把所有字符串都分词，会让订单号被切开、精确过滤变慢甚至结果错误。'
    ),{followUps:[
      f('一个字段既要中文搜索又要拼音搜索，你会把所有分词结果塞进同一字段吗？',md(
        '我更倾向多字段：原始中文、拼音或首字母分别建子字段，查询时按召回质量设置不同权重。全塞一起难以控制相关性和误召回，也不利于独立调试 analyzer。'
      ))
    ]}),
    f('产品要求支持任意位置模糊匹配，直接 wildcard *关键词* 有什么生产风险？',md(
      '前导通配符难以利用词项前缀，候选词很多时会消耗大量 CPU 和内存，热点查询能拖慢整个分片。先确认用户真正需要的是分词、前缀、纠错还是子串。',
      '',
      '前缀可用 edge n-gram，有限子串可设计专用 n-gram 字段并控制长度；任意表达式要限频、限长度，不能直接开放给公网。'
    ))
  ],
  'elasticsearch/mapping-query':[
    f('中文分词器升级后词典变化，你会直接在原索引上改 analyzer 吗？',md(
      '已有文档的倒排词项不会自动重建，搜索分析和索引分析不一致会产生漏召回。我会创建带新 Mapping 的版本索引，从数据库或旧索引重建，双写或追平增量，验证召回和延迟后切 alias。',
      '',
      '保留旧 alias 以便回滚。分析器升级本质是数据迁移，不是热改一个配置。'
    ),{followUps:[
      f('重建期间商品仍在更新，怎样避免切换时丢增量？',md(
        '记录全量开始的 CDC 位点，重建历史的同时消费之后的变更；全量结束后追平到切换水位，校验数量和抽样内容再原子切 alias。仅靠应用双写而没有失败补偿，仍可能漏一边。'
      ))
    ]}),
    f('价格字段既要区间筛选又要排序，你会用 double 还是整数分？',md(
      '金额我会用最小货币单位的 `long`，或者明确缩放的 `scaled_float`，避免二进制浮点比较和聚合误差。Mapping 上支持 range 和 doc_values 排序即可。',
      '',
      '还要统一币种和税前税后语义。搜索排序正确不只取决于字段类型，也取决于写入数据是否同一口径。'
    )),
    f('上游不断增加动态属性，ES field 数量快速上涨，你怎么阻止 Mapping 爆炸？',md(
      '不会让任意 JSON key 自动变成独立字段。已知可检索属性走受控模板和字段白名单；长尾属性可用 `flattened`、键值数组或单独属性索引，按查询需求选择。',
      '',
      '限制 `total_fields` 只是最后防线，还要监控新字段速率并在接入时拒绝非法属性，否则集群状态和堆都会持续膨胀。'
    ))
  ],
  'elasticsearch/performance-troubleshooting':[
    f('P99 升高时发现一个用户提交了超大聚合查询，你会怎样止损？',md(
      '先通过 slow log、task API 和 trace 定位查询与来源，取消确认安全的失控任务，并对入口限制聚合桶数、时间范围和并发。必要时暂停非核心报表，保护搜索流量。',
      '',
      '随后改成异步报表或预聚合。仅扩节点会让同样无界查询继续消耗更多资源。'
    ),{followUps:[
      f('取消请求后节点 CPU 仍然很高，说明取消没有生效吗？',md(
        '不一定，某些阶段需要到检查点才响应取消，后台 merge、GC 或其他查询也可能仍在运行。我要看 task 是否消失、热线程和节点指标，不能只凭客户端收到取消响应判断资源已释放。'
      ))
    ]}),
    f('只有一个数据节点延迟高、GC 也高，你会先重启它吗？',md(
      '先看该节点是否承载热点分片、磁盘水位、segment 数和 heap 占用来源。贸然重启会触发分片恢复，把 IO 压力扩散到集群；如果节点已影响服务，先限制分片分配或摘除查询流量并保留现场。',
      '',
      '根因可能是数据倾斜、大聚合、fielddata 或硬件故障，重启只会暂时清空症状。'
    )),
    f('查询命中率和文档数没变，但磁盘读取延迟突然升高，你怎么验证存储问题？',md(
      '对齐节点级 iostat、磁盘队列、吞吐和 ES 查询 read 指标，看是否与 merge、snapshot 或分片恢复同时发生；比较同类分片在其他节点的耗时。',
      '',
      '若是云盘突发额度耗尽，要从存储指标确认，而不是反复改 query DSL。止损可以迁移热点分片、降低后台 IO 或扩容 IOPS。'
    ))
  ],
  'elasticsearch/shard-replica':[
    f('十亿文档应该分多少主分片，你会先做什么样的基准测试？',md(
      '取真实文档大小、Mapping 和查询组合，向单分片持续写到不同体积，测写入、merge、恢复时间和查询 P99。分片目标不是固定 50GB，而是在硬件和 SLA 下可恢复、可查询的范围。',
      '',
      '再按总数据量、增长和节点数估算分片数，并给 rollover 留余量。拍脑袋按文档数除一个常量不可靠。'
    ),{followUps:[
      f('测试得出单分片能放 80GB，为什么生产不一定把它吃满？',md(
        '生产还有数据倾斜、峰值写入、磁盘水位和节点故障恢复。一个节点掉线时需要在目标 RTO 内复制大分片，集群还要有接收空间。会在基准上留故障与增长余量，不按实验极限规划。'
      ))
    ]}),
    f('增加副本数后查询吞吐为什么可能没有提升？',md(
      '副本增加可用的分片副本，但如果协调节点、CPU、磁盘或热点路由仍是瓶颈，吞吐不会线性增长；副本还增加写入和 merge 成本。',
      '',
      '我会看查询是否均匀分发、节点资源和写入延迟，在目标读写比例下压测。副本首先服务可用性，其次才是读扩展。'
    ))
  ],
  'elasticsearch/cluster-election':[
    f('三个 master eligible 节点和数据节点混部，数据查询高峰时频繁选主，你会怎么改？',md(
      '选主频繁通常说明 master 心跳和集群状态处理被资源竞争拖慢。我会检查 GC、CPU、网络和长时间 cluster state 发布，核心集群倾向用三个资源稳定的专用 master eligible 节点。',
      '',
      '先降低失控查询和分片变更，避免继续抖动；仅调大选举超时会让真实故障发现更慢，不能替代资源隔离。'
    ),{followUps:[
      f('专用 master 节点规格是不是可以很小，因为不存数据？',md(
        '它仍要在堆中维护并发布集群状态，索引、分片和 Mapping 很多时内存与 CPU 都重要。规格要按集群状态大小和变更频率验证，并留故障余量，不能按“无数据”理解成无负载。'
      ))
    ]}),
    f('集群状态发布越来越慢，你会从哪些对象数量入手？',md(
      '看索引、分片、字段 Mapping、模板和 alias 数量，以及是否出现动态字段爆炸。大量小索引和过多分片会让每次状态变更都更重。',
      '',
      '治理是 rollover/合并索引、控制字段和分片总量，删除无用模板；不能只增加 master heap，让状态无限增长。'
    ))
  ],
  'elasticsearch/write-search-process':[
    f('写接口返回成功后立刻搜索不到，为什么不用 refresh=true 解决所有写请求？',md(
      '`refresh=true` 会让每次写等待刷新并制造很多小 segment，降低批量写吞吐、增加 merge 压力。只有极少数必须写后立刻搜索的交互才考虑 `wait_for`，普通写接受近实时。',
      '',
      '若按 ID 读取，可以直接 GET 实时获取，不必强迫搜索刷新。要根据读路径选方案。'
    ),{followUps:[
      f('使用 refresh=wait_for 时流量高峰会有什么表现？',md(
        '请求会等下一次 refresh，延迟受 refresh interval 影响；等待监听器过多时也可能触发额外刷新或压力。应限制到关键场景，并监控 refresh 时间、segment 与写入 P99，不把它加到所有 bulk。'
      ))
    ]}),
    f('Bulk 部分成功、部分失败，重试时怎样避免覆盖更新版本？',md(
      '逐条解析 bulk response，只重试可重试的失败项，保留原业务版本和文档 ID。写入使用 external version 或带序列号条件，旧事件不能覆盖新文档。',
      '',
      '整批无脑重发既放大流量，也可能让乱序更新回退。不可重试的 Mapping 错误进入隔离并告警。'
    ))
  ],
  'elasticsearch/deep-pagination':[
    f('使用 search_after 后，翻页过程中有新商品写入，会不会重复或漏数据？',md(
      '如果每页直接查询变化中的索引，排序位置会移动，仍可能重复或漏。需要一致浏览时创建 PIT，让多页共享同一搜索视图，并使用包含唯一 tiebreaker 的稳定排序值。',
      '',
      'PIT 有保活和资源成本，客户端每页更新 token，完成后释放；它适合有限会话，不是永久快照。'
    ),{followUps:[
      f('排序只用 score，能直接作为 search_after 游标吗？',md(
        '不够。大量文档分数相同且副本内部顺序可能不同，分页会不稳定。要追加唯一且可排序的字段作为 tiebreaker，PIT 场景可使用稳定分片文档标识，确保排序元组唯一。'
      ))
    ]}),
    f('产品坚持展示“共 12,345,678 条”并允许跳任意页，你会怎么沟通和改造？',md(
      '精确总数和任意深跳页都很贵，我会先用用户行为说明几乎没人访问十万页，改成有限页、滚动加载和筛选缩小范围；总数可展示阈值化的“1 万+”或异步统计。',
      '',
      '若监管报表确需全部结果，走异步导出，不让交互搜索承担全量遍历。'
    ))
  ],
  'elasticsearch/near-real-time':[
    f('把 refresh_interval 从 1 秒改成 100ms，就能稳定满足 1 秒可搜索吗？',md(
      '不一定。更频繁 refresh 会产生小 segment 和 merge 压力，写入高峰反而可能让整体延迟与吞吐恶化；端到端还包括数据库提交、消息传输和 bulk 排队。',
      '',
      '我会先拆各阶段 P99，给 bulk 刷新设置上限，在真实流量下验证可见延迟。参数只优化 ES 内部一段。'
    ),{followUps:[
      f('只有刚修改商品的运营人员要求立刻看到，能否做局部方案？',md(
        '可以。写入完成后对这类请求使用 `refresh=wait_for`，或详情页短时间读数据库/按 ID GET；普通用户搜索仍走默认刷新。把强需求限制在小流量路径，比全索引提高刷新频率更稳。'
      ))
    ]}),
    f('监控显示 ES refresh 很快，但商品要 5 秒才搜到，下一步查哪里？',md(
      '沿事件链看数据库 outbox、Kafka lag、消费者批次等待和 bulk 重试，找到文档真正到达 ES 的时间。refresh 指标只说明已经写入内存缓冲的文档何时可见。',
      '',
      '我会让 trace 或事件时间戳贯穿写入，避免所有延迟都被归到 ES。'
    ))
  ],
  'elasticsearch/data-consistency':[
    f('同一商品先改价再下架，两条消息乱序到达 ES，怎么防止旧价格事件把下架状态覆盖？',md(
      '事件带数据库提交版本或单调序号，ES 写入使用 external version，只接受更高版本；或者消费者按商品 key 有序，但仍保留版本防重放。',
      '',
      '事件应携带完整可构建状态，或消费者从数据库读取当前版本。不能用消息到达时间判断新旧，网络延迟会骗人。'
    ),{followUps:[
      f('数据库事务回滚了，但变更消息已经发出，版本号还能救吗？',md(
        '这说明消息发送不在正确事务边界。应使用 outbox/CDC，只发布已提交变更；版本只能排序合法事件，不能把未提交数据变成正确。已污染索引要从数据库当前状态重放修复。'
      ))
    ]}),
    f('对账发现 ES 少了十万文档，你会全量重建还是增量补偿？',md(
      '先按索引版本、时间和业务分区定位缺口，确认是消费位点、批量失败还是删除错误。范围可控时按数据库版本增量补写并限速；数据可信度整体失守时创建新索引全量重建更安全。',
      '',
      '无论哪种都不在原索引上盲目边删边补，完成数量、版本和抽样校验后再切 alias。'
    )),
    f('商品在数据库删除后，ES 删除消息丢了，定期对账怎样发现这种“多出来”的文档？',md(
      '只从数据库扫现有 ID 发现不了 ES 孤儿。可以保留删除墓碑/变更日志，按版本同步删除；周期性按分桶校验数据库和 ES 的 ID 数量、摘要，差异桶再做双向明细比对。',
      '',
      '直接全量拉两边 ID 成本太高，对账要分区、可断点并有修复审计。'
    ))
  ],
  'elasticsearch/index-lifecycle':[
    f('日志每天波动很大，只按每天建一个索引会有什么运维问题？',md(
      '大促日索引可能过大、恢复慢，低流量日又制造很多小分片。更稳的是 data stream 加 rollover，同时按主分片大小、文档数和最大时间触发，让分片体积可预测。',
      '',
      '生命周期阶段基于 rollover 后年龄推进，不把自然日当唯一容量单位。'
    ),{followUps:[
      f('同时配置 max_size 和 max_age，满足哪个条件才 rollover？',md(
        '最大条件通常任一达到就应滚动，另外可用最小文档条件避免空索引频繁滚动。关键不是背语法，而是验证实际策略生成的分片大小和数量，防止时间条件制造小分片，并检查 ILM 实际执行记录。'
      ))
    ]}),
    f('删除 30 天前索引前，怎样证明审计数据不会误删？',md(
      '保留期从业务和合规标签计算，不只看索引名字；删除前确认 snapshot 成功且可恢复，ILM 执行有审计和失败告警。不同日志类型使用不同 policy，不能全站一个 30 天。',
      '',
      '我会定期从快照恢复抽样索引，验证备份不是只有状态为 success。'
    ))
  ]
}
