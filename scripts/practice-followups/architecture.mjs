import{follow,md}from'../practice-answers/shared.mjs'

const f=follow

export default{
  'architecture/cache-consistency':[
    f('商品价格更新后缓存删除成功，但消息延迟了十秒，这十秒内其他本地缓存怎么办？',md(
      'Redis 删除只能让回源读到新值，各实例本地缓存仍可能保留旧价格。我会让本地 TTL 明显更短，并用同一变更消息携带版本广播失效；关键结算流程不信任展示缓存，提交订单时重新校验权威价格。',
      '',
      '一致性窗口要分层计算，不能只证明 Redis 与数据库最终一致。'
    ),{followUps:[
      f('结算重新校验发现价格变了，已经创建的订单怎么处理？',md(
        '在创建订单事务中锁定实际成交价和价格版本，若请求携带版本过期就返回价格变化让用户确认，不能静默按新价扣款。展示缓存可旧，资金结果必须由权威版本决定，并保留调价前后的审计信息。'
      ))
    ]}),
    f('热点商品缓存失效后，怎样既避免击穿又不长期返回旧值？',md(
      '使用单飞刷新：一个请求获得刷新资格，其他请求短时间返回带版本的旧值或等待很短截止时间；刷新失败按退避重试并保持旧值的最大可服务期限。',
      '',
      '超过业务允许窗口后应降级或拒绝，而不是无限续旧。监控旧值年龄、回源并发和刷新失败率。'
    )),
    f('缓存一致性方案上线后，你会用哪几类故障演练证明它有效？',md(
      '至少演练数据库提交后删除缓存失败、失效消息重复/乱序/延迟、Redis 切主和热点 key 同时过期。每次都观察旧值最长持续多久、数据库回源峰值以及是否出现新值被旧消息覆盖。',
      '',
      '方案不是画完流程就结束，必须用可测的不一致窗口和恢复时间验收。'
    ))
  ],
  'architecture/idempotency':[
    f('同一个幂等 Key 被客户端拿来提交了不同金额，你会返回第一次结果还是报错？',md(
      '幂等记录除了 key 还要保存请求摘要。相同 key、相同业务参数才返回第一次结果；金额、收款方等关键字段不同，必须拒绝并告警，不能把另一笔请求误认为重试。',
      '',
      '幂等 key 的作用域也要包含租户和接口，避免不同用户碰撞。'
    ),{followUps:[
      f('请求摘要里要不要包含时间戳和 traceId？',md(
        '只包含决定业务结果的稳定字段。每次重试都会变化的 traceId、客户端时间不能进入摘要，否则同一请求永远不相等；但它们可以作为审计字段单独保存。摘要字段清单要随接口版本评审。'
      ))
    ]}),
    f('幂等记录状态是处理中，第二个请求应该等、返回还是接管？',md(
      '由业务时长决定。短操作可以等待很小窗口后返回处理中；支付这类外部结果未知的任务不能直接接管重做，应使用租约和稳定渠道请求号先查结果。',
      '',
      '记录里要有开始时间、拥有者和可恢复状态，后台扫描超时记录做查单或补偿。一个永久 `PROCESSING` 不是幂等完成。'
    )),
    f('数据库唯一约束冲突后，为什么不能统一返回成功？',md(
      '冲突可能是合法重试，也可能是另一个业务对象占用了同一键。必须读取已有记录并比较请求摘要、状态和归属；只有确认同一业务请求才返回原结果。',
      '',
      '把所有 Duplicate Key 都吞成成功会隐藏数据建模和恶意碰撞问题。'
    ))
  ],
  'architecture/flash-sale-system-design':[
    f('Redis 预扣库存成功，但创建订单消息发送失败，这一件库存怎么回来？',md(
      '预扣要生成唯一资格/预占记录，并通过可靠消息或本地日志驱动建单。发送失败可重试；超过建单期限仍未成功，由补偿任务按资格状态归还库存。',
      '',
      '归还也必须幂等，只允许从 `RESERVED` 转 `RELEASED`，不能简单 `INCR`，否则重复补偿会超卖。'
    ),{followUps:[
      f('补偿归还时，迟到的建单消息也到了，如何避免又下单又归还？',md(
        '资格记录需要版本和状态机，建单只能把有效预占改为 `CONFIRMED`，补偿只能把超时预占改为 `RELEASED`，两者用原子条件竞争。失败方读取终态后停止，不能各自无条件执行。'
      ))
    ]}),
    f('十万人都通过资格校验后同时查库存，Redis 还可能成为热点，你怎么继续削峰？',md(
      '入口先做活动令牌、验证码或排队，让到达库存脚本的请求接近可售量的可控倍数；按活动维度限并发并快速返回排队结果。',
      '',
      '库存 key 本身仍是热点，但短 Lua 扣减可以承受的量远高于十万请求同时涌入。系统设计要减少无效请求进入核心区。'
    )),
    f('用户看到“抢购成功”，但最终订单没创建出来，你会如何定义前端状态？',md(
      '入口成功只能表示获得排队或预占资格，不应提前承诺订单成功。前端展示排队中，通过 task/资格 ID 查询；只有订单持久化完成才显示成功，超时释放后显示失败。',
      '',
      '状态文案必须匹配后端一致性阶段，否则技术上的最终一致会变成用户投诉。'
    ))
  ],
  'architecture/config-distribution-gray-release':[
    f('配置已经推送到 80% 实例时发现错误，回滚版本怎样避免和在途推送交错？',md(
      '每份配置有单调版本，实例只接受明确的目标版本策略。回滚不是发送一条无版本旧内容，而是发布一个新的回滚版本，内容来自已验证快照；在途旧推送因版本较低被拒绝。',
      '',
      '控制面记录各实例当前版本和确认状态，先停止扩散，再分批发布回滚版本并验证核心指标。'
    ),{followUps:[
      f('有实例离线半小时后回来，它应该先拿最新版本还是补齐所有中间版本？',md(
        '普通配置只需拉取当前完整快照并校验版本，不必重放每次变化；若配置变更本身带迁移动作，就不应混在普通配置协议里，需要独立可重入的迁移流程。快照与事件用途要分开。'
      ))
    ]}),
    f('灰度按机器比例发布，为什么可能把同一个大客户全部命中？',md(
      '如果客户流量有固定路由或只落少数实例，机器比例不等于业务比例。灰度规则应支持按租户、用户或请求标签稳定分桶，并排除高风险客户；同时观察灰度群体自己的错误率和延迟。',
      '',
      '扩量按业务样本和指标进行，不只看“10% 机器没有报警”。'
    )),
    f('配置中心不可用时，业务实例启动要不要失败？',md(
      '取决于配置是否有安全的本地最后版本。已有实例继续使用已校验快照；新实例若本地快照仍在有效期且不包含敏感过期凭证，可受限启动，否则保持不 ready。',
      '',
      '不能默认空配置启动，也不能因为控制面短暂故障让所有运行实例主动退出。'
    ))
  ],
  'architecture/distributed-transaction':[
    f('库存扣成功、优惠券也锁定了，但创建支付单连续失败，Saga 应该先补偿谁？',md(
      '按已完成步骤的逆序和资源语义补偿，先释放优惠券，再归还库存；每个补偿有独立幂等键和状态条件。支付单失败是否确定也要区分，超时可能已经创建，需要先查单。',
      '',
      '补偿不是简单调用反向 API，必须能面对重复、乱序和部分失败。'
    ),{followUps:[
      f('归还库存的补偿接口也失败了，主流程能一直等吗？',md(
        '主请求记录订单取消中并返回明确状态，补偿任务持久化后异步重试和告警；资源长期未释放时进入人工处置。不能用一个跨服务同步调用链一直占着请求和数据库事务。'
      ))
    ]}),
    f('为什么不直接用分布式事务框架把三个数据库锁在一个全局事务里？',md(
      '要评估参与者、锁持有时间、故障模型和吞吐。下单链路包含远程服务和可能的第三方支付，长全局事务会放大锁、协调器和可用性风险，有些资源也不支持 XA。',
      '',
      '核心本地约束用本地事务，跨服务用可恢复状态机和可靠事件，通常更符合高并发订单。框架不能消除业务补偿。'
    )),
    f('最终一致完成了，但用户短时间看到库存扣了、订单却不存在，产品上怎么处理？',md(
      '创建一个可查询的本地订单/流程记录作为入口事实，状态显示处理中；各步骤围绕同一业务 ID 推进。用户查询不会依赖三个服务恰好同时可见。',
      '',
      '超过承诺时间转失败并触发补偿，状态变化通知用户。最终一致需要一个面向用户的确定状态机。'
    ))
  ],
  'architecture/rate-limiting':[
    f('全局限流设置 20 万 QPS，某个大租户可以把额度全部占完，怎么保证公平？',md(
      '全局容量保护之外还要按租户分配预算，基础配额加可借用余量；热点租户达到自己的突发上限后先被限制，不影响其他租户。',
      '',
      '配额来自下游成本和合同等级，监控每租户拒绝率与延迟。只做一个全局桶会让先到的流量垄断资源。'
    ),{followUps:[
      f('空闲租户的额度能不能全部借给热点租户？',md(
        '可以借一部分提高利用率，但要保留其他租户随时恢复的保护余量，并让借用快速可回收。否则平时看起来利用率高，流量同时恢复时会产生断崖式拒绝。借用比例也要进入监控和容量演练。'
      ))
    ]}),
    f('限流阈值应该按机器压测极限的 100% 设置吗？',md(
      '不能。要给 GC、流量波动、单机故障和下游抖动留余量，使用稳定运行点而不是崩溃点；集群少一台时剩余实例也应在可承受范围。',
      '',
      '阈值上线后根据 CPU、队列、下游 P99 和错误率动态校准，并有自动降级，不能只看 QPS。'
    )),
    f('网关已经限流，为什么应用和数据库前还需要保护？',md(
      '网关看不到缓存未命中、慢 SQL 或特定接口成本，同样 QPS 对下游压力差别很大；内部重试和消息消费也可能绕过网关。',
      '',
      '应用按线程池/接口隔离，数据库前按连接和查询成本保护，形成逐层预算。各层返回码要能区分，避免重复重试放大。'
    ))
  ],
  'architecture/resilience':[
    f('第三方超时率突然到 80%，熔断后所有请求都失败，会不会比不熔断更糟？',md(
      '如果没有降级语义，熔断只是更快失败；但它能释放线程和连接，防止故障拖垮本系统。关键是按接口价值设计降级：非核心查询返回缓存，支付结果未知进入处理中并查单。',
      '',
      '熔断阈值基于错误率、慢调用和最小样本，不能因为一次抖动全部打开。'
    ),{followUps:[
      f('半开时放多少探测请求才不会再次把第三方压垮？',md(
        '从极小并发开始，按连续成功、延迟和对方恢复沟通逐步放量；探测有独立预算，不让积压请求同时穿透。失败立即退回打开状态，并增加等待时间和抖动，成功也不能一步恢复到故障前峰值。'
      ))
    ]}),
    f('第三方恢复后，大量失败任务同时重试，怎样防止恢复风暴？',md(
      '重试任务使用指数退避加随机抖动，按租户和业务优先级有界调度；恢复后分阶段提高并发，观察第三方成功率和本地队列。',
      '',
      '每个任务有幂等键和总重试期限，过期进入人工或补偿。不能把队列积压一次性倾倒给刚恢复的服务。'
    )),
    f('超时应该设成第三方 SLA 的两倍，给它更多完成机会吗？',md(
      '超时要从本接口剩余截止时间和依赖历史分位数反推，通常明显短于用户总超时，还要给重试或降级留预算。设得过长会占住线程、连接并让上游先超时。',
      '',
      '连接、读和整体调用分别设置，按近期延迟监控校准，不机械乘 SLA。'
    ))
  ],
  'architecture/distributed-id':[
    f('时钟回拨时 Snowflake 生成器应该停服务、等待还是切节点号？',md(
      '先按回拨幅度分级。很小的回拨可以短暂等待到逻辑时间追平；大回拨不能继续用旧时间戳生成，要摘除实例或使用持久化逻辑时钟。随意切节点号可能与真实节点冲突。',
      '',
      '监控 NTP 和生成器回拨事件，节点号由强约束的注册系统分配。可用性选择必须以绝不重复为底线。'
    ),{followUps:[
      f('为了绝不重复，发生大回拨就让整个 ID 服务不可用合理吗？',md(
        '单实例应停止发号，但服务整体可以通过其他时钟正常、节点号不同的实例继续；路由层摘除异常节点。若所有节点同时受影响，宁可短暂拒绝也不能生成重复 ID，除非切到已验证的备用号段方案。'
      ))
    ]}),
    f('趋势递增 ID 暴露每天订单量，业务不允许被枚举，怎么处理？',md(
      '内部主键可以保持趋势递增以利存储，对外再生成不可猜的业务号或加密/编码 token，接口还要鉴权，不能只靠 ID 难猜保护数据。',
      '',
      '不要为防枚举把数据库主键换成完全随机 UUID 并承担写放大；内部定位和外部标识可以分开设计。'
    ))
  ],
  'architecture/delayed-task':[
    f('关闭订单任务触发时，支付回调刚好在路上，怎样避免已支付订单被关闭？',md(
      '延时任务不能无条件关闭。执行时重新读取订单，用条件更新从 `UNPAID` 转 `CLOSED`；支付回调也按状态机竞争。只有一个状态迁移成功，失败方根据当前状态做幂等处理或退款流程。',
      '',
      '时间到了只代表获得检查资格，不代表业务条件仍成立。'
    ),{followUps:[
      f('支付渠道已扣款，但关闭状态先提交了，回调应该直接丢弃吗？',md(
        '不能丢弃。记录异常支付并触发自动退款或人工处理，使用渠道流水保证幂等。订单状态竞争可以阻止错误发货，但不能让真实资金结果消失；对账任务还要持续确认退款最终状态。'
      ))
    ]}),
    f('延时队列服务宕机两小时，恢复后千万任务同时到期，怎么处理？',md(
      '按分片和业务优先级扫描到期任务，用有界 worker 平滑追赶；任务执行前查当前状态，已完成的快速跳过。入口新任务和历史积压使用独立预算，避免恢复流量压垮订单库。',
      '',
      '监控最老延迟、处理速率和数据库 P99，动态调速。恢复目标不是瞬间把队列清零。'
    )),
    f('时间轮里的任务只在内存，进程重启后怎么恢复？',md(
      '内存时间轮只做近端调度，任务事实必须持久化在数据库、日志或可靠消息中。启动时从持久层按分片加载未来窗口，并周期扫描已到期未完成任务补漏。',
      '',
      '任务有稳定 ID 和状态，重复装载不会重复关闭订单。不能把 JVM 内存结构当可靠队列。'
    ))
  ],
  'architecture/observability':[
    f('只有 1% 请求很慢，全量记录详细 trace 成本太高，你怎么保证能抓到它们？',md(
      '入口做低比例头采样，同时对错误、超时和高延迟使用尾采样保留完整链路；关键租户和新版本可以提高采样率。所有请求仍保留轻量指标和结构化日志关联 ID。',
      '',
      '采样规则和成本有预算，不能只随机采样后恰好丢掉所有异常。'
    ),{followUps:[
      f('尾采样要等链路结束，会不会占很多内存？',md(
        '会，所以采样器要有时间、span 数和内存上限，超长链路截断并标记；可以在独立 collector 层缓冲，不压业务进程。高峰超过预算时优先保留错误与关键服务，其他按比例降采样。'
      ))
    ]}),
    f('日志、指标和 trace 都有了，为什么事故时还是关联不起来？',md(
      '通常是标识不统一或上下文跨异步边界丢失。我会统一 traceId、requestId、业务主键、实例和版本字段，HTTP、消息、线程池提交时显式传播；日志格式和指标标签使用同一命名。',
      '',
      '再从一个真实订单做演练，确认能从告警跳到 trace，再到对应日志和变更版本。工具齐全不等于链路可用。'
    )),
    f('告警很多但值班人员不知道先看什么，你会怎样重新设计？',md(
      '告警以用户影响和可执行动作组织：先看错误率、延迟与核心业务成功率，再由服务依赖和资源指标辅助定位。每条告警附范围、近期变更、仪表盘和第一处置步骤。',
      '',
      '去掉没有行动的阈值噪音，使用持续时间和多窗口减少瞬时抖动；事故后复盘告警是否提前、是否指向正确责任人。'
    ))
  ]
}
