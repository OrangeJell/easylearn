import{follow,md}from'../practice-answers/shared.mjs'

const f=follow

export default{
  'redis/data-structures-use-cases':[
    f('商品详情既放本地缓存又放 Redis，什么时候会出现两层缓存反而更难维护？',md(
      '本地缓存带来更低延迟，但每个实例都有一份，更新失效和容量统计都会复杂。如果商品价格要求很小的不一致窗口，而实例又多，广播失效失败就可能长期看到旧值。',
      '',
      '我只给高频、可短暂过期且体积小的数据加本地层，TTL 更短，并通过版本或消息失效；强一致状态不做两层缓存。收益要由 Redis 调用量和 P99 证明。'
    ),{followUps:[
      f('失效消息漏了一台实例，本地缓存怎样最终恢复？',md(
        '本地项必须有有限 TTL，消息只负责缩短不一致窗口，不能成为唯一回收机制。还可以在 value 中带版本，关键读发现版本落后时回源；监控各实例命中和版本差异，避免漏消息无限陈旧。'
      ))
    ]}),
    f('点赞计数放 Redis 后，怎样处理 Redis 丢数据和数据库回写？',md(
      '先确定 Redis 是权威状态还是加速层。核心计数通常以事件或数据库为可恢复来源，Redis 做实时聚合；增量通过可靠消息落库，失败可重放，并用业务事件 ID 去重。',
      '',
      '不能只靠定时把当前总数覆盖数据库，因为宕机窗口里的增量无法区分。需要可追溯的增量链路和定期对账。'
    ))
  ],
  'redis/persistence-high-availability':[
    f('哨兵在 10 秒内完成切换，但业务仍报错一分钟，你会查哪些客户端行为？',md(
      '切主只是服务端恢复的一部分。我会看客户端是否及时发现新主、DNS/连接池是否缓存旧地址、失败命令是否重试、订阅是否重建，以及重试风暴有没有把新主打满。',
      '',
      '客户端超时要短于业务截止时间，拓扑刷新和退避要经过演练。只记录 Sentinel 切换耗时，会低估用户实际恢复时间。'
    ),{followUps:[
      f('所有失败命令都自动重试会有什么风险？',md(
        '命令可能已经在旧主执行但响应丢失，直接重试会重复扣减或发布。读命令通常可重试，写命令要依赖幂等键、Lua 状态条件或业务补偿；重试还必须有次数和抖动，避免新主刚起来就被打爆。'
      ))
    ]}),
    f('AOF everysec 仍可能丢一秒数据，核心业务怎样把风险降到可接受？',md(
      '不能只调 Redis 参数。关键写要有数据库、消息日志或上游事件作为可重建来源，Redis 宕机后按权威数据恢复；同时使用副本和故障切换降低单机故障窗口。',
      '',
      '若业务绝对不能丢，Redis 就不应单独承担权威账本。配置、架构和恢复演练要一起满足 RPO。'
    ))
  ],
  'redis/distributed-lock':[
    f('业务执行超过锁 TTL，但续期线程因为 GC 暂停没续上，会发生什么？',md(
      '锁可能过期后被另一个实例获得，原实例恢复后仍继续执行，于是两个持有者同时产生副作用。自动续期只能降低概率，不能把租约变成永不过期的互斥。',
      '',
      '关键写入要带 fencing token 或数据库版本条件，只接受更高租约版本；任务本身也要幂等。锁解决竞争入口，权威存储负责拒绝过期持有者。'
    ),{followUps:[
      f('数据库不方便增加 fencing token，还有什么务实做法？',md(
        '至少把副作用落到带唯一键或状态条件的数据库语句中，并让任务执行时间有硬上限。无法从资源端识别过期持有者时，就不能声称锁提供严格互斥，只能按业务接受的重复风险做补偿和对账。'
      ))
    ]}),
    f('使用 value 随机标识加 Lua 删除，是否已经解决了误删的全部问题？',md(
      '它解决“旧持有者删掉新持有者锁”的原子校验问题，但没有解决锁提前过期后旧任务继续执行、Redis 故障切换丢锁或业务操作重复。',
      '',
      '我还会设计租约、续期、资源端版本约束和幂等，并通过暂停进程、切主和超时演练验证。一个正确的 unlock 脚本不是完整锁方案。'
    ))
  ],
  'redis/cache-problems':[
    f('数据库流量暴涨时，怎么从指标快速区分热点 Key 击穿和批量雪崩？',md(
      '看未命中分布和回源 SQL 指纹。少数 key 的请求量极高、同一商品查询暴增，偏向击穿；大量不同 key 在同一时间段过期，命中率整体陡降，偏向雪崩；持续查询不存在 ID 则更像穿透。',
      '',
      '止损动作不同：热点 key 做单飞和逻辑过期，雪崩先限流并错开 TTL，穿透做参数校验与短期空值。'
    ),{followUps:[
      f('热点 Key 加互斥锁后，锁持有者回源也超时了怎么办？',md(
        '等待者不能无限排队。可以返回可接受的旧值，或在短截止时间后降级；锁持有者失败要释放并做退避，避免所有请求轮流成为回源者。下游不可用时，单飞只能减少并发，不能创造数据。'
      ))
    ]}),
    f('空值缓存能防穿透，但有人随机请求海量不存在 ID，会不会把 Redis 填满？',md(
      '会，所以空值 TTL 要短且容量可控，入口还要校验 ID 格式、鉴权和限频。对于可枚举的有效集合可以使用布隆过滤器，但它也只减少回源，不能替代反爬和请求预算。',
      '',
      '监控空值 key 新增速率和来源，异常时按用户或 IP 限流。缓存不是接收任意恶意 key 的垃圾桶。'
    ))
  ],
  'redis/expiration-eviction':[
    f('used_memory 没到 maxmemory，容器却快 OOM 了，怎么解释？',md(
      'Redis 统计与进程 RSS 口径不同，内存碎片、复制/AOF 缓冲、客户端输出缓冲和 fork 写时复制都可能占额外内存。要一起看 `used_memory_rss`、碎片率、fork 阶段和各类 buffer。',
      '',
      '不能因为 key 内存没到上限就继续写。容器限制要给持久化和故障操作留峰值余量。'
    ),{followUps:[
      f('开启主动碎片整理后 CPU 升高，应该一直开到碎片率降下来吗？',md(
        '要给 CPU 和延迟设置停止线，低峰渐进执行。碎片整理本身会消耗资源，若实例正高负载可能加重抖动；同时要找大 key 删除、数据模型和 allocator 行为的根因，不能只靠整理追数字。'
      ))
    ]}),
    f('选择 allkeys-lru 后，为什么一些重要配置 Key 也可能被淘汰？',md(
      '淘汰策略不知道业务重要性，只按访问近似和候选样本工作。低频但关键的配置可能比热点缓存更早被淘汰。',
      '',
      '关键不可丢数据应与可淘汰缓存分实例或至少分明确资源池，不能指望 key 命名得到优先级。配置加载还要有权威来源和回源保护。'
    ))
  ],
  'redis/cache-consistency':[
    f('更新数据库成功后删除缓存失败，你会在请求里一直重试到成功吗？',md(
      '不会无限占住请求。先把数据库提交作为权威结果，同时通过事务消息、outbox 或可靠重试任务记录失效事件；请求内可以做一次短重试，之后交异步并告警。',
      '',
      '缓存仍有 TTL 作为最终兜底。关键是失效任务不能只存在进程内，否则服务重启后这个旧值会留到过期。'
    ),{followUps:[
      f('异步失效消息乱序，旧更新的删除晚到会有问题吗？',md(
        '如果消息动作只是删除，晚到通常只会造成一次额外回源，不会把旧值写回；若消费者重建并写缓存，就必须带数据版本，只允许更高版本覆盖。设计时我更偏向消息只失效，由读请求回填当前数据库值。'
      ))
    ]}),
    f('读请求查库后准备回填缓存，恰好另一个请求更新了数据库并删除缓存，旧值会不会被写回来？',md(
      '会出现这个竞态。能接受短窗口时依靠较短 TTL；要求更严时，回填值带数据库版本，并用 Lua 只在缓存版本不更新时写入，或用逻辑过期与单线程刷新。',
      '',
      '双删加固定 sleep 很难覆盖所有查询耗时。我会用版本和监控把窗口变成可证明的协议。'
    ))
  ],
  'redis/hot-key-big-key':[
    f('发现热 Key 后直接加副本读，为什么不一定能解决？',md(
      '如果命令是写、Lua 或要求读到最新值，仍然要到主节点；即使可读副本，也可能把网络和序列化压力转移而没有消除单 key 请求量。',
      '',
      '我会按语义拆分：可缓存静态值放本地缓存或 CDN，计数按桶分散，热点请求做单飞和限流。副本读只是其中一个容量手段。'
    ),{followUps:[
      f('把一个计数 Key 随机拆成 100 个桶，读取总数会不会变慢？',md(
        '会增加读放大，因此适合写多、允许近实时聚合的计数。可以后台周期汇总、读取并行批量获取，或只在热点期间启用；若每次强一致读都要合并 100 个桶，方案可能得不偿失。'
      ))
    ]}),
    f('删除一个几百 MB 的大 Key，为什么不能直接 DEL？',md(
      '`DEL` 释放复杂对象可能长时间阻塞 Redis 主线程，造成所有请求延迟尖刺。优先用 `UNLINK` 异步释放，集合还可先分批 `SCAN`/删除成员，并在低峰观察 lazyfree 队列和内存。',
      '',
      '删除前确认它是否仍被写入、是否有副本和 AOF 放大；治理根因是限制单 key 元素和 value 大小。'
    ))
  ],
  'redis/cluster-sharding':[
    f('从单机迁移到 Cluster 时，多 Key Lua 脚本突然报 CROSSSLOT，怎么改？',md(
      'Cluster 要求脚本涉及的 key 在同一个槽。业务确实需要原子操作时，用 hash tag 让同一业务实体的 key 共槽，例如 `{order:123}`，但要防止 tag 过粗造成热点。',
      '',
      '跨业务实体的多 key 原子性不能靠 Cluster Lua 实现，要重设数据模型或用业务补偿。迁移前应扫描脚本、事务和批量命令的 key 分布。'
    ),{followUps:[
      f('把所有订单 Key 都放进 {order} 这个 hash tag 可以吗？',md(
        '不可以，那会让所有 key 都落在同一槽和同一主节点，Cluster 失去分片意义。hash tag 应只把必须原子处理的同一订单相关 key 聚在一起，例如 `{order:123}`，粒度要随业务实体变化。'
      ))
    ]}),
    f('扩容迁槽时接口延迟升高，你会怎样控制迁移速度？',md(
      '按节点 CPU、网络、命令 P99 和业务高峰设置批次与速率，低峰逐槽迁移；客户端必须支持 MOVED/ASK 重定向并及时刷新拓扑。',
      '',
      '出现延迟或复制积压就暂停，不为了赶进度压垮集群。迁移前还要先治理大 key，否则一个槽里的大对象会让单次迁移长时间卡住。'
    ))
  ],
  'redis/why-fast':[
    f('Redis 单线程执行命令，为什么线上仍可能出现 CPU 多核都很高？',md(
      '命令执行主路径的模型不等于整个进程永远只有一个线程。持久化、异步释放、IO 线程和后台任务都会使用其他核；Cluster 里还有多个 Redis 进程。',
      '',
      '排查要看具体线程和命令，而不是用“单线程”否定监控。主线程 CPU 高多半与命令量、复杂度或大 key 有关，后台核高则看 fork、AOF 和 lazyfree。'
    ),{followUps:[
      f('既然主要是内存访问，为什么一次大范围 ZRANGE 仍能拖慢所有请求？',md(
        '内存快不代表返回和处理无限数据是常数时间。大范围命令要遍历、组装和发送大量元素，主线程在这期间不能处理其他命令，网络输出缓冲也会增长。应该限制范围、分页，并监控慢命令和响应大小。'
      ))
    ]}),
    f('接口 Redis 耗时不高，但用户 P99 很差，你还会查哪些排队位置？',md(
      '看客户端连接池等待、DNS/网络、事件循环阻塞、序列化和请求在应用线程池里的队列。Redis 命令耗时只覆盖服务端执行，不能代表拿到连接到解析结果的完整时间。',
      '',
      '我会用 trace 拆段并与客户端指标对齐，避免看到 Redis `SLOWLOG` 为空就排除缓存链路。'
    ))
  ],
  'redis/transactions-lua-pipeline':[
    f('秒杀脚本执行时间从 1ms 涨到 80ms，会对整个 Redis 实例造成什么影响？',md(
      'Lua 脚本在执行期间阻塞其他命令，80ms 的热点脚本会直接把实例所有请求的尾延迟拉高。我要看脚本是否遍历大集合、动态生成大量 key 或返回过多数据。',
      '',
      '脚本只保留少量固定 key 的原子判断与更新，复杂计算移出 Redis；设置慢脚本监控，并在压测中看整实例 P99，不只看脚本结果正确。'
    ),{followUps:[
      f('脚本已经超时，客户端断开后 Redis 会自动终止它吗？',md(
        '客户端超时不等于服务端停止，脚本可能仍在执行并阻塞实例。需要通过脚本超时配置、受控 `SCRIPT KILL` 条件和代码约束治理；如果脚本已经执行写命令，也不能随意终止，只能等待或故障处理。'
      ))
    ]}),
    f('Pipeline 能减少网络往返，为什么不能替代 Lua 做库存扣减？',md(
      'Pipeline 只批量发送命令，其他客户端命令仍可能插入，前一条读和后一条写之间没有原子条件。库存判断与扣减必须由 Lua、单条原子命令或权威数据库条件更新完成。',
      '',
      'Pipeline 适合彼此独立的批量读写；是否成功还要逐条检查响应，不能因为一次 flush 成功就认为所有业务动作完成。'
    ))
  ]
}
