import{follow,md}from'../practice-answers/shared.mjs'

const f=follow

export default{
  'kafka/consumer-group':[
    f('Topic 有 12 个分区，消费者从 12 台扩到 24 台，为什么吞吐基本不变？',md(
      '一个消费组内同一分区同一时刻只分给一个消费者，额外 12 台会空闲。先确认瓶颈是不是分区并行度；如果单分区已经达到消费或 Broker 上限，增加分区才可能提升。',
      '',
      '扩分区还会影响 key 的分布和顺序，不能只为让机器忙起来就修改。也可能瓶颈在数据库，下游不扩容时更多消费者只会增加竞争。'
    ),{followUps:[
      f('增加分区后，同一个订单的消息为什么可能乱序？',md(
        '原分区数变化会改变默认 key 哈希到分区的结果，新消息可能进入另一个分区，与旧分区并行消费。要求订单有序时，要规划分区扩容迁移窗口、版本化路由，或在消费端按业务版本拒绝旧事件。'
      ))
    ]}),
    f('扩容触发频繁重平衡，怎样减少业务暂停？',md(
      '先处理消费者不稳定：长时间不 poll、实例频繁重启和网络抖动。使用静态成员和合作式再均衡可以减少全组停顿，但前提是消费循环及时、批处理有界。',
      '',
      '发布时控制批次，监控 rebalance 次数、分区撤销耗时和 lag。协议优化不能掩盖慢消费。'
    ))
  ],
  'kafka/reliability':[
    f('生产者收到超时，但 Broker 可能已经写入，这条订单消息应该怎么处理？',md(
      '这是结果未知，生产者可以在开启幂等和合理重试配置下重试，但业务消息必须带稳定 eventId，不能每次重试生成新 ID。下游消费者仍按业务键幂等。',
      '',
      '如果消息来自数据库状态变更，我更倾向 outbox/CDC，让可重放日志负责继续投递；不能在请求线程里凭感觉决定“当作成功”或“再发一条新消息”。'
    ),{followUps:[
      f('生产者幂等已经开启，消费者还需要幂等吗？',md(
        '需要。生产者幂等主要约束同一生产会话向分区重试的重复，业务重发、会话重建、消费者提交失败和死信重放仍可能重复。端到端可靠必须让业务副作用可识别重复，并用唯一约束验证。'
      ))
    ]}),
    f('消费成功后提交 offset 失败，服务重启会怎样？',md(
      '消息会从旧位点再次投递，所以数据库写入、外部调用都必须幂等。处理顺序通常是先完成业务副作用，再提交位点；反过来先提交会在业务失败时丢消息。',
      '',
      '对数据库操作可用事件 ID 唯一约束或 inbox 表，把去重与业务更新放同一事务。'
    ))
  ],
  'kafka/message-loss-prevention':[
    f('生产者日志显示 send 成功，但 Topic 里查不到消息，你会先核对什么？',md(
      '先核对环境、Topic、分区、时间戳和消息 key，确认“成功”是同步拿到 Broker ack，而不是仅调用异步 `send` 没检查回调。再查生产者错误、事务是否提交和权限/路由。',
      '',
      '不要直接从消费者没看到推断 Broker 丢了，消费组位点、过滤和反序列化都可能让消息不可见。'
    ),{followUps:[
      f('异步 send 的 Future 没人等待，程序正常退出会发生什么？',md(
        '缓冲区里的消息可能还没发送完成。服务关闭要停止接收新请求、调用 `flush` 并等待有上限的发送完成，失败消息进入可恢复记录。生产请求也应处理回调，不能把消息交进客户端缓冲就算成功。'
      ))
    ]}),
    f('Broker 宕机后 ISR 里只剩一个副本，继续允许写入有什么取舍？',md(
      '若 `min.insync.replicas` 仍允许单副本 ack，可用性更高，但该副本再故障就可能丢已确认消息。核心 Topic 通常要求至少两个同步副本，宁可短暂拒绝写入。',
      '',
      '选择要和 acks、复制因子及业务 RPO 一起说明，并通过故障演练验证，不是只改一个参数。'
    )),
    f('消费者处理失败却仍然提交了位点，怎么恢复已经跳过的消息？',md(
      '先停止继续扩大范围，找到受影响分区和 offset 区间；修复消费者后用新消费组或重置位点重放。业务侧必须幂等，因为区间里也包含已成功消息。',
      '',
      '长期要让失败消息进入重试/隔离流程，只有成功或可审计地转移后才推进位点，并监控异常提交。'
    ))
  ],
  'kafka/duplicate-consumption-idempotency':[
    f('支付消息重复到达时，先查数据库发现没处理，再插入，为什么仍可能重复？',md(
      '两个消费者或重试线程可以同时查到不存在，然后都执行副作用，这是典型先查后写竞态。最终防线要是数据库唯一约束，例如 eventId 或业务支付号，并把去重记录和状态更新放在同一事务。',
      '',
      '冲突后读取已有处理结果，返回幂等成功；不能只靠 Redis 标记，因为标记和数据库写之间仍可能断裂。'
    ),{followUps:[
      f('唯一记录插入成功，但随后调用第三方超时，重试时该怎么办？',md(
        '去重记录不能只存“已处理”，要存状态和稳定请求号。超时记为 `PROCESSING/UNKNOWN`，重试先查第三方结果再决定继续；若一插入就标成功，后续会把未完成副作用永久跳过。'
      ))
    ]}),
    f('用订单状态判断幂等，是否可以不保存 eventId？',md(
      '只有当同一状态迁移天然只允许一次，并且重复事件永远等价时才勉强够。不同事件可能都把状态推到相同值，却有不同金额或版本，单看状态会误吞。',
      '',
      '核心事件我会同时校验业务版本/事件 ID 和前置状态，保留可审计的处理记录。'
    ))
  ],
  'kafka/message-backlog-troubleshooting':[
    f('消费者 CPU 不高但 lag 飙升，第一份线程栈显示都在等数据库连接，你会怎么止损？',md(
      '先限制消费并发和入口非核心写，避免更多线程堵在连接池；检查是否有慢 SQL、长事务或连接泄漏。若业务允许，暂停该消费组比持续拉取并占内存更安全。',
      '',
      '恢复后逐步增加消费速率，同时观察数据库 P99 和连接等待。不能看到 CPU 空闲就继续加消费者。'
    ),{followUps:[
      f('数据库恢复后把消费者扩十倍追赶积压，会有什么风险？',md(
        '会形成恢复风暴，再次压垮数据库、缓存或第三方。应按下游可用余量设置追赶速率，优先处理时效高的消息，监控 lag 下降斜率和下游 P99，逐档扩容；指标一旦恶化就自动降速。'
      ))
    ]}),
    f('只有一个分区积压，其他分区正常，你会查什么？',md(
      '先看该分区的消息量和 key 分布，可能是热点业务键；再看负责它的消费者实例、错误重试和当前 offset 附近是否有毒消息。',
      '',
      '不能靠增加普通消费者解决单分区瓶颈。短期可迁移实例、隔离毒消息，长期调整 key 分布或拆分可并行的业务实体，同时守住顺序要求。'
    )),
    f('积压消息已经过了业务时效，还需要全部按原逻辑消费吗？',md(
      '不一定。要按事件类型定义过期策略：库存刷新可以跳到最新版本，支付和账务事件通常必须逐条处理。任何跳过都要有审计、补偿和业务批准。',
      '',
      '可以把过期非核心消息转存后推进位点，但不能为了让 lag 归零静默丢核心数据。'
    ))
  ],
  'kafka/isr-leader-election':[
    f('Leader 切换后业务继续写入，怎样确认没有发生未同步副本当选？',md(
      '检查 Topic 是否禁用 unclean leader election、当选副本是否在故障前 ISR 中，以及控制器与 Broker 日志里的 ISR 变化。再核对生产者 acks 和 `min.insync.replicas`。',
      '',
      '业务层还要对比关键事件连续性和复制滞后指标。仅看到新 Leader 可写不能证明已确认消息都在。'
    ),{followUps:[
      f('禁止非 ISR 副本当选后，所有 ISR 都不可用怎么办？',md(
        '核心数据通常选择保持不可用，等待一个同步副本恢复，避免用旧副本覆盖已确认消息。若业务决定强制非同步副本当选，就是明确接受数据回退，必须由事故决策流程批准并准备上游重放与对账。'
      ))
    ]}),
    f('Broker 恢复后很久没回到 ISR，会重点排查什么？',md(
      '看副本拉取延迟、磁盘和网络吞吐、日志损坏、leader 负载与限速配置。恢复节点可能同时追多个大分区，资源不足时进度很慢。',
      '',
      '我会控制恢复并发和流量，必要时迁移部分 leader，而不是反复重启；以 replica lag 和 ISR 恢复时间验证。'
    ))
  ],
  'kafka/message-ordering':[
    f('同一订单都用 orderId 做 key，为什么消费者仍可能把取消处理在支付前面？',md(
      '分区只保证日志中的写入顺序。如果两个生产者并发发送、上游事件生成顺序已错，Kafka 不会替业务排序；消费者重试和异步线程池也可能让完成顺序反转。',
      '',
      '事件要带单调业务版本，消费端按订单串行或有序执行，发现版本跳跃就等待/补查，旧版本幂等忽略。'
    ),{followUps:[
      f('某个版本消息永久丢失，后续消息一直等待怎么办？',md(
        '等待要有超时。超过窗口后从订单权威库拉取当前状态或触发补偿事件，校准版本再继续；同时告警并保留缺口与处理证据。不能让一个缺失版本永久堵住整个分区或订单。'
      ))
    ]}),
    f('为了提高吞吐，把一个分区消息并行交给线程池处理，如何保住单订单顺序？',md(
      '按 orderId 做分片执行器或串行队列，同一 key 永远落到同一执行通道，不同订单并行。位点提交要等前面所有消息完成，不能后面的先成功就越过失败消息。',
      '',
      '还要限制每个 key 的队列长度，热点订单不能无限占内存。'
    ))
  ],
  'kafka/transactions-exactly-once':[
    f('消费后既要写数据库又要发 Kafka，新消息事务能把数据库也一起提交吗？',md(
      '不能。Kafka 事务只能原子提交消费位点和 Kafka 生产记录，普通数据库不在这个事务里。涉及数据库时，我会在本地事务中写业务数据和 outbox，再由 CDC/投递器可靠发消息。',
      '',
      '消费者和下游仍然幂等。不要用“Exactly Once”标签掩盖跨系统边界。'
    ),{followUps:[
      f('Outbox 消息已经发出，但标记发送成功前服务崩溃，会不会重复？',md(
        '会再次投递，所以 outbox 记录有稳定事件 ID，发送端和消费者都能识别重复。标记和发送跨系统无法靠一次普通事务彻底原子化，设计目标是至少一次加幂等，而不是假装不会重复。'
      ))
    ]}),
    f('Kafka 事务长时间不提交会带来什么生产影响？',md(
      '事务中的消息对 `read_committed` 消费者不可见，长事务会拉大端到端延迟，并占用事务状态和日志；超过超时还会被中止，整批重新处理。',
      '',
      '批次要有界，处理不要包含不可控的远程调用，监控事务中止率和持续时间。'
    ))
  ],
  'kafka/zero-copy':[
    f('启用 TLS 后 Kafka 的零拷贝优势是否完全消失，吞吐下降你会怎么判断瓶颈？',md(
      '加密通常需要在用户态处理数据，可能无法走原来的 `sendfile` 路径，但顺序写、批处理、页缓存和压缩仍然有效。不能因为 TLS 就说 Kafka 不再高吞吐。',
      '',
      '我会比较 Broker CPU、网络、磁盘、请求队列和加密开销，确认是 CPU 被 TLS 打满还是磁盘/副本限制，再考虑硬件加速、批次和连接配置。'
    ),{followUps:[
      f('批次调大可以摊薄 TLS 开销，为什么不能无限增大？',md(
        '更大批次会增加生产等待、内存、单次重试范围和大消息请求延迟。要在吞吐与端到端延迟预算内压测，结合 `linger.ms`、batch size 和压缩率选择，而不是只追峰值 MB/s。'
      ))
    ]})
  ],
  'kafka/partition-planning':[
    f('按吞吐估算需要 80 个分区，但业务 key 只有 20 个大客户，会有什么问题？',md(
      '实际流量只会集中到少数分区，80 个分区并不能得到 80 路均匀并行。要分析 key 基数和头部租户占比，必要时在不破坏顺序的粒度上增加子 key，例如客户内业务实体。',
      '',
      '如果必须保证整个客户全局有序，就要接受单客户吞吐上限或重新定义顺序边界。'
    ),{followUps:[
      f('给 key 加随机后缀可以立刻打散，消费端再聚合可行吗？',md(
        '会失去原 key 顺序，聚合端需要版本、窗口和缺口处理，复杂度很高。只有业务本来允许并行、事件可交换时才能随机打散；否则应该按更细且有业务意义的实体分区。'
      ))
    ]}),
    f('分区数一次建得很大，除了 Broker 文件多还有哪些长期成本？',md(
      '每个分区增加 leader 选举、复制、元数据、客户端连接与重平衡成本，故障恢复也更慢。小流量 Topic 过多分区会让集群控制面和运维复杂度先于吞吐成为瓶颈。',
      '',
      '我会按 6～12 个月容量留余量，但用集群分区总预算约束，并把扩分区对 key 顺序的影响写进方案。'
    ))
  ]
}
