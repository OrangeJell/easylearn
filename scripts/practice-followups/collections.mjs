import{follow,md}from'../practice-answers/shared.mjs'

const f=follow

export default{
  'collections/hashmap':[
    f('聚合数据量从十万突然涨到一百万，HashMap 扩容会怎样影响接口，你怎么提前控制？',md(
      '我会先用业务键数量和单条对象大小估算内存，而不是只看 Map 节点。容量可预估时按负载因子设置初始容量，避免处理过程中多次扩容和重新分桶；同时给接口设置最大时间范围和条数。',
      '',
      '如果一百万条已经超过单请求内存预算，就改成数据库聚合、流式分批或外部排序。调大初始容量只能减少扩容，不能解决数据本身装不下。'
    ),{followUps:[
      f('为了避免扩容，初始容量直接设成一千万是不是更稳？',md(
        '不是。HashMap 会先分配很大的桶数组，即使实际只有少量数据也占用堆，还可能让并发请求同时制造内存尖峰。容量应该来自可证明的上界并留适度余量，未知规模时更需要限流和分批。'
      ))
    ]}),
    f('业务键的 hashCode 分布很差，但 Java 8 会树化桶，是不是就不用管了？',md(
      '树化只是极端冲突下的保护，而且还受桶长度和整体容量条件限制。大量冲突仍会增加比较、节点对象和缓存不命中，攻击者可控输入还可能形成哈希碰撞风险。',
      '',
      '我会修正业务键的 `equals/hashCode`，用真实数据看桶分布；如果键是复合字段，优先用不可变键类型，而不是依赖树化替错误哈希兜底。'
    ))
  ],
  'collections/arraylist-linkedlist':[
    f('订单列表中间插入很多，为什么 LinkedList 在压测里仍可能比 ArrayList 慢？',md(
      '如果每次按下标找到插入位置，`LinkedList` 先要从头或尾遍历，插入本身虽是常数时间，定位却是线性的。节点分散还会带来更多对象、指针和缓存不命中；`ArrayList` 的数组移动反而是连续内存复制。',
      '',
      '我会按完整操作链压测，不只比较 `add` 那一步。很多批处理可以先追加到数组列表，最后统一排序，根本不需要反复中间插入。'
    ),{followUps:[
      f('如果已经拿到了 ListIterator，LinkedList 中间插入就一定更合适吗？',md(
        '定位成本确实省掉了，但还要看遍历占比、元素数量和内存压力。一次遍历中少量插入可能合适；大量读取和后续随机访问仍通常偏向数组。选择要覆盖整个生命周期，不是只看当前操作的复杂度。'
      ))
    ]}),
    f('批量订单需要边遍历边删除不合格项，你会怎么写？',md(
      '数量不大时用迭代器的 `remove` 或 `removeIf`，保证修改路径受集合协议支持；数据量大且保留比例高低明确时，我更倾向一次过滤生成新列表，代码更清楚，也避免反复移动数组。',
      '',
      '不能在增强 for 循环里直接调用原列表的 `remove`。如果列表还被其他线程共享，先解决所有权和并发边界，换一种遍历写法并不会自动安全。'
    ))
  ],
  'collections/concurrenthashmap':[
    f('用 ConcurrentHashMap 做计数，代码是 get 后加一再 put，为什么还是丢数据？',md(
      '单次 `get` 和 `put` 各自线程安全，不代表“读取—计算—写回”这个组合是原子的。两个线程可能同时读到 10，最后都写成 11。',
      '',
      '简单计数可以用 `merge`、`compute`，高竞争指标可存 `LongAdder`。但 `compute` 里的函数要短，不能调远程服务，否则会长时间占住对应键的更新路径。'
    ),{followUps:[
      f('LongAdder 的 sum 能当成强一致库存值吗？',md(
        '不合适。`LongAdder` 为吞吐把更新分散到多个槽，`sum` 聚合时不提供事务式快照，适合监控计数，不适合要求每次读取都精确并参与扣减判断的库存。库存要用带原子条件的权威存储。'
      ))
    ]}),
    f('本地缓存用 computeIfAbsent 加载数据，加载函数偶尔很慢，会有什么问题？',md(
      '同一个键的其他请求会等待加载完成，慢加载会形成热点阻塞；加载函数抛异常时也不会缓存结果，流量可能不断重试打向下游。还要避免加载函数再次修改同一 Map 形成递归问题。',
      '',
      '我会给下游调用设置超时，必要时缓存的是带过期和刷新状态的 Future，或者使用成熟缓存库处理单飞、过期和失败退避。'
    ))
  ],
  'collections/set-comparison':[
    f('既要保留插入顺序，又要按金额排序，能不能靠一个 Set 同时完成？',md(
      '这是两种不同视图，通常不应该强塞给一个集合。主存储可以用 `LinkedHashMap/LinkedHashSet` 保留进入顺序，需要金额排序时生成排序视图；或者维护明确的双索引并处理一致性。',
      '',
      '`TreeSet` 只保留比较器认为不相等的元素，如果比较器只比较金额，同金额订单会被吞掉。排序规则必须追加稳定唯一键。'
    ),{followUps:[
      f('给 TreeSet 的比较器最后追加订单 ID 后，还需要 equals 一致吗？',md(
        '最好让排序比较的零结果与业务上的相等语义一致，否则 `contains`、`remove` 的行为会让人意外。若目标本来只是排序列表，我会直接用 List 排序，避免借 Set 同时承担去重和排序两种容易冲突的职责。'
      ))
    ]})
  ],
  'collections/fail-fast-iterator':[
    f('异常堆栈只指向一次普通遍历，你怎么找到真正修改集合的代码？',md(
      '先确认集合的所有权和线程边界，再围绕同一个实例查所有写入路径。必要时在测试或灰度环境包装写操作，记录集合身份、线程和调用栈；只看抛异常的迭代行，通常找不到之前发生的结构修改。',
      '',
      '同线程边遍历边直接删也会触发，不一定是多线程问题。修复要区分使用迭代器删除、先收集后修改，还是消除共享可变集合。'
    ),{followUps:[
      f('换成 CopyOnWriteArrayList 后不抛异常了，是否说明问题解决？',md(
        '只能说明遍历看到的是快照，不代表业务语义正确。写入频繁时每次复制数组成本很高；遍历期间的新数据也不可见。要验证调用方是否接受快照语义、写频率和列表大小，而不是以“不再抛异常”为唯一标准。'
      ))
    ]}),
    f('并发集合的迭代器弱一致，会不会漏掉必须处理的任务？',md(
      '会看到遍历期间某些更新，也可能看不到另一些，因此不能用一次弱一致遍历作为“所有任务都已处理”的完成证明。任务处理应有持久化状态、队列确认或版本边界。',
      '',
      '并发集合适合共享视图，不适合替代可靠任务协议。需要全量快照时，要在业务层建立明确的截点或复制快照。'
    ))
  ],
  'collections/copy-on-write-arraylist':[
    f('监听器只有几十个，但某次配置变更会批量替换，CopyOnWriteArrayList 合适吗？',md(
      '几十个元素、更新很少、通知非常频繁时通常合适。批量替换最好一次构造完整新列表再原子发布，而不是逐个 add 触发几十次数组复制。',
      '',
      '还要决定通知过程中新增或删除监听器是否应该立即生效。它的迭代器读取旧快照，这往往正好保证一轮通知的一致性，但必须和业务约定吻合。'
    ),{followUps:[
      f('某个监听器执行特别慢，会被 CopyOnWriteArrayList 隔离吗？',md(
        '不会。集合只解决遍历时的结构并发，通知如果在同一线程串行执行，慢监听器仍会拖住整轮。需要单独设计超时、线程隔离和失败策略，并避免一个监听失败阻止后续监听器。'
      ))
    ]}),
    f('监听器数量涨到几万并且上下线频繁，你会换什么方案？',md(
      '这时复制整个数组的写放大和旧快照占用都会明显。我会重新审视是否需要进程内保存全部订阅，可能改成按主题分片的并发结构、不可变快照原子替换，或者把订阅关系下沉到专门的消息系统。',
      '',
      '选择前会量化列表大小、写频率、通知频率和可接受的一致性，不能只把类名替换成另一个并发集合。'
    ))
  ],
  'collections/queue-deque-priorityqueue':[
    f('优先级队列里低优先级任务一直饿死，你会怎么改调度策略？',md(
      '单纯按固定优先级排序会让持续到来的高优任务占满队头。我会增加等待时间权重或分级队列配额，例如每处理若干高优任务必须取一个普通任务，并给紧急任务设置独立容量。',
      '',
      '同时监控各优先级等待时间而不只看总吞吐。调度策略的目标是业务 SLA，不是让比较器写得越复杂越好。'
    ),{followUps:[
      f('任务入队后优先级发生变化，直接修改对象字段可以吗？',md(
        '不可以指望堆自动重排。`PriorityQueue` 不知道元素内部字段变了，队头可能继续错误。应移除后重新入队，或保存不可变调度键并通过新事件更新；并发场景使用有明确并发协议的调度器。'
      ))
    ]}),
    f('最近访问记录既要去重又要限制一百条，只用 ArrayDeque 怎么做？',md(
      'Deque 擅长两端操作，但查找并删除中间重复项是线性的。数据只有一百条时可以接受，先删旧值再放队头；若调用很频繁或容量大，我会组合 Map 与双向链表，直接定位节点。',
      '',
      '这就是 LRU 的核心结构。实现前也会确认是否已有成熟缓存库，避免自己维护并发和节点一致性。'
    ))
  ],
  'collections/concurrent-collections':[
    f('共享 Map 里需要同时更新两组键，换成 ConcurrentHashMap 就能保证一致吗？',md(
      '不能。它保证单个操作和部分原子方法，不保证跨两个键的业务不变量。读者可能看到第一组已经更新、第二组还没更新。',
      '',
      '如果两组必须一起可见，我会用一把覆盖该不变量的锁，或者构造包含两组数据的不可变快照后原子替换引用。锁粒度由业务一致性边界决定，不是由集合类型决定。'
    ),{followUps:[
      f('用两个 ConcurrentHashMap，再按同样顺序写入，能不能避免中间状态？',md(
        '只能让写顺序稳定，不能阻止读线程在两次写之间观察。除非读者本来就接受最终一致，否则仍需版本、快照或共同锁。数据分在两个容器里并不会自动获得事务，测试也要主动制造两次写之间的读取。'
      ))
    ]}),
    f('读多写少就一定应该用读写锁吗？',md(
      '还要看临界区时长、读竞争和写延迟要求。非常短的读取用普通互斥锁可能更简单且更快；读操作如果会阻塞，读锁数量再多也可能耗尽线程。',
      '',
      '我会用真实并发比例压测吞吐与尾延迟，并检查写线程饥饿。能用不可变快照消除读锁时，通常比复杂锁协议更容易维护。'
    ))
  ],
  'collections/comparable-comparator':[
    f('订单按金额降序排序后，每次翻页顺序都不一样，比较器哪里有问题？',md(
      '只比较金额时，大量订单比较结果为 0，排序虽然可能保持当前输入顺序，但数据库返回或上游聚合顺序并不稳定。分页边界就会重复或漏数据。',
      '',
      '我会把规则补成金额降序、创建时间降序、订单 ID 降序，并让数据库 `ORDER BY` 与内存比较器使用同一组字段和 null 规则。'
    ),{followUps:[
      f('为了降序直接写成 (int)(b.amount-a.amount) 有什么风险？',md(
        '差值可能溢出，强转还会丢精度，最终违反比较器的传递性。应该使用 `Long.compare`、`BigDecimal.compareTo` 和 `Comparator.reversed()` 组合，并用极值、相等值和随机数据验证排序契约。'
      ))
    ]}),
    f('动态排序字段来自前端，你会直接用反射读取字段再比较吗？',md(
      '我会做白名单映射，把 `amount`、`createdAt` 等请求值映射到预定义比较器或数据库排序表达式。直接按任意字段反射既慢又暴露内部字段，还难以统一 null、类型和稳定排序规则。',
      '',
      '未知字段返回参数错误，所有比较器最后追加稳定唯一键；数据量大时排序应尽量下推数据库，而不是全部拉到内存。'
    ))
  ],
  'collections/immutable-collections':[
    f('配置对象外层 Map 不可变，但里面的 List 仍能修改，这算安全发布吗？',md(
      '不算。只包一层 `unmodifiableMap` 仍可能通过原始引用或嵌套集合修改内容，业务线程就会看到半更新状态。发布前应构造深度不可变的配置快照，至少复制并冻结所有会被共享的嵌套结构。',
      '',
      '构造完成后通过 `volatile` 或 `AtomicReference` 一次替换根对象。业务线程每次请求持有同一版本快照，不在读取过程中拼装。'
    ),{followUps:[
      f('配置很大，深复制成本高，能不能在原对象上局部修改再换引用？',md(
        '如果原对象已经被读线程持有，原地改会破坏旧快照。可以使用结构共享的持久化数据结构，或只复制变化路径，但前提是所有节点本身不可变。不能为了省一次复制，让旧版本也被同步改掉。'
      ))
    ]}),
    f('新配置校验失败时，业务线程应该看到什么？',md(
      '继续使用最后一份通过校验的完整版本，不发布失败快照。配置线程记录版本、失败原因和差异并告警；如果配置已经过期到不可接受，再按业务选择降级或停止接流量。',
      '',
      '发布动作必须在解析和完整校验之后。不能先替换引用，再逐字段发现错误并回滚，那段窗口已经把坏配置暴露给请求。'
    ))
  ]
}
