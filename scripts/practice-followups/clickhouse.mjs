import{follow,md}from'../practice-answers/shared.mjs'

const f=follow

export default{
  'clickhouse/mergetree':[
    f('MergeTree 后台 merge 跟不上，parts 数持续上涨，你会先处理写入还是调 merge 参数？',md(
      '先看 part 产生速度、单批大小和分区分布。大量小批甚至逐行写会持续制造小 part，后台参数调得再激进也可能追不上，还会和查询抢 IO。',
      '',
      '我会先把写入合并到合理批次并控制分区数量，再根据磁盘和 CPU 余量调整后台 merge 并发；用 parts 数、merge 队列和查询 P99 验证，不只看写入成功。'
    ),{followUps:[
      f('为了减少 part，把每批从一万行直接提高到一千万行可以吗？',md(
        '不应跨这么大步。超大批次会增加客户端内存、重试成本和单次延迟，失败时重复范围也更大。按目标吞吐逐级压测批大小，观察 part 生成、网络、服务端内存和端到端延迟，选择平衡点。'
      ))
    ]}),
    f('业务按天分区，但某天数据量特别大，查询和 merge 都变慢，怎么处理？',md(
      '分区不是自动负载均衡单元。先确认排序键能否让查询跳过大部分 granule，以及该天是否存在异常回灌。临时可限制回灌并发、拆批写入并给查询设时间范围。',
      '',
      '长期若单日远超可管理规模，可按月以外的合理维度调整新表分区，但不能为了一个异常日制造海量细分区；迁移前要评估 TTL、备份和 merge 成本。'
    ))
  ],
  'clickhouse/query-optimization':[
    f('查询扫描行数没有增加，但耗时从 3 秒涨到 40 秒，你会看哪里？',md(
      '这时重点转向资源和算子：读取字节是否变大、远端磁盘或网络是否变慢、聚合/排序是否溢写、并发查询是否抢满 CPU 和内存。相同行数并不代表相同列宽和计算量。',
      '',
      '我会对比 `system.query_log` 的 read_bytes、memory_usage、ProfileEvents 和线程使用，再查同时段 merge、mutation 与其他大查询。'
    ),{followUps:[
      f('确认是后台 merge 抢 IO，你会暂停所有 merge 吗？',md(
        '不会长期全停，parts 会继续累积并形成更大问题。先降低回灌和非核心查询，按磁盘余量调低 merge 并发或安排低峰；若必须短暂停止，要有恢复阈值并监控 parts，不能把压力无限后移。'
      ))
    ]}),
    f('加上 PREWHERE 后查询快了，怎样确认不是缓存偶然命中？',md(
      '用多次交替测试并查看读取行数、读取字节和列读取顺序，而不是只比一次墙钟时间。选择性高且列较窄的条件提前过滤，应该稳定减少后续宽列读取。',
      '',
      '还要用典型和最差参数验证。若过滤选择性很低，PREWHERE 可能没有收益，不能把语法变化当优化证据。'
    )),
    f('报表查询必须做全局排序并返回百万行，数据库侧还能怎么优化？',md(
      '先挑战输出需求：交互页面不应返回百万行，改成 Top N、聚合或异步导出。确需导出时设置独立资源组，流式写对象存储，限制并发并允许外部排序。',
      '',
      '排序键能帮助局部读取，但无法让任意全局排序免费。产品形态和执行资源必须一起改。'
    ))
  ],
  'clickhouse/advantages-limitations':[
    f('团队想把 ClickHouse 当订单主库，因为聚合快，你会怎么阻止或验证这个决定？',md(
      '我会把主库必须提供的能力列出来：高频单行更新、强事务、唯一约束、点查尾延迟和故障恢复。ClickHouse 擅长批量追加和列式分析，这些 OLTP 能力并不是它的目标。',
      '',
      '可以让订单事件同步到 ClickHouse 做报表，但权威状态仍在事务数据库。用一轮包含并发更新、重复写和故障恢复的验证，通常比争论产品标签更有说服力。'
    ),{followUps:[
      f('报表要求实时到秒级，是不是就只能直接查主库？',md(
        '不是。通过 CDC/Kafka 把变更持续写入 ClickHouse，合理批次下可以做到秒级；关键是定义可接受延迟和乱序处理。只有必须读到刚提交事务的强一致详情才走主库，分析查询不应因此回压在线库。'
      ))
    ]})
  ],
  'clickhouse/production-optimization':[
    f('面试里说“查询从 20 秒优化到 2 秒”，面试官继续问你怎么证明是你的改动，你会怎么回答？',md(
      '我会给出同一数据快照、同一参数和并发下的对照：改动前后 read_rows、read_bytes、内存、CPU 时间和 P95/P99，而不只报最好的一次耗时。',
      '',
      '同时说明具体改动，例如排序键贴合租户加时间过滤、减少读取宽列，并交代副作用：写入 part、存储或其他查询是否变差。这样结果可复现，也能说明权衡。'
    ),{followUps:[
      f('优化让一条核心报表更快，却让其他查询变慢，你如何决定是否上线？',md(
        '按查询频率、SLA 和资源总成本加权，不让单条 benchmark 决策。必要时用 projection、物化视图或独立表服务不同主访问路径；上线先灰度，监控全体查询分位数和写入成本，并保留回滚。'
      ))
    ]}),
    f('做了物化视图预聚合，数据重算或迟到时怎么保证结果可信？',md(
      '先定义聚合状态是否可增量合并，事件要有版本或去重键；迟到数据按事件时间进入正确分区。重算时写入新目标表或新版本，完成数量与汇总校验后再切读。',
      '',
      '不能在用户查询的同一张聚合表上边清空边重算。原始明细需保留足够时间，才能对账和重建。'
    ))
  ],
  'clickhouse/concurrency-limitations':[
    f('单查询只占 2GB 内存，机器有 64GB，为什么 20 个并发仍可能 OOM？',md(
      '除了简单相乘，还要算后台 merge、缓存、服务进程和查询峰值；不同查询可能同时到达聚合或排序高峰。内存限制触发前也可能因交换或 IO 抖动让延迟恶化。',
      '',
      '我会为用户或工作负载设置并发和单查询内存上限，把交互查询与离线任务隔离，入口排队而不是让所有请求同时开跑。'
    ),{followUps:[
      f('入口排队后用户等待更久，怎样避免只是把超时位置前移？',md(
        '队列必须有长度和等待截止时间，超过就快速返回繁忙或转异步任务；按租户和优先级公平调度，不能让一个大客户占满。监控排队时间与执行时间，容量不足时再扩资源或优化查询。'
      ))
    ]}),
    f('限制 max_threads 能稳定高并发吗？',md(
      '它能降低单查询并行度和瞬时资源占用，但也会延长执行时间，更多查询可能同时滞留。需要和并发数、内存、磁盘带宽一起压测。',
      '',
      '我会为交互和离线查询设不同 profile，不使用一个全局值解决所有工作负载。'
    ))
  ],
  'clickhouse/replacing-mergetree':[
    f('用户查询不能带 FINAL，但又要求不展示重复订单，你会怎么设计？',md(
      '`FINAL` 在大范围查询上成本高，我会在写入链路尽量幂等，以订单 ID 和版本控制重复；对查询频繁的最新状态，建立按 key 聚合的物化视图或使用 `argMax` 读取最高版本。',
      '',
      '具体方案取决于查询范围。不能把后台 merge 何时发生当正确性协议，因为它没有固定完成时间。'
    ),{followUps:[
      f('argMax 按版本取最新，如果两个事件版本相同怎么办？',md(
        '必须补充确定性的冲突规则，例如版本加事件序号、来源优先级或摄取时间，并在上游保证同一业务版本不产生不同内容。只按一个可能重复的版本比较，结果会随合并顺序不稳定。'
      ))
    ]}),
    f('发现重复率突然升高，你会先跑 OPTIMIZE FINAL 清理吗？',md(
      '不会把它当第一动作。全表强制合并可能消耗大量 IO，且上游仍重复写时很快复发。先按分区和来源统计重复，修复生产者重试、消费幂等或版本生成。',
      '',
      '确需清理时按受影响分区低峰执行，监控 merge 和查询延迟，并保留容量余量。'
    ))
  ],
  'clickhouse/partition-order-primary-key':[
    f('租户很多且常按 tenant_id 查询，直接按租户分区有什么问题？',md(
      '会产生海量小分区，part 管理、merge 和元数据都恶化。分区主要服务生命周期管理，我通常按月或天；租户与时间放进排序键，让查询在分区内跳过数据。',
      '',
      '只有少数超大租户需要独立生命周期时才考虑更细策略，也要控制总分区数。查询过滤列不等于分区列。'
    ),{followUps:[
      f('排序键把 tenant_id 放第一位，会不会让全站时间报表变慢？',md(
        '可能，因为不带租户的时间查询难以利用前缀局部性。这说明有两条主访问路径；可为全站报表建按时间组织的投影、物化视图或独立宽表，而不是强求一套排序同时最优。'
      ))
    ]}),
    f('表已经有主键，为什么查询仍扫描很多 granule？',md(
      'ClickHouse 主键是稀疏索引，是否跳过取决于过滤条件与排序键前缀及数据相关性。条件只命中排序键后半段，或者范围选择性很低，就仍会读很多 granule。',
      '',
      '我会从 query log 和 explain 看跳过比例，用真实过滤组合重新评估排序键或增加合适 projection，而不是把主键理解成 MySQL 唯一点查索引。'
    ))
  ],
  'clickhouse/data-skipping-index':[
    f('给高基数字段加 bloom_filter 后索引体积很大，查询却没变快，为什么？',md(
      '要看查询是不是等值/集合条件、值在 granule 间是否可被排除，以及误判率。若几乎每个 granule 都包含目标类型的值，Bloom 无法跳过多少数据，只增加写入和存储。',
      '',
      '我会用 `EXPLAIN indexes=1` 和 read_rows/read_bytes 比较命中前后，再决定删除。索引存在不等于有效跳数。'
    ),{followUps:[
      f('把 granularity 调得更小能提高跳过率，代价是什么？',md(
        '索引记录更多，存储、写入和判断开销都会增加，merge 也更重。应在代表数据上逐档测试跳过比例和总耗时；如果数据本身没有聚集性，缩小粒度也未必带来足够收益。'
      ))
    ]}),
    f('状态字段只有五个值，适合加 set 跳数索引吗？',md(
      '不只看全局基数，要看每个 granule 内的值集合。如果数据按状态高度聚集，set 索引能排除不相关块；如果每个块五种状态都有，就几乎无效。',
      '',
      '先采样 granule 分布，并比较常用状态查询的跳过率。低基数本身不是充分条件。'
    ))
  ],
  'clickhouse/distributed-table':[
    f('按 user_id 分片后，企业租户的查询集中到一个节点，你会直接改分片键吗？',md(
      '先确认企业用户是否真是单个 user_id 热点，以及查询能否拆成更细业务键。对新数据可用租户内哈希或复合键分散，但改分片键意味着历史重分布和查询路由变化。',
      '',
      '我会先做资源隔离或特殊租户路由，再设计新表迁移；不能在线修改规则后让同一用户的新旧数据散落却没有查询兼容。'
    ),{followUps:[
      f('迁移期间同一用户数据分布在新旧两套路由，查询怎么不漏？',md(
        '路由表带版本和迁移状态，处于迁移中的键受控双读并去重；增量双写或由日志同步，完成数量与校验后切到新版本。不能让客户端用当前哈希自行猜旧数据位置，旧路由还要保留到回滚窗口结束。'
      ))
    ]}),
    f('Distributed 查询所有分片时，一个慢分片会怎样影响结果？',md(
      '整体通常受最慢分片拖累，慢分片还可能让协调节点保留其他分片结果和内存。我要定位是数据倾斜、硬件、merge 还是网络，并给查询设置超时和资源限制。',
      '',
      '业务能接受部分结果时要显式标记，不能静默缺分片；强一致报表则宁可失败或转异步。'
    ))
  ],
  'clickhouse/write-optimization':[
    f('客户端批量写入失败后整批重试，怎样避免产生大量重复？',md(
      '每条事件带稳定 eventId 或业务版本，服务端表模型和查询能按它去重；客户端记录批次状态，先区分明确失败与响应丢失。不能每次重试生成新 ID。',
      '',
      'ClickHouse 的去重能力有边界，跨节点、超长时间重试不能只靠 block 去重。核心仍是可识别重复和可重放的写入协议。'
    ),{followUps:[
      f('为了绝对不重复，在写入前先逐条查询是否存在可行吗？',md(
        '不可行，会把批量追加变成大量点查并引入读写竞态：两次写仍可能同时查到不存在。应通过稳定幂等键、版本和后台合并/查询去重解决，必要时在上游事务库或流处理层保证事件唯一，并定期核对重复率。'
      ))
    ]}),
    f('Kafka 消费批次调大后吞吐提高，但消费重平衡频繁，可能是什么原因？',md(
      '单批处理和写入时间可能超过消费者心跳或 poll 间隔约束，Kafka 认为实例失活触发重平衡。我要比较批处理耗时、`max.poll.interval`、心跳和 ClickHouse 写入尾延迟。',
      '',
      '批次需要有上限和超时，可把拉取与有界写入解耦，并在确认 ClickHouse 成功后再提交位点。不能只追求最大批次。'
    ))
  ]
}
