import{follow,md}from'../practice-answers/shared.mjs'

const f=follow

export default{
  'mysql/btree-index':[
    f('这个接口有时会带 status 条件，你会把 status 放进 user_id 和 created_at 之间吗？',md(
      '我不会看到 WHERE 里有 `status` 就塞进去，先看它是不是必传以及选择性。若状态可选，把它夹在用户和时间之间，会让不带状态的主查询无法继续利用后面的时间有序性。',
      '',
      '通常保留主索引 `(tenant_id,user_id,created_at,id)`；某个状态查询量大、扫描放大明显时，再为那条稳定访问路径增加 `(tenant_id,user_id,status,created_at,id)`，并评估多一条索引对订单写入的成本。'
    ),{followUps:[
      f('两个索引都能用，怎么证明新增的状态索引值得长期保留？',md(
        '用真实状态分布和典型用户回放，比较扫描行数、回表、P99 与 buffer pool 命中，同时测订单写入 TPS、页分裂和索引体积。若它只改善低频查询却明显放大核心写入，我会改查询产品约束或走异步报表，不长期保留。'
      ))
    ]}),
    f('联合索引和游标分页都做了，超级用户查一年订单还是慢，下一步怎么处理？',md(
      '先确认返回需求。若页面只看最近订单，就限制时间窗口并提供归档查询；如果确实要遍历几十万条，不能继续当同步列表接口，应改成异步导出或历史库查询。',
      '',
      '索引能减少无关扫描，但不能让返回几十万行变成常数成本。对超级用户还会做单租户并发限制，避免一个请求占住大量连接和 IO。'
    )),
    f('订单表正在高峰写入，新增这个联合索引怎样降低上线风险？',md(
      '先在同规模副本或影子数据上测建索引时间、临时空间和写入影响，确认数据库版本支持的在线 DDL 能力。生产按低峰执行，设置锁等待上限，持续观察复制延迟、磁盘、事务和订单写入 P99。',
      '',
      '我会准备明确终止条件和回滚，不把“ONLINE”理解成零影响；大表 DDL 的扫描、日志和最后元数据锁仍可能影响业务。'
    ))
  ],
  'mysql/transactions-mvcc':[
    f('两个请求都读到订单是 PAID，一个退款、一个发货，MVCC 能避免状态走错吗？',md(
      '不能。两边的快照读都可能合法看到 `PAID`，MVCC 只是让读不阻塞写。真正约束状态迁移的是条件更新，例如退款要求当前仍为 `PAID`，发货要求当前为 `PAID` 且未退款，并带版本号。',
      '',
      '更新影响 0 行后重新读取当前状态，决定幂等成功还是冲突。不能把隔离级别当作订单状态机。'
    ),{followUps:[
      f('把隔离级别升到 SERIALIZABLE 能不能省掉状态条件？',md(
        '不能省。更高隔离会增加锁和冲突，但业务允许哪些状态迁移仍需要显式表达；多服务和消息重试也不在一个数据库事务里。用 SERIALIZABLE 替代状态条件，成本更高且边界仍不完整。'
      ))
    ]}),
    f('线上 Undo 持续增长，你怎么证明是长事务导致的？',md(
      '我会查活跃事务的开始时间、隔离级别和当前 SQL，找到长时间不提交的读写事务，再看 history list length 与其时间是否对应。常见来源是分页导出、事务里远程调用或连接归还前未结束事务。',
      '',
      '先终止确认安全的异常任务，再把长查询改成分批且不持有超长快照，缩短事务边界；同时监控 purge 是否逐步追上。'
    ))
  ],
  'mysql/sql-execution-explain':[
    f('线上 SQL 有写入副作用或扫描很大，不能直接跑 EXPLAIN ANALYZE，你怎么拿到可靠证据？',md(
      '先用普通 `EXPLAIN`、慢日志、Performance Schema 和真实参数确认计划与等待；把生产统计信息和一份脱敏数据复制到只读副本，再执行 `EXPLAIN ANALYZE`。',
      '',
      '如果副本数据分布不同，结论也不可靠，所以要对比关键列基数、热点租户和时间范围。线上取证的原则是先低风险观察，不能为了诊断再制造一次事故。'
    ),{followUps:[
      f('测试副本计划正常，但生产仍慢，你会优先比较哪些环境差异？',md(
        '比较真实参数与数据倾斜、统计信息版本、索引可见性、buffer pool 热度、并发锁等待和存储延迟。执行计划相同也可能因为缓存和并发环境完全不同而耗时不同；要把数据库执行与等待时间分开看。'
      ))
    ]}),
    f('actual rows 和估算很接近，但算子耗时仍然很高，说明什么？',md(
      '这时不应继续围绕基数估算打转。可能是大量随机回表、磁盘读、排序落盘、锁等待，或者单行处理本身很重。我要看该算子的 loops、每行成本、临时表和 IO 指标。',
      '',
      '修复可能是覆盖必要字段、减少返回列、调整连接方式或治理资源竞争。`ANALYZE TABLE` 只对估算问题有帮助，不是所有慢 SQL 的通用答案。'
    )),
    f('加 FORCE INDEX 后当前参数快了十倍，你会直接上线长期使用吗？',md(
      '不会。我要用普通用户、超级用户、不同时间范围和状态分布验证，因为强制索引关闭了优化器针对其他参数选择更好计划的机会。还要确认数据增长后该假设是否持续成立。',
      '',
      '它可以作为有监控和开关的短期止损，长期仍应修统计信息、SQL 或索引设计，并记录移除条件。'
    ))
  ],
  'mysql/locks-deadlock':[
    f('死锁日志显示同一张订单表、同一条 UPDATE，为什么仍可能死锁？',md(
      'SQL 文本相同不代表锁顺序相同。两批请求的 `IN (...)` 参数顺序、执行计划或扫描范围不同，可能按不同记录顺序拿锁；缺少索引还可能扫描并锁住大量不同行。',
      '',
      '我会还原实际参数和索引锁记录，把批量 ID 排序后按统一顺序更新，并补能精确定位的索引。只看表名相同就认为不会死锁，是常见误判。'
    ),{followUps:[
      f('把批量更新拆成单条提交能彻底解决吗？',md(
        '可能降低每次持锁范围，但会失去批次原子性、增加提交成本，并不能消除与其他路径的反向顺序。要先明确业务是否允许部分成功，再决定拆批；核心修复仍是统一顺序和缩小锁范围。'
      ))
    ]}),
    f('应用捕获死锁后只重试最后一条 SQL，为什么不正确？',md(
      'InnoDB 选择牺牲者后回滚的是整个事务，前面成功执行的语句也已经撤销。只重试最后一条会留下缺失步骤。',
      '',
      '应该从事务边界整体重放，重建上下文，并用业务请求号和状态条件保证副作用幂等；重试次数有限且带抖动，超过后进入明确失败或补偿。'
    ))
  ],
  'mysql/redo-undo-binlog':[
    f('把 innodb_flush_log_at_trx_commit 从 1 改成 2，业务实际接受的是什么风险？',md(
      '提交时 redo 只写到操作系统缓存，MySQL 进程崩溃通常还能保住，但操作系统或机器断电可能丢失最近约一秒的已提交事务，实际窗口也受调度影响。',
      '',
      '是否能改要由订单业务明确 RPO，并通过断电级演练验证，不能只看 TPS 提升。若 binlog 同步策略不同，还要考虑两套日志持久性组合。'
    ),{followUps:[
      f('有半同步复制就可以把本地刷盘策略调松吗？',md(
        '不能直接等价。半同步确认的具体时点和副本刷盘策略需要核实，网络分区时也可能退化；故障切换还涉及选主与数据一致性。要从端到端 RPO 证明至少一份持久副本，而不是看到“有副本”就放松主库。'
      ))
    ]}),
    f('binlog 都保留了，为什么仍然必须做全量备份和恢复演练？',md(
      '从空库重放全部 binlog 时间不可接受，而且 binlog 可能缺失、损坏或已过保留期。正确恢复通常是最近全量备份加之后的增量日志，再恢复到目标时间点。',
      '',
      '演练要在隔离环境真正启动恢复库，校验关键订单和耗时。只有文件存在不能证明 RTO 和数据可用性。'
    ))
  ],
  'mysql/replication-high-availability':[
    f('所有写后读都强制走主库，为什么不是一个可长期使用的简单方案？',md(
      '它能保证读到最新提交，但会把列表、详情和重试流量全部压回主库，失去读扩展能力，故障时更容易把主库打满。不同场景的一致性要求并不相同。',
      '',
      '我会只给支付结果、刚修改资料等关键窗口做主库粘滞或位点等待，普通列表允许短暂延迟并明确展示。用业务分级换取容量，而不是全站一种策略。'
    ),{followUps:[
      f('位点等待超过 200ms 仍没追上，接口该继续等还是回主库？',md(
        '由接口延迟预算决定，通常到截止时间就回主库或返回处理中，不让请求无限等待。回主库也要有限流，避免副本延迟时所有读瞬间击穿主库；超过阈值应触发流量降级和复制延迟告警。'
      ))
    ]}),
    f('发现复制延迟由一个两千万行更新事务造成，你会怎么处理和预防？',md(
      '先评估是否能暂停或终止来源任务，保护线上；已经提交的大事务只能让副本应用完，期间把关键读切主并限制非核心流量。不能随意跳过复制事件破坏一致性。',
      '',
      '预防是按主键小批更新、每批提交并监控延迟，给后台任务设置速率和停止开关；DDL 与大批任务都要进变更流程。'
    ))
  ],
  'mysql/slow-sql-troubleshooting':[
    f('连接池已经满了，是否应该立即 KILL 当前最慢的查询？',md(
      '先确认它是可终止的只读查询还是关键写事务。终止报表查询通常风险较低；杀掉写事务会触发回滚，回滚本身可能长时间占资源，并改变业务结果。',
      '',
      '我会按 SQL 指纹、来源、运行时间和事务状态筛选，优先限入口和暂停批任务，再 `KILL QUERY` 明确失控的语句。所有动作记录实例、线程和业务影响。'
    ),{followUps:[
      f('杀掉查询后连接池仍然恢复很慢，你会继续查什么？',md(
        '检查事务是否仍在回滚、锁等待链是否解除、连接是否被应用泄漏，以及数据库 CPU/IO 是否仍饱和。`KILL QUERY` 只停当前语句，不一定结束事务；应用端也可能继续重试制造新查询。'
      ))
    ]}),
    f('优化后单条 SQL 从 2 秒降到 80ms，为什么还不能直接认为事故解决？',md(
      '要乘以调用量并看端到端等待。80ms 如果每秒两万次仍可能耗尽数据库，连接池排队、锁和网络也可能继续占据接口 P99。',
      '',
      '我会在接近真实并发下比较吞吐、Rows_examined、CPU/IO、连接等待和错误率，并确认新索引没有明显拖慢核心写入。'
    ))
  ],
  'mysql/table-design-guide':[
    f('运营提出十几个筛选条件，你会为每一种组合都建联合索引吗？',md(
      '不会。先按调用量和 SLA 收敛出几条主访问路径，核心在线查询建少量联合索引；低频任意组合走搜索或分析系统。十几个组合全建会让每次订单写入维护大量 B+Tree。',
      '',
      '索引评审同时看读收益、磁盘、buffer pool 和写放大。产品筛选能力也要有时间范围和排序约束，不能把数据库当任意查询引擎。'
    ),{followUps:[
      f('怎么找到一条长期没人使用、可以删除的索引？',md(
        '结合索引使用统计、慢日志和代码访问路径观察完整业务周期，先将候选索引设为不可见并灰度验证，监控计划和延迟，再决定删除。不能只看某一天计数为零，月结或故障查询可能低频但关键。'
      ))
    ]}),
    f('历史订单归档后，用户跨冷热数据翻页怎么保证顺序和不重复？',md(
      '先定义冷热边界，例如固定截止时间，并让查询路由知道边界。游标包含排序键和数据源版本：先读热库，越过边界后切历史库；迁移窗口需要双读去重或在完成校验后再切。',
      '',
      '不要把两个库结果每页临时全量合并。归档过程要保证同一订单在可见视图中只有一个权威版本。'
    )),
    f('趋势递增全局 ID 会不会把写入都压在 B+Tree 最右侧形成热点？',md(
      '在单机 InnoDB 上，顺序主键通常减少随机页分裂，右侧写热点多半仍比随机 UUID 友好；到了分片系统，热点取决于 ID 如何映射到分片，不能只看索引页。',
      '',
      '我会让 ID 与分片路由解耦或混入槽位，并用目标写并发压测。不能为追求“绝对随机”牺牲主键局部性。'
    ))
  ],
  'mysql/deep-pagination':[
    f('导出过程中有订单状态更新，如何保证用户拿到的文件口径一致？',md(
      '先和产品定义口径。若是“任务创建时的订单集合”，记录截止时间或最大 ID，并在每批查询带上边界；状态是否也要求快照，要看是否有历史版本来源。',
      '',
      '我不会为了严格快照持有几小时数据库事务。需要审计级一致时，从 binlog/数仓快照生成；普通运营导出可以接受状态在导出过程中的最终值，但必须写清口径。'
    ),{followUps:[
      f('只记录最大 ID，能保证按 created_at 导出时集合不变化吗？',md(
        '不一定。补录数据可能 ID 更大但 created_at 更早，已有订单的时间也可能被修改。边界必须匹配数据生成和排序语义，必要时同时使用任务创建时间、不可变主键或独立快照表，不能随便挑一个字段当水位。'
      ))
    ]}),
    f('异步导出任务跑到 60% 失败，怎样从断点恢复又不重复写文件？',md(
      '持久化上一批确认完成的复合游标和分片文件编号，每批文件写到临时名，完成后原子标记。重试从已确认游标继续，最终合并或生成清单。',
      '',
      '任务和分片要有幂等键，重复 worker 只能有一个获得执行租约；下载地址只在全部分片校验通过后发布。'
    ))
  ],
  'mysql/sharding':[
    f('按 user_id 分片后出现一个拥有千万订单的超级用户，这个热点怎么处理？',md(
      '普通用户按 user_id 路由仍然有效，但超级用户需要单独策略，例如在路由表中把其订单按时间或虚拟子槽拆开，并让查询按游标访问有限分片。不能为一个异常用户立刻改变所有用户规则。',
      '',
      '写入、查询和迁移都要识别这类特殊路由，并设置单用户并发预算。热点治理是路由能力的一部分，不只是扩机器。'
    ),{followUps:[
      f('超级用户拆成多个子分片后，订单号点查会不会广播？',md(
        '不应该。维护订单号到物理分片的轻量路由索引，或让全局 ID 编码可解析的槽信息，点查先定位再访问单片。广播只适合极低频后台任务，不能进入核心详情接口。'
      ))
    ]}),
    f('扩容迁移期间同时有订单写入，怎么避免复制完快照后漏掉增量？',md(
      '先记录一致的增量起点，再搬历史快照，同时订阅 binlog 或变更事件；快照完成后追平增量，持续做数量和校验和核对。切读前进入短暂双写/双读校验窗口，并保留旧路由回滚。',
      '',
      '关键是快照与增量有可证明的衔接位点。先全量复制、过几小时再开增量，中间一定存在丢数据窗口。'
    )),
    f('财务要按时间查全站订单，你会在几十个分片上实时聚合吗？',md(
      '核心接口不会。订单事件应持续同步到按财务维度组织的分析库或汇总表，报表在那里查询；少量对账点查可以受控并行访问分片。',
      '',
      '跨片实时排序和分页成本随分片数增长，还会把报表压力扩散到全部在线库。分片设计必须同时规划异构读模型。'
    ))
  ]
}
