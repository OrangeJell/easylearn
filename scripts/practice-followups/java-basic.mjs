import{follow,md}from'../practice-answers/shared.mjs'

const f=follow

export default{
  'java-basic/string-immutable':[
    f('如果签名方法收到的是一个可变对象，你会怎么避免计算过程中字段被别人改掉？',md(
      '我不会直接拿调用方传进来的可变 DTO 做签名。进入签名模块时先挑出协议规定的字段，完成格式化并生成一份不可变快照，再按固定顺序计算。这样即使其他线程继续修改原 DTO，这次签名使用的输入也不会漂移。',
      '',
      '同时要固定空值、金额、时间和字符集规则。真正保护签名结果的不是一句“不要修改对象”，而是**在边界处把可变业务对象转换成稳定值**。'
    ),{followUps:[
      f('为了少创建对象，能不能直接缓存一个 StringBuilder 给多个请求复用？',md(
        '不能把同一个 `StringBuilder` 交给并发请求复用，它本身不保证线程安全，内容还可能残留。局部创建通常会被 JVM 很好地处理；真有分配压力，也应先用 JFR 证明，再考虑严格清理的线程内复用，而不是用共享可变对象换取不可控的数据串扰。'
      ))
    ]}),
    f('线上发现字符串拼接让 Young GC 明显升高，你会直接把所有加号改成 StringBuilder 吗？',md(
      '我会先看分配热点落在哪些调用点。常量表达式和单条语句里的少量 `+`，编译器本来就会优化；真正值得改的是循环里反复把大字符串接回原变量，或者日志参数提前完成了昂贵拼接。',
      '',
      '修复会优先改热点循环、使用参数化日志，并给预估容量；改完比较分配速率、Young GC 次数和接口 P99。全局机械替换既增加噪音，也未必改变实际字节码。'
    ))
  ],
  'java-basic/equals-hashcode':[
    f('订单还没入库时 id 是 null，入库后才有值，如果 equals 用数据库 id 会发生什么？',md(
      '对象加入 `HashSet` 时如果 `id` 还是 null，入库后 id 改变会让 `hashCode` 跟着变，集合仍把对象放在旧桶里，后续 `contains` 和 `remove` 都可能失败。两个未入库对象也可能因为 id 都是 null 被错误地当成相等。',
      '',
      '我会用写入前就稳定的业务键做去重，或者单独创建不可变的 `OrderDedupKey`。实体标识和一次业务请求的幂等键不是同一个概念。'
    ),{followUps:[
      f('如果业务键本身包含可修改字段，例如手机号，应该怎么办？',md(
        '这说明它不适合作为哈希集合中的长期身份。要找一个生命周期内不变的键，例如租户加请求号；实在没有，就在进入去重流程时生成不可变快照键。不要把会变化的字段塞进 `hashCode` 后再要求所有代码都记得不能改。'
      ))
    ]}),
    f('已经有 HashSet 去重了，为什么数据库还必须加唯一约束？',md(
      '`HashSet` 只能覆盖当前进程、当前批次。两个实例同时处理同一请求、服务重启后重放消息，或者两条链路分别创建订单，都不会共享这份内存状态。',
      '',
      '数据库唯一约束才是最终并发防线。应用先查可以改善提示，但写入时仍以唯一键冲突为准，并把冲突转换成“返回已有结果”或明确的幂等响应。'
    ))
  ],
  'java-basic/generics':[
    f('RPC 客户端把 RpcResult<List<OrderDTO>> 反序列化成了 List<Map>，你会在哪一层修？',md(
      '我会在网络客户端或序列化适配层修，不让 `Map` 泄漏到业务代码。调用方需要把完整的参数化类型传到反序列化器，例如 `TypeReference<RpcResult<List<OrderDTO>>>`，客户端据此恢复两层泛型。',
      '',
      '如果每个业务方法都手写转换，迟早会出现强转错误和字段规则不一致。类型恢复应该发生在最靠近字节流的边界，业务层拿到的就是确定类型。'
    ),{followUps:[
      f('框架想缓存反序列化器，只用 Class 作为缓存 Key 够吗？',md(
        '不够。`RpcResult<OrderDTO>` 和 `RpcResult<List<OrderDTO>>` 的原始 `Class` 都只是 `RpcResult`，会误用同一个缓存项。缓存键必须保留完整 `Type`，包括嵌套参数；上线前还要覆盖不同泛型组合并发首次创建的场景。'
      ))
    ]}),
    f('统一返回对象里有人建议把 data 改成 Object，理由是接口更灵活，你怎么回应？',md(
      '这会把协议约束推迟到运行时，调用方到处强转，文档和生成代码也失去类型信息。我会保留 `RpcResult<T>`，真正不固定的扩展字段放进有边界的扩展结构，而不是牺牲整个载荷类型。',
      '',
      '灵活性要落在明确的版本演进方案上，例如新增可选字段、兼容旧消费者；把所有东西改成 `Object` 只是让错误更晚暴露。'
    ))
  ],
  'java-basic/exception-handling':[
    f('调用支付渠道超时了，你会把它归为系统异常然后直接重试吗？',md(
      '我会单独标记为“结果未知”，因为超时只说明我没收到响应，不代表渠道没扣款。第一动作通常是用业务请求号查单；只有确认未受理且接口具备幂等能力，才做有限重试。',
      '',
      '对用户可以显示处理中，对内部要保存渠道、请求号、耗时和原始错误。把它当普通系统异常重试，最坏结果是重复扣款。'
    ),{followUps:[
      f('查单接口也超时，当前数据库事务要一直等着吗？',md(
        '不会。先把本地记录落成 `PROCESSING` 或结果未知并提交，释放数据库连接和锁，再由异步任务按退避策略查单。事务里等待第三方只会放大连接池和锁风险，而且并不能让远端结果更确定。'
      ))
    ]}),
    f('同一个异常从 DAO 到 Controller 被记录了四遍日志，你会怎么改？',md(
      '中间层如果只是补充语义，就包装并保留 cause，不重复打印。真正决定“返回什么、是否重试或是否吞掉”的边界记录一次完整日志，统一带 traceId、业务主键和下游信息。',
      '',
      '业务校验失败通常只记结构化指标或采样日志；未知系统异常才打印堆栈。这样告警数量能对应故障请求，而不是对应调用层数。'
    ))
  ],
  'java-basic/reflection-annotations':[
    f('自定义审计注解加在方法上却没有生效，你会按什么顺序排查？',md(
      '我先确认调用是否经过代理：类是不是容器管理的 Bean、是否发生同类内部调用、方法和注解是否满足代理方式的可见性要求。然后看切点是否匹配到实现方法，以及注解在接口还是实现类上。',
      '',
      '不要一上来怀疑“反射失效”。大多数问题发生在代理入口根本没有经过。修复后会用集成测试从真实 Bean 调用，验证审计记录，而不只直接 new 对象测方法。'
    ),{followUps:[
      f('为了支持内部调用，改成从容器里获取自己再调用自己合适吗？',md(
        '这会让业务类依赖容器并隐藏调用关系，我通常会把需要被拦截的方法拆到独立协作 Bean，或者把审计做成显式组件调用。只有非常局部且团队有明确约定时才考虑暴露代理，不能把它当默认修复。'
      ))
    ]}),
    f('启动扫描从 8 秒涨到 40 秒，但业务代码没明显变化，你怎么找范围膨胀的位置？',md(
      '我会先拆启动阶段耗时，确认是类路径扫描、Bean 实例化还是代理生成。若扫描慢，再对比发布前后的扫描包、依赖数量和自动配置报告，常见原因是基础包写得过大，把第三方依赖也扫进来了。',
      '',
      '修复是缩小明确包范围、移除重复扫描，并缓存已解析元数据；最后在同一镜像和资源限制下比较启动时间，避免只在开发机上判断。'
    ))
  ],
  'java-basic/serialization':[
    f('Kafka 事件新增了一个枚举值，旧消费者反序列化直接失败，怎么补救和预防？',md(
      '先隔离失败消息，不能因为持续重试把整个分区卡死，也不能跳过后直接提交。短期给旧消费者增加未知值兜底并重新发布，再从原位点或隔离队列重放。',
      '',
      '预防上，协议里的枚举要有 `UNKNOWN`，消费者对新增字段和未知值保持前向兼容；发布流程先升级消费者再让生产者发送新值，并用契约测试覆盖新旧版本组合。'
    ),{followUps:[
      f('失败消息进死信队列后，怎样避免修复时再次产生业务重复？',md(
        '死信重放仍然是一次可能重复的消费，必须沿用原事件 ID 或业务幂等键。重放工具要记录操作者、范围和结果，先小批验证；消费者不能因为消息来自死信就绕过唯一约束和状态条件。'
      ))
    ]}),
    f('Redis 里存的是旧版 Java 对象，新版本删了字段并改了类名，如何平滑迁移？',md(
      '我不会要求新代码硬兼容不可控的 Java 原生字节流。更稳妥的是使用新 key 前缀或版本字段：新版本先读新格式，未命中时读旧格式并转换回填；写入只写新格式，等旧 key 自然过期。',
      '',
      '迁移前统计旧 key 数量和 TTL，灰度观察反序列化失败与回源压力。缓存可以重建，方案重点是可回滚和不把瞬时回源打到数据库。'
    ))
  ],
  'java-basic/interface-abstract-class':[
    f('新支付渠道不支持退款，现有大接口却要求实现 refund，你会怎么演进？',md(
      '我会把能力拆成小接口，例如支付、查单、退款分别表达，渠道只注册自己支持的能力。路由层在调用前根据能力元数据拒绝，而不是让实现类在运行时抛一个模糊的 `UnsupportedOperationException`。',
      '',
      '拆接口时保持旧适配层一段时间，逐渠道迁移，避免一次改动所有调用方。能力边界清楚后，新增渠道不会被迫实现伪逻辑。'
    ),{followUps:[
      f('调用方每次都判断渠道能力，会不会把 if/else 搬到另一处？',md(
        '不会让业务调用方自己判断。由渠道注册表和编排器统一选择实现，业务只提交退款命令；不支持时返回稳定的领域结果。判断集中在路由边界，和散落在每个业务流程里的渠道分支是两回事。'
      ))
    ]}),
    f('十几家渠道都有超时查单逻辑，你会放进抽象父类吗？',md(
      '我更倾向把“结果未知后的查单编排”做成独立组件，组合渠道的 `query` 能力。因为退避、任务状态、告警和幂等属于跨渠道流程，不是渠道实现的继承属性。',
      '',
      '父类只适合非常稳定的协议骨架；一旦不同渠道在重试次数、回调优先级和查询窗口上分叉，组合策略比不断增加受保护方法更容易测试和替换。'
    ))
  ],
  'java-basic/overload-override':[
    f('SDK 只新增了一个重载，为什么没升级 SDK 的服务也可能在下次发布后改变行为？',md(
      '已经运行的旧字节码不会改变，但服务下次构建时会拿新 SDK 重新编译，编译器会重新选择最具体的重载。因此风险发生在“重新编译”而不是“运行时动态发现”。',
      '',
      '我会对比依赖锁文件和构建产物，用 `javap` 确认调用描述符，并补源码兼容测试。公共 SDK 发布流程也应检查新增重载对 null、装箱和可变参数调用点的影响。'
    ),{followUps:[
      f('短期不能改 SDK，只能改调用方，怎样让绑定结果不再含糊？',md(
        '把意图写进静态类型：不要传裸 `null`，先声明明确类型或做显式强转；装箱场景也明确使用基本类型还是包装类型。更长期的方案是把语义不同的方法改成不同名称，避免靠重载猜业务意图。'
      ))
    ]}),
    f('如果子类重写方法后行为变了，你怎么区分是重载选择问题还是动态分派问题？',md(
      '先看编译后的目标方法描述符，它决定调用的是哪个重载签名；再看接收对象的实际类型，它决定该签名最终执行哪个重写实现。前者由调用点静态类型在编译期确定，后者才在运行期分派。',
      '',
      '排查时把这两步分开，不要只打印实际对象就断定编译器选了哪个重载。'
    ))
  ],
  'java-basic/pass-by-value':[
    f('一个方法既修改传入 DTO 又返回另一个 DTO，调用方经常用错，你会怎么重构这个边界？',md(
      '我会先选一种清晰语义。计算或转换方法返回新对象，不修改输入；确实负责填充状态的方法用明确命名，并尽量不再返回另一个容易混淆的对象。',
      '',
      '在核心订单流程中，我更偏向不可变命令和显式返回结果，这样单测能直接断言输入未变化。重构期间用测试锁住旧副作用，再逐调用点迁移，不能只改方法签名。'
    ),{followUps:[
      f('复制一个 DTO 就能保证没有副作用吗？',md(
        '不一定。浅复制只复制第一层引用，里面的 `List`、地址对象等仍可能共享，修改嵌套数据照样影响原对象。要根据所有权决定深复制、不可变集合或重新组装需要的字段，不能把 `clone` 当成通用隔离。'
      ))
    ]})
  ],
  'java-basic/autoboxing-integer-cache':[
    f('把 Long 和 long 比较时线上偶发空指针，明明代码里没有调用任何方法，为什么？',md(
      '比较时包装类型会自动拆箱，`Long` 为 null 就在拆箱阶段抛 `NullPointerException`。堆栈往往指向一行很普通的 `userId == requestId`，容易被误判。',
      '',
      '我会在输入边界明确 null 是否合法：不合法就尽早校验并转成 `long`；合法就用 `Objects.equals` 表达两个可空值的比较，避免隐式拆箱。'
    ),{followUps:[
      f('为了避免空指针，统一写成 0L.equals(userId) 可以吗？',md(
        '技术上不会空指针，但会把“缺失”和“值为 0”混在一起，而且业务含义很差。应先定义 null 的语义，再选择校验或可空比较；用一个哨兵常量掩盖空值，后续很容易把脏数据当成合法 ID。'
      ))
    ]}),
    f('测试环境一直没复现 Long 用双等号的问题，你会怎样写一个能稳定暴露它的用例？',md(
      '我会同时覆盖缓存范围内和范围外的值，例如 100 和 1000，并显式构造来自 JSON、数据库或 `Long.valueOf` 的两个独立包装对象。断言业务比较使用数值语义，而不是依赖是否碰巧复用了缓存对象。',
      '',
      '再补 null 与基本类型混合比较。这样测试验证的是代码契约，不是某个 JVM 缓存实现恰好让引用相同。'
    ))
  ]
}
