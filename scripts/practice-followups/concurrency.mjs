import{follow,md}from'../practice-answers/shared.mjs'

const f=follow

export default{
  'concurrency/thread-pool':[
    f('商品和营销调用都是 IO 任务，你会用什么数据来确定线程数，而不是套一个公式？',md(
      '我会拿压测或线上 trace 里的单任务总耗时、真正占用 CPU 的时间、目标 QPS 和下游并发上限估算，再从保守值压测。线程数即使本机扛得住，也不能超过下游连接池和限流容量。',
      '',
      '上线后看队列等待、活跃线程、任务耗时、拒绝数和接口 P99，再逐步调整。公式只能给起点，实际瓶颈通常在下游容量而不是 CPU 核数。'
    ),{followUps:[
      f('压测发现加线程能提升吞吐，但数据库连接池开始等待，你会继续加吗？',md(
        '不会。线程只是把更多任务推到数据库门口，连接池等待说明并发预算已经转移到下游。应把线程池并发限制在连接池和数据库可承受范围内，并优化 SQL 或拆分非关键任务，而不是制造更多排队线程。'
      ))
    ]}),
    f('线程池队列已经满了，核心接口该选哪种拒绝策略？',md(
      '先按任务价值决定。允许降级的推荐任务可以快速失败或丢弃并记指标；订单关键任务不能静默丢失，通常让请求明确失败、转持久队列，或在严格评估后由调用线程执行形成背压。',
      '',
      '我不会直接使用无界队列掩盖问题。队列容量、拒绝响应和告警阈值要一起设计，并通过故障演练确认过载时核心链路仍有线程。'
    )),
    f('用户请求已经超时返回了，线程池里的子任务还在跑，怎么处理？',md(
      '编排层要有统一截止时间，超时后取消仍可取消的任务，并让 HTTP/RPC 客户端真正响应中断或超时。仅仅让 `Future` 超时，不代表底层调用停止。',
      '',
      '无法取消的副作用任务必须自带幂等和状态检查。还要监控“请求结束后仍运行”的任务量，否则慢下游会持续占住隔离池。'
    ))
  ],
  'concurrency/volatile-happens-before':[
    f('stop 标记加了 volatile，任务还是很久才退出，接下来查什么？',md(
      '`volatile` 只保证线程能看到新值，前提是循环会再次读取它。如果任务正阻塞在队列、网络 IO 或长时间计算里，根本没有机会检查标记。',
      '',
      '我会找到实际阻塞点：可中断等待就同时调用 `interrupt`，网络调用设置超时，计算循环在合适粒度检查标记。退出逻辑还要释放资源并保留中断状态。'
    ),{followUps:[
      f('捕获 InterruptedException 后直接继续循环会有什么后果？',md(
        '异常会清除中断标记，如果 catch 后既不退出也不重新设置，外部取消就被吞掉了。通常应完成必要清理后返回，或者调用 `Thread.currentThread().interrupt()` 重新传递取消信号，由上层决定。'
      ))
    ]}),
    f('两个字段必须成组更新，只给它们分别加 volatile 能保证读到同一版本吗？',md(
      '不能。每个字段的读写可见，但读线程可能看到新地址配旧端口这类组合状态。需要构造一个包含两者的不可变配置对象，通过单个 `volatile` 引用一次发布，或者用同一把锁保护读写。',
      '',
      '并发正确性要围绕业务不变量设计，不能把“每个字段都最新”误当成“整组状态一致”。'
    ))
  ],
  'concurrency/locks-aqs':[
    f('同一个热门商品库存竞争很高，换成 ReentrantLock 公平锁会更好吗？',md(
      '公平锁可以减少长期插队，但会增加线程调度和上下文切换，吞吐通常更低。库存接口真正的问题是所有请求争同一临界区，我不会先靠公平性解决容量。',
      '',
      '会优先把无关逻辑移出锁、用数据库条件更新或 Redis/Lua 做原子扣减，并在入口削峰。只有业务明确要求等待公平且能接受吞吐损失时才考虑公平锁。'
    ),{followUps:[
      f('数据库条件更新也有行锁，和应用锁相比优势在哪里？',md(
        '权威库存就在数据库时，`UPDATE ... WHERE stock >= ?` 把判断和扣减放在同一原子操作里，多实例共享这条约束，不需要分布式应用锁。但热点行仍会排队，所以还要限制并发、缩短事务，并用受影响行数判断失败。'
      ))
    ]}),
    f('使用 tryLock 超时拿不到锁时，接口应该怎么返回？',md(
      '要区分业务冲突和系统过载。库存锁短时间拿不到通常说明热点竞争，接口可以快速返回“稍后重试”或售罄候选结果，并记录锁等待指标；不能在请求线程无限等待。',
      '',
      '若操作已经产生部分副作用，必须先设计补偿或幂等，不能简单返回失败。`tryLock` 只是等待策略，不替你定义失败语义。'
    ))
  ],
  'concurrency/thread-lifecycle-communication':[
    f('线程快照里大量 WAITING 都在 CompletableFuture.get，这能说明线程死锁了吗？',md(
      '不能。`WAITING` 只说明在等结果，要继续找到这些 Future 对应的生产任务在哪里。如果提交任务和等待结果使用同一个已耗尽线程池，就可能是线程饥饿；如果生产任务卡在下游，则是外部阻塞。',
      '',
      '我会沿堆栈、任务日志和 trace 对上生产者，比较线程池活跃数、队列和下游耗时。只有形成互相持锁等待环才叫死锁。'
    ),{followUps:[
      f('发现父任务和子任务共用一个固定线程池，怎么在不停机情况下止损？',md(
        '先降低入口并发或关闭非核心异步分支，释放线程；能够动态配置时，把子任务切到有界的独立池。不能只临时调大线程数，因为下游如果也饱和会继续恶化。随后改成不在池内同步等待同池任务，并补饥饿压测。'
      ))
    ]}),
    f('大量 BLOCKED 都指向同一个业务锁，你如何判断是锁粒度大还是持锁代码异常？',md(
      '先找到持锁线程，不只看等待者。检查它持锁多久、栈停在哪、是否在锁内做 IO、日志或大循环；再结合该锁每秒获取次数和等待分布判断。',
      '',
      '如果持锁代码正常但覆盖了互不相关的订单，就拆成按业务键分段或下沉原子约束；如果是异常慢操作，先把它移出临界区。'
    ))
  ],
  'concurrency/cas-aba':[
    f('订单状态从待支付变成取消又被补偿回待支付，CAS 只比较状态会有什么问题？',md(
      '一个迟到线程只看到状态又回到 `PENDING`，会误以为期间什么都没发生，继续执行基于旧前提的支付操作。虽然值相同，业务版本和已经发生的副作用并不相同。',
      '',
      '我会把版本号或状态机序列一起比较，例如数据库更新条件同时带 `status` 和 `version`。失败后重新读取并按当前状态决策，而不是无脑循环 CAS。'
    ),{followUps:[
      f('CAS 失败后一直自旋重试为什么不适合订单状态更新？',md(
        '失败通常意味着业务状态真的被别人推进了，不只是瞬时 CPU 竞争。无脑重试会绕过状态机含义，还可能重复调用下游。订单更新应重新加载状态、判断当前事件是否幂等或已过期，再决定返回、补偿还是有限重试。'
      ))
    ]})
  ],
  'concurrency/threadlocal':[
    f('线程池里已经在 finally 调用 remove，为什么异步子任务还是丢了 traceId？',md(
      '`ThreadLocal` 不会自动跨线程传递。父线程提交任务后，子任务在另一个工作线程执行，看不到父线程值；`remove` 只能防止复用污染，不能负责传播。',
      '',
      '我会在任务提交边界捕获所需上下文，执行前设置、结束后恢复或清理，优先使用框架提供的上下文传播器。不要把整个请求对象塞进去，只传 traceId、租户等必要字段。'
    ),{followUps:[
      f('使用 InheritableThreadLocal 能不能省掉任务包装？',md(
        '在线程创建时它能复制父值，但线程池线程早已创建并反复复用，提交任务时不会重新继承，所以既可能拿不到新值，也可能残留旧值。线程池场景仍要显式捕获和恢复上下文。'
      ))
    ]}),
    f('发现 ThreadLocal value 很大导致堆一直不降，你会怎么定位是哪条链路没清理？',md(
      '堆转储里从工作线程的 `ThreadLocalMap` 沿引用找到 value 类型和创建来源，再对照线程池与请求链路。弱引用只作用于 key，value 仍被线程长期持有，线程不结束就不会自然释放。',
      '',
      '修复是在统一拦截器或任务装饰器里 `try/finally remove`，并禁止放大对象；之后用重复请求压测确认线程复用时内存和上下文都不串。'
    ))
  ],
  'concurrency/completable-future':[
    f('四个下游里推荐服务超时，但商品主数据已经返回，首页应该整体失败吗？',md(
      '不会一刀切。先定义依赖等级：商品主数据可能是必需，推荐和营销可以超时降级为空或默认结果。每个调用有独立超时，整体还有更短于网关的截止时间，避免最后一刻才失败。',
      '',
      '异常处理放在知道业务语义的位置，不能所有 future 都 `exceptionally` 返回 null。结果里要能区分真实空数据和降级，并记录下游降级率。'
    ),{followUps:[
      f('allOf 完成后再 join 每个结果，会不会又阻塞一次？',md(
        '`allOf` 正常完成意味着这些 future 已完成，后续 `join` 只是取结果，不会重新等待；但任一 future 异常会让 `allOf` 异常完成。因此要在组合前按依赖等级处理异常，或者逐个收集明确结果，避免一个非核心失败掩盖全部数据。'
      ))
    ]}),
    f('有人把所有异步任务都放在 commonPool，线上会有什么互相影响？',md(
      '公共池会把不同接口和不同性质的任务混在一起，一个慢 IO 或高 CPU 任务就能挤占其他业务。阻塞调用还会让以 CPU 为基准的并行度远远不够。',
      '',
      '核心链路会按下游或任务类型使用有界隔离池，明确队列、拒绝和监控；公共池只留给可控的短 CPU 任务。'
    ))
  ],
  'concurrency/deadlock':[
    f('确认 Java 级死锁后，线上流量还在进来，你会先重启还是先留现场？',md(
      '先快速抓至少两到三份间隔几秒的线程快照和关键指标，`jstack` 检出的等待环要保存；如果实例已无法服务，就摘流量后重启恢复容量。取证动作必须有时间上限，不能为了完美现场让故障继续扩大。',
      '',
      '同时限制问题入口，避免其他实例重复进入相同死锁。恢复后再用锁顺序和代码路径还原，不把重启当修复。'
    ),{followUps:[
      f('线程快照没有报告死锁，但请求仍永久卡住，还可能是什么？',md(
        '可能是线程池饥饿、Future 相互等待、数据库锁、连接池耗尽或不响应的网络 IO，这些不一定形成 JVM monitor 死锁。要沿等待资源继续查它的生产者和外部系统，并比较多份快照确认线程是否长期不动。'
      ))
    ]}),
    f('统一加锁顺序后，代码里还有 tryLock 超时，你会删掉超时重试吗？',md(
      '不一定。统一顺序消除典型等待环，但锁竞争、异常持锁和未来代码变更仍可能发生。`tryLock` 可以保留为过载保护，只是失败后必须按相反顺序释放已拿到的锁，并给出有限重试或明确失败。',
      '',
      '我会再加并发测试和锁等待监控，让顺序约定能够被持续验证。'
    ))
  ],
  'concurrency/java-memory-model':[
    f('把共享引用声明成 volatile 后，对象里面的 ArrayList 就可以并发修改了吗？',md(
      '不可以。`volatile` 引用能安全发布某次引用赋值，也保证之后读到最新引用，但不会让对象内部的复合操作变成线程安全。多个线程同时修改同一个 `ArrayList` 仍会竞争。',
      '',
      '我会发布不可变快照，或者用锁/并发结构保护内部变化。如果采用复制后替换，每次先在私有对象上构造完整状态，再一次写入 volatile 引用。'
    ),{followUps:[
      f('对象构造完再赋给普通字段，为什么另一个线程理论上可能看到未完整状态？',md(
        '没有 happens-before 时，引用赋值与构造期间的普通写对另一个线程没有可靠可见性保证。实践中可能长期不复现，但契约不成立。通过 final 字段正确构造、volatile 发布、锁或并发容器建立安全发布关系。'
      ))
    ]})
  ],
  'concurrency/virtual-threads':[
    f('把接口切到虚拟线程后吞吐翻倍，数据库却开始超时，为什么？',md(
      '虚拟线程降低了等待期间占用平台线程的成本，并没有增加数据库连接、CPU 或下游容量。原来平台线程数无意中充当了限流器，迁移后更多请求同时抵达数据库，把它压满。',
      '',
      '我会按连接池和下游 SLA 设置显式并发闸门，保留超时与背压。是否需要限并发，与线程是否轻量是两件事。'
    ),{followUps:[
      f('那还需要为每个下游保留独立隔离吗？',md(
        '需要。虚拟线程数量可以很多，但某个慢下游仍能占满连接、内存和业务并发预算。用信号量、连接池和限流器按下游隔离，比用小平台线程池间接隔离更清晰，并且要单独监控每个下游的等待与拒绝。'
      ))
    ]}),
    f('试点时发现部分请求会长时间占用平台线程，你会如何定位 pinning？',md(
      '我会用 JFR 的虚拟线程固定事件和线程栈，找出在 `synchronized` 临界区或本地调用里发生阻塞的位置。不是看到 synchronized 就全改，而是定位“持有 monitor 时执行长阻塞”的具体路径。',
      '',
      '修复可以缩短临界区、把阻塞移出去，或对该点使用适合的锁；再用同样负载比较 carrier 利用率和尾延迟。'
    )),
    f('CPU 密集的图片处理任务也适合每个请求开虚拟线程吗？',md(
      '虚拟线程主要改善阻塞等待，不会让 CPU 计算变快。CPU 密集任务仍要限制并行度到接近可用核数，放在独立执行器或任务系统中，避免大量 runnable 虚拟线程争抢同一组核心。',
      '',
      '试点应先选调用链清楚、阻塞 IO 多且下游容量可控的接口，而不是一次迁移全部任务类型。'
    ))
  ]
}
