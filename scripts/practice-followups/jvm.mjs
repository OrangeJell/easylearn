import{follow,md}from'../practice-answers/shared.mjs'

const f=follow

export default{
  'jvm/runtime-data-area':[
    f('堆只用了 2GB，但容器 RSS 已接近 8GB，你会先看哪些非堆证据？',md(
      '我会把进程内存拆开：Metaspace、Code Cache、线程数量乘栈大小、NIO/Netty 直接内存、GC 原生结构和 mmap 文件，再看 Native Memory Tracking 或 `pmap`。RSS 还可能包含已提交但当前未使用的页。',
      '',
      '先做账再调参数。只盯堆转储看不到直接内存和线程栈，贸然继续减小 Xmx 反而可能增加 GC。'
    ),{followUps:[
      f('NMT 显示的总量仍比 RSS 小很多，你会怎么继续查？',md(
        'NMT 不覆盖所有第三方本地库和部分 mmap。继续看 `/proc` 的内存映射、匿名页和文件映射，结合直接缓冲池指标、glibc arena、JNI 库及容器 page cache。要比较随时间增长的类别，而不是只拿两个口径强行相减。'
      ))
    ]}),
    f('线程数从 300 涨到 3000，对内存和稳定性会有什么实际影响？',md(
      '每个线程都有栈和 JVM/系统结构，即使栈没有全部实际驻留，也会增加虚拟内存、调度和诊断成本。3000 个线程往往说明无界创建、阻塞堆积或线程池隔离失效。',
      '',
      '我会按线程名和栈聚类找到来源，先限制创建和下游等待，再评估 `-Xss`。不能只把线程栈调小，否则深调用可能转成 StackOverflow。'
    ))
  ],
  'jvm/gc-guide':[
    f('平均暂停只有 20ms，但接口 P99 仍然明显受 GC 影响，你会看什么？',md(
      '平均值会掩盖长尾。我会把每次停顿时间和接口尖刺按时间对齐，看最大停顿、暂停频率、并发阶段 CPU 抢占和分配速率。即使单次很短，每秒几十次 Young GC 也会造成持续抖动。',
      '',
      '还要区分 STW 与 GC 后缓存变冷、线程排队。目标应是业务延迟预算，不是追求某个漂亮的平均 GC 数字。'
    ),{followUps:[
      f('换一个低延迟收集器就能直接解决吗？',md(
        '不能保证。新收集器需要合适 JDK、堆规模和 CPU 余量，分配过快、内存泄漏或对象晋升异常仍然存在。先建立同负载基线，再灰度比较暂停、吞吐、CPU 和内存余量，保留回滚。'
      ))
    ]}),
    f('业务要求 100ms 内响应，你会怎样给 GC 留预算？',md(
      '先从整体 P99 预算拆出网络、下游、排队和应用计算，GC 只能占其中一部分，例如把可接受的单次暂停和每分钟总暂停都写清楚。容量测试要在目标流量和真实对象分配下进行。',
      '',
      '如果应用本身已经消耗 90ms，再要求 GC 小于 10ms 没有余量。SLA 需要端到端共同负责。'
    ))
  ],
  'jvm/production-oom-troubleshooting':[
    f('OOM 后实例还活着但不断 Full GC，你会先抓堆还是先摘流量？',md(
      '先摘流量或限制入口，避免继续放大故障；同时确认其他实例容量是否够。取证和恢复并行：已有自动 heap dump 就保存现场，没有则评估剩余磁盘和停顿风险后再抓。',
      '',
      '不能为了抓一份完美堆让整个集群被拖死。必要时保留一台故障实例取证，其余快速重启恢复服务。'
    ),{followUps:[
      f('堆转储本身可能把磁盘打满，生产上怎么预防？',md(
        '预先把 `HeapDumpPath` 指向有容量隔离的持久盘或专用目录，设置磁盘告警和保留策略，并在演练中测过导出时间。故障时先查可用空间，避免 dump 与应用日志争同一分区。'
      ))
    ]}),
    f('堆里某类对象很多，怎么判断是泄漏还是业务高峰的正常存活？',md(
      '看它们的 GC Root 路径、年龄和随时间增长趋势，而不是只按对象数量排名。无界缓存、静态集合或 ThreadLocal 持有的对象，会有稳定异常引用链；正常高峰对象通常随请求结束可回收。',
      '',
      '最好对比高峰前后的类直方图或两份堆，并结合流量与缓存命中。对象大不等于泄漏，**不再需要却仍可达**才是。'
    )),
    f('报的是 Direct buffer memory，为什么分析 Java 堆可能找不到根因？',md(
      '直接缓冲区的数据在堆外，Java 堆里只保留小的引用和清理对象，所以 heap dump 不会显示全部字节。要看 BufferPool 指标、NMT、Netty allocator 和是否存在未释放的引用。',
      '',
      '修复可能是补 release、限制池容量或控制并发；单纯调大 `MaxDirectMemorySize` 只会延后再次耗尽。'
    ))
  ],
  'jvm/production-memory-sizing':[
    f('8GB 容器里直接给 7GB Xmx 为什么危险？',md(
      '进程还需要元空间、代码缓存、线程栈、直接内存、GC 原生结构和系统库，容器本身也要留安全余量。堆占满 7GB 时 RSS 很可能越过限制，被内核 OOMKill，来不及生成 Java OOM 现场。',
      '',
      '我会从非堆实测和峰值反推堆预算，通常先保留 25%～40% 给非堆与波动，再在压测中观察 RSS、GC 和吞吐调整。'
    ),{followUps:[
      f('Xms 是否一定要和 Xmx 设置成一样？',md(
        '稳定的核心服务常这样做，避免运行期扩堆抖动并让容量更可预测；但资源共享、启动密度高的环境会增加初始承诺。要结合容器调度、启动流量和 GC 行为决定，不能当成脱离场景的固定规则。'
      ))
    ]}),
    f('线程栈调小能多开线程，你会怎样验证不会引入 StackOverflow？',md(
      '先统计真实线程数和最深调用路径，重点覆盖递归、复杂表达式解析、序列化和框架代理层。用目标 `-Xss` 做压力与边界测试，并监控 StackOverflow，而不是只启动成功就算验证。',
      '',
      '如果线程数失控，根因应先治理线程创建和阻塞；缩栈不是替代有界并发。'
    ))
  ],
  'jvm/class-loading':[
    f('类初始化时抛了异常，后续请求为什么一直得到 NoClassDefFoundError，而不是再次初始化？',md(
      '同一个类加载器里，类初始化失败后会被标记为错误状态，后续主动使用不会重新执行静态初始化，而是抛出 `NoClassDefFoundError`。最初那次 `ExceptionInInitializerError` 的 cause 才是根因。',
      '',
      '我会保留第一次失败日志，检查静态代码块是否访问配置、网络或依赖顺序。恢复通常需要修正原因并重启或更换类加载器，不能靠接口重试。'
    ),{followUps:[
      f('把远程配置读取放进 static 初始化有什么生产风险？',md(
        '它把类能否加载和外部网络绑定，一次超时就可能让类永久进入错误状态，还阻塞所有首次使用线程。远程资源应在可重试、有超时和降级的生命周期组件中初始化，静态初始化只做确定、快速的本地工作。'
      ))
    ]})
  ],
  'jvm/classloader-delegation':[
    f('两个 jar 都有同名类，为什么日志打印类名完全一样，却仍然 ClassCastException？',md(
      'JVM 判断类型身份时同时看类的全限定名和定义它的类加载器。插件加载器各自加载了同名接口时，这两个类型在 JVM 看来不同，强转就失败。',
      '',
      '我会打印对象类和接口类的 `getClassLoader()`、保护域和代码来源，确认 API 包是否被重复打进插件。共享接口应由父加载器统一加载，插件只带实现与私有依赖。'
    ),{followUps:[
      f('直接把所有插件依赖都改成父优先能解决吗？',md(
        '可能解决共享 API 重复，却会让插件需要的不同依赖版本互相覆盖。通常只对稳定共享包父优先，插件私有依赖子优先或隔离；规则要精确到包，并用多版本插件同时加载测试。'
      ))
    ]}),
    f('NoSuchMethodError 出现在运行期，但编译完全正常，你怎么找实际加载的是哪个版本？',md(
      '错误说明调用方字节码期待某个方法，但运行时加载的类没有它。我会查看类加载日志，或打印该类的 `ProtectionDomain` 和 jar 路径，再对比编译依赖树与最终镜像内容。',
      '',
      '重点是构建产物，不只看 IDE。常见问题是传递依赖覆盖、fat jar 打包顺序或容器残留旧包；修复后加依赖收敛和启动自检。'
    ))
  ],
  'jvm/object-allocation':[
    f('JFR 显示大量 byte[] 分配来自 JSON 序列化，你会先做对象池吗？',md(
      '不会。先确认是不是重复序列化、响应字段过多、缓冲区反复扩容或日志把对象再次转 JSON。减少一次不必要的序列化，通常比维护 byte 数组池更直接。',
      '',
      '池化会带来上限、归还和大数组滞留问题。只有热点明确、库本身支持且压测证明收益时才考虑，并同时观察堆占用和尾延迟。'
    ),{followUps:[
      f('把大 byte[] 放进 ThreadLocal 复用有什么风险？',md(
        '线程池线程寿命很长，一次大请求扩出的缓冲会被长期保留，每个线程一份很快占满堆。若复用，要设置最大保留容量并在 finally 清理；多数业务更适合有界缓冲池或让成熟库管理。'
      ))
    ]}),
    f('逃逸分析理论上能消除对象，为什么线上仍看到这些分配？',md(
      '是否标量替换取决于具体编译路径、是否内联和对象是否逃逸，不能仅凭源码推断。冷代码、复杂调用、返回对象或存入字段都会让优化不成立。',
      '',
      '我会用 JFR 分配采样和 JIT 日志确认热点实际编译情况；优化代码仍以减少无意义工作为主，不把某个 JIT 优化当接口契约。'
    ))
  ],
  'jvm/gc-tuning':[
    f('GC 尖刺每十分钟出现一次，你怎么证明是晋升问题而不是定时任务本身耗 CPU？',md(
      '把 GC 日志、应用任务时间线、CPU、分配速率和老年代变化对齐。若任务启动后分配上升、对象跨过多次 Young GC 并推动老年代，才支持晋升判断；如果无明显内存变化但 CPU 满，则是任务计算竞争。',
      '',
      '还要看尖刺发生在 GC 前还是 GC 后。相关不等于因果，先建立时间顺序再改参数。'
    ),{followUps:[
      f('确认是批任务制造大量中等寿命对象，你优先改 JVM 参数还是任务？',md(
        '优先改任务的批大小、并发和对象生命周期，让它不要一次积压太多数据。参数调整只能在合理工作集基础上留余量；业务每十分钟制造超出堆预算的峰值，换收集器也只是改变崩溃方式。'
      ))
    ]}),
    f('调大堆后 Full GC 少了，但单次停顿更长，这次优化算成功吗？',md(
      '要回到业务指标判断。若接口 P99、错误率和吞吐整体改善且停顿仍在 SLA 内，可以接受；如果最大停顿越过超时阈值，就不是成功。',
      '',
      '我会同时比较分配速率、回收后占用和内存余量，避免大堆掩盖泄漏。调优不能只让某一个 GC 计数变好。'
    ))
  ],
  'jvm/stack-metaspace-errors':[
    f('StackOverflowError 只在某个异常请求出现，你怎么找到递归入口而不是只调大 Xss？',md(
      '堆栈通常会重复出现一段调用序列，我会从重复边界找到缺失的终止条件，例如对象双向序列化、`toString` 相互引用或递归遍历环。再用触发请求和数据结构复现。',
      '',
      '调大栈只让错误更晚发生，还增加所有线程内存。修复应加入终止条件、访问集合或改成显式栈迭代，并覆盖环形数据测试。'
    ),{followUps:[
      f('堆栈太长被日志截断，看不到最开始的调用怎么办？',md(
        '保留原始错误文件或提高单次异常的受控采集上限，并从重复栈模式推断递归周期。测试环境用相同输入和较小栈更快复现，再配合方法采样。不要在线上无限放大日志导致二次故障。'
      ))
    ]}),
    f('Metaspace OOM 时类数量一直上涨，你会怎样判断是动态代理正常增长还是类加载器泄漏？',md(
      '看已加载类和类加载器实例随时间是否只增不降，再用堆转储检查旧加载器为什么仍被线程、静态集合或 ThreadLocal 引用。正常代理类集合应该在业务类型稳定后趋于平稳。',
      '',
      '如果每次热部署或脚本执行都产生新加载器，而旧加载器无法卸载，就是泄漏。单纯调大 MaxMetaspace 只延迟故障。'
    ))
  ],
  'jvm/jit-compiler':[
    f('发布后预热十分钟能恢复，但滚动发布期间仍有延迟尖刺，你会怎么做可控预热？',md(
      '实例启动后先不接正式流量，用代表性的只读或可回滚请求覆盖热点路由、序列化和 SQL，让类加载、连接建立与 JIT 编译提前发生；达到延迟和编译队列阈值后再标记 ready。',
      '',
      '预热不能产生真实扣款或污染缓存，输入也不能只有一个最简单样本。滚动时控制同时预热实例数量，保留集群容量。'
    ),{followUps:[
      f('为什么只用健康检查接口循环请求，预热效果通常不好？',md(
        '它只编译健康检查自己的短路径，覆盖不到业务方法、JSON 模型、数据库驱动和不同分支。有效预热要来自真实热点画像，选择少量代表请求并验证接流量前的业务 P95，而不是只看进程存活。'
      ))
    ]}),
    f('如何区分冷启动延迟来自 JIT、类加载还是连接池未建立？',md(
      '我会把请求阶段与 JFR 事件对齐：看类加载、编译活动和 safepoint，同时观察 DNS、TLS、数据库连接创建。用分阶段实验也能验证，例如提前建连接但不做业务预热，或只预加载类。',
      '',
      '不同原因的修复完全不同，不能看到“十分钟后变快”就全部归因 JIT。'
    ))
  ]
}
