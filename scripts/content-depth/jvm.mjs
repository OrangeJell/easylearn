import{profile as p}from'./profile.mjs'

export const jvmProfiles={
  'jvm/production-oom-troubleshooting':p(
    ['告警或进程退出','第一时间保留现场','区分堆/元空间/直接内存','关联 GC、线程与系统证据','止损后复现并修复根因'],
    ['先区分 Java 抛出 OutOfMemoryError、容器 OOMKilled 和 native 分配失败，三者保留现场的窗口不同。','自动 HeapDump、JFR、GC 日志、NMT 与容器事件应在事故前配置，发生后临时开启往往已经来不及。','堆 OOM 看对象与 GC Root，Metaspace 看类加载器，Direct Memory 看 NIO/池化分配，不能只分析 hprof。','时间线需要把分配速率、GC 后基线、线程数、RSS 和发布事件对齐，确定哪个区域先失控。','止损可限流、摘流量或滚动重启，但必须保存至少一个代表现场并在同数据模型下验证修复。'],
    '堆使用不高但容器反复 OOMKilled',
    '网络服务的池化直接内存与线程栈持续增长，Xmx 只占容器限制的一半，监控却只采集 Java 堆。结合 cgroup memory.events、NMT 和线程数后确认本地内存超限，限制直接内存与并发连接才修复。',
    [['Heap Dump + MAT','Java 堆对象无法回收','对象、引用链和支配树证据直接','只覆盖堆且转储会产生 I/O 峰值'],['JFR + GC/NMT','需建立分配与 native 时间线','开销可控、证据连续','需要事先开启并正确留存'],['在线直方图/采样','故障窗口短且不能完整 dump','快速、侵入较低','只能给候选，不能单独证明根因']],
    ['事故发生后自动重启覆盖唯一现场','只分析堆却忽略 RSS 与 cgroup','dump 过程中磁盘写满造成二次故障'],
    ['GC 后堆基线','进程 RSS/cgroup usage','类加载器与线程数','分配速率和 OOM 类型'],
    'OOM 是某个内存区域无法满足分配，不等于 Java 堆一定泄漏；诊断必须先确定区域，再用该区域对应的证据工具。'
  ),
  'jvm/runtime-data-area':p(
    ['类加载建立元数据','线程创建私有栈','对象分配进入堆','方法执行创建栈帧','GC 与卸载回收区域'],
    ['类元数据、运行时常量池和方法信息主要进入方法区的具体实现 Metaspace，Class 对象仍位于堆。','每个线程拥有程序计数器和 Java 虚拟机栈，线程数直接影响本地内存与栈容量。','绝大多数对象在堆中分配，逃逸分析可能标量替换，但不能简单理解为“对象都在栈上”。','每次方法调用创建栈帧，包含局部变量表、操作数栈、动态链接和返回信息。','堆由 GC 管理，线程栈随线程结束释放，类元数据只有在类加载器可回收等条件满足时才能卸载。'],
    '增加线程后进程被操作系统杀死',
    '团队只关注 -Xmx，忽略每个线程的 -Xss、直接内存、Metaspace 和 native 库。线程数上升后 RSS 超过容器限制，尚未发生 Java 堆 OOM 就被 OOM Killer 终止。容量设计应从进程总内存反推各区域预算。',
    [['Java 堆','普通对象与数组','GC 自动管理、容量可观测','过大增加停顿与 RSS'],['线程栈','每线程调用帧与局部状态','线程隔离、随线程释放','线程多时总量显著，过小易栈溢出'],['直接/本地内存','NIO、JIT、类元数据与 native 组件','减少部分复制或承载 JVM 自身','不完全受 Xmx 约束']],
    ['只看堆忽略进程总 RSS','盲目增大 Xss 降低可创建线程数','类加载器泄漏导致 Metaspace 持续增长'],
    ['Heap committed/used','线程数与栈预算','Metaspace 使用','进程 RSS 与容器限制'],
    '运行时数据区是规范模型，HotSpot 具体实现和不同 JDK 会变化；排障必须同时看 JVM 指标与操作系统进程内存。'
  ),
  'jvm/gc-guide':p(
    ['对象持续分配','可达性标记','存活对象复制或整理','回收不可达空间','调整代际与并发周期'],
    ['分配速率、对象大小和引用关系决定 GC 压力，吞吐问题常先从分配端产生。','GC Roots 沿引用图标记存活对象，引用类型、类加载器和线程局部变量都会影响可达性。','复制算法适合低存活率区域，标记整理减少碎片但移动成本更高，标记清除可能留下碎片。','回收阶段可能 Stop-The-World 或与应用并发；并发收集仍有短暂停顿和额外 CPU。','分代假设让年轻对象高频回收，晋升与老年代并发周期需要根据实际存活率和暂停目标调整。'],
    '低延迟服务 P99 周期性抖动',
    '监控只显示平均响应正常，GC 日志却表明每两分钟出现 Mixed GC，原因是缓存批量刷新制造大量中等寿命对象并集中晋升。拆散刷新批次、减少临时对象后，比先改收集器参数更直接。',
    [['G1','中大堆与通用暂停目标','Region 化、成熟通用','极低停顿目标下并发余量有限'],['ZGC','大堆与亚毫秒级停顿诉求','并发移动、停顿短','需要较新 JDK 与足够 CPU/内存余量'],['Parallel GC','吞吐优先的离线计算','实现简单、吞吐高','停顿随堆和存活对象增长']],
    ['只调停顿目标不降低分配速率','堆设得过满导致并发周期来不及','用平均停顿掩盖 Full GC 长尾'],
    ['分配速率','停顿 P95/P99','晋升与存活率','GC 后堆占用'],
    '收集器选择服从 SLO、堆规模和 CPU 预算；先确认对象分配与存活模式，再调整参数，不能把 GC 调优等同于换算法。'
  ),
  'jvm/production-memory-sizing':p(
    ['确认容器总内存','预留系统与 native','估算线程与直接内存','确定堆和元空间','压测验证峰值余量'],
    ['容量起点是容器或主机可用内存，而不是先拍一个 Xmx；还要给 page cache 和守护进程留空间。','JIT Code Cache、GC 结构、native 库和 JVM 自身会占用堆外内存，不能归入业务对象预算。','线程栈总量约等于线程数乘 Xss，DirectByteBuffer 和网络框架池化内存需按峰值估算。','堆要容纳稳态存活对象、短期峰值与 GC 安全余量；Metaspace 则与类数量、动态生成和类加载器生命周期相关。','最终通过与生产相近流量、数据和故障注入验证 RSS 峰值、GC 周期与 OOM 行为。'],
    '容器设置 2GiB 但 Xmx 也配置 2GiB',
    '应用压测时堆只用到 1.5GiB，进程仍被系统杀死，因为直接内存、线程栈和 Metaspace 把 RSS 推过限制。重新建立总内存预算表，把 Xmx 调到约 60% 并限制线程和直接内存后才稳定。',
    [['固定 Xms=Xmx','负载稳定且希望避免堆伸缩','容量可预测','一次性占用高，容器密度下降'],['按容器百分比','多规格部署与统一镜像','随限制自动适配','必须理解非堆仍占固定或独立比例'],['动态垂直扩容','平台支持并且业务峰谷明显','提高资源利用率','JVM 参数、RSS 和调度联动复杂']],
    ['Xmx 占满容器不给堆外留余量','线程增长导致本地内存耗尽','压测数据规模过小低估老年代'],
    ['进程 RSS 峰值','GC 后堆基线','直接内存使用','线程与 Metaspace 水位'],
    '内存参数必须形成总账：容器限制大于堆、Metaspace、线程栈、直接内存、JIT/GC native 开销和安全余量之和。'
  ),
  'jvm/class-loading':p(
    ['按名称发起加载','读取并校验字节码','准备静态字段','解析符号引用','执行类初始化'],
    ['加载阶段由类加载器查找字节并创建 Class 对象，同名类在不同加载器中是不同类型。','验证检查格式、元数据、字节码和符号引用，防止非法代码破坏 JVM 安全。','准备阶段为静态字段分配空间并设默认值，编译期常量可能在此表现不同。','解析把常量池符号引用转换为直接引用，可按实现选择提前或延迟进行。','初始化执行 <clinit>，按父类优先和主动使用规则触发，并由 JVM 保证同一类初始化的线程安全。'],
    '静态初始化互相依赖导致启动失败',
    '两个类的静态字段在初始化时互相读取，其中一个看到对方准备阶段的默认值，配置被错误缓存。通过移除静态副作用、显式构建依赖并在启动阶段校验后，初始化顺序不再决定业务结果。',
    [['主动初始化','首次 new、静态方法/字段访问','语义直接','重型静态逻辑会拖慢首请求'],['延迟持有者模式','单例按需加载','利用类初始化线程安全','异常初始化后该类可能不可用'],['显式启动装配','依赖复杂且需失败前置','顺序、错误和观测清晰','启动时间增加']],
    ['把加载与初始化混为一谈','静态初始化执行网络 I/O','初始化异常后反复期待类自动恢复'],
    ['类加载数量与耗时','初始化失败堆栈','Metaspace 增长','启动阶段慢点'],
    '类被加载不代表已初始化；只有主动使用触发初始化，编译期常量还可能被内联而不初始化声明类。'
  ),
  'jvm/classloader-delegation':p(
    ['收到类名请求','检查已加载缓存','委派父加载器','父级无法找到','当前加载器定义类'],
    ['loadClass 先检查 findLoadedClass，避免同一加载器重复定义同名类。','父加载器优先使 JDK 核心类型不容易被应用代码替换，形成基本安全与一致性边界。','委派链逐级向 Bootstrap 等上层查找，父级可见不代表父级能看见子加载器类。','父级找不到时子加载器才执行 findClass，从自己的路径、插件包或网络来源读取字节。','类型身份由类名与定义它的类加载器共同决定，强转失败常来自两个加载器各自定义了同名类。'],
    '插件接口明明同名却无法强转',
    '插件包把宿主 API 也打进自身依赖，子加载器定义了第二份接口。实现类实现的是插件加载器版本，宿主看到的是父加载器版本，因此 ClassCastException。把共享 API 只放父加载器并在打包时排除后恢复。',
    [['父优先','普通应用与共享 API','核心类一致、安全边界清晰','依赖冲突隔离能力有限'],['子优先','插件或容器需隔离依赖版本','应用可携带独立版本','必须保护 JDK 与共享接口包'],['模块层/独立进程','强隔离与明确导出','边界清楚、卸载更可控','部署和通信成本更高']],
    ['线程上下文类加载器未恢复导致泄漏','插件重复打包共享接口','自定义加载器持有缓存无法卸载'],
    ['类加载器实例数','同名类来源','Metaspace 与卸载数','ClassCastException 类型身份'],
    '双亲委派是常用实现约定而非 JVM 强制算法；打破它时必须明确哪些包父优先、哪些子优先，以及卸载和安全边界。'
  ),
  'jvm/jit-compiler':p(
    ['解释执行并采集 Profiling','热点方法达到阈值','生成优化机器码','运行守卫与内联代码','假设失效后去优化'],
    ['方法初期由解释器执行，同时记录调用次数、分支概率和接收者类型等运行画像。','热点计数触发分层编译，C1 快速编译并采样，C2 使用更多画像做激进优化。','常见优化包括内联、逃逸分析、标量替换、锁消除和循环优化。','优化代码带有类型和分支守卫；稳定单态调用点更容易内联，多态膨胀会阻止优化。','类加载或运行分布改变使假设不成立时，JVM deopt 回解释器或较低层代码并重新编译。'],
    '服务预热后仍出现周期性 CPU 毛刺',
    'JIT 日志显示某核心方法因新实现类加载从单态变多态，已编译代码反复去优化和重编译。减少热路径动态类型、检查代码缓存并稳定加载时机后，毛刺消失。',
    [['默认分层编译','通用服务','启动与峰值性能平衡','画像变化仍可能去优化'],['AOT/CDS 辅助','冷启动敏感','减少类加载或初始编译成本','峰值优化通常仍需 JIT'],['禁用/限制高级编译','诊断或极特殊稳定场景','行为更可预测','长期吞吐显著下降']],
    ['用一次微基准测试解释器阶段下结论','忽略逃逸导致基准被完全消除','Code Cache 满后停止编译'],
    ['编译队列长度','Code Cache 使用','deoptimization 事件','热点方法编译层级'],
    'JIT 依据真实运行画像优化，性能结论需要足够预热并防止死代码消除；不要把某次生成的机器码当成永久不变。'
  ),
  'jvm/object-allocation':p(
    ['计算对象布局','TLAB 快速分配','执行零值初始化','写对象头与构造字段','逃逸并晋升或被回收'],
    ['对象大小由对象头、字段排列、压缩指针和对齐共同决定，源码字段大小之和不是最终占用。','多数小对象在线程本地 TLAB 通过指针碰撞分配，无需每次竞争全局堆。','JVM 保证新对象字段先处于零值，之后才执行构造器与显式初始化。','对象头记录 Mark Word 和类型指针，构造器完成不代表对象已安全发布给其他线程。','短命对象在年轻代回收，存活对象按年龄或担保条件晋升；逃逸分析还可能消除实际分配。'],
    '批量解析造成分配率暴涨',
    '解析器为每个字段创建临时包装对象和子字符串，吞吐下降同时 Young GC 密集。JFR 分配火焰图定位到转换链后，通过复用缓冲、避免装箱和流式解析降低了分配速率。',
    [['普通 new','大多数短命业务对象','JVM 分配极快、代码清晰','高频临时对象仍增加 GC'],['对象池','创建极昂贵或外部资源有限','复用昂贵资源','生命周期复杂，普通小对象池常更慢'],['值化/扁平数据','超大规模同构数据处理','减少对象头与指针追踪','代码抽象和可维护性成本']],
    ['为普通 DTO 建对象池增加竞争','估算内存时忽略对象头与对齐','构造中 this 逸出导致半初始化可见'],
    ['分配速率','TLAB refill','对象年龄分布','晋升速率'],
    'new 本身通常很快，真正成本是对象数量、存活时间和引用遍历；优化应由分配画像证明，而不是机械消除所有对象。'
  ),
  'jvm/stack-metaspace-errors':p(
    ['方法递归压入栈帧','线程栈逼近上限','类持续加载元数据','本地内存接近预算','抛出 StackOverflow 或 Metaspace OOM'],
    ['每次方法调用占用栈帧，局部变量、操作数和 JIT 情况影响实际深度。','无限递归或过深数据结构遍历会触发 StackOverflowError，增大 Xss 只能延后而不修复无穷递归。','类元数据进入 Metaspace，动态代理、脚本和热部署会持续创建新类。','Metaspace 使用本地内存，类只有在对应类加载器不可达且发生类卸载时才可能释放。','接近限制时应保留错误前后的类加载器统计和 native memory 证据，避免只看 Java 堆。'],
    '热部署数次后 Metaspace OOM',
    '旧应用类加载器被日志线程的 ThreadLocal 和 JDBC Driver 注册表引用，导致整批类无法卸载。堆转储按 ClassLoader 统计并查看 GC Root 后，清理线程上下文和注销资源才解决。',
    [['修复递归/改迭代','StackOverflow 来自算法深度','从根因消除栈增长','需要改写遍历状态'],['调整 Xss','合法深调用且线程数可控','快速扩大单线程深度','减少可创建线程并增加本地内存'],['限制 Metaspace + 治理加载器','动态类场景','故障更早且可定位','限制过小会误伤正常启动']],
    ['只增大 Xss 掩盖无限递归','动态代理类缓存按请求无限增长','类加载器被后台线程强引用'],
    ['最大递归深度','线程数乘 Xss','已加载/卸载类数','按类加载器统计的 Metaspace'],
    'StackOverflowError 与 Metaspace OOM 都不是普通堆调优问题；一个关注调用深度与线程栈，一个关注类生命周期和本地内存。'
  ),
  'jvm/gc-tuning':p(
    ['定义吞吐与停顿 SLO','采集稳定基线','定位分配和存活模式','一次调整一个变量','压测并灰度验证'],
    ['调优前先确定吞吐、P99 停顿、堆成本和容器限制，目标之间通常存在取舍。','至少采集完整 GC 日志、业务延迟、分配率和 GC 后占用，单次停顿样本不能代表基线。','年轻 GC 频繁可能是分配高，老年代上升可能是缓存或泄漏，必须区分可回收与真正存活。','优先改对象生命周期和业务批量，再调整堆、并发周期或收集器参数；每次只改少数变量才能归因。','在同数据、同流量下对比尾延迟与成本，灰度时设置回滚阈值并观察至少一个完整业务周期。'],
    '把 MaxGCPauseMillis 调低后吞吐反而下降',
    '暂停目标过于激进让 G1 使用更多 CPU、缩短年轻代并更频繁回收，应用吞吐下降。恢复合理目标并减少请求中的临时 JSON 对象后，停顿和吞吐同时改善。',
    [['先优化分配','热点来自临时对象或批处理方式','根因收益持续且减少 GC 工作','需要代码与数据流改造'],['调整堆与代际','存活集明确但空间或周期不合适','改动小、见效快','参数可能只适合当前负载'],['更换收集器','SLO 与现有算法能力不匹配','获得不同停顿/吞吐特征','迁移验证和资源需求较大']],
    ['只看 GC 次数不看停顿与业务影响','一次修改多个参数无法归因','把内存泄漏误当成堆太小'],
    ['分配与晋升速率','停顿分位值','GC 后堆基线','应用吞吐与 CPU'],
    '调优不是追求零 GC，而是在业务 SLO、资源成本和稳定性之间找到可验证平衡；参数模板不能替代真实负载测量。'
  )
}
