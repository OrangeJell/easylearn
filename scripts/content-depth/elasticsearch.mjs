import{profile as p}from'./profile.mjs'

export const elasticsearchProfiles={
  'elasticsearch/inverted-index':p(
    ['文档字段进入 Analyzer','生成规范化 Term','写入倒排词典与 Postings','查询解析得到 Term','求交并评分返回文档'],
    ['文本按字符过滤、Tokenizer 和 Token Filter 分析，索引与查询分析器不一致会导致看似存在却搜不到。','Term 是索引最小词项，大小写、词干、同义词和中文分词都在这里决定召回边界。','倒排表从 Term 指向包含它的文档 ID，并可保存词频、位置和 offset 支持评分与短语。','查询字符串被同样或指定 Analyzer 处理，term 查询则通常不分析输入。','执行器对 postings 求交/并集并结合 BM25 等评分，最终从正排 doc values 或 _source 取字段。'],
    '商品标题明明包含关键词却搜索不到',
    '字段使用 keyword 映射，索引中只有完整标题一个 Term，match 查询分析出的单词无法命中。改为 text 用于全文检索、keyword 子字段用于聚合排序后语义才分离。',
    [['text + Analyzer','全文检索、相关性和短语','支持分词与评分','聚合排序需额外 keyword 子字段'],['keyword','精确过滤、聚合和排序','Term 稳定、doc values 友好','不做全文分词'],['search_as_you_type/edge ngram','前缀联想','低延迟候选召回','索引体积和写入成本增加']],
    ['索引与查询分词器不一致','用 term 查询分析型 text 字段','同义词更新造成历史索引与新规则不一致'],
    ['分析后 token 列表','Term 命中 doc_freq','召回率与零结果率','索引大小'],
    '倒排索引优化 Term 到文档的查找，不适合任意关系 Join；Mapping 和 Analyzer 一旦写入历史文档，修改通常需要重建索引。'
  ),
  'elasticsearch/mapping-query':p(
    ['识别字段业务语义','定义 Mapping 与 Analyzer','写入并建立索引结构','选择 Query/Filter 上下文','聚合排序并返回'],
    ['字段类型决定可用查询、存储格式和内存结构，动态映射方便但可能把 ID、日期或数值识别错误。','text/keyword、多字段、normalizer 和 nested 等设计应来自查询模式而不是样例 JSON。','写入后字段值分别进入倒排、doc values 和 _source；关闭某能力会影响搜索、聚合或回显。','Query Context 计算相关性，Filter Context 只判断匹配并更易缓存，精确条件不应无谓评分。','聚合与排序通常依赖 doc values，高基数字段和大 size 会显著增加内存与跨分片合并。'],
    '订单 ID 自动映射成 long 后前导零丢失',
    '测试样本全是数字，动态映射把订单号识别为 long，后续带字母数据写入失败，精确格式也无法保留。通过索引模板显式定义 keyword 并版本化 alias 后，数据契约才稳定。',
    [['显式 Mapping','生产核心索引','类型可控、错误前置','演进需模板与重建'],['动态模板','字段模式可预测的大量日志','减少手工定义','规则优先级错误会污染索引'],['runtime field','临时派生或历史兼容','无需立即重建','查询时计算成本高']],
    ['字段爆炸触发 mapping limit','数组对象误用 object 导致跨对象匹配','在 filter 条件使用 script 扫描每文档'],
    ['字段总数','mapping 冲突数','查询 cache 命中','聚合内存与拒绝'],
    'Mapping 是写入前的物理设计，许多字段类型不能原地改变；重要索引应通过模板、版本索引和 alias 切换演进。'
  ),
  'elasticsearch/near-real-time':p(
    ['请求写入主分片','写 translog 与内存 buffer','refresh 生成可搜索 segment','查询新 Searcher','后台 flush/merge 持久化整理'],
    ['写请求成功先表示主分片与约定副本已接受操作，不等于普通搜索立即可见。','操作追加 translog 并写入内存索引 buffer，translog 提供崩溃恢复而非搜索结构。','refresh 把 buffer 打开为新的只读 segment 并切换 Searcher，默认周期形成近实时窗口。','refresh 后查询可见但数据可能尚未 flush 到 Lucene commit；实时 GET 可从事务状态获取最新版本。','频繁 refresh 产生许多小 segment，后台 merge 增加 I/O 与 CPU，必须平衡可见延迟和写吞吐。'],
    '创建商品后立刻搜索偶发为空',
    '写接口返回后前端立即走 search，refresh 尚未发生。把强一致回显改为按 ID GET，列表接受一秒内可见；管理操作必要时使用 refresh=wait_for，而不是每次强制 refresh。',
    [['默认周期 refresh','普通搜索可接受秒级延迟','写入吞吐平衡','写后立即搜索可能不可见'],['refresh=wait_for','少量写后搜索必须可见','不主动制造额外 refresh','等待当前刷新周期增加响应延迟'],['refresh=true','测试或极低频管理操作','立即可搜索','每次写生成小 segment，吞吐差']],
    ['把 refresh 当 fsync 持久化','每次写 refresh=true 造成 segment 爆炸','轮询搜索放大集群压力'],
    ['refresh latency/count','segment 数量','merge time','写后可见延迟'],
    '近实时是写入吞吐与搜索可见性的取舍；真正需求要区分“按 ID 读自己写”与“搜索列表立即包含新文档”。'
  ),
  'elasticsearch/shard-replica':p(
    ['路由文档到主分片','主分片校验并写入','并行复制到副本','搜索协调各分片','故障后副本晋升与重分配'],
    ['routing 值经哈希映射到固定主分片，主分片数通常不能简单原地修改。','主分片确定操作顺序并执行版本检查，然后把操作复制给当前 in-sync 副本。','副本提高可用性并可服务搜索，但不会拆分单主分片的写入容量。','协调节点向每个相关分片发查询，再合并 top hits 或聚合；分片越多扇出成本越高。','节点故障后合格副本晋升，新副本恢复会占用网络、磁盘和 CPU。'],
    '索引按天创建 20 个主分片导致小集群不稳定',
    '每日数据只有几 GB，却累计数万个小分片，Cluster State、文件句柄和堆开销远大于数据本身。通过模板减少主分片、ILM rollover 按大小滚动并合并历史索引后恢复。',
    [['增加主分片','单索引数据/写吞吐需并行','扩展写入和数据分布','查询扇出与固定开销增加'],['增加副本','读吞吐和高可用','搜索副本与故障接管','存储、复制与恢复成本'],['拆索引/时间流','生命周期与租户边界明确','易管理冷热和删除','跨索引查询与模板治理']],
    ['把副本当写扩展','过度分片消耗堆与协调开销','自定义 routing 形成热点分片'],
    ['分片大小与数量','主副本写延迟','查询分片扇出','recovery 带宽'],
    '分片是 Lucene 实例和故障恢复单位，不是免费并行度；目标应是可恢复、大小合理且数量可控的分片。'
  ),
  'elasticsearch/write-search-process':p(
    ['协调节点解析请求','路由到主分片并复制','refresh 后建立 Searcher','query phase 收集候选','fetch phase 拉取文档并合并'],
    ['任意节点可做协调节点，写请求依据 routing 定位一个主分片，搜索则可能扇出多个分片。','主分片执行写入并复制，wait_for_active_shards 只约束活跃副本数量，不是跨集群事务。','refresh 切换可搜索 segment，旧 Searcher 仍服务进行中查询，形成 point-in-time 视图。','Query phase 每分片返回本地 top N 的 doc ID 与分数，协调节点合并全局候选。','Fetch phase 再向命中分片拉取 _source/fields；深分页会让每片都保留大量候选。'],
    '大 size 查询让协调节点 OOM',
    '请求 size=100000，20 个分片各返回大量候选，协调节点建立巨大优先队列并 fetch 大量 _source。改用 search_after + PIT 流式翻页并限制最大窗口后消除峰值。',
    [['普通 from/size','浅页交互搜索','简单、支持跳页','深页协调内存线性增长'],['search_after + PIT','稳定深度遍历','每页成本稳定、视图一致','需保存排序游标，不能随机跳页'],['scroll','离线批量读取旧接口','快照式批量拉取','占用搜索上下文，不适合用户分页']],
    ['忽略协调节点资源','写成功后立即 search 却没处理 refresh','fetch 返回完整 _source 放大网络'],
    ['query/fetch phase 时间','协调节点内存','分片扇出','返回字节与命中数'],
    '搜索成本不仅在匹配文档，还在每分片候选合并和 fetch；限制 size、返回字段与分片数通常比单纯扩节点有效。'
  ),
  'elasticsearch/deep-pagination':p(
    ['固定排序与查询快照','首次查询得到最后 sort 值','携带 search_after 请求下一页','各分片从游标后继续','协调节点合并并返回新游标'],
    ['from+size 要求每个分片保留前 from+size 条候选，页越深内存和 CPU 越高。','search_after 使用上一页最后一条的完整 sort 数组作为边界，排序必须稳定且包含唯一 tie-breaker。','PIT 固定一组 segment 视图，避免翻页期间 refresh 导致文档重复或遗漏。','每个分片在本地跳过游标前候选，只返回下一批，协调节点合并规模接近 page size。','PIT 和搜索上下文有 keep_alive，客户端中断后要允许自然清理并控制并发数量。'],
    '日志导出中出现重复和缺失',
    '只按 timestamp 排序，大量日志同毫秒，search_after 边界无法唯一定位；同时未使用 PIT，刷新改变结果集。加入 timestamp+_shard_doc 或业务唯一 ID，并绑定 PIT 后稳定。',
    [['from/size','浅页和随机跳页','使用简单','受 max_result_window 与深页放大'],['search_after + PIT','在线连续深翻页','性能稳定、一致视图','游标有状态，不能随意跳页'],['异步导出任务','全量大结果','隔离资源、可断点','延迟、任务状态和文件治理']],
    ['排序字段不唯一','每页重新创建 PIT 导致视图变化','长期 PIT 阻止旧 segment 清理'],
    ['每页 took 与扫描数','PIT 上下文数量','协调内存','重复/遗漏校验'],
    '深分页接口应优先改产品交互或异步导出；若必须连续读取，稳定排序、PIT 和游标完整性缺一不可。'
  ),
  'elasticsearch/index-lifecycle':p(
    ['写入当前 write index','按大小/时间 rollover','迁移 warm/cold tier','只读压缩与降副本','过期删除并更新 alias'],
    ['Data Stream 或 alias 把写入路由到当前 backing index，模板保证新索引 Mapping 和设置一致。','rollover 依据 max_primary_shard_size、age 或 docs 创建新索引，按大小通常比固定按天更稳定。','历史索引迁移到不同数据层，节点角色、磁盘和分配规则决定实际移动。','只读后可 force merge、shrink 或减少副本，但这些操作 I/O 重，需错峰和容量预算。','delete 阶段按保留期删除整个索引，失败步骤会重试，策略与执行状态必须持续监控。'],
    'ILM 配了删除策略但磁盘仍持续上涨',
    '索引 alias 未设置 rollover_alias，策略卡在 rollover 步骤，后续删除从未执行。通过 _ilm/explain 定位阻塞原因、修复模板并手动重试后才恢复。',
    [['Data Stream + ILM','追加型日志和指标','滚动与隐藏 backing index 集成','不适合任意原地更新模型'],['Alias + ILM','需要自定义索引和更新','灵活','模板、write alias 配置更易出错'],['手工定时管理','极少索引或特殊流程','完全可控','容易遗漏失败、无状态机重试']],
    ['只按天滚动产生大小悬殊分片','force merge 与线上写查询争资源','策略卡住没有告警'],
    ['ILM step/失败数','主分片大小','各 tier 磁盘水位','迁移与 merge 时间'],
    '生命周期策略必须同时满足保留、分片大小、节点容量和恢复时间；配置策略不等于执行成功，需要对卡住步骤告警。'
  ),
  'elasticsearch/data-consistency':p(
    ['写请求带版本条件','主分片顺序执行','复制到 in-sync 副本','refresh 提供搜索可见性','失败重试与外部数据源对账'],
    ['seq_no 与 primary_term 标识主分片上的操作顺序，可用于乐观并发控制避免旧写覆盖新写。','主分片对同一文档串行裁决，if_seq_no/if_primary_term 不匹配时返回冲突。','副本异步应用主分片操作并保持 in-sync 集合，故障选主仍有明确历史。','GET 与 search 可见性不同：实时 GET 可读最新操作，search 依赖 refresh。','与 MySQL 等外部事实源同步时，重试、乱序和删除都需业务版本及对账，ES 不能提供跨系统事务。'],
    'CDC 延迟恢复后旧事件覆盖新文档',
    '重放队列把较早更新晚于新更新送达，普通 index API 覆盖了当前状态。把数据库单调版本作为 external_gte 或脚本条件后，旧事件被拒绝并记录。',
    [['内部 seq_no 控制','读改写都在同一 ES 索引','原生并发控制','不能跨重建或外部系统直接沿用'],['外部业务版本','CDC/多源按单调版本同步','能拒绝乱序旧事件','版本必须全局可比较且处理删除'],['全量重建 + alias','大范围修复或 Mapping 变化','结果可审计、切换原子','资源、时间和增量追平复杂']],
    ['用 last-write-wins 接受乱序覆盖','把 refresh 延迟误判为数据丢失','重试删除与写入没有墓碑版本'],
    ['版本冲突数','CDC Lag','源库/索引差异','写成功到搜索可见时间'],
    'ES 一致性要分为单文档并发、主副本复制、搜索可见性和外部数据源同步四层，不能用一个“最终一致”概括全部。'
  ),
  'elasticsearch/cluster-election':p(
    ['节点发现并加入','Master-eligible 节点投票','选出唯一 Master','发布集群状态','故障后重新选举并恢复分片'],
    ['discovery.seed_hosts 和 initial_master_nodes 只服务发现/首次引导，错误重复引导可能形成独立集群。','符合任期与投票配置的多数派才能选主，避免网络分区两侧都合法修改 Cluster State。','Master 管理元数据、分片分配和状态发布，不负责汇总所有业务查询数据。','Cluster State 先发布给节点并等待确认，字段/索引和分片过多会放大发布成本。','Master 故障后新任期选举，随后处理未分配分片和恢复；数据节点 I/O 压力可能使控制面更慢。'],
    '网络抖动时集群状态频繁变红',
    'Master 与数据节点混部且堆压力高，长 GC 被其他节点视为失联，反复选举与分片重分配。隔离专用 Master、稳定堆和调整故障检测前先解决停顿根因后恢复。',
    [['专用 Master 节点','中大型生产集群','控制面资源隔离','额外节点成本'],['混合角色','小型非关键集群','资源利用率高','数据查询/GC 会影响选举'],['Voting-only 节点','需增加投票容错但不承担 Master','提高多数派稳定性','仍需资源和拓扑规划']],
    ['重复设置 initial_master_nodes 误建新集群','Master 节点偶数且故障域分布不当','节点短暂抖动触发昂贵分片搬迁'],
    ['Master 选举次数','Cluster State 发布时间','pending tasks','未分配分片与 recovery'],
    '集群控制面依赖多数派与稳定状态发布；先保证 Master 资源和故障域，再讨论数据节点数量。'
  ),
  'elasticsearch/performance-troubleshooting':p(
    ['确定慢查询/写入时间窗','按 query shape 聚合证据','Profile 与系统指标定位阶段','控制扫描/分片/并发','回放灰度验证'],
    ['先区分客户端排队、协调、query、fetch、refresh、merge 和 GC，单个 took 不能解释端到端全部时间。','按查询 DSL 形态、索引和租户聚合，避免把一个异常请求当成系统性瓶颈。','Profile 解释算子 CPU，slowlog、hot_threads、nodes stats 和 query_log 类指标解释资源与等待。','优先减少分片扇出、命中文档、返回字段和聚合桶，再治理线程池、缓存与硬件。','用同数据快照和并发回放验证 P99、拒绝、堆与业务召回，不让性能优化改变搜索语义。'],
    '增加节点后查询仍变慢',
    '新节点让每个索引拥有更多小分片，请求扇出和协调成本上升，缓存也更分散。合并小索引、调整分片布局并限制高基数聚合后，性能才改善。',
    [['查询与 Mapping 优化','扫描、脚本或字段设计不合理','根因收益、资源更省','可能需要重建索引'],['分片与生命周期治理','过度分片或冷热混部','改善控制面与查询扇出','迁移和恢复有成本'],['扩容/资源隔离','负载合理但容量确实不足','增加总资源','不修复读放大和热点']],
    ['只看 CPU 忽略协调和队列','Profile 每个线上重查询造成额外压力','扩容掩盖过度分片'],
    ['query/fetch 分阶段耗时','线程池队列与拒绝','heap/GC','每请求分片数与 read docs'],
    '性能排查应先定位“时间和资源花在哪个阶段”，再选择索引、查询、分片或容量动作；节点多并不自动更快。'
  )
}
