import{profile as p}from'./profile.mjs'

export const architectureProfiles={
  'architecture/flash-sale-system-design':p(
    ['边缘与网关淘汰无效流量','资格校验与本地售罄','Redis 原子预扣','MQ 削峰异步下单','数据库唯一约束收口','支付关单与库存对账'],
    ['静态化、验证码、动态令牌与多维限流让绝大多数失败请求不进入核心库存链路。','资格可提前计算，本地售罄只用于减流量，库存回补时必须失效该标记。','Lua 在一个原子步骤检查用户资格、请求去重和库存扣减，单热点 Key 达瓶颈后才考虑分桶。','预扣成功事件进入有界 MQ，消费者速率受数据库可持续写能力限制，不能把队列当无限缓冲。','订单事务用请求 ID 或活动用户商品唯一约束，数据库条件更新是最终不超卖防线。','支付与关单通过状态机竞争，库存释放幂等；预扣、事件、订单、支付和库存流水按请求 ID 对账。'],
    '库存预扣成功但消息发布失败',
    '高峰时 MQ 客户端超时，接口已扣 Redis 库存却没有订单事件。Lua 同时写待投递 Stream/状态记录，后台扫描补发；超过恢复窗口仍无订单的请求才幂等回补库存。',
    [['Redis Lua + MQ','十万级以上瞬时流量、库存有限','快速原子筛选与削峰','跨 Redis/MQ 窗口需补偿'],['数据库条件更新','普通活动或库存竞争不高','正确性简单、事务收口','热点行锁和连接池上限明显'],['资格券分层','超大活动且参与者可提前筛选','进一步减少库存请求','券分发、回收与公平性复杂']],
    ['库存分桶后余量碎片导致少卖','Redis 故障时全量降级到数据库','补偿重复执行把库存加多'],
    ['边缘到库存各层淘汰率','Redis Lua P99','MQ Lag/净消费速率','预扣与成功订单差异'],
    '秒杀首先保证不超卖和核心链路存活，再通过补偿减少少卖；任何降级都不能把被挡住的洪峰直接转移到数据库。'
  ),
  'architecture/config-distribution-gray-release':p(
    ['控制面校验并发布版本','配置存储形成不可变快照','推送/拉取到客户端','本地原子切换与灰度判定','指标守护、回滚和审计'],
    ['发布入口完成 Schema、范围、权限和依赖校验，同一环境只产生单调版本。','配置中心保存不可变版本与变更 diff，不能覆盖历史值，否则回滚和审计失去依据。','长轮询、推送或 watch 只负责通知，客户端还要校验版本、签名并处理断线全量同步。','客户端先构建完整不可变快照，再原子替换引用；灰度规则必须稳定命中同一用户或实例。','业务指标和技术指标共同守护发布，达到阈值自动停止扩量或回滚，并记录操作人与原因。'],
    '错误线程池配置一分钟内扩散到全量实例',
    '发布系统只校验 JSON 格式，没有校验 corePoolSize<=maximumPoolSize，客户端应用后大量任务被拒绝。增加领域 Schema、1% 实例预检和错误率守护后，非法值在控制面直接拒绝。',
    [['长轮询/Watch','配置量中等、需要秒级更新','实现成熟、连接成本可控','断线重连与版本补偿'],['消息广播','变更频繁且已有事件基础设施','扩展性好、可审计','重复、乱序和离线客户端需处理'],['GitOps/部署配置','低频强审计配置','评审与回滚清晰','实时性较弱，不适合动态开关']],
    ['客户端逐字段修改看到半新半旧状态','灰度规则不稳定让用户反复切组','回滚只改配置却未回滚不兼容代码'],
    ['版本收敛率与最大落后','配置应用失败率','灰度组业务 SLO','发布到回滚耗时'],
    '配置发布本质是生产变更系统，必须具备类型校验、版本、灰度、守护、回滚和审计；推送成功不代表业务安全。'
  ),
  'architecture/cache-consistency':p(
    ['业务写入事实库','事务提交形成版本','删除/更新缓存','并发读回源与回填','可靠重试和对账收敛'],
    ['先明确数据库或事件日志是事实源，缓存只能从可恢复事实派生。','事务提交点给数据一个确定版本，失败时不能提前让缓存暴露未提交状态。','Cache Aside 常在提交后删除缓存，删除失败必须进入可观测重试而不是忽略。','并发慢查询可能在删除后回填旧值，版本校验、短 TTL 或延迟双删用于缩小窗口。','消息、CDC 和周期对账让长期差异收敛，并明确最大陈旧时间与人工处置。'],
    '用户资料更新后不同接口读到两个版本',
    '写服务更新数据库后删除一个缓存 Key，但聚合服务维护另一份派生缓存且未收到事件。通过统一版本事件、所有缓存记录 source_version，并用对账扫描补发后，跨服务缓存才收敛。',
    [['Cache Aside','通用读多写少','简单、按需加载','删除失败与并发回填窗口'],['写穿/写后同步','写后读必须快速一致','缓存及时更新','双写失败和顺序复杂'],['CDC/事件驱动','多缓存或多写入口','以提交日志可靠收敛','延迟、乱序和运维成本']],
    ['缓存故障时无限回源压垮数据库','旧事件晚到覆盖新版本','一致性修复无审计导致重复副作用'],
    ['源版本与缓存版本差','删除/更新重试积压','陈旧读取比例','数据库回源并发'],
    '缓存一致性的目标不是追求抽象的“绝对同步”，而是定义事实源、可接受陈旧窗口、失败恢复和最终验证；不能容忍陈旧的操作应绕过缓存。'
  ),
  'architecture/idempotency':p(
    ['客户端生成业务请求号','服务端原子登记幂等键','执行业务状态迁移','保存结果快照','重复请求返回同一结果'],
    ['幂等键必须代表同一业务意图，随机重试每次换键会完全失去去重作用。','登记与业务写最好在同一数据库事务通过唯一约束完成，先查后写存在并发窗口。','状态至少区分处理中、成功和确定失败，结果未知不能随意当失败重做。','成功结果或关键标识应保存，使重复调用能返回原订单号而不是空洞的“重复”。','幂等记录保留期要覆盖客户端、MQ 和人工重放的最大窗口，清理前确认业务已不可重试。'],
    '支付回调重复导致积分发放两次',
    '回调按 HTTP 请求 ID 去重，但渠道每次重试都会生成新请求 ID。改用支付单号+事件类型作为业务幂等键，并以积分流水唯一约束最终裁决后才正确。',
    [['唯一约束','最终副作用在单数据库','并发裁决强、证据持久','热点索引与冲突处理'],['状态表','多阶段操作和结果查询','状态可恢复、便于补偿','生命周期与清理复杂'],['下游幂等令牌','跨服务最终资源','端到端保护','依赖下游契约和保留窗口']],
    ['把请求 ID 当业务 ID 导致重试绕过去重','处理中超时后两个执行者并发恢复','幂等记录先成功但业务事务回滚'],
    ['幂等命中率','处理中超时数','唯一冲突','重复请求返回结果一致率'],
    '幂等不是简单“拒绝第二次”，而是同一业务意图重复执行仍收敛到同一状态，并能给调用方稳定结果。'
  ),
  'architecture/rate-limiting':p(
    ['请求携带身份与资源维度','选择本地/集中限流器','原子消耗令牌或配额','超限快速失败/排队','动态调整并观测拒绝'],
    ['限流维度应包含用户、IP、租户、接口和下游资源，单维度容易误伤或被绕过。','本地限流延迟低但总量不精确，集中限流全局一致但增加网络和热点。','令牌桶允许受控突发，漏桶平滑速率，滑动窗口更接近统计上限，各自语义不同。','超限应返回明确错误、Retry-After 或降级结果；无界排队只是把拒绝变成超时。','配额根据下游水位、错误率和业务优先级动态调整，发布和故障时需有保守默认值。'],
    '全局限流配置正确但数据库仍被打满',
    '总 QPS 未超阈值，但一个昂贵查询占用远多于普通请求。引入按接口成本加权的并发许可，并以数据库连接与慢查询为反馈后，限流才对准真实瓶颈。',
    [['固定窗口','简单计数和粗粒度配额','实现低成本','边界双倍突发'],['滑动窗口','需要更精确速率','边界平滑','存储与计算更高'],['令牌桶','允许短时突发的 API','吞吐与突发可配置','不直接限制在途并发']],
    ['只限 QPS 不限并发和成本','限流器故障时默认全放行','排队无上限导致超时雪崩'],
    ['放行/拒绝率','在途并发','下游饱和水位','不同租户公平性'],
    '限流阈值应来自被保护资源的可持续能力；对慢请求，限制并发往往比只限制每秒请求数更有效。'
  ),
  'architecture/resilience':p(
    ['请求进入隔离舱','应用超时预算','失败触发有界重试','熔断并返回降级','半开探测后恢复'],
    ['线程池、连接池或信号量隔离不同依赖，避免一个故障耗尽全部执行资源。','端到端超时预算要逐级递减，内部调用不能各自使用比入口更长的超时。','重试只适合瞬时故障和幂等操作，应使用退避、抖动和重试预算防止流量放大。','熔断器依据失败/慢调用在窗口内达到阈值后快速拒绝，降级必须是业务可接受结果。','半开状态只允许少量探测，恢复应渐进，避免依赖刚好转立即被全量流量再次打垮。'],
    '下游超时引发整站线程耗尽',
    '每个请求串行调用下游三次重试，每次 2 秒，入口线程大量阻塞。将下游隔离、总预算 800ms、只对安全错误重试一次并设置熔断后，故障不再跨服务扩散。',
    [['超时+有界重试','短暂网络抖动、操作幂等','提高瞬时成功率','放大下游压力和尾延迟'],['熔断+降级','依赖持续故障','快速失败保护资源','阈值错误会误熔断'],['隔离舱','多依赖/租户风险不同','限制故障范围','资源碎片与配额治理']],
    ['每层各重试三次形成指数放大','降级返回成功却写入错误业务状态','半开一次放全量流量'],
    ['超时与慢调用率','重试放大倍数','熔断状态变化','隔离池饱和与拒绝'],
    '韧性设计目标是控制故障半径和恢复速度，不是隐藏所有错误；降级结果必须在业务语义上真实且可追踪。'
  ),
  'architecture/delayed-task':p(
    ['创建任务与执行时间','持久化并进入时间索引','调度器领取到期任务','工作者幂等执行','重试/取消/对账收口'],
    ['任务记录至少包含业务 ID、执行时间、状态、重试次数和幂等键，创建与业务事务需避免双写丢失。','短延迟可用时间轮，长延迟可用数据库扫描、Redis ZSet 或分层消息，精度和容量不同。','调度器通过分片租约或 SKIP LOCKED 领取，必须避免多实例重复占有且允许租约超时恢复。','执行端默认至少一次，外部副作用由业务幂等键裁决；处理时间不能阻塞调度扫描。','失败按错误类型退避重试，取消与执行并发通过状态机条件更新决定唯一结果，周期对账修复遗漏。'],
    '订单超时关闭任务漏执行',
    '服务先创建订单事务，提交后再写延迟队列，进程崩溃留下永不关闭的订单。将任务记录与订单放同一事务并由调度器扫描，或用 Outbox 投递延迟消息后才消除窗口。',
    [['数据库时间索引','任务量中等且要求可靠','事务与查询直观','高频扫描和热点需治理'],['Redis ZSet/时间轮','短延迟、高吞吐','调度效率高','持久性、故障恢复需补充'],['延迟消息/分层 Topic','已有 MQ 且延迟档位固定','削峰、消费体系成熟','精度、回溯和取消较复杂']],
    ['业务提交与任务创建双写不一致','任务执行超时后租约过期导致并发执行','重试所有错误造成永久失败风暴'],
    ['到期到执行延迟','待执行/重试积压','重复执行率','超时未关闭业务差异'],
    '延迟任务不是定时器 API 问题，而是持久状态、至少一次执行、幂等和对账的完整系统；精度越高，成本和故障复杂度越大。'
  ),
  'architecture/distributed-id':p(
    ['请求 ID','选择时间/号段/随机源','生成节点与序列字段','校验唯一与时钟','传播到存储日志与链路'],
    ['ID 需求先明确唯一范围、趋势递增、长度、信息泄漏、生成吞吐和多机房容灾。','Snowflake 用时间戳、节点号和序列，号段服务从数据库批量领取区间，UUID 则靠随机空间。','节点号分配和序列溢出必须受控，容器弹性环境不能手工写死重复 workerId。','时钟回拨会让时间型 ID 重复或停发，应检测、等待、切换逻辑时钟或隔离节点。','ID 要作为请求、订单、消息和日志的统一关联键，跨边界不能重新生成导致证据链断裂。'],
    '容器扩容后出现极少量主键冲突',
    '多个实例从环境变量读取相同 workerId，毫秒内序列也相同。改用租约式节点号分配、启动冲突检测和数据库唯一约束兜底后才安全。',
    [['数据库自增/号段','单库或可接受集中分配','简单/趋势递增','中心依赖或批次空洞'],['Snowflake','高吞吐本地生成','趋势递增、无中心请求','时钟和节点号治理复杂'],['UUID/ULID','跨环境独立生成','碰撞概率低、接入简单','索引局部性、长度或排序语义不同']],
    ['workerId 重复','时钟回拨未处理','把趋势递增误认为全局严格连续'],
    ['生成失败/等待数','时钟偏差','节点号租约冲突','数据库唯一冲突'],
    '分布式 ID 只能在明确假设下保证唯一；最终存储仍应保留唯一约束，趋势递增也不等于业务时间顺序。'
  ),
  'architecture/distributed-transaction':p(
    ['定义跨服务不变量','本地事务落状态与事件','异步驱动后续步骤','失败补偿或重试','对账确认最终收敛'],
    ['先识别必须原子的不变量与允许的中间状态，避免一开始就要求所有服务强一致。','每个服务只在自己的数据库完成本地事务，Outbox 可把状态与待发布事件原子写入。','Saga 事件/命令驱动后续参与者，步骤必须幂等并记录全局事务与本地状态。','失败时可重试可恢复错误，业务拒绝则执行补偿；补偿是新业务动作，不是时间倒流。','对账按全局事务 ID 比较各参与者状态，修复长期卡住或人工处理不可自动补偿的差异。'],
    '下单成功但优惠券一直未返还',
    '订单取消事件发送失败，券服务没有收到补偿。订单状态正确但全局事务未闭环。通过事务 Outbox、补偿状态机和超时扫描重发后，失败窗口可恢复。',
    [['2PC/XA','参与资源少且强一致优先','统一提交语义','阻塞、协调器和跨服务可用性成本'],['Saga','长事务、业务可补偿','服务自治、可用性高','中间状态与补偿复杂'],['TCC','资源可显式 Try/Confirm/Cancel','预留清晰、可控','接口侵入大、悬挂空回滚治理']],
    ['补偿操作无幂等导致重复退款','只发 MQ 不解决本地提交与发送窗口','全局状态机没有超时终态'],
    ['各状态停留时长','事件投递重试','补偿成功率','对账差异数'],
    '分布式事务的核心是把部分失败变成可记录、可重试、可补偿和可审计的状态；协议选择取决于不变量与业务可补偿性。'
  ),
  'architecture/observability':p(
    ['请求进入生成 Trace/Request ID','各服务记录结构化日志','采集指标与分布式 Span','关联发布和业务事件','告警定位并驱动处置'],
    ['入口生成或接受可信请求标识，并在日志、消息和下游调用中传播，避免链路断点。','日志记录离散事件和上下文，必须结构化、分级并控制敏感字段与高基数。','指标适合趋势和告警，Trace 展示单请求因果与耗时；三者不能互相完全替代。','发布、配置、扩容和业务活动作为事件叠加到时间轴，能快速判断相关性。','告警应从 SLO 与用户影响出发，包含负责人、Runbook 和可执行上下文，避免只报 CPU 高。'],
    '接口错误率升高却无法定位租户',
    '日志只有异常堆栈，没有 tenant_id、request_id 和发布版本；指标又只按全局聚合。补齐统一上下文、按受控维度拆分 RED 指标并串联 Trace 后，发现单租户异常输入触发。',
    [['日志','离散错误与详细上下文','证据丰富、可审计','成本高且全文检索慢'],['指标','趋势、聚合和告警','存储高效、适合 SLO','高基数受限，细节少'],['Trace','跨服务因果与单请求长尾','定位链路瓶颈','采样、上下文传播与成本']],
    ['日志高基数字段导致成本失控','只监控资源不监控业务成功率','Trace 采样漏掉稀有错误且无错误保留策略'],
    ['RED/USE 指标','SLO burn rate','Trace 完整率','日志延迟与丢弃率'],
    '可观测性不是“收集更多数据”，而是让系统内部状态能被外部证据解释；每个信号都应服务一个诊断或决策问题。'
  )
}
