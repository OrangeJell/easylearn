import{profile as p}from'./profile.mjs'

export const kafkaProfiles={
  'kafka/message-loss-prevention':p(
    ['业务事务形成待发送事件','生产者带幂等与确认发送','Broker 在 ISR 内复制','消费者处理业务副作用','提交位点并通过对账闭环'],
    ['数据库状态与发送动作之间用 Outbox 或 CDC 形成可恢复证据，避免提交后进程崩溃丢消息。','生产者启用幂等、acks=all 和有界重试，超时按结果未知处理而不是生成新业务请求。','replication.factor 与 min.insync.replicas 共同决定故障时写入是否继续以及能承受几个副本损失。','消费者先完成业务事务，最终写入点用唯一约束或状态机幂等，不能把内存处理完成当成成功。','offset 在业务成功后提交，投递记录、业务结果和位点按事件 ID 对账，修复长期差异。'],
    '数据库提交成功但 Kafka 中没有订单事件',
    '服务在事务提交后同步 send，进程在两步间崩溃。Kafka 配置再可靠也看不到未发送事件。把事件写入同库 Outbox，由独立发布器重试并记录 broker offset 后消除窗口。',
    [['同步 send + 回调','低频且业务可接受明确失败','链路简单','数据库提交与发送仍有双写窗口'],['事务 Outbox','数据库状态必须对应事件','本地事务原子、可重试对账','投递延迟与表治理'],['CDC 读取 binlog','多服务统一捕获数据库变更','业务侵入低、顺序证据强','运维、Schema 演进和回放复杂']],
    ['生产者超时后换业务 ID 重发造成重复','ISR 不足仍允许不干净选主','消费先提交 offset 后写数据库'],
    ['Outbox 未投递数','生产确认与错误率','ISR/URP','事件到业务结果差异'],
    '“不丢消息”必须定义确认边界和故障模型；跨数据库与 Kafka 的可靠性只能由持久事件、幂等和对账共同完成。'
  ),
  'kafka/consumer-group':p(
    ['消费者加入组','协调器完成分区分配','按 offset 拉取批次','处理并提交位点','成员变化触发再均衡'],
    ['group.id 标识逻辑消费订阅，同组内一个分区在稳定时期只分配给一个消费者。','Coordinator 与分区分配策略决定成员和分区映射，成员数超过分区数会有空闲消费者。','消费者按各分区 offset 拉取，拉取批次、等待和最大记录数共同影响吞吐与内存。','处理完成后提交“下一条要消费的 offset”，自动提交可能发生在业务成功之前。','心跳超时、订阅变化和扩缩容会触发 rebalance；协作式策略可减少不必要的全量撤销。'],
    '扩容消费者后延迟反而抖动',
    '频繁滚动发布让组不断再均衡，消费者停止处理并重复转移分区。使用静态成员、协作式粘性分配与更平缓发布后，分区所有权稳定，Lag 才下降。',
    [['Range/RoundRobin','简单订阅与分区较均匀','易理解','多 Topic 或异构分区可能倾斜'],['Sticky','希望减少迁移并保持均衡','再均衡后缓存局部性较好','仍可能是 eager 全量撤销'],['CooperativeSticky','大组和频繁扩缩容','增量再均衡、停顿小','客户端与协议版本需兼容']],
    ['消费者数超过分区数期待继续扩吞吐','处理未完成就自动提交','耗时处理超过 max.poll.interval 反复再均衡'],
    ['Consumer Lag','rebalance 次数与时长','poll 间隔','各分区消费速率'],
    '消费组提供分区级负载均衡，不提供业务幂等；扩容上限由分区数和下游处理能力共同决定。'
  ),
  'kafka/reliability':p(
    ['生产者发送并等待确认','Leader 追加日志','Follower 拉取并进入 ISR','高水位推进可见性','消费者处理并提交位点'],
    ['acks、重试和幂等生产者决定客户端如何确认写入；超时可能代表失败也可能代表结果未知。','Leader 先把记录追加到本地分区日志，顺序仅在单分区内定义。','ISR 副本持续追赶，min.insync.replicas 与 acks=all 共同约束可接受写副本数。','高水位限制消费者只读取已被足够副本确认的日志，避免暴露随后丢失的记录。','消费端可靠性取决于处理与 offset 提交顺序，外部副作用仍需幂等或事务协调。'],
    'Broker 故障后仍出现少量消息丢失',
    'Topic 有三副本，但生产者使用 acks=1，Leader 本地确认后尚未复制就宕机。副本数不等于写确认强度；改为 acks=all、min.insync.replicas=2 并禁用不干净选主后才符合 RPO。',
    [['acks=1','允许极小丢失窗口且延迟敏感','确认快','Leader 故障可能丢已确认写'],['acks=all + min ISR','核心事件','副本确认更可靠','副本不足时拒绝写入'],['端到端 Outbox + 幂等','数据库状态与消息需一致','跨系统有可恢复证据','链路和运维更复杂']],
    ['只配 replication.factor 忽略 acks','允许 unclean election 换可用性','消费成功前提交 offset'],
    ['UnderReplicatedPartitions','ISR 收缩','生产错误与重试','消费位点与业务结果差异'],
    '可靠性是生产、Broker、消费和业务落库的端到端属性；任何一段只做到“不容易丢”都不能推出整体恰好一次。'
  ),
  'kafka/duplicate-consumption-idempotency':p(
    ['拉取消息','执行业务副作用','记录幂等键/状态','提交 offset','失败后重新投递并识别重复'],
    ['Kafka 默认至少一次场景下，崩溃、超时和再均衡都可能让同一 offset 再次交付。','业务处理可能写数据库、调用外部接口或发新消息，不同副作用需要各自的幂等证据。','幂等键通常来自业务请求 ID，并由唯一约束或状态机在最终写入点原子裁决。','只有业务事务成功后才提交 offset；提交成功与否仍可能结果未知，因此不能靠提交消灭重复。','重复到达时查询已有结果并返回成功，而不是再次执行；过期清理需覆盖 Kafka 最大重放窗口。'],
    '优惠券发放偶发重复',
    '消费者先调用券系统再记录已处理标记，进程在两步之间宕机，重试再次发券。将券请求号作为下游幂等键，并在本地数据库用唯一约束记录处理状态后，跨系统两端都有防线。',
    [['数据库唯一约束','副作用最终落同一数据库','正确性证据强、实现直接','冲突处理与热点索引成本'],['幂等状态表','多步骤业务需记录处理中/成功','状态可追踪、可补偿','表增长与清理治理'],['下游幂等 API','副作用在外部系统','真正保护最终资源','依赖对方契约和保留窗口']],
    ['用内存 Set 去重重启后失效','幂等记录和业务写不在同一事务','幂等键 TTL 小于消息回放周期'],
    ['唯一冲突/幂等命中率','处理到提交耗时','重复投递来源','处理中超时记录数'],
    '幂等不是“忽略重复”，而是相同业务请求无论执行多少次都收敛到同一结果，并能返回原结果或可解释状态。'
  ),
  'kafka/message-backlog-troubleshooting':p(
    ['发现 Consumer Lag 上升','判断生产增速或消费降速','定位分区/实例瓶颈','止损扩容或降级','估算追赶时间并核对结果'],
    ['Lag 是生产 offset 与已提交 offset 的差，应按分区观察趋势而非只看总量。','首先比较生产速率和消费速率，区分流量突增、消费者变慢、再均衡或提交停滞。','热点分区、下游慢调用、GC、错误重试和单条毒消息都会造成局部或全局积压。','止损可暂停非核心生产、扩容到分区上限、批量处理或隔离失败消息，但不能压垮数据库。','预计追赶时间约为 backlog/(消费能力-新增速率)，净能力不为正时单纯等待永远追不上。'],
    '订单事件积压数小时仍无法追平',
    '消费者扩容后数据库连接池已饱和，更多实例只增加等待，净消费能力未提升。优化批量写、提高分区并按数据库可承载并发设置消费者后，才形成正的追赶速率。',
    [['增加消费者','有空闲分区且下游有余量','快速提高并行度','受分区数和下游容量限制'],['批量处理','业务可批量写入/调用','摊薄网络和事务成本','失败粒度与内存变大'],['降级/跳过非核心事件','积压威胁核心 SLO','快速恢复核心链路','需要补偿、审计和业务批准']],
    ['总 Lag 掩盖单分区热点','无限重试毒消息阻塞分区','扩容消费者压垮下游'],
    ['各分区 Lag','生产/消费净速率','单批处理时长','下游连接与错误率'],
    '积压恢复必须同时保证正确性；跳过、改 offset 或扩大批次都要明确重复、丢失和乱序的补偿方式。'
  ),
  'kafka/isr-leader-election':p(
    ['Leader 接收写入','Follower 持续拉取','满足条件留在 ISR','Leader 故障触发选举','新 Leader 恢复服务并截断分叉'],
    ['分区 Leader 负责读写，生产者与消费者通过元数据找到当前 Leader。','Follower 从 Leader 复制日志，复制速度和网络决定落后程度。','ISR 是当前与 Leader 保持足够同步的副本集合，不等同于所有配置副本。','正常选举从 ISR 选择新 Leader，配合 acks=all 可以限制已确认数据丢失。','落后副本上的分叉日志在重新加入时会按 Leader epoch 等信息截断，保证单一日志历史。'],
    '机架故障后部分 Topic 无法写入',
    '三副本实际都落在同一机架，机架断电后无可用 ISR。配置副本数没有解决故障域问题；启用 rack awareness 并校验副本分布后才能承受整机架故障。',
    [['unclean election=false','核心数据不能接受已确认丢失','只从 ISR 选主','全部 ISR 不可用时分区不可用'],['unclean election=true','可用性优先且数据可重建','更快恢复服务','可能丢数据并产生历史截断'],['多机架 ISR','需承受故障域损失','副本真正隔离','跨机架带宽与延迟成本']],
    ['副本数足够但故障域集中','ISR 长期收缩未告警','为可用性开启不干净选主却未接受数据损失'],
    ['ISR 数量','UnderReplicatedPartitions','Follower lag','选举次数与不可用时长'],
    'ISR 是动态健康集合；可靠性要看最坏故障时仍有多少同步副本，而不是只看 replication.factor 配置值。'
  ),
  'kafka/message-ordering':p(
    ['选择消息 Key','分区器映射到单分区','Leader 按追加顺序写日志','消费者按分区读取','业务按版本/状态机应用'],
    ['Kafka 只保证单分区日志顺序，因此有顺序关系的消息必须使用稳定 Key 路由到同一分区。','分区器变更、分区数增加或 Key 缺失会改变映射，历史与新消息可能不在同一分区。','幂等生产者保持单会话内重试顺序，但错误配置并发和跨会话重发仍需理解。','同一分区可以顺序拉取，但消费者把批次提交到并行线程后可能在业务完成层乱序。','最终状态应用可携带单调版本，拒绝旧版本，把传输顺序问题转化为可检测状态规则。'],
    '订单创建和取消事件倒序生效',
    '两类事件使用不同 Key，进入不同分区后取消先被消费，创建后到又把订单恢复。统一 order_id 分区并在数据库按 version 条件更新后，顺序和最终裁决都有保证。',
    [['同 Key 单分区','单实体事件有严格顺序','Kafka 原生顺序语义','单实体吞吐受一个分区限制'],['消费者按 Key 串行','分区内仍需并行不同实体','提高总体吞吐','调度、失败和提交复杂'],['版本号/状态机','允许传输乱序但最终状态可判定','跨分区也可拒绝旧事件','需要业务版本和补偿缺口']],
    ['扩分区后 Key 映射改变','消费者并行线程完成顺序失控','全局顺序要求导致单分区瓶颈'],
    ['同 Key 分区一致性','版本拒绝次数','分区处理延迟','乱序时间差'],
    '多数业务只需要实体内顺序而非全局顺序；先缩小顺序域，再用 Key、串行执行和版本号形成多层保证。'
  ),
  'kafka/partition-planning':p(
    ['估算目标吞吐','测量单分区生产消费能力','考虑消费者并行度','加入故障与增长余量','创建并监控分区分布'],
    ['分区数下限取决于目标吞吐除以单分区生产或消费能力的较大需求。','单分区能力必须用真实消息大小、压缩、acks 和磁盘网络测量，不能引用固定经验值。','同组消费者并行上限受分区数限制，但分区过多会增加文件句柄、控制器和选举负担。','需要为单 Broker 故障、流量增长和再分配保留余量，同时评估副本网络放大。','上线后观察 Key 倾斜、分区字节和 Lag；增加分区不会自动重平衡历史数据，也可能影响 Key 顺序。'],
    'Topic 分区翻倍后同用户顺序被打乱',
    '默认哈希取模因分区数变化而改变 user_id 映射，新消息进入新分区，与旧消息并行消费。扩容前应评估顺序域、使用版本裁决或新 Topic 迁移。',
    [['少量大分区','吞吐不高且顺序域集中','管理简单','扩容和并行余量小'],['按容量预留分区','增长可预估','减少频繁扩分区','空分区和元数据成本'],['新 Topic 迁移','需改变分区策略或大幅扩容','可灰度并保持旧规则','双写、切换与回放复杂']],
    ['按峰值 QPS 除经验常数拍分区数','Key 倾斜让平均容量失真','增加分区破坏既有顺序映射'],
    ['单分区 bytes/sec','分区 Lag 偏斜','Broker 网络/磁盘','Leader 分布'],
    '分区是吞吐、顺序和运维开销的共同单位；规划要保留增长余量，但不能把“越多越好”当成扩展策略。'
  ),
  'kafka/transactions-exactly-once':p(
    ['初始化事务生产者','消费输入并处理','事务内发送输出','提交 offset 到同一事务','Broker 提交标记后下游 read_committed'],
    ['transactional.id 让 Broker 识别生产者世代并 fencing 旧实例，避免僵尸生产者继续提交。','消费-处理-生产循环把输出记录和输入 offset 放入一个 Kafka 事务。','sendOffsetsToTransaction 绑定当前消费组元数据，使输入进度与输出结果共同提交。','Broker 追加事务提交/中止标记，失败重试通过幂等序列号避免日志重复。','read_committed 消费者跳过未提交和已中止记录，但数据库、HTTP 等外部副作用不在 Kafka 事务内。'],
    '流处理输出不重复但数据库仍重复更新',
    'Kafka Streams 输出 Topic 使用 EOS，随后消费者调用数据库普通 INSERT，重放时仍重复。EOS 边界只覆盖 Kafka 内部；数据库需唯一业务键、幂等 upsert 或 Outbox/CDC 协调。',
    [['幂等生产者','只防生产重试写重复','默认易用、开销低','不原子绑定多个分区与消费位点'],['Kafka 事务/EOS','consume-transform-produce 全在 Kafka','位点与输出原子','事务开销、超时与隔离级别复杂'],['业务幂等/Outbox','副作用跨数据库或外部系统','覆盖最终资源','需要状态表、重试和对账']],
    ['把 Kafka EOS 延伸解释到数据库','transactional.id 多实例冲突互相 fencing','事务过长超过 timeout'],
    ['事务提交/中止率','producer fencing','read_committed 延迟','外部幂等冲突'],
    'Exactly-once 必须说明边界；Kafka EOS 可以保证 Kafka 读写闭环一次生效，边界外仍需要幂等和可恢复协议。'
  ),
  'kafka/zero-copy':p(
    ['日志页进入 Page Cache','消费者请求连续日志段','Broker 定位文件区间','sendfile 传给 Socket','网卡发送并由客户端解析'],
    ['Kafka 追加日志主要依赖操作系统 Page Cache，顺序写让磁盘与缓存效率较高。','拉取请求按 offset 定位 segment 和 position，批次格式可直接复用生产端压缩记录。','传统 read+write 会把数据从内核复制到用户缓冲再写回内核 Socket 缓冲。','sendfile/transferTo 可让内核在文件页和网络栈之间传输，减少用户态复制与上下文切换。','TLS、压缩重编码或某些平台能力可能改变路径，最终收益应以 CPU 与吞吐测量。'],
    '开启 TLS 后 Broker CPU 明显升高',
    '团队假设零拷贝始终存在，忽略 TLS 加密需要处理数据。开启加密后用户态/内核路径变化，CPU 成为瓶颈。通过真实 TLS 压测、硬件加速和连接批次调优重新规划容量。',
    [['普通 read/write','通用变换与小数据','实现灵活','多次复制与系统调用'],['sendfile','文件到 Socket 且无需内容变换','减少复制和 CPU','受 TLS、平台与协议路径限制'],['mmap','随机读或共享文件页','减少显式 read','页故障、地址空间和生命周期复杂']],
    ['把零拷贝理解为完全没有复制','忽略 TLS 使容量估算失真','小消息无批量导致系统调用仍多'],
    ['Broker network/CPU','Page Cache 命中','系统调用与上下文切换','TLS 加密耗时'],
    '零拷贝是减少数据跨内核/用户边界移动的技术，不代表磁盘、网络或校验成本消失；Kafka 吞吐还依赖批量和顺序 I/O。'
  )
}
