import{profile as p}from'./profile.mjs'

export const mysqlProfiles={
  'mysql/btree-index':p(
    ['根据条件构造搜索键','从 B+Tree 根节点下降','定位叶子页区间','回表或直接取覆盖列','按顺序返回结果'],
    ['优化器先依据谓词和索引定义构造可用搜索边界，联合索引遵守最左前缀与范围截断规律。','非叶子节点只保存键和子页指针，扇出高使数千万行通常只需少数层随机访问。','叶子页有序并通过双向链连接，范围扫描可顺序读取相邻记录。','二级索引叶子保存主键，缺少目标列时要回聚簇索引；覆盖索引可消除这一步。','顺序与索引一致时可直接输出，反向或不匹配排序可能产生 filesort 与额外临时空间。'],
    '联合索引存在但查询仍扫描大量行',
    '索引为 (tenant_id,status,created_at)，查询只给 status 和时间，缺少第一列，无法建立连续搜索区间。补齐租户条件或按真实访问模式重建索引后，扫描行数从百万降到数百。',
    [['单列索引','独立高选择性条件','定义简单、复用直接','难覆盖组合过滤与排序'],['联合索引','固定组合过滤和排序','一次定位并可覆盖查询','列顺序依赖访问模式'],['覆盖索引','热点查询返回列稳定且少','减少回表 I/O','索引更宽，写放大与缓存占用增加']],
    ['函数或隐式转换让索引列失去可搜索性','低选择性索引回表成本高于全表扫','过多冗余索引拖慢写入'],
    ['EXPLAIN 实际扫描行数','回表次数','Buffer Pool 读页','索引大小与写放大'],
    '索引是否有效由数据分布、过滤组合、返回列和排序共同决定；“建了索引”不是结论，必须用真实执行计划和 rows examined 验证。'
  ),
  'mysql/transactions-mvcc':p(
    ['事务分配标识','更新生成新版本与 undo','读取建立 Read View','沿版本链判断可见性','提交后 Purge 清理历史'],
    ['InnoDB 事务开始与首次一致性读的时机受隔离级别影响，Read View 不一定在 begin 时创建。','更新记录写入新值，并通过隐藏事务 ID 与 roll pointer 连接 undo 中的旧版本。','Read View 保存活跃事务范围，用于判断某版本在当前快照是否可见。','不可见时沿 undo 版本链回溯；长事务会让链条变长并增加读放大。','历史版本只有在不再被任何活跃快照需要时才能 Purge，长事务会阻塞回收。'],
    '报表长事务导致 undo 膨胀',
    '只读报表保持事务数小时，线上更新持续产生 undo，History List Length 不断上升，磁盘和查询延迟恶化。把报表拆成短快照批次并在只读副本执行后，Purge 才能追上。',
    [['READ COMMITTED','允许同事务两次读看到新提交','版本链压力较低、语义直观','不可重复读'],['REPEATABLE READ','事务内需要一致快照','InnoDB 默认且配合 Next-Key Lock','长事务持有旧快照更久'],['SERIALIZABLE/显式锁读','关键不变量需强串行','语义最强','锁竞争与吞吐成本高']],
    ['把快照读误认为完全无锁事务','长事务阻塞 Purge','更新后又把当前读和快照读结果混用'],
    ['活跃事务时长','History List Length','undo 表空间','版本链读取耗时'],
    'MVCC 解决读写并发的版本可见性，不替代唯一约束和业务不变量；写写冲突仍需锁，当前读也会参与锁定。'
  ),
  'mysql/locks-deadlock':p(
    ['事务定位索引记录','申请记录或间隙锁','冲突后进入等待','等待图形成环或超时','选择牺牲者回滚'],
    ['InnoDB 锁作用于索引记录；没有合适索引时扫描和锁定范围可能远超业务认知。','Record、Gap 与 Next-Key Lock 组合用于记录互斥和防止特定范围幻读，隔离级别与语句类型会改变行为。','锁冲突线程等待持有者释放，长事务和外部 I/O 会放大等待链。','两个事务以不同顺序获取资源会形成 wait-for 环，InnoDB 检测后选择成本较小事务回滚。','死锁是并发控制的正常可恢复结果，应用应识别错误码、回滚完整事务并做有界重试。'],
    '批量更新顺序不一致导致持续死锁',
    '两个订单任务分别按输入顺序更新商品库存，相同商品集合顺序不同，形成交叉锁。提交前按 sku_id 排序并缩短事务后，死锁显著下降；重试仍保留以处理其他不可避免冲突。',
    [['一致加锁顺序','同类批量资源','从设计上减少环','所有路径必须遵守'],['短事务 + 有界重试','冲突偶发且操作幂等','恢复简单、吞吐较好','重试会增加瞬时负载'],['串行化/分区队列','热点键冲突极高','消除同键并发竞争','增加排队与分区治理']],
    ['缺少索引导致锁范围扩大','事务内调用远程服务长期持锁','捕获死锁后只重试单条语句'],
    ['锁等待时间','死锁日志频率','事务持续时间','扫描并锁定行数'],
    '死锁回滚的是完整事务，不是“失败的那条 SQL”；重试必须从事务边界重新开始并确保外部副作用幂等。'
  ),
  'mysql/redo-undo-binlog':p(
    ['修改 Buffer Pool 页','生成 undo 旧版本','追加 redo 物理日志','两阶段协调 binlog','刷盘恢复与复制消费'],
    ['数据页通常先在内存修改为脏页，不要求事务提交时立即刷回表空间。','undo 保存逻辑旧版本，支持事务回滚和 MVCC 快照读取，最终由 Purge 清理。','redo 记录页级物理变化并按 WAL 顺序追加，使崩溃后可重放已提交修改。','MySQL Server 层 binlog 与 InnoDB redo 通过 prepare/commit 两阶段协调，避免主库恢复与复制日志不一致。','恢复时根据 redo 状态和 binlog 事务决定提交或回滚，副本与 CDC 则消费 binlog 的逻辑变更。'],
    '主库宕机后数据存在但下游未收到事件',
    '错误地把业务消息发送放在数据库提交之后，进程在提交成功与发送之间崩溃。redo/binlog 能恢复数据库，却不能自动补发外部消息。使用事务 Outbox 从 binlog 或可靠扫描投递后才补齐跨系统窗口。',
    [['redo','InnoDB 崩溃恢复','顺序 WAL、恢复页修改','不提供跨库逻辑复制接口'],['undo','回滚与一致性读','保留旧版本','长事务会阻塞清理'],['binlog','复制、备份和 CDC','逻辑事务流、跨存储消费','保留与格式需要治理']],
    ['把 redo 当成数据库备份','长事务导致 undo 历史增长','binlog 未持久化策略与故障目标不匹配'],
    ['redo 写入与 checkpoint 年龄','History List Length','binlog flush 延迟','脏页比例'],
    '三类日志服务不同目标：redo 保崩溃恢复，undo 保回滚与版本，binlog 保逻辑复制；跨系统一致性仍需额外协议。'
  ),
  'mysql/replication-high-availability':p(
    ['主库提交事务','生成并发送 binlog','副本接收 relay log','并行重放事务','故障检测后切换流量'],
    ['主库提交成功的持久化级别由 redo 与 binlog 刷盘参数决定，复制承诺从这里开始。','dump 线程或协议把 binlog 发送给副本；异步、半同步决定主库等待到什么确认点。','副本 I/O 线程接收事件写 relay log，网络抖动会形成接收延迟。','SQL/applier 线程按依赖并行执行，热点大事务和 DDL 可能限制并行度。','高可用系统判断主库故障、选择数据最新且健康的副本晋升，并通过 fencing 阻止旧主继续写。'],
    '网络分区导致双主写入',
    '控制面无法访问旧主就直接晋升副本，但旧主仍服务部分客户端，形成两个写入源。增加租约或 STONITH fencing、单写入口和旧主只读校验后，切换才不会只解决可用性却破坏一致性。',
    [['异步复制','允许少量 RPO 且延迟敏感','主库提交快','主故障可能丢未复制事务'],['半同步复制','希望降低数据丢失窗口','至少一个副本收到日志','副本或网络慢会影响提交'],['组复制/共识方案','自动成员管理与更强单主约束','故障转移集成度高','运维复杂、写延迟与限制更多']],
    ['只看 Seconds_Behind_Master 判断延迟','切换未 fencing 旧主','大事务让复制追赶时间超出 RTO'],
    ['复制字节与应用位置差距','事务重放延迟','副本并行工作线程利用率','切换 RTO 与数据差异'],
    '高可用必须明确 RPO、RTO 和故障模型；“有主从”不等于可切换，切换还需要选主、隔离旧主、路由刷新和数据核对。'
  ),
  'mysql/sql-execution-explain':p(
    ['解析与权限检查','优化器枚举访问路径','依据统计信息估算成本','执行器拉取并连接记录','EXPLAIN ANALYZE 对比估算与实际'],
    ['SQL 先解析为内部结构并完成对象、权限与语义检查。','优化器考虑索引、连接顺序、访问方法和排序聚合策略，搜索空间大时会做剪枝。','基数、选择性和直方图决定行数估算，统计失真会让正确索引也被错误排序。','执行器按计划从存储引擎取行并执行过滤、连接、排序和聚合，中间结果大小决定真实成本。','EXPLAIN ANALYZE 实际执行并给出每节点行数与时间，可定位“估算错”还是“操作本身贵”。'],
    '新增索引后查询反而更慢',
    '优化器高估过滤选择性，选择新索引后回表数十万次；全表顺序扫描原本更便宜。更新统计、增加覆盖列并用 EXPLAIN ANALYZE 核对估算与实际后，计划才稳定。',
    [['普通 EXPLAIN','上线前查看计划结构','安全快速','只有估算没有真实耗时'],['EXPLAIN ANALYZE','测试环境或可控查询','实际行数和节点时间清晰','会真正执行，写语句和慢查询需谨慎'],['Optimizer Trace','计划选择原因难理解','展示候选成本与裁剪','输出复杂且有运行开销']],
    ['只看 type 不看实际扫描行数','把 Using filesort 一律视为错误','在生产直接分析不可控重查询'],
    ['估算与实际行数偏差','rows examined/returned','回表与临时表','排序和连接节点耗时'],
    '执行计划是某一时刻统计、参数和数据分布下的选择；优化要验证实际工作量，并考虑计划随数据增长和参数偏斜变化。'
  ),
  'mysql/slow-sql-troubleshooting':p(
    ['确认慢请求时间窗','从慢日志定位 SQL 指纹','分析计划与等待','最小改动优化','回放灰度并持续观测'],
    ['先区分单条 SQL 慢、排队慢、网络慢还是事务等待，调用链时间不等于数据库执行时间。','按归一化指纹聚合总耗时和调用次数，高频中等慢 SQL 往往比极少数最慢查询影响更大。','结合 EXPLAIN ANALYZE、锁等待、Buffer Pool 和 I/O 判断是扫描、回表、排序、锁还是资源饱和。','优先减少读取行数和返回数据，再考虑索引、SQL 重写、缓存或模型调整；强制 Hint 只作为受控手段。','使用真实参数分布回放，灰度观察 P99、CPU、写成本和其他查询计划是否回归。'],
    '大促期间接口从 50ms 升到 3s',
    '慢日志显示 SQL 本身执行约 80ms，但连接池等待超过 2s；根因是另一批报表查询占满连接。隔离报表池、限制并发并优化扫描后才恢复，单纯给接口 SQL 加索引没有作用。',
    [['索引/SQL 重写','访问路径和扫描量不合理','通常收益直接','增加写成本或只适配特定条件'],['缓存/预计算','结果复用高且允许一定陈旧','大幅减少数据库负载','一致性与击穿治理复杂'],['读写分离/分片','单实例容量真实不足','扩大读或数据容量','复制延迟和路由复杂度上升']],
    ['只按耗时排序忽略调用次数','优化一条 SQL 却压垮写入','参数偏斜导致测试计划与生产不同'],
    ['SQL 指纹总耗时','连接池等待','实际扫描行数','锁等待与 I/O 延迟'],
    '慢 SQL 排查要从请求端到数据库形成时间分解；只有证明时间花在执行器和具体节点，索引优化才是正确动作。'
  ),
  'mysql/deep-pagination':p(
    ['接收页码与排序','索引扫描跳过 offset','逐行回表或过滤','取 limit 条记录','返回下一页定位信息'],
    ['LIMIT offset,limit 需要找到并丢弃前 offset 行，页码越深工作量线性增长。','若排序索引不覆盖返回列，跳过的每行仍可能回表，I/O 放大更严重。','WHERE 过滤不能完全在索引完成时，执行器还需读取记录再判断。','真正返回的行数很少，但 rows examined 和临时排序空间可能巨大。','游标分页把上一页最后排序键作为下一页起点，扫描量接近 limit，并能提供更稳定翻页语义。'],
    '后台导出任务拖慢线上查询',
    '导出按页码每次 offset 增加 1000，到后期单页扫描数百万行。改为按 (created_at,id) 复合游标推进，并记录检查点后，单批成本稳定且可断点续跑。',
    [['offset 分页','浅页、允许随机跳页','接口简单、总数展示自然','深页线性变慢且并发变更会漂移'],['Keyset 游标','顺序翻页与无限滚动','性能稳定、重复遗漏少','不能自然随机跳页'],['异步导出','全量或超大结果集','隔离在线请求、可断点','结果延迟且需任务治理']],
    ['只用非唯一时间字段做游标导致遗漏','深分页加缓存仍计算昂贵','COUNT(*) 与数据页一起拖慢接口'],
    ['rows examined/returned','回表次数','页码与延迟曲线','排序临时空间'],
    '游标排序必须稳定且唯一，通常使用业务排序字段加主键；过滤条件和排序方向也要编码进游标，防止跨查询误用。'
  ),
  'mysql/sharding':p(
    ['估算单库容量上限','选择分片键与路由','执行单片读写','处理跨片查询事务','扩容迁移并核对'],
    ['分片前先证明单机在数据量、写吞吐或维护窗口上达到真实瓶颈，避免过早引入分布式复杂度。','分片键决定流量与数据分布，应兼顾高频查询路由、热点和未来迁移；低基数字段通常不适合。','带分片键请求可直达单片，唯一 ID、二级索引和缓存 Key 都要携带路由信息。','跨片聚合、排序、Join 和事务需要上层合并或专门协议，成本远高于单库。','扩容通过双写/变更日志与存量迁移完成，必须定义切读、回滚和逐行校验。'],
    '按用户分片后商家查询变成全库扫描',
    '订单以 user_id 分片满足用户侧查询，却忽略商家按店铺和时间查询。请求广播所有分片后容量随分片数恶化。增加面向商家的异构索引表或事件驱动查询库，比给每个请求并行扫全库更可控。',
    [['垂直拆分','业务域边界清晰、单表未超限','隔离依赖与团队','跨域事务增加'],['水平分片','单表数据或写吞吐超限','容量近似水平扩展','跨片查询、事务和扩容复杂'],['异构查询存储','多种访问路径冲突','按查询模型优化','需要同步、延迟和对账']],
    ['分片键形成超级热点','全局唯一约束无法由单片保证','迁移期间双写无幂等与核对'],
    ['分片数据与 QPS 偏斜','跨片请求比例','迁移积压与差异','单片容量水位'],
    '分库分表不是数据库优化的第一步；一旦引入，路由、全局 ID、事务、扩容、备份恢复和数据治理都必须同时设计。'
  ),
  'mysql/table-design-guide':p(
    ['识别实体与不变量','选择字段类型和主键','设计约束与索引','评估写放大和演进','用真实数据验证'],
    ['表设计从领域实体、生命周期和必须由数据库保证的不变量开始，不应先堆字段。','字段类型影响范围、排序、编码和存储；金额用定点数，时间与时区语义要明确。','主键决定聚簇组织，唯一约束是并发下最终正确性防线，索引则服务具体访问路径。','宽行、随机主键和过多索引会增加页分裂、日志与缓存压力，DDL 演进也要考虑锁和回填。','通过代表性数据量验证行大小、索引选择性、查询计划和迁移时间，避免只在空表评审。'],
    '订单表增加万能 JSON 后查询失控',
    '团队把所有新字段写入 JSON，短期避免 DDL，但筛选、约束和索引逐渐困难，字段语义也无人治理。将稳定高频字段提升为类型化列，保留低频扩展区并建立 Schema 版本后恢复可维护性。',
    [['规范化关系表','一致性与更新语义复杂','约束清晰、冗余少','查询可能需要 Join'],['受控反规范化','高频读模型稳定','减少 Join、读延迟低','写同步与一致性成本'],['JSON 扩展列','低频、稀疏且快速演进字段','减少频繁 DDL','类型约束、索引和治理较弱']],
    ['用 varchar 存所有类型','没有唯一约束只靠应用判重','索引覆盖所有字段导致写放大'],
    ['行平均与 P99 大小','索引总量/数据量','页分裂与写放大','DDL 执行和回填时间'],
    '表结构是长期数据契约；正确类型、约束和访问路径应优先于短期开发便利，任何冗余都要明确维护者和修复方式。'
  )
}
