import{profile as p}from'./profile.mjs'

export const clickhouseProfiles={
  'clickhouse/mergetree':p(
    ['批量写入数据块','排序并生成新 Part','记录列文件与稀疏索引','后台选择 Parts 合并','查询按分区和主键裁剪'],
    ['客户端批次先转换为 Block，批次过小会产生大量独立写入事务和元数据。','每次 INSERT 把数据按 ORDER BY 排序并落成不可变 Part，写入不是原地修改旧文件。','各列独立存储并压缩，主键稀疏索引按 granule 保存标记，减少需要读取的数据范围。','后台合并把多个小 Part 重写为更大 Part，同时执行 TTL、去重或聚合语义。','查询先做分区裁剪，再按主键和跳数索引筛 granule，最后只读所需列。'],
    '实时埋点写入后查询延迟持续升高',
    '每条事件单独 INSERT，单分区产生数万小 Part，后台合并追不上，查询需打开大量文件。客户端攒批、Kafka Engine 汇聚并限制分区粒度后，active parts 恢复稳定。',
    [['MergeTree','通用明细事实表','列存、排序与后台合并能力完整','更新删除不是行存式原地操作'],['Log 系列表引擎','小型临时数据','结构简单','缺少主键和高级合并能力'],['外部表/湖存储','低频历史与跨系统共享','存算分离、成本低','延迟和功能依赖外部格式']],
    ['小批写入制造 Part 爆炸','ORDER BY 与查询过滤无关','高基数分区导致目录和元数据膨胀'],
    ['active parts 数','后台 merge 队列','读 granule/行数','压缩比'],
    'MergeTree 的主键不保证唯一，核心价值是数据排序和稀疏裁剪；唯一或替换语义由具体家族引擎和查询时机决定。'
  ),
  'clickhouse/partition-order-primary-key':p(
    ['按 PARTITION BY 路由数据','按 ORDER BY 建立物理顺序','PRIMARY KEY 生成稀疏标记','查询逐级裁剪','TTL/删除按分区管理'],
    ['分区用于数据生命周期和粗粒度裁剪，通常按月/日等低基数时间窗口设计。','ORDER BY 决定 Part 内物理排序、压缩局部性和主键裁剪能力，是最重要的数据布局。','PRIMARY KEY 默认等于 ORDER BY，也可取其前缀，只生成稀疏索引而不提供唯一约束。','查询依次利用分区条件、主键范围和数据跳数索引，过滤顺序与列相关性决定读取量。','DROP PARTITION 和 TTL 可快速管理整段数据，粒度过细则让 Part 与 ZooKeeper/元数据成本上升。'],
    '按 user_id 分区导致集群无法写入',
    '设计者希望查询单用户更快，把高基数 user_id 作为分区，结果每天产生海量分区与小 Part。改为按月分区、ORDER BY (tenant_id,user_id,event_time) 后，生命周期管理与用户过滤都能兼顾。',
    [['按月分区','长期留存与常规时间查询','分区数量稳定、管理方便','删除粒度为月'],['按日分区','日级删除或单日数据巨大','裁剪与运维粒度细','分区数量和小 Part 更多'],['不分区/单分区','小表或无需周期删除','结构最简单','长期大表维护和删除成本高']],
    ['高基数业务字段做分区','ORDER BY 首列与主要过滤无关','把 PRIMARY KEY 当唯一约束'],
    ['分区数量','每分区 active parts','主键裁剪率','查询实际读取行数'],
    '分区不是越细查询越快；先用少量分区管理生命周期，再用 ORDER BY 和主键前缀解决高频过滤。'
  ),
  'clickhouse/query-optimization':p(
    ['解析 SQL 与谓词','分区/主键/跳数索引裁剪','并行读取所需列','执行聚合连接排序','输出结果并记录 query_log'],
    ['查询优化从读取量开始：PREWHERE 可先读过滤列，再读取命中行的其他列。','分区和主键裁剪依赖条件能映射到表定义，函数包装和弱相关排序会扩大扫描。','ClickHouse 按数据片段并行读列，线程过多会竞争 CPU、内存和磁盘，单查询快不等于并发稳定。','GROUP BY、JOIN 和 ORDER BY 可能建立大哈希表或中间结果，应利用字典、预聚合和外部溢写控制。','system.query_log 保存 read_rows、read_bytes、memory_usage 等证据，应以其验证优化而非只看耗时。'],
    '报表只返回百行却扫描数十亿行',
    'WHERE 对 event_time 使用 toDate 函数且表按原时间排序，无法形成有效范围。改写为半开时间区间，并让租户列位于 ORDER BY 前缀后，读取行数下降几个数量级。',
    [['改写查询利用排序键','过滤条件与主键相关','无需额外存储、收益直接','受现有表布局限制'],['物化视图预聚合','固定聚合高频重复','查询读取极少','写放大与回填一致性'],['新建投影/宽表','多种稳定访问路径','优化器可选择更合适布局','存储与维护成本增加']],
    ['只调 max_threads 掩盖扫描过大','SELECT * 读取无关列','大 JOIN 全部放入内存'],
    ['read_rows/read_bytes','result_rows 比例','peak_memory_usage','并发下 P95/P99'],
    'ClickHouse 优化的第一原则是少读数据，其次才是增加线程；扫描量不降时，并行只会更快耗尽共享资源。'
  ),
  'clickhouse/data-skipping-index':p(
    ['按 granule 生成索引摘要','查询计算谓词范围','索引判断可能命中','跳过确定不匹配 granule','读取剩余列并过滤'],
    ['跳数索引在写入时按 GRANULARITY 记录 minmax、集合、Bloom 等摘要，不保存每行位置。','查询条件必须被索引类型支持，函数和数据类型转换可能阻止使用。','索引只能证明一段数据“不可能命中”从而跳过，可能命中的 granule 仍需读取验证。','效果取决于列与 ORDER BY 的相关性、基数和数据聚集；完全随机高基数值可能让每段都“可能命中”。','索引增加写入计算、磁盘与合并成本，应通过 EXPLAIN indexes 和 read_rows 实测收益。'],
    '给随机 UUID 加 Bloom 索引仍未加速范围查询',
    'Bloom 适合等值或集合包含，不支持大小范围，且 UUID 在每个 granule 中高度分散。调整查询模型、把可范围过滤的时间/租户放进排序键，比继续放大 Bloom 参数更有效。',
    [['minmax','数值/时间在数据中有聚集','摘要小、范围跳过有效','随机分布时几乎无收益'],['set','每 granule 低基数枚举','等值判断精确','基数超阈值会失效或膨胀'],['Bloom filter','字符串等值、token 或 ngram','允许概率摘要高基数值','有假阳性且写入/存储成本较高']],
    ['索引类型与谓词不匹配','未验证 read_rows 就判断索引有效','为大量列建索引拖慢写入和合并'],
    ['索引命中过滤率','read_rows 前后差异','索引文件大小','写入和 merge CPU'],
    '跳数索引是主键排序的补充，不是行存二级索引；如果绝大多数 granule 都可能命中，它就无法减少读取。'
  ),
  'clickhouse/write-optimization':p(
    ['客户端聚合批次','解析并排序 Block','写入临时 Part','原子发布 Part','后台合并与复制'],
    ['合理批次通常包含数千到数万行并控制字节大小，过小增加 Part，过大增加客户端和服务端内存峰值。','格式解析、类型转换和按 ORDER BY 排序占用 CPU，预排序收益要与客户端成本权衡。','数据先写临时目录与校验文件，完成后原子 rename 发布为可见 Part。','ReplicatedMergeTree 还需在 Keeper 记录元数据并由副本拉取或复制。','后台 merge 消化小 Part，写入速率长期超过合并能力时 eventually 会触发延迟或拒绝。'],
    '日志平台峰值写入被 too many parts 拒绝',
    '采集器按租户和秒切批，单批几十行，单分钟分区持续生成小 Part。合并租户批次、调整异步插入并降低分区粒度后，写入与后台 merge 达到稳态。',
    [['客户端批量 INSERT','能控制生产者批次','链路简单、延迟可控','客户端需处理重试和内存'],['async_insert','请求小且需要服务端聚合','减少小 Part','确认语义和缓冲丢失窗口需理解'],['Kafka Engine + MV','持续流式接入','解耦生产与落表、可削峰','消费、重复和监控链路更复杂']],
    ['按行写入制造小 Part','重试整批没有去重语义','过大批次导致内存峰值和长尾'],
    ['每次 INSERT 行数/字节','parts_to_merge','insert 延迟与拒绝','后台 merge 带宽'],
    '写入优化的目标是让 Part 生成速率不长期超过合并能力；追求单批最大并不等于端到端最优。'
  ),
  'clickhouse/replacing-mergetree':p(
    ['写入同一排序键多个版本','版本共同存在于不同 Part','后台 merge 选择最新行','查询可用 FINAL 强制合并视图','业务按最终一致性读取'],
    ['ReplacingMergeTree 的“重复”由 ORDER BY 键定义，主键设计不当会误合并不同业务行。','新旧版本写入后可同时可见，因为去重通常发生在未来某次后台 merge。','带 version 列时 merge 保留版本最大行；没有版本时保留顺序受 Part 合并影响。','SELECT FINAL 在查询时执行替换语义，结果更准确但增加 CPU、内存和延迟。','高频查询可通过 argMax 聚合、物化视图或应用侧版本过滤，接受明确的一致性窗口。'],
    '用户状态查询偶发返回旧值',
    '服务写入新版本后立即普通 SELECT，旧新两行尚未 merge，调用方随读取顺序拿到旧行。改用 version + argMax 读取当前态，并把明细与当前快照模型分开后语义稳定。',
    [['普通查询','允许重复行短暂存在的分析','速度最快','结果需容忍未合并版本'],['SELECT FINAL','低频且必须获得替换视图','查询语义直接','大范围查询成本高'],['argMax/物化当前态','高频按键获取最新版本','可控且无需全表 FINAL','查询或写入模型更复杂']],
    ['把后台 merge 当实时去重','ORDER BY 包含变化字段导致无法匹配旧版本','全表长期使用 FINAL'],
    ['同键版本数','merge 延迟','FINAL 查询内存','旧版本可见窗口'],
    'ReplacingMergeTree 提供最终替换，不提供事务式唯一约束；需要实时唯一状态时必须设计查询口径或单独快照层。'
  ),
  'clickhouse/distributed-table':p(
    ['客户端访问 Distributed 表','按 sharding_key 选分片','发送到本地表副本','查询各分片并行执行','协调节点合并结果'],
    ['Distributed 表通常不存数据，只保存集群、远端数据库表和分片规则。','写入根据 sharding_key 权重路由，键选择决定数据倾斜和同一实体是否聚合在单片。','数据真正落在各节点的本地 MergeTree，副本负责高可用而分片负责容量。','查询下推到所有相关分片并行执行，缺少分片裁剪时每次都广播。','协调节点汇总聚合、排序和限制，中间结果过大会成为网络与内存瓶颈。'],
    '新增节点后历史与新数据分布不一致',
    '简单取模分片在节点数变化后路由改变，历史数据没有自动重平衡，同一用户跨多个分片。扩容前设计稳定分片槽或显式迁移，并用查询路由兼容过渡期。',
    [['随机分片','无实体局部性要求的明细流','写入均匀','按实体查询需广播'],['业务键哈希','同实体聚合和局部查询','可减少跨片','超级实体可能热点'],['预定义槽/权重','需要可控扩容迁移','路由稳定、可逐槽搬迁','管理元数据与迁移复杂']],
    ['sharding_key 低基数造成倾斜','协调节点承担大结果集合并','误以为增加副本能提升分片总容量'],
    ['分片行数/字节偏差','远程读写延迟','协调节点内存','广播查询比例'],
    '分片和副本职责不同：分片扩容量，副本提可用与部分读能力；Distributed 查询仍要为网络与聚合成本付费。'
  ),
  'clickhouse/advantages-limitations':p(
    ['确认分析型访问模式','列式压缩存储','向量化并行执行','预聚合/跳过无关数据','评估更新事务与并发边界'],
    ['ClickHouse 适合扫描少量列、聚合大量行的 OLAP，而不是点更新频繁的事务系统。','同列数据类型一致、相邻值相关，压缩比和读取带宽利用率通常优于行存。','执行器按 Block 向量化计算并利用 SIMD、多核和分布式分片。','排序键、分区、物化视图和聚合引擎减少实际读取与现场计算。','行级高频更新、强事务、多表小结果 Join 和超高并发短查询不是其天然强项。'],
    '把订单主库迁到 ClickHouse 后更新链路失控',
    '团队看重查询性能，却忽略订单状态频繁更新和唯一约束。Mutation 堆积、查询读到多版本，最终又引入复杂补偿。正确架构是 MySQL 保持事务事实，ClickHouse 通过 CDC 承载分析。',
    [['ClickHouse','大规模明细聚合与日志分析','扫描吞吐、压缩和并行强','事务更新与高并发点查受限'],['MySQL/PostgreSQL','事务、约束与点查','一致性与更新成熟','大范围分析扫描成本高'],['Elasticsearch','全文检索与相关性查询','倒排、模糊检索和聚合','存储成本与强事务能力有限']],
    ['用 OLAP 替代唯一约束事务库','无数据布局就期待自动加速','高并发小查询把线程和内存打满'],
    ['扫描吞吐','压缩比','并发下尾延迟','Mutation/merge 积压'],
    '技术选型应从访问模式和不变量出发；ClickHouse 的优势来自批量、列式和顺序数据布局，不能覆盖所有数据库职责。'
  ),
  'clickhouse/concurrency-limitations':p(
    ['请求进入查询队列','分配线程与内存','并行扫描和聚合','争用磁盘/CPU/网络','限流或溢写后完成'],
    ['单个查询会使用多个线程和较大内存，几十个复杂查询可能放大为数百执行线程。','并发查询共享 CPU、page cache、磁盘和网络，资源达到饱和后尾延迟非线性上升。','聚合、排序和 Join 的哈希表按查询独立分配，内存总量不能只看单查询限制。','后台 merge、mutation 和复制也消耗 I/O 与 CPU，会与前台读取竞争。','max_concurrent_queries、用户配额、队列和 workload management 应按业务优先级设置，而不是让所有请求无限进入。'],
    'BI 用户并发刷新使核心看板超时',
    '每个看板触发十余个重聚合查询，上午整点同时刷新，磁盘和 CPU 饱和。把低优先级探索查询限流、缓存共享结果并对核心看板预聚合后，资源隔离才生效。',
    [['全局并发限制','实例资源统一保护','配置简单','无法区分业务优先级'],['按用户/工作负载配额','多租户和核心/离线混部','隔离清晰','配额设计与动态调整复杂'],['预聚合+结果缓存','重复查询比例高','减少根本计算量','数据新鲜度与失效治理']],
    ['只限制单查询内存忽略并发总量','merge 与查询争用磁盘未隔离','客户端超时后查询仍在服务端运行'],
    ['运行/排队查询数','总内存与查询峰值','磁盘带宽/IO wait','超时后仍执行的查询'],
    'ClickHouse 高吞吐依赖让少量查询充分并行，不等于无限并发；并发控制应保护总资源并为核心业务保留预算。'
  ),
  'clickhouse/production-optimization':p(
    ['建立业务与系统基线','定位读写/合并瓶颈','优化表布局与查询','设置资源隔离和容量','灰度验证并持续治理'],
    ['基线同时包含业务 QPS、数据增长、查询分位值和 system 表证据，避免只看 CPU。','写入慢要区分小 Part、复制、磁盘和 merge；查询慢要区分扫描、聚合、网络和排队。','优先调整分区、ORDER BY、批次和查询读取量，再考虑参数与硬件。','生产设置用户配额、内存上限、并发队列、磁盘水位和副本容错，保留后台任务余量。','优化通过同流量回放和灰度验证 read_rows、成本与正确性，记录可回滚变更。'],
    '升级配置后查询快了但写入开始拒绝',
    '提高前台查询线程数占满 CPU，后台 merge 速度下降，小 Part 积压最终触发 too many parts。恢复资源平衡并为 merge 保留预算后说明，局部查询优化不能牺牲写入稳态。',
    [['表与查询优化','扫描或 Part 结构不合理','根因收益、成本可持续','需要数据迁移或回填'],['资源参数调整','布局合理但配额失衡','实施较快','容易把瓶颈转移到后台任务'],['扩容分片','单机总容量确实不足','横向增加存储与计算','数据迁移、倾斜和运维复杂']],
    ['压测只测单查询不测混合负载','优化前台导致 merge 饿死','磁盘水位过高才开始扩容'],
    ['查询读放大','Part/merge 稳态','资源水位与排队','副本延迟和数据差异'],
    '生产优化目标是读、写、后台合并和故障恢复共同稳定；任何提升单项峰值的变更都要验证系统稳态。'
  )
}
