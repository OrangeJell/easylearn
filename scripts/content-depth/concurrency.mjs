import{profile as p}from'./profile.mjs'

export const concurrencyProfiles={
  'concurrency/thread-pool':p(
    ['任务提交','判断核心线程','进入有界队列','扩容至最大线程','执行拒绝策略'],
    ['execute/submit 接收任务，submit 还会包装为 FutureTask，因此异常传播方式不同。','运行线程少于 corePoolSize 时优先创建核心线程，即使已有空闲线程也按实现状态判断。','核心线程满后任务先进入工作队列，队列类型决定排队、公平和内存风险。','队列满且线程数未达 maximumPoolSize 时再创建非核心线程，keepAliveTime 控制其回收。','线程与队列都饱和后触发拒绝策略，必须把过载反馈给调用方而不是静默丢任务。'],
    '接口高峰期线程池队列持续增长',
    '服务使用无界 LinkedBlockingQueue，maximumPoolSize 实际失效，慢下游让任务不断排队，最终超时与堆占用一起上升。改为按下游容量设置有界队列、显式拒绝并对调用方限流后，系统能够快速失败而不是积压到崩溃。',
    [['CPU 密集固定池','计算任务且很少阻塞','线程数接近 CPU 核心，切换少','混入阻塞会拖慢全部任务'],['有界弹性池','I/O 阻塞比例可测且需吸收短峰值','可用线程覆盖等待时间','必须限制最大线程与队列'],['虚拟线程/同步编排','大量独立阻塞 I/O','代码直观、并发数高','仍需限制数据库等稀缺资源']],
    ['无界队列掩盖过载直到 OOM','任务异常被 submit 的 Future 吞住','多个线程池共同耗尽同一下游连接池'],
    ['活跃线程数','队列长度与等待时间','拒绝次数','任务执行 P95/P99'],
    '线程池隔离的是调度资源，不会提升下游实际容量；线程数必须与 CPU、连接池和远端并发上限共同设计。'
  ),
  'concurrency/volatile-happens-before':p(
    ['线程执行普通写','写入 volatile 变量','刷新并发布先前状态','另一线程读取 volatile','读取线程观察已发布数据'],
    ['普通字段写可暂存在寄存器或缓存中，单靠时间先后不能建立跨线程可见性。','volatile 写具有 release 语义，禁止关键重排序，并把此前操作纳入发布边界。','JMM 规定同一变量的 volatile 写 happens-before 后续读，这是一条语言级顺序保证。','volatile 读具有 acquire 语义，读取到发布值后，也能观察发布线程在写前完成的普通字段更新。','可见性不等于复合操作原子性，i++ 仍包含读、计算和写三个竞争步骤。'],
    '配置开关已更新但工作线程仍用旧参数',
    '代码只把 ready 标志设为 volatile，却在标志之后才写配置字段，读线程可能看到 ready=true 但配置仍旧。把所有配置写放在 volatile 发布之前，或直接发布一个不可变配置对象，才能利用正确的 happens-before 关系。',
    [['volatile 状态/引用','单写多读、独立状态发布','低开销可见性与顺序性','不能保护跨变量复合不变量'],['synchronized/Lock','读改写与多个状态必须原子变化','互斥加可见性，语义完整','存在阻塞与竞争成本'],['Atomic 类','单变量原子更新和 CAS 循环','无锁进展、API 明确','复杂状态组合难表达']],
    ['用 volatile 修饰计数器后仍丢更新','先发布标志再初始化数据','双重检查遗漏 volatile 导致半初始化观察'],
    ['旧值命中样本','CAS 或锁竞争次数','线程状态与 CPU 使用','发布到消费延迟'],
    'volatile 适合表达状态发布与独立标志；只要正确性条件需要“检查 A 后同时修改 B、C”，就必须重新寻找原子边界。'
  ),
  'concurrency/locks-aqs':p(
    ['线程尝试获取状态','CAS 修改同步状态','失败后进入 CLH 队列','前驱释放并唤醒','节点重新竞争成功'],
    ['AQS 用一个 volatile state 表示同步状态，具体子类解释 0/1、重入次数或许可数量。','tryAcquire 由同步器实现，通过 CAS 或独占规则决定当前线程是否成功。','失败线程封装为节点加入双向等待队列，避免所有线程持续自旋消耗 CPU。','释放状态后检查后继节点并 unpark，条件队列的 signal 还要先把节点转移到同步队列。','被唤醒不等于直接获得锁，线程仍需确认前驱和状态，处理取消与伪唤醒。'],
    '自定义限流器偶发永久等待',
    '同步器在 release 时只修改 state，没有在状态变为可用时返回 true，AQS 因此不唤醒后继节点。修复需要遵守 acquire/release 返回值契约，并用中断、取消和超时场景测试队列清理。',
    [['synchronized','普通互斥且不需高级能力','JVM 优化成熟、语法简单','不支持多个条件与可中断获取'],['ReentrantLock','需公平、超时、可中断或多个 Condition','能力完整、观测更直接','必须 finally 解锁'],['自定义 AQS','同步语义无法由现有工具表达','复用排队与唤醒框架','正确性复杂，维护和测试成本高']],
    ['忘记 finally unlock 造成永久占用','在锁内执行慢 I/O 放大队列','错误实现 release 返回值导致不唤醒'],
    ['锁等待时间','AQS 队列长度','park/unpark 频率','持锁时间分布'],
    'AQS 提供排队骨架而非业务正确性；优先使用成熟同步器，自定义实现必须覆盖取消、中断、超时、重入和传播等边界。'
  ),
  'concurrency/thread-lifecycle-communication':p(
    ['创建线程对象','start 进入 RUNNABLE','竞争锁或等待条件','被通知后重新竞争','完成任务进入 TERMINATED'],
    ['NEW 线程尚未启动，同一 Thread 实例只能成功 start 一次。','Java 的 RUNNABLE 同时涵盖操作系统可运行与正在运行，不能仅据此判断是否占用 CPU。','BLOCKED 等待 monitor，WAITING/TIMED_WAITING 常来自 wait、join、park、sleep 等不同机制。','notify/unpark 只是让线程有资格继续，条件必须在循环中重新检查，且共享状态要受同一同步规则保护。','run 正常返回或抛出未捕获异常后进入 TERMINATED，线程对象不能重启，应重新创建任务或交给执行器。'],
    '消费者被通知后仍然读取空队列',
    '代码用 if 判断队列为空后 wait，被唤醒时未重新检查条件，多个消费者竞争后其中一个拿走任务，另一个继续执行并失败。改为 while 条件循环，并在同一锁下修改队列和发送通知后才正确。',
    [['wait/notify','同一 monitor 下的底层条件协作','无需额外类','易丢通知，单条件表达困难'],['Condition','显式锁且有多个等待条件','条件队列清晰、支持超时中断','必须严格持锁调用'],['BlockingQueue/同步器','标准生产消费或阶段协作','高层语义、安全成熟','需要选择容量与一致性语义']],
    ['用 if 代替 while 等待条件','持锁 sleep 导致其他线程无法推进','混用 wait 与 park 却没有统一状态协议'],
    ['各线程状态数量','等待持续时间','通知到真正运行延迟','未捕获异常数'],
    '线程通信的核心不是“唤醒动作”，而是共享条件、状态修改与可见性的统一协议；能用高层并发工具时不要手写通知。'
  ),
  'concurrency/cas-aba':p(
    ['读取旧值与版本','计算新值','CAS 比较内存值','成功发布或失败重试','必要时检测 ABA'],
    ['CAS 操作以期望值、更新值和目标地址为输入，读取与条件写入作为单个原子步骤完成。','计算新值应是纯函数或可安全重试，因为失败线程可能多次执行更新逻辑。','实际值等于期望值时交换成功，并建立相应的 volatile 读写语义。','竞争失败通常进入自旋；高冲突下反复 CAS 会浪费 CPU，退避、分段或锁可能更合适。','ABA 表示值从 A 变 B 又回 A，纯值比较无法发现中间变化；版本戳或不可复用节点可保留历史信息。'],
    '无锁栈在节点复用后丢失元素',
    '线程 T1 读到栈顶 A 后暂停，T2 弹出 A、B 再把复用的 A 压回。T1 CAS 发现仍是 A 并成功，却把栈顶指向旧 next，导致新状态损坏。使用带版本的引用或避免节点立即复用可识别这次变化。',
    [['AtomicReference','引用状态且不关心中间历史','API 简单、无锁更新','无法识别 ABA'],['AtomicStampedReference','中间变化影响正确性','版本戳显式识别 ABA','对象与比较成本更高'],['锁保护临界区','冲突高或状态更新复杂','易表达多变量不变量','线程可能阻塞且有调度成本']],
    ['高竞争自旋导致 CPU 满载','CAS 回调含副作用被重复执行','忽略节点复用引发 ABA'],
    ['CAS 失败率','自旋次数','CPU 利用率','版本冲突和重试延迟'],
    '无锁不等于无竞争或一定更快；CAS 适合短小单变量状态更新，复杂不变量和高冲突负载常由锁获得更稳定尾延迟。'
  ),
  'concurrency/completable-future':p(
    ['提交异步阶段','在线程池执行任务','组合依赖与汇聚','传播结果或异常','设置超时并释放资源'],
    ['supplyAsync/runAsync 决定初始阶段与执行器，默认 commonPool 不适合混入不可控阻塞 I/O。','每个阶段在指定或继承的执行器中运行，thenApply 与 thenApplyAsync 的调度语义不同。','thenCompose 适合有依赖的异步扁平化，thenCombine/allOf 适合并行汇聚；错误并行会放大下游压力。','异常会沿链传播直到 exceptionally、handle 或 whenComplete；join 会包装为 CompletionException。','orTimeout 只改变 Future 结果，不一定取消底层 I/O，必须配合客户端超时、取消信号和资源隔离。'],
    '聚合接口超时后数据库仍被持续访问',
    '接口对 CompletableFuture 设置 300ms 超时后返回，但 JDBC 查询不可中断，后台任务继续占用连接，流量高峰时连接池耗尽。修复同时设置数据库查询超时、独立有界执行器和并发许可，并对取消无效的任务做观测。',
    [['同步调用','依赖少且延迟预算充足','控制流直观、异常清晰','串行累加等待时间'],['CompletableFuture','多个独立 I/O 可并行且需组合','编排表达力强','执行器、异常和取消复杂'],['响应式流','长链路流式数据与背压','端到端非阻塞和流控','学习与调试成本高']],
    ['默认 commonPool 被阻塞任务占满','只设置 Future 超时却未终止底层调用','异常处理过早转换为 null 掩盖失败'],
    ['各阶段耗时','执行器队列长度','超时后存活任务数','异常根因分布'],
    '异步化不会缩短单个依赖本身的耗时，只能重叠独立等待；下游容量、超时和取消必须与编排一起设计。'
  ),
  'concurrency/threadlocal':p(
    ['线程访问 ThreadLocal','按弱引用 Key 查找槽位','初始化并保存线程上下文','业务链路读取','finally remove 清理'],
    ['每个 Thread 持有自己的 ThreadLocalMap，数据跟随线程而不是 ThreadLocal 对象本身。','Key 是弱引用，ThreadLocal 无强引用后可被回收，但 Value 仍可能留在线程 Map 中直到清理。','withInitial/get 初始化应轻量，在线程池中同一线程会服务多个请求，旧值不会自动隔离。','上下文跨异步线程不会自然传播，InheritableThreadLocal 对线程池复用也不可靠。','请求边界必须 try/finally remove；更复杂上下文应显式传参或使用框架提供的作用域传播。'],
    '线程池复用导致租户串号',
    '请求 A 设置租户后异常返回，未执行 remove；同一工作线程处理请求 B 时读到 A 的租户，造成越权查询。把设置与清理封装为可关闭作用域，并在入口过滤器 finally 清理后才消除污染。',
    [['ThreadLocal','同步调用链中的请求级隐式上下文','接入简单、读取快','线程池污染和异步传播困难'],['显式参数','核心业务数据与跨线程调用','依赖清晰、易测试','调用链参数较多'],['ScopedValue/框架上下文','结构化并发或已有上下文传播设施','作用域明确、自动恢复','受 JDK 或框架版本约束']],
    ['线程池请求结束未 remove','在线程本地缓存大对象造成长时间存活','误用 InheritableThreadLocal 传播到复用线程'],
    ['线程本地 Value 大小','请求后残留上下文','租户或 Trace 串号样本','长生命周期线程占用'],
    'ThreadLocal 适合基础设施上下文，不应隐藏核心业务依赖；只要数据跨线程、跨进程或需要审计，就应显式建模传播。'
  ),
  'concurrency/deadlock':p(
    ['线程持有第一把锁','请求第二把锁','形成等待依赖边','依赖环闭合','检测并中断或重启恢复'],
    ['死锁需要互斥、持有并等待、不可剥夺和循环等待四个条件同时成立。','线程在持有资源时请求另一资源，会在 wait-for graph 中形成有向边。','多个锁顺序不一致最常见，数据库行锁、连接池与应用锁也可能跨层组成环。','环形成后线程无法自行推进，CPU 可能不高，但请求堆积和连接占用持续上升。','jstack、JFR 或 ThreadMXBean 可识别 Java monitor/ownable synchronizer，外部资源死锁还需联合其他系统证据。'],
    '转账接口请求全部卡住',
    '两个线程分别锁定账户 A、B 后再请求对方账户，构成经典循环等待。按账户 ID 全局排序获取锁可以破坏环；同时增加 tryLock 超时与事务回滚，避免未来新增资源时无限等待。',
    [['全局锁顺序','同类多资源加锁','从设计上消除循环等待','所有调用方必须遵守'],['tryLock 超时','无法完全控制获取顺序','故障可恢复、避免无限卡住','需要回滚和重试幂等'],['单线程所有者/消息串行化','按键操作可分区','不需要多锁组合','吞吐取决于分区且有排队']],
    ['锁顺序在不同代码路径不一致','锁内调用外部服务形成未知依赖','线程池与连接池互相等待形成资源死锁'],
    ['BLOCKED 线程数','锁拥有者与等待者','请求在途与连接占用','死锁检测告警'],
    '超时只能让系统从等待中退出，不证明业务状态完整；恢复路径必须释放已持有资源，并确保重试不会重复产生副作用。'
  ),
  'concurrency/java-memory-model':p(
    ['源代码操作','编译器与 CPU 重排序','线程本地执行与缓存','同步动作建立 happens-before','其他线程获得合法可见结果'],
    ['JMM 定义程序动作与线程间可观察结果，屏蔽不同 CPU 内存模型差异。','只要不改变单线程语义，编译器和处理器可以重排序；数据竞争程序不能用直觉推断执行顺序。','工作内存是规范抽象，实际可能对应寄存器、缓存和编译器优化，不能机械等同某一级硬件缓存。','锁释放/获取、volatile 写/读、线程 start/join 等规则建立 happens-before，并具有传递性。','正确同步程序具有顺序一致性推理效果；没有 happens-before 的读可能看到旧值，但读取仍受类型安全等规则约束。'],
    '双重检查单例偶发读取未初始化字段',
    'instance 引用未声明 volatile 时，对象分配、构造和引用赋值可能被重排序。另一个线程看到非 null 引用，却读取默认字段值。volatile 发布或使用静态内部类初始化可以建立构造完成到读取之间的顺序。',
    [['锁','多变量不变量与临界区','互斥和可见性一起保证','存在竞争与阻塞'],['volatile 发布','独立状态或不可变对象发布','开销低、语义精确','不提供复合原子性'],['final 安全初始化','构造后不变的对象图','正确构造后可安全读取 final 字段','this 逸出会破坏保证']],
    ['用 sleep 代替同步等待可见性','对象构造期间 this 逸出','认为原子读写自动建立跨变量顺序'],
    ['数据竞争检测结果','旧值或默认值样本','锁与 volatile 热点','编译后字节码与 JIT 日志'],
    'happens-before 是“必须可见的顺序关系”，不是现实时间顺序，也不意味着前一个动作在物理上先执行；它是判断并发程序是否合法的核心工具。'
  ),
  'concurrency/virtual-threads':p(
    ['创建海量虚拟线程','在载体线程上运行','遇到可挂起阻塞','卸载并释放载体','I/O 就绪后重新调度'],
    ['虚拟线程由 JVM 管理，创建与栈内存成本远低于平台线程，适合一任务一线程模型。','可运行虚拟线程挂载到有限 ForkJoinPool 载体线程执行，业务不应把载体数当成下游并发上限。','支持的阻塞操作会让虚拟线程 park 并从载体卸载，载体可执行其他任务。','在 synchronized、native 或某些外部调用中可能发生 pinning，阻塞时持续占住载体线程。','I/O 完成后虚拟线程重新排队调度，ThreadLocal 与调度量仍会产生内存和观测成本。'],
    '迁移虚拟线程后数据库被打满',
    '服务把固定 200 线程池替换为每请求虚拟线程，应用可同时发起数万 JDBC 查询，数据库连接池和排队迅速饱和。保留连接池与 Semaphore 并发许可后，虚拟线程只简化等待模型，不再绕过下游容量保护。',
    [['平台线程池','CPU 密集或并发规模受控','工具成熟、资源上限显式','阻塞并发规模受线程成本限制'],['虚拟线程','大量独立阻塞 I/O','同步代码风格、并发成本低','下游限流和 pinning 仍需治理'],['响应式模型','已有非阻塞生态和流式背压','少量线程承载大量连接','编排、调试与上下文传播复杂']],
    ['去掉并发上限压垮数据库','锁内阻塞造成载体线程 pinning','为每个虚拟线程存放大 ThreadLocal'],
    ['虚拟线程数量','载体线程利用率','pinned 事件','下游连接等待与拒绝'],
    '虚拟线程降低的是线程等待成本，不创造 CPU、连接或远端吞吐；所有稀缺资源仍必须独立限流并设置超时。'
  )
}
