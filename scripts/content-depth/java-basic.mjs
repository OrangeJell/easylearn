import{profile as p}from'./profile.mjs'

export const javaBasicProfiles={
  'java-basic/string-immutable':p(
    ['字面量或构造输入','常量池查找','创建不可变对象','计算并缓存哈希','安全共享与返回'],
    ['编译期字面量会进入运行时常量池，运行期输入则先以普通对象存在，两者的创建时机与对象数量不同。','intern 会查询字符串池并返回池中规范引用，但它不是通用去重工具，调用成本和池容量都要评估。','不可变性来自 final 类、私有存储和不暴露原地修改能力的组合，而不只是字段被 final 修饰。','内容不变使 hashCode 可以缓存，也保证对象作为 HashMap 键后不会因内容变化而落入错误桶。','所有拼接、替换和截取操作都返回新对象，因此实例可跨线程共享，但高频修改会产生额外分配。'],
    '订单签名偶发校验失败',
    '某支付系统把可变字符缓冲区转换后的结果作为签名缓存键，后续又复用了缓冲区，导致日志中的原文与缓存命中不一致。排查时应确认真正进入 Map 的对象类型、创建时机和编码步骤；改为不可变 String 值并在边界固定字符集后，签名键才具有稳定语义。',
    [['String','只读文本、少量拼接和 Map 键','不可变、可共享、API 通用','循环修改会产生中间对象'],['StringBuilder','单线程局部大量拼接','可变且无同步开销','不能跨线程无保护共享'],['StringBuffer','遗留接口要求共享可变缓冲区','方法级同步','锁粒度粗，通常不解决完整并发流程']],
    ['在循环中使用加号造成分配风暴','把 final 引用误认为对象绝对不可变','用 intern 处理无界用户输入造成池压力'],
    ['对象分配速率','Young GC 次数与停顿','字符串拼接热点栈','缓存键命中率'],
    'String 在正常 Java API 语义下不可变，但反射或 Unsafe 可突破封装；工程设计不能依赖这些非常规行为，也不应把敏感明文长期保存在不可回收的字符串中。'
  ),
  'java-basic/equals-hashcode':p(
    ['接收两个对象','equals 判断业务相等','hashCode 定位桶','桶内再次比较','集合判重或命中'],
    ['比较开始前先处理同一引用、null 和类型兼容性，避免业务字段比较掩盖基本契约问题。','equals 必须满足自反、对称、传递、一致以及对 null 返回 false，继承层次尤其容易破坏对称性。','相等对象必须产生相同哈希值；不相等对象允许哈希冲突，哈希值只负责缩小候选范围。','HashMap 先按哈希定位桶，再通过引用或 equals 确认键，冲突时还可能遍历链表或红黑树。','Set 判重和 Map 查找依赖对象在存放期间保持相等性字段稳定，否则数据仍在集合中却可能无法再次找到。'],
    '订单去重集合出现重复记录',
    '订单导入任务使用只重写 equals 的 DTO 放入 HashSet。相同订单号对象得到不同 hashCode，进入不同桶后不会互相比较，最终生成重复订单。修复不仅要同时重写两个方法，还要明确订单号是否稳定、空值如何处理，以及跨类型对象是否应该相等。',
    [['record/IDE 生成','值对象且参与比较字段明确','契约完整、维护成本低','字段变化时仍需审查语义'],['手写 equals/hashCode','需要归一化或特殊业务键','可精确表达领域规则','容易漏字段或破坏契约'],['独立 Comparator/Key','同一对象有多种排序或判重口径','不污染实体默认相等性','调用处必须始终使用同一口径']],
    ['只重写 equals 没有重写 hashCode','把会变化的状态放入哈希键','父子类 equals 使用不同类型判断导致不对称'],
    ['集合去重前后数量','HashMap 冲突与桶分布','业务唯一键重复率','查找失败样本'],
    '数据库主键相同不必然意味着两个内存对象在所有上下文都相等；相等性是领域契约，应明确比较字段、生命周期和跨类型规则。'
  ),
  'java-basic/generics':p(
    ['声明类型参数','编译期类型检查','执行类型擦除','生成桥接或强转','运行期实际调用'],
    ['类型参数把容器和算法的输入输出关系写入签名，使错误在编译期暴露，并减少调用处显式强制转换。','编译器检查赋值、调用和通配符边界；泛型默认不协变，所以 List<Integer> 不是 List<Number>。','多数类型参数在字节码中擦除为上界，JVM 通常看不到 List<String> 与 List<Integer> 的元素类型差异。','为保持重写后的多态语义，编译器可能生成 bridge 方法，并在读取泛型值的位置插入 checkcast。','运行期分派仍依据擦除后的真实方法和对象类型，反射只能从签名元数据读取保留的泛型声明。'],
    '通用复制方法无法同时支持多个数字类型',
    '工具方法最初声明为 copy(List<Object>, List<Object>)，导致 List<Integer> 和 List<Number> 都无法自然调用。把生产者参数改为 List<? extends T>、消费者改为 List<? super T> 后，类型关系由编译器验证，同时保持调用方无需强转。',
    [['具体类型参数 T','输入输出需要保持同一类型关系','签名清晰且可推断','无法表达只读或只写的宽泛边界'],['? extends T','只从参数读取 T','接受 T 的任意子类型容器','不能安全写入非 null 值'],['? super T','需要向参数写入 T','接受 T 的任意父类型容器','读取结果通常只能视为 Object']],
    ['使用原始类型导致堆污染','滥用无界通配符丢失输入输出关系','认为擦除后所有泛型检查都不存在'],
    ['编译告警数量','ClassCastException 分布','桥接方法与字节码签名','API 调用处强转数量'],
    '泛型解决编译期类型关系，不提供运行期容器元素校验；需要反序列化或反射构造时，应额外传入 Class、TypeToken 或显式解析器。'
  ),
  'java-basic/exception-handling':p(
    ['异常被抛出','调用栈逐层展开','匹配 catch 边界','执行资源释放','转换并返回稳定语义'],
    ['异常对象记录类型、消息、堆栈和 cause，抛出点应保留足以定位业务动作的上下文。','JVM 沿调用栈寻找可处理类型，期间跳过正常返回路径；把异常用于普通分支会引入控制流与对象创建成本。','只有真正能恢复、转换或增加有效上下文的层才应捕获，静默吞掉异常会让状态与调用方认知不一致。','try-with-resources 按声明逆序关闭资源，关闭失败作为 suppressed exception 保留，不覆盖主异常。','对外边界把内部异常映射为稳定错误码、可重试语义和请求标识，同时把完整根因留在受控日志中。'],
    '支付超时被误报为支付失败',
    '调用支付渠道超时时，渠道可能已扣款但响应丢失。简单 catch 后返回“失败”并自动重试会造成重复扣款。正确模型应把状态标记为“结果未知”，保存幂等请求号，通过查询与对账确认最终结果。',
    [['受检异常','调用方存在明确恢复动作','编译器强制处理契约','层级过多时声明噪声较大'],['运行时异常','参数错误或无法就地恢复','API 简洁、便于统一边界处理','容易被忽略直到运行期'],['结果对象/错误码','预期内业务分支且需批量处理','控制流明确、便于组合','可能丢失堆栈，类型设计更复杂']],
    ['捕获 Exception 后只打印消息','重复记录同一异常造成日志风暴','在 finally 中 return 压制原异常'],
    ['错误码分布','非预期异常率','同一请求重复堆栈数','下游超时与重试次数'],
    '异常表示“非正常完成”，但不自动说明能否重试；重试必须结合操作幂等性、失败发生阶段和结果是否确定。'
  ),
  'java-basic/reflection-annotations':p(
    ['加载类元数据','读取注解配置','查找成员','访问或调用目标','缓存元数据并治理边界'],
    ['Class 对象连接字节码元数据与运行期类型，类加载时机和类加载器决定同名类型是否真正相同。','只有保留策略为 RUNTIME 的注解才能在运行期读取，Target 决定它允许出现在哪类程序元素上。','getDeclaredXxx 与 getXxx 的继承和可见性规则不同，框架必须明确扫描范围并处理桥接方法。','反射调用涉及访问检查、参数装箱和异常包装；突破模块或私有边界还可能在新 JDK 中失败。','稳定框架应缓存已解析的构造器、方法和注解模型，并设置白名单，避免每次请求重复扫描或执行任意类型。'],
    '接口首请求延迟异常升高',
    '对象映射框架在每次请求中扫描字段、读取注解并执行 setAccessible，首请求与高并发下都产生明显开销。把扫描移动到启动阶段、缓存不可变元数据并为失败映射提前校验后，延迟和线上不确定性都显著下降。',
    [['直接反射','低频工具、插件发现和框架启动期','灵活且无需生成代码','运行期错误和访问成本较高'],['MethodHandle','高频动态调用且签名可管理','JIT 优化空间更好','API 与类型适配更复杂'],['编译期代码生成','映射关系稳定、性能敏感','运行时快速且错误前置','构建链更复杂，动态性较弱']],
    ['每次请求重复扫描元数据','无白名单地实例化外部传入类名','忽略模块边界和注解继承规则'],
    ['冷启动扫描时长','反射调用热点','元数据缓存命中率','非法访问与映射失败数'],
    '反射适合把变化从编译期推迟到运行期，但会把一部分类型安全和错误发现也推迟；核心高频路径应优先显式代码或生成代码。'
  ),
  'java-basic/serialization':p(
    ['遍历对象图','确定字段与类型标识','编码为字节','存储或跨网络传输','按兼容规则重建对象'],
    ['序列化器从根对象遍历引用关系，循环引用、共享引用和懒加载代理都会影响结果与大小。','协议需要决定字段编号、类型信息、空值和默认值；使用字段名更直观但体积通常更大。','编码阶段还涉及字符集、压缩、校验和及大对象限制，不能只比较单条消息的理想吞吐。','传输边界要定义最大报文、版本、加密和重放策略，避免不受信任数据直接进入对象构造。','反序列化必须处理缺失字段、未知字段和版本演进，并在创建对象前完成类型白名单与尺寸校验。'],
    '消息升级后旧消费者无法解析',
    '订单事件把 Java 类名和完整对象直接写入消息。服务重构包名后，历史消息无法反序列化，回放链路中断。改用稳定事件 Schema、字段编号与兼容性检查，并把领域对象和传输 DTO 分离后，生产者与消费者才能独立演进。',
    [['JSON','外部接口、调试和异构系统','可读、生态成熟','体积较大且类型约束较弱'],['Protobuf/Avro','高吞吐消息与明确 Schema','紧凑、兼容规则清晰','需要 Schema 管理与代码生成'],['Java 原生序列化','封闭遗留场景','接入简单','安全风险高、跨版本脆弱，不适合外部输入']],
    ['反序列化不可信类型导致代码执行风险','没有版本策略导致历史数据无法读取','序列化超大对象造成内存峰值'],
    ['报文大小分布','序列化与反序列化耗时','兼容性失败数','拒绝的超限或非法类型数'],
    '序列化格式是长期接口契约，不应等同于某个语言的内存对象；跨服务消息必须优先考虑版本演进、信任边界和可回放性。'
  ),
  'java-basic/interface-abstract-class':p(
    ['识别变化维度','抽取能力契约','决定是否共享状态','选择继承或组合','通过实现完成替换'],
    ['设计开始先区分“是什么”与“能做什么”，避免仅因代码相似就建立脆弱继承关系。','接口适合表达跨层能力和替换点，一个类型可以实现多个接口，调用方只依赖最小契约。','抽象类可持有受保护状态、构造逻辑和模板方法，但共享可变状态会放大子类之间的隐式耦合。','组合通过显式字段委托复用行为，通常比深继承更容易测试和替换，但对象协作会多一层。','替换性需要实现遵守输入、输出和异常契约，不能只满足语法上的 implements 或 extends。'],
    '支付渠道接入导致基类不断膨胀',
    '所有支付方式继承同一个抽象基类后，二维码、银行卡和余额支付的差异被迫塞进条件分支。重构为支付接口、独立能力接口和可组合的风控/路由策略后，共享流程留在编排器，不再要求每个子类拥有无关状态。',
    [['接口','跨实现稳定契约和多能力组合','低耦合、便于替换与测试','不能自然承载实例状态'],['抽象类','同族对象共享受控状态与模板流程','可复用字段和部分实现','单继承且容易形成脆弱基类'],['组合与委托','行为可独立变化或运行时替换','职责清晰、扩展灵活','对象数量和协作关系更多']],
    ['把工具方法相似误判为 is-a 关系','默认方法冲突未明确解决','子类破坏父类前置条件或异常契约'],
    ['实现类数量与变更频率','基类条件分支数量','接口方法被空实现的比例','替换实现的测试覆盖'],
    '接口与抽象类不是互斥选项：常见设计是接口定义边界，抽象类提供可选骨架，组合承载可变能力；是否共享状态是关键判断。'
  ),
  'java-basic/overload-override':p(
    ['收集候选方法','编译期选择重载','必要时装箱或提升','运行期选择重写实现','返回结果或抛出异常'],
    ['重载候选由方法名和可见范围确定，返回类型不能单独区分两个重载。','编译器依据变量的静态类型和实参类型选择最具体签名，因此重载是早绑定。','精确匹配、基本类型提升、装箱与可变参数存在优先级，null 还可能让多个引用重载产生歧义。','真正调用哪个重写方法由接收者运行期类型决定，因此重写是多态分派；静态方法与字段不参与这种分派。','子类重写不能缩小可见性，受检异常范围也不能更宽，协变返回类型则允许返回更具体类型。'],
    '日志工具新增重载后调用行为改变',
    '工具类同时提供 log(Object) 与 log(String...)。某调用传入 null 后编译器无法确定更具体目标，升级即编译失败；另一些调用因静态类型为 Object 落到非预期重载。解决方案是减少语义相近的重载，并在边界使用明确类型或不同方法名。',
    [['重载','同一动作对不同静态参数形态提供便利','调用自然、编译期发现','过多重载会产生歧义与兼容风险'],['重写','子类替换父类行为','支持运行期多态','必须遵守里氏替换与父类契约'],['策略对象','行为需要独立扩展和组合','避免继承层级与重载爆炸','调用方需显式选择或注入策略']],
    ['null 在多个引用重载间产生歧义','误以为父类变量会选择子类特有重载','重写方法放宽受检异常破坏调用方契约'],
    ['编译歧义与升级失败','重载数量与调用分布','运行期实现类型','异常类型变化'],
    '重载解决调用便利性，重写解决多态替换性；当不同参数实际代表不同业务语义时，使用不同方法名往往比继续增加重载更清晰。'
  ),
  'java-basic/pass-by-value':p(
    ['计算实参表达式','复制值到形参槽位','方法内部操作副本','可能修改共享对象','返回后形参消失'],
    ['调用前先计算实参；基本类型得到数值，对象表达式得到的是指向对象的引用值。','Java 总是把这个值复制给形参，因此基本数值和引用值本身都不会被方法反向替换。','给形参重新赋值只改变局部槽位，不会让调用方变量指向新对象。','通过两个引用访问同一个可变对象时，方法内部字段修改会被调用方观察到，这属于共享对象变化而非引用传递。','方法结束后形参生命周期结束；要改变调用方持有的值，应返回新值、修改明确的可变容器或使用领域命令。'],
    '交换两个对象的方法始终无效',
    '代码试图通过 swap(User a, User b) 交换调用方变量，方法内打印正常，返回后引用却未变化。原因是交换的只是两个引用副本。若需求是调整列表顺序，应操作调用方传入的列表位置；若是替换对象，应返回包含两个新引用的结果。',
    [['返回新值','不可变对象或需要明确替换','副作用小、数据流清晰','调用方必须接收返回值'],['修改可变对象','聚合内部状态有明确所有者','避免额外复制','别名共享会扩大副作用'],['包装器/数组持有槽位','框架 API 必须模拟输出参数','可以原地写回','语义绕、通常不如结果对象清晰']],
    ['把对象可变误判为引用传递','给形参赋新对象后期待调用方改变','隐式修改共享集合造成并发问题'],
    ['对象身份标识','方法前后字段差异','共享引用数量','并发修改异常'],
    '“值传递”描述的是调用语义，不代表对象不可变；引用值被复制后仍可访问同一个对象，所以必须另外讨论对象所有权和可变性。'
  ),
  'java-basic/autoboxing-integer-cache':p(
    ['基本类型参与表达式','编译器插入装箱','查询包装类缓存','生成或复用对象','拆箱比较与运算'],
    ['自动装箱是编译器语法糖，例如 int 到 Integer 通常转换为 Integer.valueOf，而不是 JVM 新的参数传递规则。','valueOf 会先检查缓存范围；Integer 默认缓存 -128 到 127，其他包装类范围与行为并不完全相同。','命中缓存返回共享实例，未命中通常创建新对象，因此用引用相等比较包装类会得到随数值变化的结果。','拆箱调用 xxxValue；null 包装类参与算术、比较或三元表达式时会在拆箱位置抛出 NullPointerException。','高频集合和流式计算中的装箱会增加分配、指针访问和 GC 压力，性能敏感路径可使用基本类型数组或专用集合。'],
    '金额比较在不同数值范围表现不一致',
    '业务代码用 Integer 的 == 判断券数量，小值测试全部通过，生产中超过缓存范围后比较失败。改为 Objects.equals 或先拆箱比较，并为 null 建立明确语义后，行为才与对象身份无关。',
    [['基本类型','高频计算且不存在 null','无装箱分配、语义直接','不能表达缺失值且不能直接用于泛型'],['包装类型','泛型容器、可空字段和反射 API','可表达 null、API 兼容','存在装箱、拆箱和身份误用风险'],['OptionalInt 等','返回值需要表达可能缺失的基本数值','避免包装类 null','不适合作为实体字段或通用容器']],
    ['用 == 比较包装对象','null 自动拆箱触发异常','热循环装箱造成大量短命对象'],
    ['包装类分配速率','拆箱 NPE 堆栈','Young GC 压力','数值比较异常样本'],
    '缓存是实现优化而不是业务契约；代码应依据数值相等性编写，不能通过扩大 Integer 缓存范围来修复错误的引用比较。'
  )
}
