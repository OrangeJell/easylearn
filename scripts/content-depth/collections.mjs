import{profile as p}from'./profile.mjs'

export const collectionProfiles={
  'collections/arraylist-linkedlist':p(
    ['接收访问模式','选择连续数组或链节点','定位目标元素','执行读写或插入','扩容回收并返回'],
    ['先确认主要操作是随机访问、尾部追加还是中间插入，不能只依据 Big-O 口号选择。','ArrayList 用连续引用数组获得局部性，LinkedList 为每个元素额外保存前后节点指针。','数组可按下标 O(1) 定位，链表必须从头尾遍历；现代 CPU 缓存使实际差距通常比复杂度表更明显。','ArrayList 中间插入要移动元素，LinkedList 在已持有节点时改指针很快，但通过下标找节点仍是 O(n)。','ArrayList 扩容会复制数组并短暂占用两份空间，链表则持续承担节点对象与 GC 成本。'],
    '十万条分页结果处理变慢',
    '代码认为链表插入快，把数据库结果装入 LinkedList 后反复 get(i) 遍历，实际退化为 O(n²)。改为增强 for 顺序遍历已经改善，但由于主要是追加与扫描，最终使用预估容量的 ArrayList 获得更低内存和更好的缓存局部性。',
    [['ArrayList','随机访问、尾部追加和批量遍历','内存紧凑、CPU 缓存友好','中间移动与扩容复制'],['LinkedList','频繁操作两端且直接持有迭代位置','双端增删语义直接','节点开销高、随机访问慢'],['ArrayDeque','队列、栈和双端操作','连续数组、性能稳定','不支持 null，按下标访问不是主要能力']],
    ['在 LinkedList 上按索引循环','ArrayList 未预估容量导致反复扩容','迭代时结构修改引发并发修改异常'],
    ['集合尺寸分布','扩容与数组复制次数','对象分配率','遍历热点耗时'],
    '复杂度是上界模型，不包含缓存局部性、对象头和 GC；除非确有链表语义，ArrayList 或 ArrayDeque 通常是更稳妥的默认选择。'
  ),
  'collections/hashmap':p(
    ['计算键哈希','扰动并定位桶','检查首节点','遍历链表或红黑树','命中更新或插入扩容'],
    ['HashMap 先取得 hashCode 并做高低位扰动，让容量为 2 的幂时高位也参与桶索引。','索引通过 (n-1)&hash 计算，容量为 2 的幂可以用位运算并保持分布规律。','桶首节点若哈希与键相等即可命中，否则才进入冲突结构，null 键固定落在特定桶。','冲突较多且容量达到阈值时链表可树化；删除或容量条件变化时也可能退回链表。','size 超过 capacity*loadFactor 会扩容，节点根据旧容量对应位拆到原位置或原位置+oldCap。'],
    '缓存键分布倾斜导致 CPU 突增',
    '自定义键把 hashCode 固定为地区编号，百万用户只有几十个哈希值，大量元素聚集在少数桶。即使红黑树限制最坏查找，比较与对象访问仍很重。重新组合稳定且高区分度的用户标识，并在写入前确认键不可变后，冲突和 CPU 都恢复正常。',
    [['HashMap','单线程或外部同步的一般键值映射','均摊 O(1)、API 通用','非线程安全且扩容有瞬时成本'],['LinkedHashMap','需要插入顺序或访问顺序','可实现 LRU 语义','额外双向链表开销'],['TreeMap','需要有序遍历和范围查询','稳定 O(log n)、支持导航','比较成本高且键顺序契约更严格']],
    ['可变字段参与 hashCode 后修改键','初始容量过小触发多次扩容','低质量哈希造成热点桶'],
    ['桶冲突分布','集合尺寸与扩容次数','get/put 尾延迟','键 hashCode 离散度'],
    'HashMap 的 O(1) 是良好哈希与合理负载因子下的均摊结论；它不保证遍历顺序，也不能通过“读多写少”自然获得线程安全。'
  ),
  'collections/concurrenthashmap':p(
    ['计算哈希定位桶','读取 volatile 桶节点','CAS 初始化或插入','桶冲突时局部加锁','协助扩容并发布结果'],
    ['并发映射仍先通过扰动哈希定位桶，读取路径尽量依靠 volatile 可见性而不获取全局锁。','table 和节点关键字段的发布关系保证已完成写入对后续读可见，但复合业务操作仍可能需要原子 API。','空桶插入优先 CAS，竞争失败后重试；这使不同桶写入可以并行。','非空桶更新在桶首节点上同步，锁粒度是局部桶而不是整个 Map，树桶有独立并发控制。','扩容时线程可以领取迁移区间共同搬迁，ForwardingNode 引导其他线程访问新表或参与迁移。'],
    '本地缓存计数偶发丢失',
    '代码先 get 计数、加一再 put，多线程下两个更新读取同一旧值，ConcurrentHashMap 本身没有损坏但业务复合操作丢失。改用 compute 或 LongAdder 作为值，并确保映射函数短小且无外部阻塞后，更新才真正原子。',
    [['ConcurrentHashMap','高并发通用键值映射','读路径轻、桶级并发','复合操作必须使用原子 API'],['同步包装 Map','低并发且需要整体互斥语义','实现简单','全局锁限制吞吐和遍历'],['不可变快照','配置等读极多、整批更新','读取无锁且视图一致','更新需复制，实时单键写不合适']],
    ['get 后 put 的检查再执行竞态','compute 回调阻塞导致桶热点','键分布差让所有写集中到少数桶'],
    ['热点桶锁等待','compute 执行时间','扩容协助耗时','读写吞吐与尾延迟'],
    'ConcurrentHashMap 保证单个方法的线程安全，不保证跨多个调用的业务事务；映射回调也不应递归修改同一键或执行慢 I/O。'
  ),
  'collections/set-comparison':p(
    ['接收元素','计算哈希或比较顺序','查找已有等价元素','插入内部映射','按实现规则遍历'],
    ['Set 的核心语义是不重复，但“相同”由 equals/hashCode 或排序比较器决定。','HashSet 使用哈希定位，TreeSet 使用 compareTo/Comparator，LinkedHashSet 同时维护哈希与插入顺序链。','已有等价元素时 add 返回 false，调用方不应仅依赖最终 size 才发现重复。','多数 Set 实际委托给 Map 保存元素，Value 只是共享占位对象，因此键契约决定正确性。','遍历顺序分别是不保证、插入顺序和排序顺序；顺序要求必须写进接口契约。'],
    '用户标签导出顺序每日变化',
    '服务使用 HashSet 去重后直接导出，测试数据较小时顺序看似稳定，扩容或 JDK 变化后顺序改变。业务实际要求首次出现顺序，因此替换为 LinkedHashSet，并增加顺序断言，而不是依赖 HashSet 当前实现。',
    [['HashSet','只需快速判重、不关心顺序','均摊 O(1)、额外开销较低','遍历顺序不稳定'],['LinkedHashSet','需要保持插入顺序','判重同时稳定输出','维护链表增加内存'],['TreeSet','需要排序、范围和邻近查询','有序且支持导航 API','O(log n)，比较器必须与相等语义协调']],
    ['比较器返回 0 但 equals 不相等导致元素被吞','依赖 HashSet 偶然遍历顺序','元素可变后无法正常 remove'],
    ['去重前后元素数','比较器冲突样本','集合内存占用','add/contains 尾延迟'],
    'TreeSet 的唯一性由比较结果为 0 决定，不是再次调用 equals；比较器与领域相等性不一致时必须明确这是业务意图还是缺陷。'
  ),
  'collections/queue-deque-priorityqueue':p(
    ['接收入队元素','按 FIFO/双端/优先级组织','读取队头','移除并调整结构','处理空队列语义'],
    ['选择队列前要明确顺序规则：普通 Queue 是 FIFO，Deque 支持两端，PriorityQueue 按优先级而非插入顺序。','ArrayDeque 使用循环数组管理头尾索引，PriorityQueue 使用二叉堆维护最小或最大元素。','peek/element 以及 poll/remove 的差异在空队列时体现为返回 null 或抛异常，接口层应统一语义。','堆删除队头后把末尾元素移到根并下沉，复杂度 O(log n)，读取队头 O(1)。','优先级相同元素不保证稳定顺序；并发生产消费需要 BlockingQueue 或外部同步。'],
    '延迟任务执行顺序偶发错乱',
    '任务只按执行时间比较，相同时间返回 0，但业务又要求同一时间按序号执行。PriorityQueue 不提供稳定性，导致回放顺序变化。比较器增加单调序号作为第二关键字，并把并发等待交给 DelayQueue 后，语义才完整。',
    [['ArrayDeque','单线程栈、队列和双端操作','连续存储、通常优于 Stack/LinkedList','不支持 null，也不线程安全'],['PriorityQueue','每次取当前最高优先级元素','堆操作高效','遍历无序，同优先级不稳定'],['BlockingQueue','生产消费与背压','提供阻塞和容量控制','选型需权衡公平性、容量和锁开销']],
    ['把 PriorityQueue 迭代结果当成排序结果','使用无界队列掩盖下游过载','用 null 同时表示合法元素和空队列'],
    ['队列长度与增长速率','入队拒绝或阻塞时间','队头等待时长','优先级分布'],
    '队列解决的是排序和缓冲，不自动提供任务幂等、失败重试或持久化；进程内队列丢失可接受与否必须单独定义。'
  ),
  'collections/copy-on-write-arraylist':p(
    ['读取当前数组快照','写操作获取互斥锁','复制完整数组','在副本上修改','发布新数组引用'],
    ['读取只抓取当前 volatile 数组引用，因此无需锁，迭代器也基于创建时的固定快照。','所有写入串行获取锁，避免多个写线程互相覆盖副本。','每次写都复制整个数组，时间和临时内存与元素数量成正比，大列表写入成本很高。','修改只发生在新数组上，旧数组仍供已有读者使用，所以读者不会看到半完成结构。','新数组通过 volatile 引用发布，后续读可见；旧迭代器不会自动看到更新，这是快照语义而非一致性缺陷。'],
    '监听器列表读快写少却出现内存尖峰',
    '监听器注册本来很少，但一次配置重载逐个 add 数千项，每次都复制不断增长的数组，产生大量短命对象。改为先构建普通 List，再一次 addAll 发布，并限制列表规模后，保留了无锁读取优势。',
    [['CopyOnWriteArrayList','小规模、读极多写极少且允许快照读取','读无锁、遍历稳定','写放大和旧快照占内存'],['同步 List','读写都不频繁且需简单互斥','语义直接','遍历需持锁，全局竞争'],['不可变 List + volatile','整批替换配置快照','读取最轻、版本清晰','不适合高频单元素修改']],
    ['大列表频繁单元素写导致复制风暴','误以为迭代器能看到最新元素','长生命周期迭代器延长旧数组存活'],
    ['列表长度','单位时间写次数','数组复制分配量','旧数组晋升与 GC 压力'],
    'Copy-on-write 用写成本换读性能和快照一致性，适合监听器、路由快照等场景；“线程安全”不代表所有负载下都高效。'
  ),
  'collections/fail-fast-iterator':p(
    ['创建迭代器并记录版本','读取下一个元素','比较结构修改计数','发现不一致抛异常','通过迭代器安全删除'],
    ['迭代器创建时保存集合 modCount，只有结构性修改通常会递增该计数。','next 不只是返回元素，还检查当前位置、边界与预期版本。','集合被其他路径结构修改后，expectedModCount 与 modCount 不同，尽力抛出 ConcurrentModificationException。','fail-fast 是缺陷探测机制而非并发保证，竞态下不承诺百分之百检测，也不能用捕获异常恢复业务。','Iterator.remove 会同步更新集合与迭代器版本，因此是单线程遍历删除的合法方式。'],
    '过滤列表时偶发 ConcurrentModificationException',
    '代码在增强 for 中直接调用 list.remove，修改了集合版本但迭代器不知道。单线程场景改用 removeIf 或 Iterator.remove；并发场景则重新定义快照、锁或并发集合语义，而不是 catch 后重跑。',
    [['Iterator.remove/removeIf','单线程遍历期间删除','遵守迭代协议、代码清晰','复杂复合修改仍需谨慎'],['复制快照后遍历','读视图可稍旧且集合规模可控','遍历不受后续写影响','复制内存与一致性延迟'],['并发集合弱一致迭代','并发读写且允许看到部分更新','不抛 fail-fast、无需全局锁','视图不是固定时点快照']],
    ['把 fail-fast 当成线程安全机制','捕获异常后从头重试造成活锁','只修改元素字段却误判为结构修改'],
    ['并发修改异常堆栈','集合修改来源','重试次数','遍历视图版本差异'],
    'ConcurrentModificationException 的目标是尽早暴露错误使用，不是可靠的业务检测协议；真正并发访问必须选择明确的同步或快照策略。'
  ),
  'collections/immutable-collections':p(
    ['收集输入元素','执行空值与重复校验','复制或接管数据','封装不可修改视图','跨线程安全发布'],
    ['创建不可变集合前要决定是否允许 null、重复键和空集合，不同工厂方法的失败语义不同。','List.of/Set.of/Map.of 在构造期执行严格校验，能够把数据错误提前暴露。','真正不可变实现不暴露底层可变容器；仅包一层 unmodifiableView 仍会随原容器变化。','所有修改 API 都拒绝执行，但元素对象本身可能可变，所以集合不可变不等于对象图深度不可变。','构建完成后通过 final/volatile 或安全容器发布，读线程才能稳定看到完整快照。'],
    '配置中心快照在读取过程中变化',
    '服务把可变 HashMap 包成 unmodifiableMap 后发布，却继续复用原 Map 做增量更新，读线程看到同一“不可修改视图”内容变化。改为每次构建新 Map、使用 Map.copyOf 固化并原子替换引用后，版本与内容才一一对应。',
    [['List.of/Map.of','固定少量元素和立即校验','简洁、真正不可修改','拒绝 null，工厂重载有数量边界'],['copyOf','从现有集合创建不可变快照','隔离后续结构修改','浅拷贝，元素仍可能可变'],['unmodifiableView','需要只读 API 但允许后台数据变化','无需复制','不是快照，底层变化会透出']],
    ['把只读视图当成不可变快照','集合元素内部可变导致读结果变化','每次请求复制大集合造成分配压力'],
    ['快照构建耗时','配置版本与内容哈希','集合复制分配量','非法修改调用数'],
    '不可变集合主要约束容器结构；需要深度不可变时，元素也必须是值对象或在边界复制，并建立清晰的所有权规则。'
  ),
  'collections/comparable-comparator':p(
    ['提取排序字段','处理 null 与类型','逐关键字比较','返回负零正结果','排序或有序集合使用'],
    ['Comparable 定义类型的自然顺序，适合全局稳定且唯一的默认语义。','Comparator 是外部策略，可组合字段、升降序和 null 规则，同一类型可以有多个业务顺序。','比较应按主键、次键依次进行，避免直接相减产生整数溢出。','返回 0 表示排序等价；在 TreeSet/TreeMap 中还决定唯一性，因此最好与 equals 语义协调。','排序算法和有序集合假设比较器满足反对称、传递和一致性，违反契约可能得到异常或无序结果。'],
    '价格排序在大数值时顺序反转',
    '比较器使用 (int)(a.price-b.price)，金额差超过 int 范围后溢出，且小数精度被截断。改为 Comparator.comparing(Order::price).thenComparing(Order::id)，同时明确 null 与同价顺序后，契约和可读性都改善。',
    [['Comparable','类型有唯一自然顺序','调用简洁、有序容器默认可用','把业务口径固化进实体'],['Comparator','多个排序口径或外部类型','可组合、可注入','调用处需选择正确策略'],['预计算排序键','比较昂贵且数据批量排序','减少重复计算','占额外内存，键变化需同步']],
    ['比较器不传递导致排序结果异常','相减比较溢出','compare 为 0 与 equals 不一致导致 TreeSet 丢元素'],
    ['排序耗时与比较次数','compare 返回 0 的比例','排序契约异常','有序集合元素差异'],
    '排序只定义相对次序，不自动保证稳定性；需要同值保持输入顺序时，要选择稳定排序或显式加入唯一序号作为最后关键字。'
  ),
  'collections/concurrent-collections':p(
    ['识别并发访问模式','选择映射队列或快照','执行单操作原子更新','建立复合操作边界','观测竞争与容量'],
    ['先区分读多写少、生产消费、计数聚合还是有序调度，不存在适合所有模式的“并发集合”。','ConcurrentHashMap、BlockingQueue、CopyOnWriteArrayList 和 ConcurrentSkipListMap 分别优化不同结构与一致性。','线程安全集合通常只保证单个 API 的原子性；compute、putIfAbsent 等组合 API 才能封装常见竞态。','跨多个键或集合的业务不变量仍需锁、不可变快照、串行化或事务协议。','容量和竞争必须可观测，无界队列或热点键会把线程安全问题转化为延迟与内存故障。'],
    '任务系统换成并发集合后仍然重复执行',
    '代码用 containsKey 后再 put 标记任务，两个线程都通过检查。ConcurrentHashMap 没有损坏，但两步之间存在竞态。改用 putIfAbsent 返回值决定唯一执行者，并在失败回滚时明确标记生命周期后才解决。',
    [['ConcurrentHashMap','高并发键值访问','桶级并发与原子复合 API','不支持跨键事务'],['BlockingQueue','有界生产消费与背压','容量、等待语义明确','锁竞争和队头阻塞需评估'],['ConcurrentSkipListMap','并发有序键与范围查询','有序、导航操作丰富','O(log n) 且节点开销较高']],
    ['用线程安全集合拼出非原子业务流程','无界队列导致 OOM','映射回调执行外部慢调用'],
    ['队列长度与等待时间','热点键更新冲突','原子 API 重试次数','集合内存水位'],
    '并发集合减少底层数据结构损坏风险，不替代业务并发控制；必须把不变量映射到一个原子操作、一个所有者或明确的事务边界。'
  )
}
