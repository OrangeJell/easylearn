import{profile as p}from'./profile.mjs'

export const redisProfiles={
  'redis/why-fast':p(
    ['客户端发送命令','事件循环读取请求','单线程执行核心命令','内存数据结构完成操作','缓冲响应并批量写回'],
    ['RESP 协议解析简单，连接复用和 pipeline 可以摊薄网络往返，但大请求仍会占用带宽和解析时间。','Redis 用事件驱动 I/O 处理大量连接，新版本可用 I/O 线程分担读写，命令执行核心仍保持顺序语义。','单线程避免大多数锁竞争与上下文切换，但意味着慢命令会阻塞其他客户端。','高效编码、渐进式 rehash 和针对场景的数据结构让常见命令接近 O(1) 或 O(log n)。','响应先进入客户端输出缓冲，慢消费者可能让缓冲增长，因此服务端快不代表端到端永远低延迟。'],
    '平均延迟很低但偶发所有请求超时',
    '某任务对超大 Set 执行 SMEMBERS，命令执行期间事件循环无法服务其他请求，形成数百毫秒阻塞。改为 SSCAN 分批、限制集合大小并监控慢命令后，尾延迟恢复。',
    [['单次命令','低频或强依赖前序结果','语义直接','多次网络 RTT'],['Pipeline','批量独立命令','显著减少 RTT','响应积累占内存，非原子'],['Lua/Function','短小读改写需原子执行','服务端一次完成','长脚本会阻塞整个实例']],
    ['O(n) 命令阻塞事件循环','大 Key 删除造成长停顿','输出缓冲被慢客户端撑大'],
    ['commandstats 延迟','SLOWLOG','事件循环延迟','客户端输入输出缓冲'],
    'Redis 快来自内存、数据结构和事件模型的组合，不意味着所有命令都是常数时间；单线程尤其要求每次执行短小可控。'
  ),
  'redis/data-structures-use-cases':p(
    ['明确业务访问模式','选择数据结构与编码','设计 Key 和 TTL','执行原子命令','控制基数并治理生命周期'],
    ['先列出读写、排序、去重、范围和聚合需求，再选 String、Hash、Set、ZSet、Stream 等结构。','同一逻辑结构在小规模时可能使用紧凑编码，超过阈值后转换，内存与性能会突变。','Key 应包含业务域、版本和分片信息，TTL 需要抖动以避免集中到期。','优先用单命令表达原子语义，跨命令读改写用 Lua 或事务，但脚本必须短小。','数据结构必须有规模上限、清理和监控；无限增长的排行榜、集合或 Stream 最终会变成大 Key。'],
    '排行榜更新正确但内存持续增长',
    'ZSet 保存所有历史用户且从不淘汰，成员数增长到数千万，备份与故障恢复都变慢。按赛季分 Key、只保留活跃窗口并把历史落到持久存储后，实时榜才保持可控。',
    [['Hash','对象字段局部读写','紧凑且支持单字段更新','超大 Hash 成为热点与大 Key'],['Set/ZSet','去重、集合运算或排序','语义直接、服务端计算','高基数与复杂范围操作成本'],['Stream','消费组和短期事件流','服务端持久队列语义','不是 Kafka 替代品，长度与 PEL 需治理']],
    ['用 KEYS 扫描生产全库','集合无上限演变为大 Key','TTL 同秒到期引发负载尖峰'],
    ['每类 Key 数量','Value 大小分位值','命令复杂度与延迟','过期/淘汰速率'],
    '数据结构选择要同时满足操作语义和生命周期；能用 Redis 表达不代表应把无界历史或复杂分析长期放在 Redis。'
  ),
  'redis/expiration-eviction':p(
    ['写入 Key 与过期时间','惰性访问检查','周期主动采样删除','内存达到 maxmemory','按策略淘汰并反馈'],
    ['EXPIRE 与数据写入应尽量在同一原子操作中完成，避免进程崩溃留下永不过期 Key。','客户端访问 Key 时先检查 TTL，已过期则删除并返回不存在，这是惰性删除。','后台定期随机采样带 TTL Key，若过期比例高会继续循环，但不会逐键精确按时删除。','used_memory 超过 maxmemory 后按 noeviction、LRU/LFU、random、TTL 等策略选择候选。','淘汰是容量保护而非业务过期，关键数据被驱逐后调用方必须有回源、降级或错误语义。'],
    '缓存集中失效导致数据库雪崩',
    '百万 Key 使用同一个整点 TTL，主动过期与回源请求同时出现。写入 TTL 加随机抖动、热点逻辑过期并限制回源并发后，负载被摊平。',
    [['惰性+定期过期','通用 TTL 数据','Redis 自动平衡 CPU 与内存','删除时间不精确'],['allkeys-lfu','纯缓存且热点相对稳定','保留高频数据','频率估算并非业务优先级'],['noeviction','不能丢的状态数据','写失败显式暴露容量问题','达到上限后新写失败']],
    ['缓存与业务状态混用同一淘汰实例','过期时间无抖动集中触发','误把 TTL 到期当成实时定时器'],
    ['expired_keys','evicted_keys','内存水位','回源 QPS 与命中率'],
    'TTL 只保证访问时不会返回过期值，不保证物理删除时刻；需要精确定时业务应使用延迟队列或持久状态机。'
  ),
  'redis/cache-problems':p(
    ['请求查询缓存','未命中或值过期','控制回源并发','数据库加载并回填','设置随机 TTL 与降级'],
    ['穿透是查询不存在数据，击穿是热点瞬间失效，雪崩是大量 Key 或实例同时失效，三者治理目标不同。','缓存查询要区分空值、逻辑过期和真实错误，不能把 Redis 超时当成缓存未命中直接放大回源。','互斥重建、singleflight 或限流把同一热点的回源合并，等待者可读旧值或快速失败。','数据库结果回填前要检查版本或使用一致性策略，避免慢旧请求覆盖新值。','TTL 加抖动、实例隔离和多级缓存降低相关性，熔断则保护数据库在缓存故障时不被打穿。'],
    'Redis 抖动一分钟拖垮数据库',
    '客户端超时后所有请求直接查询数据库，连接池迅速满，缓存恢复后应用仍在排队。加入回源并发闸门、热点本地短缓存和降级数据后，缓存故障不再转化为数据库全量压力。',
    [['缓存空值','不存在查询可枚举且变化不频繁','实现简单','占内存且需短 TTL'],['布隆过滤器','Key 规模大、允许极低误判','拦截绝大多数穿透','删除和重建复杂'],['逻辑过期+异步重建','热点允许短暂旧值','避免击穿、延迟稳定','一致性和后台任务更复杂']],
    ['缓存超时全部直打数据库','互斥锁无超时导致热点永久阻塞','慢重建把旧数据覆盖新值'],
    ['命中率按 Key 分布','回源并发','热点重建耗时','数据库连接等待'],
    '缓存治理的最终目标是保护数据源并给用户可解释结果；任何缓存故障路径都必须有明确的回源上限。'
  ),
  'redis/distributed-lock':p(
    ['生成唯一持有者标识','SET NX PX 竞争','临界区执行业务','Lua 校验后释放','超时续期与故障恢复'],
    ['锁值必须唯一标识本次持有，避免旧请求误删后来者的锁。','SET key value NX PX 原子完成获取与租期设置，分开的 SETNX/EXPIRE 存在崩溃窗口。','临界区时间应短且有界，锁只协调竞争，不自动使数据库和外部副作用成为事务。','释放使用 Lua 比较锁值后 DEL，保证检查与删除原子。','任务可能超过租期，续期需要确认仍是持有者；即使如此，GC 停顿和网络分区仍可能造成双持有。'],
    '库存任务因 GC 停顿出现并发执行',
    '线程获得 10 秒锁后发生 15 秒停顿，锁过期被另一实例取得；旧线程恢复后继续写库。仅有锁无法阻止过期持有者，数据库写入必须携带 fencing token 并拒绝较旧令牌。',
    [['单 Redis NX PX','可容忍极小故障窗口的协调','简单、性能高','故障转移和超时可能双持有'],['成熟客户端看门狗','任务时长不稳定','自动续期与可重入','仍不能替代资源端 fencing'],['数据库唯一约束/版本','最终状态可由单库裁决','正确性证据强','吞吐和锁等待受数据库限制']],
    ['无唯一锁值误删他人锁','租期短于最坏执行时间','把锁成功当成外部副作用恰好一次'],
    ['获取等待与失败率','持锁时间分布','续期失败','fencing token 拒绝数'],
    '分布式锁提供互斥意图，不等于线性一致事务；关键资源应通过版本、唯一约束或 fencing 在最终写入点再次裁决。'
  ),
  'redis/persistence-high-availability':p(
    ['内存写入完成','AOF 或 RDB 持久化','主从复制传播','Sentinel/Cluster 检测故障','选主恢复并核对数据'],
    ['写命令先改变内存数据，持久化策略决定宕机时可能丢失多长窗口。','RDB 生成紧凑快照适合备份和快速加载，fork 与写时复制会产生内存峰值。','AOF 追加命令并按 everysec/always 等策略刷盘，重写用于压缩历史。','副本异步接收复制流，断线重连可能部分同步或全量同步，积压缓冲区需按故障窗口配置。','Sentinel 或 Cluster 根据主观/客观下线选新主，客户端路由更新期间仍可能失败或读到差异。'],
    '故障切换后少量订单资格丢失',
    '主库返回成功后尚未把写传播到副本就宕机，新主缺少该 Key。异步复制不能承诺零丢失；资格状态若不可丢，应落可靠数据库/日志或在写入时等待足够副本并接受延迟。',
    [['RDB','备份、灾备和快速全量恢复','文件紧凑、加载快','快照间写入可能丢失'],['AOF everysec','多数缓存与状态场景','RPO 约秒级、性能平衡','重写与 fsync 抖动需治理'],['外部事实源重建','Redis 只是派生缓存','容灾语义简单','恢复时间取决于回灌容量']],
    ['fork 时写时复制导致内存翻倍','复制积压不足频繁全量同步','把异步副本当成强一致备份'],
    ['aof_delayed_fsync','fork 耗时与 COW','master_repl_offset 差距','故障切换 RTO/RPO'],
    '高可用保证服务尽快恢复，不自动保证所有已确认写都存在；必须按数据价值选择 Redis 是事实源还是可重建派生数据。'
  ),
  'redis/hot-key-big-key':p(
    ['采样识别异常 Key','判断访问热点或体积过大','限制单次操作成本','拆分/复制/本地缓存','迁移并持续观测'],
    ['热 Key 是访问集中，Big Key 是元素或字节体积大，两者可能同时存在但治理方式不同。','可结合客户端埋点、MONITOR 采样、hotkeys 与 slowlog，避免在线全量扫描带来二次影响。','大集合的全量读取、删除、过期和迁移都会阻塞或放大网络，应改为分批迭代与 UNLINK。','热读可用本地缓存和副本分担，热写需要分桶或重新设计聚合，但会增加一致性。','拆分迁移要支持双读/双写或版本路由，并核对数量、TTL 与访问命中。'],
    '促销配置 Key 让单分片带宽打满',
    '所有请求读取一个数百 KB JSON，QPS 并不极端但网络带宽和序列化成为瓶颈。把配置拆为稳定小字段并加入进程内版本缓存后，Redis 只在版本变化时读取。',
    [['本地缓存','热读且允许短暂旧值','消除网络与单 Key 读压力','失效传播与内存副本'],['分桶拆 Key','热写或大集合可按维度拆分','分散单槽成本','聚合、迁移和一致性复杂'],['专用实例隔离','短期无法改模型的核心热点','降低对其他业务影响','热点本身容量未消失']],
    ['在线使用大范围 SCAN 也造成压力','DEL 超大 Key 阻塞主线程','随机分桶导致读取必须扫所有桶'],
    ['单 Key QPS','Value 字节与成员数','分片带宽/CPU','慢命令与阻塞时长'],
    '拆分前必须确认瓶颈是 CPU、网络、命令复杂度还是内存；热读复制与热写分片解决的是不同问题。'
  ),
  'redis/cache-consistency':p(
    ['业务写数据库','提交事务','删除或更新缓存','并发读回源','通过重试/消息最终收敛'],
    ['数据库通常是事实源，写操作先提交数据库可避免缓存成功而数据库失败造成虚假新值。','Cache Aside 常选择删除而非更新，因为更新可能浪费且并发顺序更难控制。','删除失败会留下旧值，需要可靠重试、事务消息或 CDC 补偿，不能只记录一条日志。','删除与并发慢查询存在回填旧值窗口，短 TTL、延迟双删或版本校验可缩小窗口。','最终一致必须定义最大陈旧时间、可观测差异和人工/自动修复，而不是一句“下次过期就好”。'],
    '更新成功后用户仍看到旧资料',
    '数据库提交后删除缓存超时，接口仍返回成功，旧值持续到一小时 TTL。将删除事件写事务 Outbox 并异步重试，同时把 TTL 缩短到业务可接受窗口后，失败可被观测并最终修复。',
    [['Cache Aside 删除','通用读多写少','实现简单、按需加载','有删除失败与并发回填窗口'],['写穿/同步更新','写后读必须快速看到新值','缓存立即更新','双写顺序和失败处理复杂'],['CDC 驱动缓存','多写入源或需可靠收敛','与数据库提交日志绑定','链路延迟、乱序和运维成本']],
    ['先删缓存再写库导致旧值回填','删除失败无重试','消息乱序让旧版本覆盖新版本'],
    ['缓存与数据库版本差','删除重试积压','陈旧读取比例','回源与回填延迟'],
    '缓存一致性不是二选一算法，必须把事实源、陈旧窗口、失败补偿和版本顺序共同定义；强一致需求应减少或绕过缓存。'
  ),
  'redis/cluster-sharding':p(
    ['对 Key 计算 CRC16','映射到 16384 槽','客户端路由到节点','MOVED/ASK 重定向','迁移槽并恢复副本'],
    ['Cluster 以 hash slot 而非节点数直接映射，槽作为扩容迁移的最小路由单位。','普通 Key 通过 CRC16%16384 定位；hash tag 可让多个 Key 落同槽以支持多键命令。','客户端缓存槽位拓扑并直连目标主节点，拓扑变化时更新映射。','MOVED 表示稳定归属改变，ASK 表示迁移中的临时访问，客户端处理方式不同。','reshard 在节点间迁移槽内 Key，故障转移由副本晋升，但少数写入仍受异步复制窗口影响。'],
    '使用 hash tag 后单节点过热',
    '为支持批量 Lua，团队把所有租户 Key 都写成 {global}:xxx，结果全部落同一槽，集群形同单节点。hash tag 应只绑定确需原子操作的小范围业务实体，而不是全局命名空间。',
    [['Cluster','数据量和吞吐需水平扩展','原生分片与故障转移','跨槽多键限制、客户端更复杂'],['客户端一致性哈希','可控分片中间层','规则灵活','迁移和高可用需自行实现'],['单实例主从','容量足够且操作依赖多键','语义简单','垂直容量上限明显']],
    ['全局 hash tag 制造单槽热点','客户端不处理 ASK/MOVED','槽迁移时执行大 Key 阻塞'],
    ['各槽 Key/QPS 分布','cluster_state','重定向次数','复制偏移与故障转移时间'],
    'Cluster 扩展的是多 Key 总吞吐，无法拆分一个热 Key；跨槽原子性和强一致也不会因增加节点自动获得。'
  ),
  'redis/transactions-lua-pipeline':p(
    ['客户端组织多条命令','选择批量/事务/脚本','服务端顺序执行','处理错误与返回值','重试并保证业务幂等'],
    ['Pipeline 只批量发送和读取响应，减少 RTT，不提供隔离与原子性。','MULTI/EXEC 把命令排队后连续执行，但不支持自动回滚，运行期错误不会撤销已成功命令。','WATCH 提供乐观检查，监视 Key 变化后 EXEC 失败，调用方必须有界重试。','Lua 在事件循环中原子执行，可读取前序结果并条件写入，但长脚本会阻塞所有客户端。','无论选择哪种机制，网络超时都可能让结果未知，业务仍需请求 ID 和幂等检查。'],
    'Pipeline 超时后重复扣减余额',
    '客户端未收到响应就重发整批命令，第一次其实已执行成功，余额被再次扣减。Pipeline 不能提供恰好一次；改用带请求 ID 的 Lua，在同一脚本中检查去重记录并更新余额。',
    [['Pipeline','大量独立命令批量执行','减少网络 RTT、吞吐高','不原子，错误逐条处理'],['MULTI/EXEC + WATCH','短事务与乐观冲突','Redis 原生、无需脚本','冲突重试且无回滚'],['Lua/Function','有条件的原子读改写','一次往返、语义集中','脚本阻塞风险与版本治理']],
    ['把 Pipeline 当事务','Lua 遍历大集合阻塞实例','WATCH 冲突无限重试放大负载'],
    ['批次大小与响应字节','脚本执行时长','WATCH abort 率','超时后的幂等命中'],
    'Redis 原子性只覆盖脚本或事务在单实例中的执行，不包含数据库、MQ 或客户端结果确认；跨系统仍需状态机与补偿。'
  )
}
