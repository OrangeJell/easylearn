import{answer,md}from'./shared.mjs'

export default{
  'clickhouse/mergetree':answer(
    '我们用 ClickHouse 保存追加型明细和聚合分析，MergeTree 通过列式存储、排序数据段和后台合并适合大扫描，不拿它做高频事务更新。',
    md(
      '典型是订单事件、用户行为和调用日志：每天持续追加，查询按租户、时间和业务维度聚合。MySQL 做这类多列扫描和聚合会影响在线事务，ClickHouse 只读取涉及的列，并利用向量化批量计算，单位硬件吞吐更高。',
      '',
      'MergeTree 写入后形成不可变 Part，Part 内按 `ORDER BY` 排序并建立稀疏索引；后台把小 Part 合并成大 Part。查询如果带排序键前缀和分区条件，可以跳过大量数据块。',
      '',
      '我们不会把订单主状态只放 ClickHouse。它不适合逐行事务、频繁小更新和强一致点查；数据通常从 Kafka/binlog 异步进入，允许分钟或秒级延迟，权威值仍在 OLTP。',
      '',
      '上线会看写入批次、active parts、合并积压、扫描行数/字节和查询延迟。MergeTree 的优势建立在合理分区、排序键和批量写入上，不是换引擎名就会变快。'
    )
  ),
  'clickhouse/query-optimization':answer(
    '报表从 3 秒变 40 秒，先对比扫描数据、分区裁剪、查询日志和 Part 状态，确认是数据增长、计划变化还是资源争抢。',
    md(
      '我会在 `system.query_log` 对比快慢两次的 `read_rows`、`read_bytes`、内存、ProfileEvents 和等待时间，并拿到完整 SQL 与参数。扫描量突然扩大，先查时间条件是否还能分区裁剪、过滤是否匹配排序键、函数或类型转换是否让条件下推失效。',
      '',
      '扫描量没明显变化但耗时暴涨，就看同时间并发、CPU、磁盘吞吐、远程读、merge/mutation 和内存超限后的外部排序。分布式查询还要拆到各分片，确认是不是一个慢分片或数据倾斜拖住整体。',
      '',
      '修复可能是收紧时间范围、调整排序键、预聚合、改写 JOIN、限制返回列，或把高频维度做物化视图。改结构前会用生产数据影子表验证跳过比例和写入成本。',
      '',
      '> 只看 SQL 文本很容易误判。ClickHouse 最有用的第一证据通常是“实际读了多少行和字节”。'
    ),
    {pitfallsMarkdown:md(
      '- **用 FINAL 解决所有重复。** 大范围 FINAL 会显著增加查询成本。',
      '- **SELECT * 再由应用丢列。** 列式存储的优势被主动放弃。',
      '- **只扩 CPU。** 如果瓶颈是扫描错误、慢磁盘或单分片倾斜，扩容未必有效。'
    )}
  ),
  'clickhouse/advantages-limitations':answer(
    '我们引入 ClickHouse 是把大规模明细扫描和聚合从 MySQL 拆走，但交易写入、强一致点查和频繁小更新仍留在 OLTP。',
    md(
      '项目里原来的运营报表会扫 MySQL 几千万行，还和下单共享 IO。把订单事件通过 Kafka 同步到 ClickHouse 后，按时间、渠道、地区聚合从几十秒降到秒级，同时不再拖慢主库。列裁剪、压缩和向量化非常适合这种读多、写入追加、允许短暂延迟的场景。',
      '',
      '我坚决不用它处理这些事情：',
      '',
      '- 余额扣减、订单状态机等多行事务；',
      '- 每次只按主键查一行且要求立即读到最新值；',
      '- 高频逐行更新、删除；',
      '- 高并发短查询且没有接口层并发保护。',
      '',
      '引入前还要算完整成本：数据同步与补数、重复与乱序、集群运维、冷热存储、查询治理。MySQL 里一个正确索引就能解决的问题，不值得为此引入 ClickHouse。'
    )
  ),
  'clickhouse/production-optimization':answer(
    '真实优化要用扫描量、Part 数、写入批次和并发指标证明；我会优先修表模型和数据链路，再调参数。',
    md(
      '一次订单明细项目里，单条 Kafka 消息就写一次 ClickHouse，产生大量小 Part，merge 长期追不上，查询从 2 秒抖到十几秒。我们把消费端改成按分区聚合，达到约 5~10 万行或一秒就批量写，active parts 和 merge IO 明显下降。',
      '',
      '随后按 Top 查询把 `ORDER BY` 从单纯时间改成 `(tenant_id, event_date, user_id, event_time)`，并限制报表必须带租户和时间。常用日报做物化聚合，接口不再每次扫原始明细。',
      '',
      '我会用这组前后指标说明结果，而不是只说“快了”：',
      '',
      '- 单批行数、每分钟新 Part 和 active parts；',
      '- 每个查询的 read_rows/read_bytes；',
      '- P95/P99、峰值内存和磁盘吞吐；',
      '- 合并积压以及写入端到可查询的延迟。',
      '',
      '任何数字都必须来自同一数据量和并发条件。把查询从 40 秒测到 3 秒但换了时间范围，没有意义。'
    )
  ),
  'clickhouse/concurrency-limitations':answer(
    'ClickHouse 单查询能并行吃满 CPU 和磁盘，所以高并发会互相争抢；接口层必须限制并发、时间范围和资源配额。',
    md(
      '我会先看并发升高时 `system.processes`、CPU、磁盘、网络和 memory tracker。单条报表可能使用多个执行线程，10 条并发并不是 10 个轻请求，而是几十上百个线程同时扫数据；merge 也在争磁盘和 CPU。',
      '',
      '保护会分层做：接口对同租户和同报表限流，查询设置最大执行时间、最大扫描行数和内存；核心实时查询与离线导出使用不同用户/profile 和配额；重复查询可做短缓存，耗时导出改异步队列。',
      '',
      '突发事故时先暂停离线任务、降低单查询线程数、取消明确失控查询并限制入口并发。扩容只在确认长期资源不足后做，而且要看数据是否均匀分布。',
      '',
      '> 提高 `max_concurrent_queries` 只是允许更多查询一起抢资源。容量不变时，它经常让所有查询都更慢。'
    ),
    {problemAnalysisMarkdown:md(
      'OLAP 和普通接口的差别是：单请求本身就可能是并行大任务。并发治理看的不是连接数，而是 **每条查询的扫描量、线程数、内存和持续时间**。'
    )}
  ),
  'clickhouse/replacing-mergetree':answer(
    'ReplacingMergeTree 只在后台合并时按排序键保留版本，不能保证刚写完或未 FINAL 的查询绝对无重复。',
    md(
      '我会把业务唯一键放进 `ORDER BY`，再指定版本列，例如更新时间或单调版本。重复上报时写入新版本，后台 merge 最终可能清掉旧版本。但 merge 何时发生不确定，因此普通查询在一段时间内会同时看到多个版本。',
      '',
      '`FINAL` 可以在查询时做去重，但对大范围扫描成本很高，不能所有线上报表都默认加。更稳妥的做法按场景选：',
      '',
      '- 展示允许短暂重复：普通查询，等待后台合并；',
      '- 小范围精确查询：带 FINAL，并严格限制分区；',
      '- 统计必须准确：查询中按版本 `argMax`，或构建已去重的物化层；',
      '- 上游能幂等：尽量在写入前就减少重复。',
      '',
      '删除旧版本还依赖相同排序键落在可合并范围内。分区键或排序键设计错了，ReplacingMergeTree 也不会跨分区神奇去重。'
    ),
    {pitfallsMarkdown:md(
      '- **把最终一致去重理解成唯一约束。** 写入阶段不会拒绝重复。',
      '- **全表使用 FINAL。** 查询成本可能随数据量显著上升。',
      '- **版本列不单调。** 乱序事件可能留下错误版本。'
    )}
  ),
  'clickhouse/partition-order-primary-key':answer(
    '百亿日志表通常按月或天分区管理生命周期，排序键围绕高频过滤顺序设计，不能把高基数用户直接做分区。',
    md(
      '我会先拿 Top 查询和保留策略。每天几百 GB、保留半年时可以按天或月分区：删除旧数据直接 drop partition，也避免分区过多。把 `user_id` 作为分区会产生海量小分区和 Part，运维会先崩。',
      '',
      '排序键决定 Part 内物理顺序。若查询总是租户 + 时间，再按服务或用户过滤，可以设计：',
      '',
      '```sql',
      'PARTITION BY toYYYYMM(event_time)',
      'ORDER BY (tenant_id, service, event_date, event_time, trace_id)',
      '```',
      '',
      '低基数且高频等值条件放前面，时间字段保证范围裁剪；高基数 traceId 放后，避免点查之外的查询失去前缀。Primary key 可以是 ORDER BY 的前缀以控制索引大小。',
      '',
      '我会用真实查询回放比较 marks 命中、read_rows、压缩率和写入排序成本。排序键一旦选错，后续变更通常需要重写数据，不能只凭字段选择性拍板。'
    )
  ),
  'clickhouse/data-skipping-index':answer(
    '跳数索引只有当数据在 granule 内有可跳过的局部规律时才有价值；先看排序键和扫描分布，再用 read_rows 证明。',
    md(
      '如果查询扫描太多，我先确认分区和 ORDER BY 是否匹配。排序键能从根本上让相近值聚集，优先级高于补跳数索引。像状态、错误码在局部数据块中分布稀疏，可以尝试 set/bloom/minmax；完全随机且每个 granule 都出现的字段，加索引也跳不过任何块。',
      '',
      '我会在影子表或一个代表性分区上建索引并 materialize，用相同参数比较：',
      '',
      '- `read_rows`、`read_bytes` 和 marks 数；',
      '- 查询时间与索引判断 CPU；',
      '- 索引磁盘占用和写入/merge 开销；',
      '- 不同时间范围、稀有值和热门值的效果。',
      '',
      '只有跳过比例稳定且总成本下降才推广。为了让 EXPLAIN 显示用了索引而增加写入成本，没有价值。'
    ),
    {problemAnalysisMarkdown:md(
      '跳数索引不是 BTree 点查。它回答的是“这一大块数据肯定没有目标值吗”。数据越聚集、目标越稀疏，越可能跳过；数据均匀随机时效果通常很差。'
    )}
  ),
  'clickhouse/distributed-table':answer(
    '分片键要让写入均匀并尽量让高频查询落单片；选择后还要用最大/平均负载和跨片比例持续验证。',
    md(
      '订单或用户事件常用稳定业务键哈希，例如 `cityHash64(user_id)`，同一用户落同一分片，用户查询可带分片键路由，整体分布也比较均匀。不能用低基数地区，也不能用当前日期简单取模，否则热点会集中到少数分片。',
      '',
      '我会回放 Top 查询，看有多少能携带分片键。跨全局聚合本来就要访问所有分片，但高频点查如果每次广播，分片越多延迟和资源越差。必要时为另一查询维度构建独立聚合表，而不是让所有需求共用一张分布式表。',
      '',
      '上线前用真实键分布计算每片行数、字节和 QPS，特别模拟超级用户。监控最大/平均负载比、远程读取、最慢分片和写入队列。',
      '',
      '扩容后旧数据不会自动按新规则完美重排，需要明确新写路由、历史迁移和查询兼容期。分片键不是只影响今天建表，还决定未来扩容成本。'
    ),
    {pitfallsMarkdown:md(
      '- **用随机分片换绝对均匀。** 同一用户查询会被迫广播。',
      '- **只看平均分布。** 少数超级租户足以拖垮一个分片。',
      '- **认为加节点后旧数据自动平衡。** 迁移与路由兼容需要单独设计。'
    )}
  ),
  'clickhouse/write-optimization':answer(
    'ClickHouse 要批量写，单条写会制造大量小 Part 和合并压力；入口应聚合、限时刷新并支持背压与重放。',
    md(
      '我会让日志先进入 Kafka，消费端按目标表和分区聚合，达到例如 5 万行、几 MB 或 1 秒任一条件就提交一批。这样既控制端到端延迟，又避免单条 INSERT 每次创建 Part。',
      '',
      '写入链路需要这几个能力：',
      '',
      '- 批次行数和字节双阈值，防止一条超大日志撑爆内存；',
      '- ClickHouse 变慢时暂停消费或降速，不能无限堆在进程内；',
      '- 批次失败可重试，消费位点在写入确认后提交；',
      '- 通过 eventId 或表引擎策略处理重试重复。',
      '',
      '会监控每秒 INSERT 次数、平均批次、active/inactive parts、merge 队列、Kafka lag 和可查询延迟。批次也不是越大越好，过大会增加单次失败重试和内存峰值。',
      '',
      '> `async_insert` 可以改善小写入，但上游仍应有批量与背压设计，不能用一个参数掩盖失控的写入模型。'
    )
  )
}
