import{answer,md}from'./shared.mjs'

export default{
  'kafka/consumer-group':answer(
    '同一消费组内一个分区同一时刻只交给一个消费者实例，所以有效并行度上限是分区数，扩容超过分区数不会增加吞吐。',
    md(
      '订单 Topic 假设有 12 个分区、同一组 4 个实例，协调器会让每个实例大约消费 3 个分区。扩到 12 个可以提升并行度；扩到 20 个，仍有 8 个实例空闲。不同消费组互不影响，风控和通知可以各自完整消费同一批订单事件。',
      '',
      '扩缩容、实例超时或分区变化会触发 rebalance。我们会使用 cooperative sticky 分配，减少不必要的全量撤销；消费处理过长时调大 `max.poll.interval.ms` 不是唯一办法，更重要的是把单批大小、处理耗时和 poll 节奏控制住。',
      '',
      '如果扩容后没效果，我会看：分区是否已经一实例一个、lag 是否集中在某几个分区、消费者是在 CPU 忙还是等数据库/下游、单批处理是否串行。热点订单键造成分区倾斜时，加实例也救不了那个单分区。',
      '',
      '最终容量按最慢分区规划，不只看全 Topic 平均 lag。'
    )
  ),
  'kafka/reliability':answer(
    'Kafka 端到端可靠要把生产确认、Broker 副本、消费位点和业务落库连起来；任何一段只配置“可靠”都不够。',
    md(
      '生产订单事件时会开启幂等生产者，`acks=all`，失败有限重试；Broker 要有合适副本数、`min.insync.replicas`，核心 Topic 禁止不干净选主。这样已确认消息至少进入足够的 ISR，但应用还要保留发送失败证据。',
      '',
      '真正棘手的是数据库事务和发消息之间的空隙。我们会用本地 outbox：订单与待发送事件在同一数据库事务提交，后台可靠投递 Kafka，发送成功后标记；或者通过 CDC 订阅变更。',
      '',
      '消费者关闭自动提交，只有业务处理成功才提交 offset。数据库写入和位点提交无法天然原子，所以消费端按 `eventId` 或业务唯一键幂等；失败进入重试/死信，但死信也必须告警和可回放。',
      '',
      '监控不只看 Kafka 可用性，还看 outbox 积压、生产错误、ISR、消费 lag、重试与死信、业务事件数量对账。可靠性最终要用故障演练证明。'
    ),
    {problemAnalysisMarkdown:md(
      '“不丢消息”至少有三个边界：**业务事务到生产者、Broker 内部、消费者到业务结果**。每个边界的确认时点和重试语义都不同，必须逐段闭环。'
    )}
  ),
  'kafka/message-loss-prevention':answer(
    '订单消息疑似丢失要用业务 eventId 沿生产者、Broker 和消费者逐段查证，不能先凭 lag 或日志猜是哪一层。',
    md(
      '我会选一笔明确缺失的订单，拿到 `eventId`、Topic、key 和大致发送时间。生产者侧查发送回调：是否进入 outbox、调用 send 是否成功、返回的 partition/offset 是多少，超时后有没有错误地当成功。',
      '',
      'Broker 侧根据 partition/offset 检查日志与保留策略，核对当时 ISR 收缩、Leader 切换、`acks`、`min.insync.replicas` 和 unclean election。如果生产者拿到成功 offset，但该位置后来不存在，才重点怀疑副本与选主配置。',
      '',
      '消费者侧查该组 offset 是否已经越过消息、反序列化是否失败、过滤条件是否误丢、业务事务失败后是否仍提交位点。很多“Kafka 丢消息”其实是消费代码 catch 后提交，或写库成功但查询侧数据不一致。',
      '',
      '止损会暂停可疑位点推进、保存日志，在隔离环境按 eventId 重放。修复后建立生产发送数、Broker 记录数、业务成功数和死信数的对账。'
    ),
    {pitfallsMarkdown:md(
      '- **只看 consumer lag 为 0。** 它只能说明位点追上，不说明业务处理成功。',
      '- **send 没抛异常就算成功。** 异步发送必须检查回调或 Future。',
      '- **直接重置整个消费组重放。** 没有幂等时会制造更大范围重复。'
    )}
  ),
  'kafka/duplicate-consumption-idempotency':answer(
    'Kafka 的常见交付语义允许重复，消费端要用稳定事件 ID 和业务唯一约束，让同一支付消息多次到达只产生一次结果。',
    md(
      '重复通常来自业务处理成功后、offset 提交前进程崩溃；rebalance、超时重试和生产者重发也会造成。我们不会追求“永远不重复”，而是让副作用幂等。',
      '',
      '支付消息会带不可变 `eventId` 和支付流水号。在本地事务中先插入消费记录或直接依赖业务唯一约束，再更新订单：',
      '',
      '```sql',
      'INSERT INTO consumed_event(event_id, consumed_at)',
      'VALUES (?, NOW()); -- event_id UNIQUE',
      '```',
      '',
      '若唯一键冲突，说明已经处理，返回成功并提交 offset。消费记录和订单变更必须同事务，否则记了“已消费”但订单没更新会永久漏处理。',
      '',
      '调用外部系统时会把业务幂等号传过去；对方不支持时用本地状态机与查单补偿。Redis SETNX 可以做快速挡板，但不能作为唯一证据，因为过期、淘汰和故障转移都可能失去记录。'
    )
  ),
  'kafka/message-backlog-troubleshooting':answer(
    'lag 几千万但消费者 CPU 不高，通常是在等下游、分区没分满、卡毒消息或频繁 rebalance；先找最慢分区和耗时阶段。',
    md(
      '我会按 Topic/partition 看 lag 和消费速率，而不是只看总数。若 lag 集中一个分区，检查 key 倾斜和该分区当前 owner；所有分区都慢，再拆消费耗时：poll、反序列化、数据库、RPC、提交位点分别多久。CPU 低往往说明线程在等连接、锁或下游。',
      '',
      '还会查 rebalance 次数、`max.poll.interval`、消费错误和重试。某条毒消息无限重试会让一个分区完全不前进；数据库连接池满时扩消费者只会增加等待。',
      '',
      '恢复时先保护依赖：暂停非核心消费，修复或隔离毒消息，扩容前确认分区数和下游余量；能批量写库就提高批次，暂时关闭昂贵的非核心步骤。按“当前净消化速度”估算清空时间。',
      '',
      '积压追平后再恢复正常限额，持续看生产速率、消费速率和 lag 斜率。一次性开很大并发容易把积压事故升级成数据库事故。'
    ),
    {problemAnalysisMarkdown:md(
      'CPU 不高是重要线索：消费者不是算力不足，而可能在 **等待、空闲或反复重平衡**。先找时间花在哪里，再决定扩容是否有效。'
    )}
  ),
  'kafka/isr-leader-election':answer(
    'Broker 宕机后是否有丢数据风险，要看新 Leader 是否来自 ISR、生产确认配置，以及故障前 ISR 是否已经收缩。',
    md(
      '我会查 controller/metadata 日志和 Topic 分区状态，确认旧 Leader、新 Leader、ISR 列表以及选举时间。若新 Leader 是 ISR 成员，且生产者使用 `acks=all`、Broker 设置合理的 `min.insync.replicas`，已确认消息的风险较低。',
      '',
      '如果开启了 unclean leader election，在所有 ISR 都不可用时可能让落后的副本当 Leader，换可用性但会截断未同步数据。核心订单 Topic 通常关闭它，宁可分区暂时不可用。',
      '',
      '还会核对生产者当时的成功 offset、Leader epoch 和日志末端，比较新旧副本差异；业务侧用 eventId 对账，不能只凭集群恢复绿色就宣布没丢。',
      '',
      '故障后重点修复 ISR 为什么提前缩小：磁盘慢、网络抖动、长 GC 还是副本抓取跟不上。副本数等于 3 但长期只有一个 ISR，并没有三副本的保护能力。'
    )
  ),
  'kafka/message-ordering':answer(
    '同一订单要有序，就用 orderId 作为消息 key，让相关事件进入同一分区，再由单分区串行消费并用状态机抵抗重试与乱序。',
    md(
      '生产者发布创建、支付、取消事件时都设置同一个 `orderId` key，Kafka 的分区器会把它们放到同一分区，分区内有序。消费组内这个分区只由一个实例处理，不能把同一分区消息随意扔到并发线程池后失去顺序。',
      '',
      '但链路仍可能出现业务乱序：不同上游各自发消息、失败重试、历史回放。事件会带订单版本或状态序号，消费者只接受合法迁移：',
      '',
      '```text',
      'CREATED(v1) -> PAID(v2) -> SHIPPED(v3)',
      '```',
      '',
      '收到未来版本时可短暂缓冲或重试，收到旧版本直接幂等忽略；长期缺版本要告警并查源数据。',
      '',
      '增加分区时，默认 hash 结果可能变化，同一 key 的新旧消息短期落不同分区。核心有序 Topic 扩分区前要评估生产切换、旧消息清空或版本隔离，不能在线直接加完就结束。'
    ),
    {pitfallsMarkdown:md(
      '- **全 Topic 只建一个分区。** 能保全局顺序，但吞吐和可用性代价过大。',
      '- **按消息类型做 key。** 同一订单的创建和支付会去不同分区。',
      '- **分区内消费后再无序并发处理。** Kafka 顺序会在应用层被打破。'
    )}
  ),
  'kafka/transactions-exactly-once':answer(
    '消费后再产生 Kafka 消息时可用 Kafka 事务原子提交输出消息和消费位点；若还写数据库，仍需 outbox 与幂等。',
    md(
      '纯 Kafka 链路里，我会使用事务生产者：读取输入，处理后发送输出消息，再把本批消费位点通过 `sendOffsetsToTransaction` 加入同一事务，最后 commit。失败 abort 后，输入位点不会前进，`read_committed` 下游也看不到未提交输出。',
      '',
      '```text',
      'poll -> beginTransaction -> send output',
      '     -> sendOffsetsToTransaction -> commit',
      '```',
      '',
      '每个实例的 `transactional.id` 必须稳定且唯一，重启后通过 producer epoch fencing 旧实例。事务要短，批次和超时受控。',
      '',
      '如果处理中还更新 MySQL，Kafka 事务不能把数据库一起纳入。我会让数据库变更和 outbox 同本地事务，再由 CDC/投递器发 Kafka；消费者和外部调用仍按 eventId 幂等。',
      '',
      '> Exactly-once 是限定边界内的处理语义，不代表邮件、支付和任意数据库副作用自动只发生一次。'
    )
  ),
  'kafka/zero-copy':answer(
    'Kafka 高吞吐来自顺序追加、批量压缩、页缓存和减少数据拷贝共同作用；零拷贝主要优化 Broker 向消费者发送日志的路径。',
    md(
      '生产消息按分区追加到日志文件，顺序写比随机更新更适合磁盘和页缓存。生产者把多条消息批成 record batch 并压缩，Broker 可以保存并转发整批，减少协议解析与系统调用。',
      '',
      '消费者读取时，传统路径可能是磁盘/页缓存到内核缓冲、再拷到用户态、最后回 socket。使用 `sendfile` 一类机制时，数据可以在内核页缓存与网络之间传输，减少用户态拷贝和上下文切换。',
      '',
      '但线上瓶颈不会只看“有没有零拷贝”。如果消息很小且批次差、分区太多、压缩 CPU 高、磁盘或网卡饱和，吞吐仍会差。我会看 batch size、压缩比、网络、磁盘、请求队列和 page cache 命中。',
      '',
      '所以项目优化通常先从批量、分区和消息大小入手，而不是把零拷贝当成万能结论。'
    )
  ),
  'kafka/partition-planning':answer(
    '日均十亿消息的分区数要按峰值吞吐、单分区实测能力、消费者并行度和故障恢复时间共同估算，并留增长余量。',
    md(
      '日均十亿约 1.16 万条/秒，但平均值没有用。我会拿峰值，例如 10 万条/秒、单条 1KB，再在目标副本、压缩和磁盘配置下压测单分区生产与消费能力。若单分区稳定处理 5MB/s，而峰值写入 100MB/s，理论至少 20 个，再考虑消费者并行、流量增长和单 Broker 分布，可能从 36 或 48 开始。',
      '',
      '分区不是越多越好。更多分区意味着更多文件、复制连接、Leader 选举、controller 元数据和客户端缓冲，也会延长故障恢复。还要保证每个 Broker 的 Leader 和磁盘分布均匀。',
      '',
      '扩分区容易，缩分区很难，而且默认 key hash 会因分区数变化改变映射，可能破坏同 key 顺序。设计时会为有序 Topic 留足余量，扩容前先清积压或做版本化 Topic 切换。',
      '',
      '最终用峰值生产/消费速率、最慢分区 lag、Broker 资源和故障演练校正，不用“每个消费者一个分区”的单一公式。'
    ),
    {problemAnalysisMarkdown:md(
      '容量估算至少有两个下限：**写入吞吐需要的分区数** 和 **消费 SLA 需要的并行度**。取较大值后再检查 Broker 运维成本与顺序约束。'
    )}
  )
}
