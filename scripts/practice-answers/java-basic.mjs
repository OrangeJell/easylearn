import{answer,md}from'./shared.mjs'

export default{
  'java-basic/string-immutable':answer(
    'String 的不可变不是为了“语法好看”，而是让常量池复用、哈希键和跨线程传递都有稳定语义；大量拼接则交给 StringBuilder。',
    md(
      '项目里我最在意的是 **String 作为值传来传去时不会被别人悄悄改掉**。请求参数、配置项、类名和 Map 的 key 都大量使用字符串；如果内容能原地变化，常量池无法安全复用，放进 `HashMap` 后哈希值也可能变化，多线程共享时还要额外加锁。',
      '',
      '`final` 只表示类不能被继承，真正的不可变来自内部状态不对外暴露，而且 `replace`、`substring`、`concat` 都返回新对象。业务代码因此可以放心缓存 String 的哈希值，也可以直接跨线程传递。',
      '',
      '我不会把“不可变”理解成任何场景都用 `+` 拼接。少量表达式编译器会优化；循环中持续拼接日志或 SQL 时会用 `StringBuilder`，否则会制造很多短命对象，增加 Young GC。'
    )
  ),
  'java-basic/equals-hashcode':answer(
    'HashSet 仍有重复，通常不是集合失效，而是业务去重键没有被 equals 和 hashCode 一致、稳定地表达。',
    md(
      '我会先拿两条“看起来重复”的订单打印真正参与比较的字段和运行时类型。常见情况是 `equals` 比较了订单号，但 `hashCode` 还带着数据库主键；或者用了 Lombok，却把更新时间这类会变化的字段也生成进去了。这样两个对象要么落在不同桶里，要么加入 Set 后哈希值发生变化，后续就再也找不到原来的位置。',
      '',
      '修复时不会为了迁就 Set 随便改整个订单实体。我更倾向于明确业务键，例如：',
      '',
      '```java',
      'record OrderDedupKey(long tenantId, String requestNo) {}',
      '```',
      '',
      '然后用 `Set<OrderDedupKey>` 或 `Map<OrderDedupKey, Order>` 去重。这样“什么叫同一笔订单”写在类型里，也不会受实体其他字段变化影响。',
      '',
      '> 内存去重只能处理当前进程这一批数据。真正防止重复下单，数据库还需要 `(tenant_id, request_no)` 唯一约束，消费重试时按唯一键查询已有结果。'
    ),
    {pitfallsMarkdown:md(
      '- **只重写 `equals`，忘了同步重写 `hashCode`。** 相等对象必须产生相同哈希值。',
      '- **把可变字段放进哈希计算。** 对象加入 Set 后再改状态或时间，会破坏集合定位。',
      '- **只靠 HashSet 保证业务幂等。** 多实例、重启和消息重放都会绕过进程内集合。'
    )}
  ),
  'java-basic/generics':answer(
    '统一返回对象用泛型表达“外壳稳定、载荷变化”，但反序列化边界必须显式保留实际类型，不能把所有数据都退化成 Object。',
    md(
      '我通常把协议外壳和业务数据分开：`RpcResult<T>` 只放错误码、提示、traceId 和 `T data`；分页再用 `PageResult<T>` 表达记录、游标或总数。调用方拿到 `RpcResult<OrderDTO>` 后不需要强转，也不会把分页字段复制到每个接口里。',
      '',
      '真正容易踩坑的是 JSON 或缓存反序列化。Java 运行期只看到 `RpcResult`，看不到里面原本是 `OrderDTO` 还是 `List<OrderDTO>`。跨这个边界时我会传入完整类型：',
      '',
      '```java',
      'new TypeReference<RpcResult<List<OrderDTO>>>() {}',
      '```',
      '',
      '如果框架层把结果先读成 `Map` 再到处强转，泛型设计就只剩表面。我的做法是让 HTTP/RPC 客户端在最靠近网络的位置完成类型恢复，业务层只接触确定类型。',
      '',
      '另外会控制泛型边界：生产者常用 `? extends T`，消费者常用 `? super T`，但公共 API 不会堆太多通配符，让调用方根本读不懂。'
    )
  ),
  'java-basic/exception-handling':answer(
    '异常体系的目标不是把类分得漂亮，而是让调用方知道能否重试、用户看到什么、事务是否回滚、日志由谁记录。',
    md(
      '我会按处理方式分，而不是按技术名词分。**业务异常** 是库存不足、优惠券不可用这类可预期结果，带稳定错误码，通常不打印堆栈；**系统异常** 是空指针、数据库不可用，统一转成内部错误并在边界记录完整现场；**第三方异常** 还要区分超时、限流、明确失败和结果未知，因为只有一部分适合重试。',
      '',
      '落地时，领域层抛带业务语义的异常，接口层统一映射 HTTP/RPC 响应。日志只在“已经决定如何处理”的边界记一次，里面带 `traceId`、订单号、下游、耗时和原始错误码，避免每层 catch 一次、同一故障刷十遍日志。',
      '',
      '重试也不会写成通用注解无脑执行：',
      '',
      '- 参数错误和明确拒绝不重试；',
      '- 超时且操作幂等时，指数退避并限制次数；',
      '- 支付结果未知时先查单，不能直接再次扣款。',
      '',
      '事务边界会显式约定哪些异常回滚。对外返回不暴露 SQL、文件路径等内部信息，但告警和日志必须保留原始 cause。'
    ),
    {problemAnalysisMarkdown:md(
      '设计异常体系时，我会从四个问题反推：**用户能否修正、调用方能否重试、当前事务是否还能提交、值班人员需要什么证据。** 同一个“调用失败”，如果是余额不足、网络超时和第三方受理后无响应，处理动作完全不同，不能只用一个 `RuntimeException`。'
    )}
  ),
  'java-basic/reflection-annotations':answer(
    '反射适合框架扩展点和元数据驱动，不适合把核心业务藏进运行期魔法；出问题时要先定位扫描范围、类来源和具体失败成员。',
    md(
      '我们实际用得最多的是 Spring 依赖注入、ORM 字段映射、Jackson 序列化和自定义审计注解。它们共同的特点是 **启动或首次调用时解析元数据，运行期尽量走缓存后的结果**，而不是每个请求都重新扫描类和查方法。',
      '',
      '如果启动突然变慢，我会用启动阶段耗时、JFR 和框架 condition report 看时间花在类路径扫描、Bean 创建还是代理生成；再检查是不是把扫描包从业务模块误扩大到了整个依赖树。能缩小扫描范围就先缩小，反射得到的 `Method`、字段映射和构造器也应缓存。',
      '',
      '反射调用报错时会保留三层信息：目标对象的实际类、成员完整签名、最里面的 `getCause()`。`NoSuchMethodException` 常见于版本不一致，`IllegalAccessException` 可能是模块开放或可见性问题，`InvocationTargetException` 只是外壳，真正业务异常在 cause 里。',
      '',
      '> 核心支付或订单流程如果只能靠字符串方法名串起来，我会改成显式接口注册。反射应该减少重复胶水，不应该让编译器失去检查能力。'
    )
  ),
  'java-basic/serialization':answer(
    '序列化方案按链路寿命和兼容要求选择：RPC 重类型约束，Kafka 重长期演进，Redis 重版本与可清理，不能一套格式到处复用。',
    md(
      '服务间 RPC 如果双方都是内部系统，我会优先使用有 schema 的 Protobuf；需要人肉排查、跨语言接入频繁时会用 JSON，但字段、时间和金额格式要固定。Kafka 事件会保存 `eventType`、`schemaVersion`、业务主键和发生时间，因为消息可能几个月后还要重放，消费者必须能兼容旧字段。',
      '',
      'Redis 里我不会直接塞 Java 原生序列化对象。它和类名、`serialVersionUID` 绑得太死，还存在安全与跨语言问题。更常见的做法是存紧凑 JSON/Protobuf，并把版本放进 key 或 value；不兼容升级时双读或换 key 前缀，让旧缓存自然过期。',
      '',
      '上线前会做兼容测试：',
      '',
      '- 新消费者读取旧消息；',
      '- 旧消费者忽略新增字段；',
      '- 枚举出现未知值时不直接崩溃；',
      '- 大对象的序列化耗时、消息大小和 Redis 内存都在预算内。',
      '',
      '序列化失败要进死信或隔离队列，不能跳过后继续提交位点，否则一条坏消息会被悄悄丢掉。'
    )
  ),
  'java-basic/interface-abstract-class':answer(
    '支付渠道我会以接口定义能力、用组合编排共性流程，只有确实存在稳定骨架时才放一层很薄的抽象类。',
    md(
      '我不会让十几个渠道都继承一个几千行的 `AbstractPayment`。先定义小接口，例如 `createPayment`、`queryPayment`、`refund`，每个渠道适配自己的签名、字段和错误码；统一流程由一个编排器组合幂等校验、渠道路由、请求落库、调用、结果转换和监控。',
      '',
      '```java',
      'interface PaymentChannel {',
      '  PayResult pay(PayCommand command);',
      '  QueryResult query(String channelOrderNo);',
      '}',
      '```',
      '',
      '组合的好处是重试、签名器、HTTP 客户端、限流器可以单独替换和测试。某几家渠道如果确实共享固定的“组装请求—发送—验签—转换”骨架，可以用抽象类提供模板方法，但渠道差异仍通过受保护的扩展点注入，不能在父类里堆渠道判断。',
      '',
      '接入新渠道的验收不是只跑通成功路径，还会覆盖重复请求、超时后查单、回调乱序、退款部分成功和密钥轮换。设计是否合理，看新增一家渠道要改多少旧代码，而不是用了多少设计模式。'
    ),
    {pitfallsMarkdown:md(
      '- **用一个大接口要求所有渠道实现全部能力。** 不支持分账的渠道只能抛异常，能力边界会越来越模糊。',
      '- **把重试写进渠道实现。** 支付超时可能已经受理，正确动作通常是先查单。',
      '- **父类里出现大量渠道 if/else。** 这说明继承已经没有提供稳定抽象。'
    )}
  ),
  'java-basic/overload-override':answer(
    '重载在编译期按静态类型选方法，重写才在运行期按实际对象分派；SDK 新增重载后，重新编译可能改变原来的绑定结果。',
    md(
      '我会先确认这次升级有没有重新编译调用方。已经编译好的字节码里保存的是目标方法描述符，不会因为 jar 多了一个重载就自动改道；但源码重新编译后，编译器会重新选择“最具体”的重载，`null`、自动装箱和可变参数最容易让结果变化。',
      '',
      '例如原来只有 `send(Object)`，SDK 新增 `send(String)` 后，`send(null)` 重新编译就会绑定到 String 版本。若新增两个互不包含的引用类型，甚至会直接编译歧义。',
      '',
      '排查时我会对比升级前后的 API 签名和调用点静态类型，再用 `javap -c` 看字节码里的 `invokevirtual` 目标，而不是只盯运行时对象。修复通常是让调用意图显式：避免裸 `null`，必要时强转，或者改成不同方法名。',
      '',
      '> 公共 SDK 不应随意增加容易与装箱、可变参数冲突的重载。源码兼容不等于重新编译后的行为兼容。'
    )
  ),
  'java-basic/pass-by-value':answer(
    'Java 永远按值传递；对象参数传递的是引用值的副本，所以能通过它改对象，却不能靠给形参重新赋值来替换调用方变量。',
    md(
      '调用方法时，外面的 `order` 引用被复制了一份给形参。执行 `param.setStatus(...)` 时，两份引用指向同一个订单对象，所以外面能看到字段变化；执行 `param = new OrderDTO()` 只是让方法里的那份副本改指向，新对象和外部变量没有关系。',
      '',
      '```java',
      'void update(OrderDTO param) {',
      '  param.setStatus("PAID"); // 外部能看到',
      '  param = new OrderDTO();   // 只改变局部形参',
      '}',
      '```',
      '',
      '项目里真正要解决的是副作用边界。如果方法只负责计算，我会返回新 DTO 或不可变结果，不直接修改传入对象；如果约定就是填充字段，方法名和文档会明确写出来，避免调用方以为输入不会变化。'
    )
  ),
  'java-basic/autoboxing-integer-cache':answer(
    '用户 ID 偶发比较错误，第一反应是包装类型用了 ==：它比较对象身份，小值缓存让问题在测试数据上被掩盖。',
    md(
      '我会搜索这条链路里所有 `Long`、`Integer` 的 `==` 和 `!=`。包装类型之间这样比较的是引用，不是数值；`Integer` 常见的 `-128~127` 会复用缓存对象，所以小测试账号看起来正常，换成真实的大用户 ID 就失败。',
      '',
      '业务比较统一写成：',
      '',
      '```java',
      'Objects.equals(requestUserId, order.getUserId())',
      '```',
      '',
      '如果字段业务上不允许为空，我会在进入领域逻辑前校验并拆箱成 `long`，后面用基本类型比较。这样还避免了 `Long` 和 `long` 自动拆箱时因为 null 抛 `NullPointerException`。',
      '',
      '修复后会补三组用例：缓存范围内、缓存范围外和 null。仅把当前一行改成 `.equals` 不够，DTO 映射、缓存反序列化和数据库 nullable 定义也要一起确认。'
    )
  )
}
