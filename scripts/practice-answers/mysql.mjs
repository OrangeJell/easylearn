import{answer,md}from'./shared.mjs'

export default{
  'mysql/btree-index':answer(
    '我不会因为表到千万级就直接分库分表。先确认扫描、排序和分页成本，再让联合索引贴合用户按时间查订单的访问路径。',
    md(
      '建索引前，我会从慢日志和调用链拿到真实 SQL、典型参数和执行计划。**千万级只是现象，访问路径才决定这个接口快不快。** 先确认慢的是所有用户，还是订单特别多的少数用户；时间究竟耗在扫描、排序、回表、深分页，还是连接池等待。',
      '',
      '假设接口长期稳定地按租户、用户和时间范围过滤，再按时间倒序展示，我会建立下面这条联合索引：',
      '',
      '```sql',
      'CREATE INDEX idx_orders_tenant_user_time',
      'ON orders (tenant_id, user_id, created_at DESC, id DESC);',
      '```',
      '',
      '`tenant_id`、`user_id` 负责等值定位，`created_at` 接住时间范围和倒序读取，`id` 解决同一时间多笔订单时翻页不稳定的问题。列表只取二十条，少量回表通常可以接受；我不会为了追求覆盖索引，把金额、状态等字段全部塞进去。',
      '',
      '如果接口还在使用 `LIMIT 100000, 20`，仅仅加索引只解决了一半。我会改成基于上一页末尾记录的游标翻页：',
      '',
      '```sql',
      'AND (created_at < ?',
      '  OR (created_at = ? AND id < ?))',
      'ORDER BY created_at DESC, id DESC',
      'LIMIT 21',
      '```',
      '',
      '上线前不会只看 `EXPLAIN` 里是否出现目标索引，而会用普通用户、超级用户和大时间区间分别验证扫描行数、接口 P99、数据库 IO 和建索引期间的写入延迟。',
      '',
      '> 如果单个用户几年的历史订单仍然很多，我会再做按时间归档或冷热分离。**总表一千万行本身，不是立刻分库分表的理由。**'
    ),
    {problemAnalysisMarkdown:md(
      '这道题真正考察的不是能不能背出 B+Tree，而是能不能从线上接口反推出完整访问路径。**“上千万行”只是背景，不是根因。**',
      '',
      '- `tenant_id`、`user_id` 是等值条件，先缩到单个用户；',
      '- `created_at` 承担时间过滤和倒序读取；',
      '- `id` 提供稳定排序，避免翻页重复或漏单。',
      '',
      '`status` 是否进入联合索引要看它是否必传。可选状态夹在 `user_id` 与 `created_at` 中间，反而会破坏不带状态查询的范围与排序能力。'
    ),pitfallsMarkdown:md(
      '- **看到千万级就分库分表。** 正确索引通常仍能很好支撑。',
      '- **把 WHERE 每列都建单列索引。** 完整访问路径比字段清单重要。',
      '- **制造超宽覆盖索引。** 会占缓存、磁盘并放大订单写入。',
      '- **只测第一页。** 深分页、超级用户和大时间范围才是风险点。'
    )}
  ),
  'mysql/transactions-mvcc':answer(
    '订单查询通常用 MVCC 做非阻塞快照读，状态更新用条件更新或显式锁保证并发正确；隔离级别按业务一致性选择。',
    md(
      '大多数订单系统我会从 `READ COMMITTED` 或 `REPEATABLE READ` 中选。列表查询接受同一事务内再次查询看到新提交数据时，RC 更直观；对账或一次事务内需要稳定快照时用 RR。选择不是背默认值，而是把“同一请求能否看到别人刚提交的状态”写清楚。',
      '',
      'MVCC 让普通 `SELECT` 通过 Read View 和版本链读取快照，不和更新行直接互斥，但它不会自动防止两个请求同时把 `PAID` 改成不同状态。更新会写成带前置状态或版本的条件语句：',
      '',
      '```sql',
      'UPDATE orders',
      'SET status = ?, version = version + 1',
      'WHERE id = ? AND status = ? AND version = ?;',
      '```',
      '',
      '受影响行为 0 就说明状态已变化，业务决定返回幂等成功、冲突还是重试。确实需要“读后立刻改且中间不能变”时才用 `SELECT ... FOR UPDATE`，并把事务保持很短。',
      '',
      '还会关注长事务：它会让旧版本无法清理，Undo 增长并拖慢查询。隔离级别不能替代正确的状态机和幂等。'
    ),
    {pitfallsMarkdown:md(
      '- **认为 MVCC 能解决丢更新。** 快照读不等于写入冲突检测。',
      '- **所有查询都加 FOR UPDATE。** 会把本可并发的订单查询串行化。',
      '- **事务里调用远程服务。** 锁和旧版本会被无谓地长时间保留。'
    )}
  ),
  'mysql/sql-execution-explain':answer(
    '昨天快今天慢，要比较真实参数和实际执行计划，找到估算偏差、扫描放大、排序或等待，而不是看到某个 key 就结束。',
    md(
      '我会把昨天和今天的 SQL 指纹、绑定参数、返回行数、表数据量、索引和统计信息放在一起。很多“同一条 SQL”其实是今天来了一个超级租户或查了一整年，数据分布已经不同。',
      '',
      '在只读副本或可控环境执行 `EXPLAIN ANALYZE`，沿算子看 `actual rows`、`loops` 和耗时：预计 10 行实际 100 万行，说明统计信息或列相关性有偏差；嵌套循环 loops 被放大，通常是连接顺序或驱动集有问题；扫描不多但耗时长，要转去看锁、IO 或连接等待，因为这些不一定完整体现在计划里。',
      '',
      '重点会核对：',
      '',
      '- 访问范围和返回行数比例；',
      '- 是否大量回表、临时表或额外排序；',
      '- 统计信息更新时间和直方图是否适合倾斜列；',
      '- 当时 buffer pool、磁盘和锁等待是否变化。',
      '',
      '修复可能是更新统计、调整联合索引、改写 SQL 或限制查询范围。`FORCE INDEX` 只会作为短期止损并经过多组参数验证，不会当永久答案。'
    )
  ),
  'mysql/locks-deadlock':answer(
    '支付回调与退款死锁，先从 InnoDB 死锁日志还原双方持锁与等待顺序，再统一访问顺序、缩短事务并加有限重试。',
    md(
      '我会保存 `SHOW ENGINE INNODB STATUS` 的 latest deadlock 或开启死锁日志，拿到两边 SQL、事务持有的锁、等待的索引记录和被回滚的一方。再把 SQL 映射回支付回调与退款代码，确认是否一个先更订单再更支付单，另一个顺序相反。',
      '',
      '修复通常是所有路径按同一顺序访问资源，例如都先锁订单、再锁支付记录；查询条件补正确索引，避免锁住比预期更多的行或 gap；远程调用、消息发送移出数据库事务。',
      '',
      '应用层会捕获死锁错误做 **有限次数、带抖动的整体事务重试**，因为被回滚事务之前的所有 SQL 都已失效。业务请求号和状态条件保证重试幂等。',
      '',
      '上线后观察死锁次数、事务时长、锁等待和受影响订单，并并发回放“回调 + 退款”相反时序。只把隔离级别调低，未必能解决记录锁顺序问题。'
    ),
    {problemAnalysisMarkdown:md(
      '死锁不是“数据库偶尔抽风”，而是多个事务形成等待环。数据库主动回滚一方是保护机制；真正要修的是 **资源访问顺序、锁范围和事务长度**。'
    )}
  ),
  'mysql/redo-undo-binlog':answer(
    'redo 保证已提交页修改能崩溃恢复，undo 支持回滚和 MVCC，binlog 用于复制与时间点恢复；生产配置要围绕可接受的数据损失来定。',
    md(
      '数据库宕机后，内存里还没刷回数据页的已提交修改靠 redo 重放；未提交事务则利用 undo 回滚。普通快照读也通过 undo 版本链看到历史版本。binlog 位于 Server 层，记录逻辑变更，副本和备份系统据此复制或做 PITR。',
      '',
      '提交时 InnoDB 通过 redo 与 binlog 的两阶段提交保持两者一致，避免主库恢复后有数据、binlog 却缺事件，或者反过来。',
      '',
      '核心订单库通常会选择：',
      '',
      '```ini',
      'innodb_flush_log_at_trx_commit = 1',
      'sync_binlog = 1',
      'binlog_format = ROW',
      '```',
      '',
      '这意味着每次提交更强调持久性，也要接受 fsync 成本。若业务允许极小窗口丢失才会评估放宽，而且要用故障演练量化，不会为了跑分快直接改成 0。',
      '',
      'binlog 不是备份替代品：仍需全量备份、binlog 保留和定期恢复演练，确认真的能恢复到指定时间。'
    )
  ),
  'mysql/replication-high-availability':answer(
    '读不到刚提交的数据先确认是不是被路由到延迟副本；关键读采用主库粘滞或位点等待，后台再治理复制延迟。',
    md(
      '我会从 trace 看写入返回时间、读请求路由实例和副本 replay 位点，确认不是缓存旧值或事务未提交。若确实是主从延迟，用户刚完成支付后查看订单这种 read-your-write 场景，会在短时间内固定读主库，或者把提交位点带给读侧，等待副本追到该 GTID 后再读，超时则回退主库。',
      '',
      '```text',
      '关键状态：写后读主库 / 等位点',
      '普通列表：允许读副本，展示“更新中”',
      '离线报表：接受更大延迟',
      '```',
      '',
      '根因治理会看 relay log、SQL 线程、复制 worker、磁盘和网络。大事务、无索引更新、DDL、热点单线程依赖都会造成延迟；处理方式可能是拆小事务、补索引、开启合理并行复制或隔离报表负载。',
      '',
      '我不会全站切主库长期止痛，那会让主库读压力失控。按业务一致性分级，才能同时守住正确性和容量。'
    )
  ),
  'mysql/slow-sql-troubleshooting':answer(
    '连接池被慢 SQL 打满时先限流和终止失控查询恢复服务，同时保留参数、计划和锁现场，再针对真正瓶颈优化。',
    md(
      '先从链路拆出连接池等待和数据库执行时间。如果大量请求只是等连接，继续扩应用实例会制造更多数据库连接。止损会限制问题接口并发、暂停报表或批任务、缩小查询时间范围；对确认安全且失控的只读查询执行 `KILL QUERY`，必要时把非核心读切到有余量的副本。',
      '',
      '现场至少保留 SQL 指纹、真实参数、调用量、`Rows_examined/Rows_sent`、执行计划、锁等待、活跃事务和资源曲线。扫描千万行返回十行，重点是访问路径；扫描不多但 `Lock_time` 高，要查长事务；单条 50ms 但每秒几万次，也可能是总负载主因。',
      '',
      '优化可能落在联合索引、游标分页、拆批、减少返回列、事务范围或数据归档。变更在真实数据分布上回放，并比较接口 P99、吞吐、扫描行数、CPU/IO 和写入成本。',
      '',
      '> 把连接池调大通常只是让更多请求同时压向已经饱和的数据库，不是第一修复动作。'
    ),
    {pitfallsMarkdown:md(
      '- **线上直接跑高成本 EXPLAIN ANALYZE。** 它会真实执行，应先评估或在副本验证。',
      '- **只优化最慢的一条。** 高频中慢 SQL 可能消耗更多总资源。',
      '- **只看 SQL，不看锁和连接等待。** 数据库执行快也可能接口等待很久。'
    )}
  ),
  'mysql/table-design-guide':answer(
    '日增千万订单明细要从查询、写入和生命周期反推字段与索引；主键、唯一约束、在线索引和归档策略要一起设计。',
    md(
      '我会先列出 Top 查询：用户查订单、商家按状态处理、按订单号点查、财务按时间导出。字段使用明确类型，金额存整数分，状态用小整数，时间用毫秒精度；大文本和低频扩展信息拆到详情表，避免主表过宽。',
      '',
      '主键选择趋势递增的全局 ID，减少随机写导致的页分裂；业务订单号另建唯一约束。索引只围绕真实访问路径，例如：',
      '',
      '```sql',
      'PRIMARY KEY (id),',
      'UNIQUE KEY uk_tenant_order_no (tenant_id, order_no),',
      'KEY idx_user_time (tenant_id, user_id, created_at, id),',
      'KEY idx_status_time (tenant_id, status, created_at, id)',
      '```',
      '',
      '日增千万意味着一年数十亿，建表当天就要定义在线保留期。比如主库保留 3~6 个月，按主键/时间小批归档到历史库或分析系统，校验数量和摘要后再删除，整个过程控制 Undo、binlog 和复制延迟。',
      '',
      '分区可以帮助生命周期管理，但不会自动让坏 SQL 变快。只有单实例在容量、备份恢复或写吞吐上接近上限，才进入分库分表设计。'
    ),
    {problemAnalysisMarkdown:md(
      '表设计不是字段清单。需要同时回答：**最常用查询怎样定位、每次写入要维护多少索引、历史数据何时离开在线库、唯一性由谁保证。** 日增量只是让错误设计更快暴露。'
    )}
  ),
  'mysql/deep-pagination':answer(
    '几百万订单导出不应由一个同步 HTTP 请求用 OFFSET 翻完，而应改成游标扫描的异步任务，分批写对象存储。',
    md(
      '`LIMIT 1000000, 1000` 需要扫描并丢弃前面一百万行，越往后越慢。我会选择稳定排序键，用上一批最后的时间和 ID 继续：',
      '',
      '```sql',
      'WHERE tenant_id = ?',
      '  AND (created_at < ?',
      '       OR (created_at = ? AND id < ?))',
      'ORDER BY created_at DESC, id DESC',
      'LIMIT 2000;',
      '```',
      '',
      '导出请求只创建任务并返回 taskId。后台 worker 控制并发，批量读取后流式写 CSV/Parquet 到对象存储，记录游标和进度，失败从检查点恢复；完成后发通知并提供有时效的下载地址。',
      '',
      '一致性要和产品确认。要求导出某一时刻快照时，可以先记录截止时间或版本边界，避免任务运行两小时期间新订单不断混入；不建议持有一个两小时数据库事务。',
      '',
      '整个链路要限制单租户并发、批大小和下载权限，并监控扫描速率、复制延迟、任务积压和失败重试。'
    ),
    {pitfallsMarkdown:md(
      '- **把 OFFSET 改大批次。** 页越深仍然越慢。',
      '- **一次查完放进内存。** 应边读边写并有背压。',
      '- **长事务保持快照。** 会阻碍 Undo 清理并放大主库压力。'
    )}
  ),
  'mysql/sharding':answer(
    '分库分表是单实例容量与运维边界逼近后的选择，不是行数阈值；决定方案的是主访问路径、扩容方式和跨片需求。',
    md(
      '我会先证明单库已接近边界：索引和归档做完后，写入、存储、备份恢复时间或热点仍无法满足未来 6~12 个月增长。几亿行如果按主键点查仍可能很快；反过来，几千万行但写热点和大事务严重，也可能需要拆。',
      '',
      '订单用户侧查询占主流时，可以按 `user_id` 映射到虚拟槽，再路由到物理分片。这样点查单片，但商家按店铺和时间的查询会跨片，所以要通过订单事件构建商家维度的异构索引或查询库，不能在接口里广播所有分片。',
      '',
      '扩容会预先设计：',
      '',
      '- 全局 ID 不依赖单库自增；',
      '- 虚拟槽减少迁移范围；',
      '- 迁移期间按版本双写或订阅 binlog，并持续核对数量与校验和；',
      '- 切读、回滚和补偿都有明确开关。',
      '',
      '跨片事务尽量转成单片本地事务加事件驱动；报表和全局搜索走专门系统。分片后复杂度是永久成本，所以收益必须能被容量数据证明。'
    ),
    {pitfallsMarkdown:md(
      '- **只按当前最方便字段分片。** 要回放用户、商家、客服和财务全部 Top 查询。',
      '- **依赖跨库分页和排序。** 分片数增长后成本线性放大。',
      '- **没有扩容与回滚设计就上线。** 第一次迁移时才补会非常危险。'
    )}
  )
}
