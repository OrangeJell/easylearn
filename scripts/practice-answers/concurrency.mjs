import{answer,md}from'./shared.mjs'

export default{
  'concurrency/thread-pool':answer(
    '核心接口的线程池要围绕下游容量和超时预算设计：隔离、有限并发、有界队列、明确拒绝，并让每个阶段都可观测。',
    md(
      '我不会直接套“CPU 核数乘二”。先画出这些异步任务调用哪些资源：商品和库存可能走不同 RPC，推荐可能更慢且允许降级。不同故障域会拆独立线程池，避免推荐超时把库存查询的线程也占满。',
      '',
      '线程数从下游允许并发和本服务耗时估算，再用压测修正；队列一定有界。核心任务队列满时可以让调用线程执行形成背压，非核心任务则快速失败或降级，不能继续堆积。每个 Future 都继承总请求 deadline，单任务超时后取消，接口结束后不让后台任务继续消耗资源。',
      '',
      '上线会盯这些信号：',
      '',
      '- active 线程、队列长度和最老任务等待时间；',
      '- 提交量、完成量、拒绝量和任务耗时分位数；',
      '- 下游超时率，以及主接口还有多少剩余时间预算。',
      '',
      '> 线程池只能管理本机排队，不能提高下游容量。即使线程还能开，也会用信号量或限流保护数据库和 RPC 服务。'
    ),
    {pitfallsMarkdown:md(
      '- **多个核心链路共用一个大池。** 一个慢下游会拖住所有任务。',
      '- **使用无界队列。** 告警出现时任务可能已经排了几分钟。',
      '- **只配置任务超时，不配置请求总 deadline。** 多层重试后总耗时仍会失控。'
    )}
  ),
  'concurrency/volatile-happens-before':answer(
    '如果线程只是在循环中读取停止标志，volatile 能解决可见性；但任务阻塞、复合状态更新或需要清理资源时，它不够。',
    md(
      '普通 `boolean` 存在数据竞争。工作线程可能一直读取寄存器或缓存里的旧值，写线程改成 false 也没有 happens-before 关系。把标志声明成 `volatile` 后，写入对后续读取可见，简单轮询可以停下来。',
      '',
      '```java',
      'while (running) {',
      '  doOneShortTask();',
      '}',
      '```',
      '',
      '但我会继续看循环里做什么。如果卡在 `queue.take()`、网络读取或 sleep，线程根本没有机会重新读标志，这时要配合 `interrupt()`，并让阻塞 API 正确响应中断。退出前还要在 `finally` 里提交位点、关闭文件或释放锁。',
      '',
      '`volatile` 也不能让 `count++` 原子化，不能维护多个字段的一致状态。涉及“停止中、已停止、失败”这类状态机会用锁、CAS 或更明确的生命周期组件。'
    )
  ),
  'concurrency/locks-aqs':answer(
    '库存扣减优先让数据库或库存服务做条件更新；JVM 锁只对单实例有效，选 synchronized 还是 ReentrantLock 反而是第二层问题。',
    md(
      '如果多台应用同时扣同一个商品，本机锁挡不住其他实例。我会把正确性落在库存存储上，例如：',
      '',
      '```sql',
      'UPDATE sku_stock',
      'SET available = available - 1',
      'WHERE sku_id = ? AND available > 0;',
      '```',
      '',
      '根据受影响行数判断是否成功，并用业务请求号做幂等。热点很高时再通过分桶、排队或 Redis 预扣降低数据库竞争。',
      '',
      '单进程内如果确实要保护共享对象，临界区短且只需要互斥，我会用 `synchronized`，语义简单、释放锁不容易写错；需要可中断、超时尝试或多个 Condition 时才用 `ReentrantLock`，并严格在 `finally` 解锁。',
      '',
      '锁粒度通常按 `skuId` 做固定数量的条带，而不是每个商品永久创建一把锁，也不是锁整个扣库存方法。最终用热点商品压测观察等待时间、失败率和吞吐，不只验证库存没有负数。'
    ),
    {problemAnalysisMarkdown:md(
      '这题容易一开始就在两种 Java 锁之间选边，但实际约束是 **库存状态在哪里、是否多实例、失败是否会重试**。锁只解决互斥，幂等、持久化和跨进程正确性必须另行处理。'
    )}
  ),
  'concurrency/thread-lifecycle-communication':answer(
    'CPU 不高但请求卡住，多半不是算不动，而是在等锁、条件、连接或下游；线程快照要结合锁拥有者和请求链路看。',
    md(
      '我会连续抓三份线程快照，间隔几秒，先按线程状态和相同堆栈聚类。大量 `BLOCKED` 时找到它们等待的 monitor，再沿 `locked` 信息定位持锁线程；大量 `WAITING/TIMED_WAITING` 则区分正常的线程池空闲、`CountDownLatch` 未完成、连接池取连接和 RPC Future 等待。',
      '',
      '如果几百个请求都卡在拿数据库连接，根因可能是少数慢 SQL 或连接泄漏，不是线程问题；如果都等同一把业务锁，就看持锁线程是不是在锁内做远程调用。线程名、traceId、APM span、连接池和数据库活动会放在同一时间线上。',
      '',
      '止损会根据阻塞点处理：限流新请求、降级非核心下游、终止明确失控的查询，必要时摘除并重启问题实例。保留现场后再修复锁范围、超时和资源池边界。',
      '',
      '> 单张线程快照只能说明一个瞬间。连续快照里同一批线程和同一持锁者都不动，才更像真正卡死。'
    )
  ),
  'concurrency/cas-aba':answer(
    'ABA 的风险是值看起来回到原样，但中间发生过业务变化；库存和订单状态通常用版本号或单调状态规则解决。',
    md(
      '比如订单状态从 `INIT` 变成 `CANCELLED`，又因补偿被写回 `INIT`。另一个线程只比较当前值，会以为从未变化，继续执行一段基于旧前提的逻辑。内存结构可以用 `AtomicStampedReference`，把值和版本一起 CAS。',
      '',
      '数据库里更常见的是乐观锁：',
      '',
      '```sql',
      'UPDATE orders',
      'SET status = ?, version = version + 1',
      'WHERE id = ? AND version = ?;',
      '```',
      '',
      '状态机如果本身只能单向流转，也会把合法前置状态写进 WHERE，拒绝倒退。消息消费再用事件 ID 或业务请求号幂等，不能只依赖状态值。',
      '',
      '简单库存数字从 10 变 9 再回 10 是否有风险，要看业务是否关心中间过程；不关心就没必要为了术语增加版本。先找被破坏的不变量，再决定是否需要防 ABA。'
    )
  ),
  'concurrency/threadlocal':answer(
    'ThreadLocal 适合在同一线程调用链传递少量上下文，但线程池复用前必须清理，异步跨线程时还要显式传播。',
    md(
      'Web 请求进入时可以放用户 ID 或 traceId，最外层过滤器必须用 `try/finally`：',
      '',
      '```java',
      'try {',
      '  RequestContext.set(context);',
      '  chain.doFilter(request, response);',
      '} finally {',
      '  RequestContext.clear();',
      '}',
      '```',
      '',
      '线程池线程会服务下一位用户，不 remove 就可能串号；value 还可能被线程长期引用，形成内存泄漏。上下文里只放标识，不放大 DTO、连接或可变集合。',
      '',
      '提交到另一个线程池后，原 ThreadLocal 不会自动过去。我会用任务装饰器在提交时复制必要字段、执行前安装、结束后恢复和清理；链路追踪优先采用 OpenTelemetry/MDC 已有的上下文机制。',
      '',
      '使用 `InheritableThreadLocal` 也不是通用修复，池中线程早已创建，而且复用时仍有污染问题。'
    ),
    {pitfallsMarkdown:md(
      '- **只调用 set，不在 finally remove。** 异常路径最容易留下脏上下文。',
      '- **把 ThreadLocal 当跨线程消息总线。** 异步、响应式和虚拟线程边界都需要重新审视。',
      '- **保存重量对象。** 线程生命周期通常比请求长得多。'
    )}
  ),
  'concurrency/completable-future':answer(
    '首页聚合要并行的是相互独立的 I/O，核心做法是独立执行器、统一 deadline、局部降级和明确的结果组合。',
    md(
      '商品、库存、营销和推荐之间没有依赖时可以并行，但我不会用默认 `commonPool`。这些都是阻塞 RPC，会放到专用有界线程池，并按下游分别限制并发。请求先算出总 deadline，各子调用拿剩余时间，不能每个都再等完整的两秒。',
      '',
      '```java',
      'var product = supplyAsync(this::loadProduct, productPool);',
      'var stock = supplyAsync(this::loadStock, stockPool);',
      'var promo = supplyAsync(this::loadPromo, promoPool)',
      '    .completeOnTimeout(Promo.empty(), 120, MILLISECONDS);',
      '```',
      '',
      '商品和库存失败可能直接失败，推荐和营销可以返回空结果。组合时用 `thenCombine` 表达依赖，不在异步任务内部反复 `join` 把线程重新阻塞。接口结束或超时后取消还没必要执行的任务，并把 trace 上下文传进去。',
      '',
      '压测会比较串行与并行后的 P95/P99、线程池队列、下游并发和超时率。并行能降低等待总时长，也会瞬间放大四倍下游流量，所以必须和限流一起设计。'
    )
  ),
  'concurrency/deadlock':answer(
    '怀疑死锁时先用线程快照确认锁等待环，止损恢复流量，再通过统一加锁顺序和缩小临界区根治。',
    md(
      '我会连续执行 `jcmd <pid> Thread.print -l` 或抓取线程转储。JVM 如果检测到 Java monitor 死锁，会直接列出参与线程、等待的锁和持有者；否则也会沿大量 `BLOCKED` 线程手工追锁。把线程名和 trace 对上，确认是业务锁、类初始化锁还是第三方库。',
      '',
      '线上请求已经堆积时，先停止向问题实例分流，限制重试风暴；Java 内部锁通常无法安全地从外部释放，保存现场后重启实例往往比继续等待更可靠。',
      '',
      '修复会把资源排序后按同一顺序加锁，例如总是先锁较小的账户 ID；移出锁内 RPC、数据库和日志 IO，减少嵌套锁。确实无法固定顺序时，用 `tryLock(timeout)` 失败后释放已持有资源并重试，但重试次数必须有限。',
      '',
      '回归测试会并发运行两条相反业务路径，持续检查线程状态和超时，而不是只写一个顺序执行的单测。'
    ),
    {pitfallsMarkdown:md(
      '- **看到很多 WAITING 就判死锁。** 线程池空闲、队列等待本来就是 WAITING。',
      '- **只给锁加超时。** 如果失败后不释放前面拿到的资源，仍然可能形成活锁或数据错误。',
      '- **重启后不保留线程转储。** 现场消失后很难确认真正的锁环。'
    )}
  ),
  'concurrency/java-memory-model':answer(
    '线上偶尔读到旧值，说明读写之间没有 happens-before；单测时序稳定只是把数据竞争藏住了。',
    md(
      'CPU 缓存、编译器优化和指令重排都允许一个线程暂时看不到另一个线程的普通写入。问题不在“主内存多久同步一次”，而在代码没有建立可见性和顺序保证。',
      '',
      '我会找到共享字段的所有读写，判断它是单个状态还是一组不变量。单个停止标志可以用 `volatile`；计数更新用原子类；多个字段必须一起变化时用锁或发布不可变对象。对象构造完成后通过 `final` 字段、volatile 引用、锁或并发容器安全发布。',
      '',
      '例如双重检查单例缺少 volatile 时，引用可能先于构造完成被看到：',
      '',
      '```java',
      'private static volatile Client instance;',
      '```',
      '',
      '修复后会用 jcstress 或高并发循环放大竞态，并用代码审查确认所有访问都经过同一同步协议。加 sleep 让用例“稳定”并没有建立 happens-before。'
    )
  ),
  'concurrency/virtual-threads':answer(
    '我会先试点线程长期阻塞在网络或数据库上的接口；虚拟线程能减少线程等待成本，但数据库、下游和 CPU 仍必须限流。',
    md(
      '首批会选代码以同步阻塞为主、并发高、单请求 CPU 很少的查询接口，例如聚合多个 HTTP/RPC 的页面。纯计算、长时间持锁和已经成熟的响应式链路不急着迁。',
      '',
      '迁移前记录平台线程数、队列等待、吞吐、P99 和下游并发。改成每请求虚拟线程后，代码可以保留同步写法，但会重点排查 `synchronized` 中执行阻塞调用、native 调用等 pinning；用 JFR 看固定载体线程是否被占住。',
      '',
      '并发限制仍然存在：连接池只有 100 个连接，就用信号量或池本身把数据库并发限制在 100 左右；不能因为可以创建十万个虚拟线程，就同时打十万个请求给下游。ThreadLocal 的数量和内容也要检查，因为每个虚拟线程一份上下文仍会占内存。',
      '',
      '我会灰度一个低风险接口，对比资源和延迟后再扩大，而不是一次把所有 executor 删除。虚拟线程解决的是等待线程成本，不解决容量规划。'
    ),
    {problemAnalysisMarkdown:md(
      '判断是否适合虚拟线程，我看的是 **阻塞等待占比**，不是接口名字。等待越多、调用栈越适合同步写法，收益越明显；CPU 已经跑满的服务换线程模型不会凭空增加算力。'
    )}
  )
}
