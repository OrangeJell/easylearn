import{answer,md}from'./shared.mjs'

export default{
  'architecture/cache-consistency':answer(
    '商品详情以数据库为权威，缓存承担读性能；更新通过事务后失效和可靠事件收敛，结算等关键链路再次校验权威值。',
    md(
      '读链路会做多级缓存：热点基础信息可放短 TTL 本地缓存，完整详情放 Redis，miss 后查询数据库并通过单飞加载限制同 key 回源并发。缓存 value 带商品版本，TTL 加抖动，避免批量同刻失效。',
      '',
      '写链路采用 cache-aside：数据库事务提交后发布 outbox/binlog 事件，消费者删除或刷新 Redis，再通知本地缓存失效。删除失败会重试并告警，定期对账版本。不会在数据库事务里同步调用缓存，把缓存故障扩大成写事务失败。',
      '',
      '并发读写仍可能把旧值回填，所以回填使用读取到的版本，发现缓存已有更高版本就拒绝覆盖；或者更新后做一次延迟失效收敛竞态。',
      '',
      '降级边界按字段定：商品描述可以短暂旧，价格展示允许几秒窗口，但创建订单和结算必须向价格/库存权威服务再次确认。这样即使缓存短暂不一致，也不会造成实际错价成交。'
    ),
    {problemAnalysisMarkdown:md(
      '设计一致性时先回答三个问题：**谁是权威源、允许多长旧值窗口、哪一步必须强校验。** 没有这三个边界，所谓“双删、延迟队列、订阅 binlog”只是方案清单。'
    ),pitfallsMarkdown:md(
      '- **数据库和缓存同步双写。** 任一边失败都会留下不一致。',
      '- **缓存故障时所有请求回源。** 必须有限流、旧值或降级。',
      '- **展示缓存直接作为结算价格。** 关键交易动作要重新校验。'
    )}
  ),
  'architecture/idempotency':answer(
    '幂等要让同一个业务意图有稳定键，并把“占用幂等键”和业务写入放进同一事务，重复请求返回第一次结果。',
    md(
      '订单创建会要求客户端生成 `requestId`，服务端唯一约束 `(tenant_id, request_id)`。第一次请求创建订单并保存响应摘要；超时重试命中同一键时查询原订单返回，而不是再创建一笔。只在 Redis SETNX 挡一下不够，key 过期或故障后仍可能重复。',
      '',
      '支付回调则优先使用渠道流水号作为幂等键，在数据库事务中插入回调记录并做订单状态条件更新：',
      '',
      '```sql',
      'UPDATE orders',
      'SET status = "PAID"',
      'WHERE id = ? AND status = "PENDING";',
      '```',
      '',
      '重复 PAID 回调返回成功；若订单已取消，则进入人工/补偿规则，不能简单当幂等成功吞掉。幂等还要校验同一 key 的请求参数摘要，防止客户端误用同一个 requestId 提交不同金额。',
      '',
      '记录会按业务最长重试窗口保留，指标包含重复率、参数冲突和处理中的超时。幂等不是“所有重复都忽略”，而是相同意图得到相同可解释结果。'
    )
  ),
  'architecture/flash-sale-system-design':answer(
    '秒杀的核心是入口削峰、资格校验、库存原子预扣和异步落单，并让每一步可限流、幂等和补偿。',
    md(
      '十万人抢一千件时，我不会让十万请求直达数据库。CDN 承担静态资源，网关校验活动令牌、用户与设备限流；进入秒杀服务后先做活动时间、用户资格和一人一单校验。',
      '',
      '库存提前加载到 Redis，Lua 在同一 slot 内原子检查购买标记并扣减，成功才写入有界消息队列：',
      '',
      '```text',
      '请求 -> 资格/限流 -> Redis 原子预扣',
      '                    -> Kafka -> 创建订单 -> 通知结果',
      '```',
      '',
      '消费者按请求 ID 幂等创建订单，数据库对 `(activity_id, user_id)` 建唯一约束。落单失败通过补偿事件归还 Redis 库存；支付超时由延时任务关闭订单并释放最终库存。客户端查询排队结果，不同步等待整条链路。',
      '',
      '容量以峰值而非平均估算，队列长度和 Redis 并发有硬上限。压测会模拟热点 key、消费者停机、重复消息和 Redis 主从切换，并对 Redis 预扣数、消息成功数、订单数、支付数做持续对账。'
    ),
    {pitfallsMarkdown:md(
      '- **入口只做验证码，不做容量限流。** 请求最终仍会压到库存层。',
      '- **Redis 扣减成功就算下单成功。** 消息与数据库失败需要补偿和对账。',
      '- **所有用户轮询数据库查结果。** 应查轻量状态或通过推送通知。'
    )}
  ),
  'architecture/config-distribution-gray-release':answer(
    '配置中心需要持久化版本、推拉结合的秒级通知、客户端本地快照，以及按实例/标签灰度和一键回滚。',
    md(
      '控制面保存 namespace、key、版本、内容摘要、发布人和审计记录。发布不是直接覆盖：先创建不可变版本，完成格式、范围和依赖校验，再把“某环境当前指向哪个版本”原子切换。',
      '',
      '服务端通过长轮询或流式连接推送版本通知，客户端收到后再拉取内容、校验摘要，完整构建新配置后原子替换本地快照。连接断开时按退避重连并定期拉取兜底；配置中心不可用时继续使用最近一次成功快照，而不是清空配置。',
      '',
      '灰度按应用、机房、实例标签或用户桶建立发布规则：先 1%，观察错误率、延迟和业务指标，再逐步放量。回滚只需把指针切回旧版本并再次通知，不能依赖人工重新编辑旧内容。',
      '',
      '需要防止推送风暴：通知只携带版本，客户端随机抖动拉取；大规模发布分批执行。整个链路记录每个实例当前版本，能回答“哪些机器还没生效”。'
    ),
    {problemAnalysisMarkdown:md(
      '配置中心不是一个带通知的数据库。真正难点是 **发布版本化、客户端断网语义、全量推送风暴、灰度选择和回滚可证明**。'
    )}
  ),
  'architecture/distributed-transaction':answer(
    '下单跨库存、优惠券和支付时，我会用本地事务加可靠事件/Saga，让每个服务幂等执行并提供补偿，不追求长时间全局锁。',
    md(
      '订单服务先创建 `PENDING` 订单和 outbox 事件并同事务提交。库存服务收到事件后按订单 ID 幂等预留库存，优惠券服务预占券；每一步成功/失败都发结果事件，订单状态机汇总后决定进入待支付或取消。',
      '',
      '```text',
      'PENDING -> STOCK_RESERVED -> COUPON_RESERVED -> WAIT_PAY',
      '    \-> FAILED -> release stock / release coupon -> CANCELLED',
      '```',
      '',
      '补偿不是数据库 rollback。库存释放、优惠券返还都可能失败，所以也要幂等、重试、死信和人工处置。支付属于外部系统，超时后先查单；已扣款但订单取消要进入退款流程，不能简单反向调用。',
      '',
      '消息链路使用 outbox/CDC，消费按 eventId 幂等，状态迁移带版本条件。后台扫描长时间停留在中间态的订单并补发事件，对订单、库存预留、券占用和支付流水做对账。',
      '',
      '如果业务规模小且所有表在同库，本地事务更可靠简单；不会为了“微服务化”主动引入 Saga。'
    ),
    {pitfallsMarkdown:md(
      '- **把补偿理解成必定成功的反向操作。** 补偿本身也需要重试和人工兜底。',
      '- **消息发出与本地事务分离。** 容易出现订单已提交但事件没发。',
      '- **状态机允许任意覆盖。** 迟到事件会把终态订单改回中间态。'
    )}
  ),
  'architecture/rate-limiting':answer(
    '峰值 20 万 QPS 要在 CDN/网关、应用和依赖入口分层限流，阈值来自容量压测和业务优先级，并配合排队与降级。',
    md(
      '边缘层先挡恶意和无效流量，按 IP、设备和账号做粗粒度限制；网关按接口、租户和活动令牌控制全局进入量；应用内部对数据库、Redis、第三方分别设并发上限。单纯在最里面限数据库连接已经太晚，前面的线程和队列会先耗尽。',
      '',
      '阈值不按日常 1 万乘一个经验系数。我会通过压测找到在目标 P99 和错误率下的安全吞吐，例如应用 8 万 QPS、库存服务 5 万、数据库写入 1 万，再按最窄瓶颈和故障余量设置。不同业务分优先级，支付查询保留配额，推荐和装饰信息先降级。',
      '',
      '算法上持续流量用令牌桶允许合理突发；昂贵资源再加信号量限制并发。被限请求返回明确错误和 `Retry-After`，客户端重试带抖动，不能同步整齐重试。',
      '',
      '上线监控通过率、拒绝率、等待时间、各依赖利用率和业务成功量，并演练单依赖降容时阈值能否动态收紧。'
    ),
    {problemAnalysisMarkdown:md(
      '限流目标不是把 QPS 卡成一个数字，而是 **在最坏流量下仍让核心交易和依赖保持可恢复状态**。所以阈值必须和延迟、队列及下游容量一起看。'
    )}
  ),
  'architecture/resilience':answer(
    '第三方大面积超时时先缩短等待、限制并发并降级业务，防止线程和连接被耗尽；恢复时半开探测、缓慢放量并对结果未知请求查单。',
    md(
      '告警后先确认是全量还是某地区/接口，观察超时率、连接池、线程池和在途请求。立即收紧超时到业务可接受值，对第三方调用设置并发隔离和熔断；非核心能力返回缓存/默认值，核心支付若结果未知则进入处理中，不能直接宣布失败并重试扣款。',
      '',
      '重试只针对幂等且短暂的网络错误，次数有限、指数退避带抖动，并受总 deadline 约束。熔断打开后快速失败，保留少量半开请求探测恢复；若对方按租户或区域故障，可以分维度熔断而不是全站关闭。',
      '',
      '恢复阶段不会瞬间放开全部流量。逐级提高并发，观察对方和自身错误率；积压任务按限速补偿。支付、下单等结果未知请求通过查单和对账收敛。',
      '',
      '事后记录对方 SLA、故障窗口、降级损失和重试放大量，并用故障注入验证超时、熔断、缓存和人工开关。'
    ),
    {pitfallsMarkdown:md(
      '- **超时后立即重试多次。** 会把对方故障放大成重试风暴。',
      '- **只加熔断，不限制在途并发。** 熔断触发前线程池可能已经耗尽。',
      '- **恢复后一次性放量。** 对方刚恢复时最容易再次被压垮。'
    )}
  ),
  'architecture/distributed-id':answer(
    '百亿级趋势递增 ID 我会优先采用 Snowflake 类方案，但要把时钟回拨、worker 分配、容量位数和故障切换设计完整。',
    md(
      '每天百亿平均约 11.6 万/秒，峰值可能高一个数量级。64 位 ID 可以拆成时间戳、workerId 和序列号；例如每节点每毫秒 4096 个序列，按峰值反推节点数与位数。ID 趋势递增有利于数据库索引，但不保证全局严格按生成时间排序。',
      '',
      'workerId 不能靠手工配置后永久相信。我会通过注册中心/租约分配并防止重复，进程只有持有有效租约才能发号。时钟回拨时，小幅可以等待，大幅则熔断该节点并告警，不能继续生成可能重复的时间段。',
      '',
      '对外不会直接暴露可推算业务量的连续 ID；需要时再加随机业务号。生成器要监控每毫秒序列耗尽、时钟偏移、worker 冲突和发号速率，并做断网、回拨、重启和跨机房演练。',
      '',
      '如果只是单库中等吞吐，自增 ID 最简单可靠；不会因为“分布式”三个字就引入复杂发号服务。'
    )
  ),
  'architecture/delayed-task':answer(
    '千万级订单超时关闭用持久化时间桶或延时消息触发，但最终以订单状态和截止时间为准，扫描补偿保证不漏。',
    md(
      '创建订单时保存 `expire_at`，同时发送带订单 ID 和到期时间的延时任务。规模很大时可以按分钟时间桶分片存储，调度器只加载临近窗口；也可使用支持延时的消息系统。不会为每个订单在单机内存创建一个 Timer。',
      '',
      '任务触发后用条件更新：',
      '',
      '```sql',
      'UPDATE orders',
      'SET status = "CLOSED"',
      'WHERE id = ? AND status = "PENDING" AND expire_at <= NOW();',
      '```',
      '',
      '重复触发受影响行数为 0，天然幂等；支付与关闭并发时由状态机和数据库条件决定赢家。关闭成功再发库存释放事件，释放也按订单 ID 幂等。',
      '',
      '延时消息可能丢、重复或晚到，所以后台按 `expire_at + status` 分片扫描超时订单做兜底，对任务触发数、成功关闭数和待支付超时数对账。积压时按到期时间优先恢复并限制数据库并发。'
    ),
    {pitfallsMarkdown:md(
      '- **单机 DelayQueue 放全部任务。** 重启丢失且无法横向扩展。',
      '- **收到任务直接关闭。** 必须重新检查状态和真实截止时间。',
      '- **只有延时消息没有扫描兜底。** 长期运行后遗漏会积累。'
    )}
  ),
  'architecture/observability':answer(
    '偶发慢请求要让日志、指标、trace 和运行时剖析通过 traceId、实例与时间关联，先定位慢在哪一段，再保留足够现场。',
    md(
      '每个入口生成或透传 traceId，span 记录服务、实例、接口、下游、数据库语句指纹、状态和耗时，不能把用户隐私和完整 SQL 参数全打进去。日志使用结构化字段，至少能按 traceId、订单号、错误码和实例查询。',
      '',
      '指标会同时覆盖 RED 和资源饱和度：请求率、错误率、P50/P95/P99；线程池队列、连接池等待、GC、CPU、磁盘；数据库扫描与锁、Kafka lag、缓存命中。告警以用户影响和持续时间为主，避免每个节点抖一下就报警。',
      '',
      'trace 采用头部采样会漏掉偶发慢请求，所以关键链路会保留错误和高延迟尾部采样，或在入口记录轻量慢请求索引。实例出现尖刺时触发一段 JFR/async-profiler，和同时间 trace 对齐。',
      '',
      '仪表盘要能从“接口 P99 上升”下钻到具体下游、实例和资源。最后用故障演练验证：制造连接池耗尽、慢 SQL 或 GC，确认告警能发现、trace 能定位、日志能解释。'
    ),
    {problemAnalysisMarkdown:md(
      '可观测性不是把三套工具装上。真正目标是让一次用户请求的 **业务标识、调用路径、资源状态和代码现场** 在同一时间轴上互相跳转。'
    ),pitfallsMarkdown:md(
      '- **日志全量打印请求与响应。** 成本、隐私和检索噪声都会失控。',
      '- **只看平均延迟。** 偶发问题正好藏在 P99 和少数实例里。',
      '- **采样完全随机。** 低频错误和长尾请求可能一条都留不下。'
    )}
  )
}
