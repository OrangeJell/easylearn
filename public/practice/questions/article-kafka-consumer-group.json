{"id":"article-kafka-consumer-group","sourceRef":"kafka/consumer-group","category":"Kafka","difficulty":"进阶","durationMinutes":2,"type":"mechanism","prompt":"订单事件有多个消费者实例，Kafka 如何分配分区？扩容后为什么可能没有效果？","answer":["订单 Topic 假设有 12 个分区、同一组 4 个实例，协调器会让每个实例大约消费 3 个分区。扩到 12 个可以提升并行度；扩到 20 个，仍有 8 个实例空闲。不同消费组互不影响，风控和通知可以各自完整消费同一批订单事件。 扩缩容、实例超时或分区变化会触发 rebalance。我们会使用 cooperative sticky 分配，减少不必要的全量撤销；消费处理过长时调大 max.poll.interval.ms 不是唯一办法，更重要的是把单批大小、处理耗时和 poll 节奏控制住。 如果扩容后没效果，我会看：分区是否已经一实例一个、lag 是否集中在某几个分区、消费者是在 CPU 忙还是等数据库/下游、单批处理是否串行。热点订单键造成分区倾斜时，加实例也救不了那个单分区。 最终容量按最慢分区规划，不只看全 Topic 平均 lag。"],"keyPoints":[],"relatedArticles":["kafka/consumer-group","kafka/message-backlog-troubleshooting","kafka/reliability","kafka/message-loss-prevention"],"followUps":[{"id":"article-kafka-consumer-group-follow-up-1","prompt":"Topic 有 12 个分区，消费者从 12 台扩到 24 台，为什么吞吐基本不变？","durationMinutes":1,"answer":["一个消费组内同一分区同一时刻只分给一个消费者，额外 12 台会空闲。先确认瓶颈是不是分区并行度；如果单分区已经达到消费或 Broker 上限，增加分区才可能提升。 扩分区还会影响 key 的分布和顺序，不能只为让机器忙起来就修改。也可能瓶颈在数据库，下游不扩容时更多消费者只会增加竞争。"],"answerHtml":"<p>一个消费组内同一分区同一时刻只分给一个消费者，额外 12 台会空闲。先确认瓶颈是不是分区并行度；如果单分区已经达到消费或 Broker 上限，增加分区才可能提升。</p>\n<p>扩分区还会影响 key 的分布和顺序，不能只为让机器忙起来就修改。也可能瓶颈在数据库，下游不扩容时更多消费者只会增加竞争。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[{"id":"article-kafka-consumer-group-follow-up-1-1","prompt":"增加分区后，同一个订单的消息为什么可能乱序？","durationMinutes":1,"answer":["原分区数变化会改变默认 key 哈希到分区的结果，新消息可能进入另一个分区，与旧分区并行消费。要求订单有序时，要规划分区扩容迁移窗口、版本化路由，或在消费端按业务版本拒绝旧事件。"],"answerHtml":"<p>原分区数变化会改变默认 key 哈希到分区的结果，新消息可能进入另一个分区，与旧分区并行消费。要求订单有序时，要规划分区扩容迁移窗口、版本化路由，或在消费端按业务版本拒绝旧事件。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}]},{"id":"article-kafka-consumer-group-follow-up-2","prompt":"扩容触发频繁重平衡，怎样减少业务暂停？","durationMinutes":1,"answer":["先处理消费者不稳定：长时间不 poll、实例频繁重启和网络抖动。使用静态成员和合作式再均衡可以减少全组停顿，但前提是消费循环及时、批处理有界。 发布时控制批次，监控 rebalance 次数、分区撤销耗时和 lag。协议优化不能掩盖慢消费。"],"answerHtml":"<p>先处理消费者不稳定：长时间不 poll、实例频繁重启和网络抖动。使用静态成员和合作式再均衡可以减少全组停顿，但前提是消费循环及时、批处理有界。</p>\n<p>发布时控制批次，监控 rebalance 次数、分区撤销耗时和 lag。协议优化不能掩盖慢消费。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}],"shortAnswer":"同一消费组内一个分区同一时刻只交给一个消费者实例，所以有效并行度上限是分区数，扩容超过分区数不会增加吞吐。","answerHtml":"<p>订单 Topic 假设有 12 个分区、同一组 4 个实例，协调器会让每个实例大约消费 3 个分区。扩到 12 个可以提升并行度；扩到 20 个，仍有 8 个实例空闲。不同消费组互不影响，风控和通知可以各自完整消费同一批订单事件。</p>\n<p>扩缩容、实例超时或分区变化会触发 rebalance。我们会使用 cooperative sticky 分配，减少不必要的全量撤销；消费处理过长时调大 <code>max.poll.interval.ms</code> 不是唯一办法，更重要的是把单批大小、处理耗时和 poll 节奏控制住。</p>\n<p>如果扩容后没效果，我会看：分区是否已经一实例一个、lag 是否集中在某几个分区、消费者是在 CPU 忙还是等数据库/下游、单批处理是否串行。热点订单键造成分区倾斜时，加实例也救不了那个单分区。</p>\n<p>最终容量按最慢分区规划，不只看全 Topic 平均 lag。</p>\n"}