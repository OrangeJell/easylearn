{"id":"article-java-basic-serialization","sourceRef":"java-basic/serialization","category":"Java基础","difficulty":"进阶","durationMinutes":2,"type":"project","prompt":"微服务之间传对象、Redis 存对象、Kafka 发消息时，你们怎么选择序列化方式？踩过什么坑？","answer":["服务间 RPC 如果双方都是内部系统，我会优先使用有 schema 的 Protobuf；需要人肉排查、跨语言接入频繁时会用 JSON，但字段、时间和金额格式要固定。Kafka 事件会保存 eventType、schemaVersion、业务主键和发生时间，因为消息可能几个月后还要重放，消费者必须能兼容旧字段。 Redis 里我不会直接塞 Java 原生序列化对象。它和类名、serialVersionUID 绑得太死，还存在安全与跨语言问题。更常见的做法是存紧凑 JSON/Protobuf，并把版本放进 key 或 value；不兼容升级时双读或换 key 前缀，让旧缓存自然过期。 上线前会做兼容测试： 新消费者读取旧消息； 旧消费者忽略新增字段； 枚举出现未知值时不直接崩溃； 大对象的序列化耗时、消息大小和 Redis 内存都在预算内。 序列化失败要进死信或隔离队列，不能跳过后继续提交位点，否则一条坏消息会被悄悄丢掉。"],"keyPoints":[],"relatedArticles":["java-basic/serialization","java-basic/reflection-annotations","java-basic/interface-abstract-class"],"followUps":[{"id":"article-java-basic-serialization-follow-up-1","prompt":"Kafka 事件新增了一个枚举值，旧消费者反序列化直接失败，怎么补救和预防？","durationMinutes":1,"answer":["先隔离失败消息，不能因为持续重试把整个分区卡死，也不能跳过后直接提交。短期给旧消费者增加未知值兜底并重新发布，再从原位点或隔离队列重放。 预防上，协议里的枚举要有 UNKNOWN，消费者对新增字段和未知值保持前向兼容；发布流程先升级消费者再让生产者发送新值，并用契约测试覆盖新旧版本组合。"],"answerHtml":"<p>先隔离失败消息，不能因为持续重试把整个分区卡死，也不能跳过后直接提交。短期给旧消费者增加未知值兜底并重新发布，再从原位点或隔离队列重放。</p>\n<p>预防上，协议里的枚举要有 <code>UNKNOWN</code>，消费者对新增字段和未知值保持前向兼容；发布流程先升级消费者再让生产者发送新值，并用契约测试覆盖新旧版本组合。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[{"id":"article-java-basic-serialization-follow-up-1-1","prompt":"失败消息进死信队列后，怎样避免修复时再次产生业务重复？","durationMinutes":1,"answer":["死信重放仍然是一次可能重复的消费，必须沿用原事件 ID 或业务幂等键。重放工具要记录操作者、范围和结果，先小批验证；消费者不能因为消息来自死信就绕过唯一约束和状态条件。"],"answerHtml":"<p>死信重放仍然是一次可能重复的消费，必须沿用原事件 ID 或业务幂等键。重放工具要记录操作者、范围和结果，先小批验证；消费者不能因为消息来自死信就绕过唯一约束和状态条件。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}]},{"id":"article-java-basic-serialization-follow-up-2","prompt":"Redis 里存的是旧版 Java 对象，新版本删了字段并改了类名，如何平滑迁移？","durationMinutes":1,"answer":["我不会要求新代码硬兼容不可控的 Java 原生字节流。更稳妥的是使用新 key 前缀或版本字段：新版本先读新格式，未命中时读旧格式并转换回填；写入只写新格式，等旧 key 自然过期。 迁移前统计旧 key 数量和 TTL，灰度观察反序列化失败与回源压力。缓存可以重建，方案重点是可回滚和不把瞬时回源打到数据库。"],"answerHtml":"<p>我不会要求新代码硬兼容不可控的 Java 原生字节流。更稳妥的是使用新 key 前缀或版本字段：新版本先读新格式，未命中时读旧格式并转换回填；写入只写新格式，等旧 key 自然过期。</p>\n<p>迁移前统计旧 key 数量和 TTL，灰度观察反序列化失败与回源压力。缓存可以重建，方案重点是可回滚和不把瞬时回源打到数据库。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}],"shortAnswer":"序列化方案按链路寿命和兼容要求选择：RPC 重类型约束，Kafka 重长期演进，Redis 重版本与可清理，不能一套格式到处复用。","answerHtml":"<p>服务间 RPC 如果双方都是内部系统，我会优先使用有 schema 的 Protobuf；需要人肉排查、跨语言接入频繁时会用 JSON，但字段、时间和金额格式要固定。Kafka 事件会保存 <code>eventType</code>、<code>schemaVersion</code>、业务主键和发生时间，因为消息可能几个月后还要重放，消费者必须能兼容旧字段。</p>\n<p>Redis 里我不会直接塞 Java 原生序列化对象。它和类名、<code>serialVersionUID</code> 绑得太死，还存在安全与跨语言问题。更常见的做法是存紧凑 JSON/Protobuf，并把版本放进 key 或 value；不兼容升级时双读或换 key 前缀，让旧缓存自然过期。</p>\n<p>上线前会做兼容测试：</p>\n<ul>\n<li>新消费者读取旧消息；</li>\n<li>旧消费者忽略新增字段；</li>\n<li>枚举出现未知值时不直接崩溃；</li>\n<li>大对象的序列化耗时、消息大小和 Redis 内存都在预算内。</li>\n</ul>\n<p>序列化失败要进死信或隔离队列，不能跳过后继续提交位点，否则一条坏消息会被悄悄丢掉。</p>\n"}