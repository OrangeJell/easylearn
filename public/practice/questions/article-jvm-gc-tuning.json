{"id":"article-jvm-gc-tuning","sourceRef":"jvm/gc-tuning","category":"JVM","difficulty":"高级","durationMinutes":1,"type":"mechanism","prompt":"接口 RT 出现周期性尖刺，确认和 GC 有关，你会如何建立基线并逐步调优？","answer":["确认尖刺和 GC 时间重合后，我会从 GC 日志与 JFR 看是哪类事件：Young GC 太频繁、Mixed 回收跟不上、Full GC、humongous allocation，还是并发标记抢 CPU。还会比较 GC 前后 Old 区，如果每次回收后基线持续上升，更像泄漏或缓存无界。 处理顺序不会从复制网上参数开始： 1. 找分配热点和异常晋升，先改代码、批量大小或缓存； 2. 检查堆是否过小或容器没有 native 余量； 3. 再根据目标调整暂停目标、并发线程或区域相关参数； 4. 用同一流量回放，并包含发布预热和峰值阶段。 每次只改一类变量，比较 P99、GC CPU、吞吐、Old 占用和 Full GC。平均暂停下降但 CPU 打满、业务吞吐变低，不算调优成功。"],"keyPoints":[],"relatedArticles":["jvm/gc-tuning","jvm/gc-guide","jvm/production-memory-sizing","jvm/stack-metaspace-errors"],"followUps":[{"id":"article-jvm-gc-tuning-follow-up-1","prompt":"GC 尖刺每十分钟出现一次，你怎么证明是晋升问题而不是定时任务本身耗 CPU？","durationMinutes":1,"answer":["把 GC 日志、应用任务时间线、CPU、分配速率和老年代变化对齐。若任务启动后分配上升、对象跨过多次 Young GC 并推动老年代，才支持晋升判断；如果无明显内存变化但 CPU 满，则是任务计算竞争。 还要看尖刺发生在 GC 前还是 GC 后。相关不等于因果，先建立时间顺序再改参数。"],"answerHtml":"<p>把 GC 日志、应用任务时间线、CPU、分配速率和老年代变化对齐。若任务启动后分配上升、对象跨过多次 Young GC 并推动老年代，才支持晋升判断；如果无明显内存变化但 CPU 满，则是任务计算竞争。</p>\n<p>还要看尖刺发生在 GC 前还是 GC 后。相关不等于因果，先建立时间顺序再改参数。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[{"id":"article-jvm-gc-tuning-follow-up-1-1","prompt":"确认是批任务制造大量中等寿命对象，你优先改 JVM 参数还是任务？","durationMinutes":1,"answer":["优先改任务的批大小、并发和对象生命周期，让它不要一次积压太多数据。参数调整只能在合理工作集基础上留余量；业务每十分钟制造超出堆预算的峰值，换收集器也只是改变崩溃方式。"],"answerHtml":"<p>优先改任务的批大小、并发和对象生命周期，让它不要一次积压太多数据。参数调整只能在合理工作集基础上留余量；业务每十分钟制造超出堆预算的峰值，换收集器也只是改变崩溃方式。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}]},{"id":"article-jvm-gc-tuning-follow-up-2","prompt":"调大堆后 Full GC 少了，但单次停顿更长，这次优化算成功吗？","durationMinutes":1,"answer":["要回到业务指标判断。若接口 P99、错误率和吞吐整体改善且停顿仍在 SLA 内，可以接受；如果最大停顿越过超时阈值，就不是成功。 我会同时比较分配速率、回收后占用和内存余量，避免大堆掩盖泄漏。调优不能只让某一个 GC 计数变好。"],"answerHtml":"<p>要回到业务指标判断。若接口 P99、错误率和吞吐整体改善且停顿仍在 SLA 内，可以接受；如果最大停顿越过超时阈值，就不是成功。</p>\n<p>我会同时比较分配速率、回收后占用和内存余量，避免大堆掩盖泄漏。调优不能只让某一个 GC 计数变好。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}],"shortAnswer":"GC 调优先建立“流量—分配—堆占用—暂停—RT”基线，先修对象生命周期，再一次只改一个参数。","answerHtml":"<p>确认尖刺和 GC 时间重合后，我会从 GC 日志与 JFR 看是哪类事件：Young GC 太频繁、Mixed 回收跟不上、Full GC、humongous allocation，还是并发标记抢 CPU。还会比较 GC 前后 Old 区，如果每次回收后基线持续上升，更像泄漏或缓存无界。</p>\n<p>处理顺序不会从复制网上参数开始：</p>\n<ol>\n<li>找分配热点和异常晋升，先改代码、批量大小或缓存；</li>\n<li>检查堆是否过小或容器没有 native 余量；</li>\n<li>再根据目标调整暂停目标、并发线程或区域相关参数；</li>\n<li>用同一流量回放，并包含发布预热和峰值阶段。</li>\n</ol>\n<p>每次只改一类变量，比较 P99、GC CPU、吞吐、Old 占用和 Full GC。平均暂停下降但 CPU 打满、业务吞吐变低，不算调优成功。</p>\n","pitfallsHtml":"<ul>\n<li><strong>只盯单次最大停顿。</strong> 频繁小停顿和并发 GC 抢 CPU 同样会推高尾延迟。</li>\n<li><strong>把堆越调越大。</strong> 容器 native 余量不足时会从 JVM OOM 变成直接 OOMKill。</li>\n<li><strong>一次改十个参数。</strong> 指标好了也不知道是哪一项生效，回滚困难。</li>\n</ul>\n"}