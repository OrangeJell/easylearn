{"id":"article-mysql-slow-sql-troubleshooting","sourceRef":"mysql/slow-sql-troubleshooting","category":"MySQL","difficulty":"高级","durationMinutes":2,"type":"mechanism","prompt":"线上出现一批慢 SQL，把数据库连接池打满了，你会如何止损、定位和验证优化结果？","answer":["先从链路拆出连接池等待和数据库执行时间。如果大量请求只是等连接，继续扩应用实例会制造更多数据库连接。止损会限制问题接口并发、暂停报表或批任务、缩小查询时间范围；对确认安全且失控的只读查询执行 KILL QUERY，必要时把非核心读切到有余量的副本。 现场至少保留 SQL 指纹、真实参数、调用量、Rows examined/Rows sent、执行计划、锁等待、活跃事务和资源曲线。扫描千万行返回十行，重点是访问路径；扫描不多但 Lock time 高，要查长事务；单条 50ms 但每秒几万次，也可能是总负载主因。 优化可能落在联合索引、游标分页、拆批、减少返回列、事务范围或数据归档。变更在真实数据分布上回放，并比较接口 P99、吞吐、扫描行数、CPU/IO 和写入成本。 把连接池调大通常只是让更多请求同时压向已经饱和的数据库，不是第一修复动作。"],"keyPoints":[],"relatedArticles":["mysql/slow-sql-troubleshooting","mysql/btree-index","mysql/sql-execution-explain","mysql/locks-deadlock"],"followUps":[{"id":"article-mysql-slow-sql-troubleshooting-follow-up-1","prompt":"连接池已经满了，是否应该立即 KILL 当前最慢的查询？","durationMinutes":1,"answer":["先确认它是可终止的只读查询还是关键写事务。终止报表查询通常风险较低；杀掉写事务会触发回滚，回滚本身可能长时间占资源，并改变业务结果。 我会按 SQL 指纹、来源、运行时间和事务状态筛选，优先限入口和暂停批任务，再 KILL QUERY 明确失控的语句。所有动作记录实例、线程和业务影响。"],"answerHtml":"<p>先确认它是可终止的只读查询还是关键写事务。终止报表查询通常风险较低；杀掉写事务会触发回滚，回滚本身可能长时间占资源，并改变业务结果。</p>\n<p>我会按 SQL 指纹、来源、运行时间和事务状态筛选，优先限入口和暂停批任务，再 <code>KILL QUERY</code> 明确失控的语句。所有动作记录实例、线程和业务影响。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[{"id":"article-mysql-slow-sql-troubleshooting-follow-up-1-1","prompt":"杀掉查询后连接池仍然恢复很慢，你会继续查什么？","durationMinutes":1,"answer":["检查事务是否仍在回滚、锁等待链是否解除、连接是否被应用泄漏，以及数据库 CPU/IO 是否仍饱和。KILL QUERY 只停当前语句，不一定结束事务；应用端也可能继续重试制造新查询。"],"answerHtml":"<p>检查事务是否仍在回滚、锁等待链是否解除、连接是否被应用泄漏，以及数据库 CPU/IO 是否仍饱和。<code>KILL QUERY</code> 只停当前语句，不一定结束事务；应用端也可能继续重试制造新查询。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}]},{"id":"article-mysql-slow-sql-troubleshooting-follow-up-2","prompt":"优化后单条 SQL 从 2 秒降到 80ms，为什么还不能直接认为事故解决？","durationMinutes":1,"answer":["要乘以调用量并看端到端等待。80ms 如果每秒两万次仍可能耗尽数据库，连接池排队、锁和网络也可能继续占据接口 P99。 我会在接近真实并发下比较吞吐、Rows examined、CPU/IO、连接等待和错误率，并确认新索引没有明显拖慢核心写入。"],"answerHtml":"<p>要乘以调用量并看端到端等待。80ms 如果每秒两万次仍可能耗尽数据库，连接池排队、锁和网络也可能继续占据接口 P99。</p>\n<p>我会在接近真实并发下比较吞吐、Rows_examined、CPU/IO、连接等待和错误率，并确认新索引没有明显拖慢核心写入。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}],"shortAnswer":"连接池被慢 SQL 打满时先限流和终止失控查询恢复服务，同时保留参数、计划和锁现场，再针对真正瓶颈优化。","answerHtml":"<p>先从链路拆出连接池等待和数据库执行时间。如果大量请求只是等连接，继续扩应用实例会制造更多数据库连接。止损会限制问题接口并发、暂停报表或批任务、缩小查询时间范围；对确认安全且失控的只读查询执行 <code>KILL QUERY</code>，必要时把非核心读切到有余量的副本。</p>\n<p>现场至少保留 SQL 指纹、真实参数、调用量、<code>Rows_examined/Rows_sent</code>、执行计划、锁等待、活跃事务和资源曲线。扫描千万行返回十行，重点是访问路径；扫描不多但 <code>Lock_time</code> 高，要查长事务；单条 50ms 但每秒几万次，也可能是总负载主因。</p>\n<p>优化可能落在联合索引、游标分页、拆批、减少返回列、事务范围或数据归档。变更在真实数据分布上回放，并比较接口 P99、吞吐、扫描行数、CPU/IO 和写入成本。</p>\n<blockquote>\n<p>把连接池调大通常只是让更多请求同时压向已经饱和的数据库，不是第一修复动作。</p>\n</blockquote>\n","pitfallsHtml":"<ul>\n<li><strong>线上直接跑高成本 EXPLAIN ANALYZE。</strong> 它会真实执行，应先评估或在副本验证。</li>\n<li><strong>只优化最慢的一条。</strong> 高频中慢 SQL 可能消耗更多总资源。</li>\n<li><strong>只看 SQL，不看锁和连接等待。</strong> 数据库执行快也可能接口等待很久。</li>\n</ul>\n"}