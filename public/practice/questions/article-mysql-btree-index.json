{"id":"article-mysql-btree-index","sourceRef":"mysql/btree-index","category":"MySQL","difficulty":"进阶","durationMinutes":2,"type":"design","prompt":"订单表已经上千万行，一个按用户和时间查询的接口越来越慢，你会怎么设计和验证索引？","answer":["建索引前，我会从慢日志和调用链拿到真实 SQL、典型参数和执行计划。 千万级只是现象，访问路径才决定这个接口快不快。 先确认慢的是所有用户，还是订单特别多的少数用户；时间究竟耗在扫描、排序、回表、深分页，还是连接池等待。 假设接口长期稳定地按租户、用户和时间范围过滤，再按时间倒序展示，我会建立下面这条联合索引： tenant id、user id 负责等值定位，created at 接住时间范围和倒序读取，id 解决同一时间多笔订单时翻页不稳定的问题。列表只取二十条，少量回表通常可以接受；我不会为了追求覆盖索引，把金额、状态等字段全部塞进去。 如果接口还在使用 LIMIT 100000, 20，仅仅加索引只解决了一半。我会改成基于上一页末尾记录的游标翻页： 上线前不会只看 EXPLAIN 里是否出现目标索引，而会用普通用户、超级用户和大时间区间分别验证扫描行数、接口 P99、数据库 IO 和建索引期间的写入延迟。 如果单个用户几年的历史订单仍然很多，我会再做按时间归档或冷热分离。 总表一千万行本身，不是立刻分库分表的理由。"],"keyPoints":[],"relatedArticles":["mysql/btree-index","mysql/slow-sql-troubleshooting","mysql/transactions-mvcc","mysql/sql-execution-explain"],"followUps":[{"id":"article-mysql-btree-index-follow-up-1","prompt":"这个接口有时会带 status 条件，你会把 status 放进 user_id 和 created_at 之间吗？","durationMinutes":1,"answer":["我不会看到 WHERE 里有 status 就塞进去，先看它是不是必传以及选择性。若状态可选，把它夹在用户和时间之间，会让不带状态的主查询无法继续利用后面的时间有序性。 通常保留主索引 (tenant id,user id,created at,id)；某个状态查询量大、扫描放大明显时，再为那条稳定访问路径增加 (tenant id,user id,status,created at,id)，并评估多一条索引对订单写入的成本。"],"answerHtml":"<p>我不会看到 WHERE 里有 <code>status</code> 就塞进去，先看它是不是必传以及选择性。若状态可选，把它夹在用户和时间之间，会让不带状态的主查询无法继续利用后面的时间有序性。</p>\n<p>通常保留主索引 <code>(tenant_id,user_id,created_at,id)</code>；某个状态查询量大、扫描放大明显时，再为那条稳定访问路径增加 <code>(tenant_id,user_id,status,created_at,id)</code>，并评估多一条索引对订单写入的成本。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[{"id":"article-mysql-btree-index-follow-up-1-1","prompt":"两个索引都能用，怎么证明新增的状态索引值得长期保留？","durationMinutes":1,"answer":["用真实状态分布和典型用户回放，比较扫描行数、回表、P99 与 buffer pool 命中，同时测订单写入 TPS、页分裂和索引体积。若它只改善低频查询却明显放大核心写入，我会改查询产品约束或走异步报表，不长期保留。"],"answerHtml":"<p>用真实状态分布和典型用户回放，比较扫描行数、回表、P99 与 buffer pool 命中，同时测订单写入 TPS、页分裂和索引体积。若它只改善低频查询却明显放大核心写入，我会改查询产品约束或走异步报表，不长期保留。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}]},{"id":"article-mysql-btree-index-follow-up-2","prompt":"联合索引和游标分页都做了，超级用户查一年订单还是慢，下一步怎么处理？","durationMinutes":1,"answer":["先确认返回需求。若页面只看最近订单，就限制时间窗口并提供归档查询；如果确实要遍历几十万条，不能继续当同步列表接口，应改成异步导出或历史库查询。 索引能减少无关扫描，但不能让返回几十万行变成常数成本。对超级用户还会做单租户并发限制，避免一个请求占住大量连接和 IO。"],"answerHtml":"<p>先确认返回需求。若页面只看最近订单，就限制时间窗口并提供归档查询；如果确实要遍历几十万条，不能继续当同步列表接口，应改成异步导出或历史库查询。</p>\n<p>索引能减少无关扫描，但不能让返回几十万行变成常数成本。对超级用户还会做单租户并发限制，避免一个请求占住大量连接和 IO。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]},{"id":"article-mysql-btree-index-follow-up-3","prompt":"订单表正在高峰写入，新增这个联合索引怎样降低上线风险？","durationMinutes":1,"answer":["先在同规模副本或影子数据上测建索引时间、临时空间和写入影响，确认数据库版本支持的在线 DDL 能力。生产按低峰执行，设置锁等待上限，持续观察复制延迟、磁盘、事务和订单写入 P99。 我会准备明确终止条件和回滚，不把“ONLINE”理解成零影响；大表 DDL 的扫描、日志和最后元数据锁仍可能影响业务。"],"answerHtml":"<p>先在同规模副本或影子数据上测建索引时间、临时空间和写入影响，确认数据库版本支持的在线 DDL 能力。生产按低峰执行，设置锁等待上限，持续观察复制延迟、磁盘、事务和订单写入 P99。</p>\n<p>我会准备明确终止条件和回滚，不把“ONLINE”理解成零影响；大表 DDL 的扫描、日志和最后元数据锁仍可能影响业务。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}],"shortAnswer":"我不会因为表到千万级就直接分库分表。先确认扫描、排序和分页成本，再让联合索引贴合用户按时间查订单的访问路径。","answerHtml":"<p>建索引前，我会从慢日志和调用链拿到真实 SQL、典型参数和执行计划。<strong>千万级只是现象，访问路径才决定这个接口快不快。</strong> 先确认慢的是所有用户，还是订单特别多的少数用户；时间究竟耗在扫描、排序、回表、深分页，还是连接池等待。</p>\n<p>假设接口长期稳定地按租户、用户和时间范围过滤，再按时间倒序展示，我会建立下面这条联合索引：</p>\n<pre><code class=\"language-sql\">CREATE INDEX idx_orders_tenant_user_time\nON orders (tenant_id, user_id, created_at DESC, id DESC);\n</code></pre>\n<p><code>tenant_id</code>、<code>user_id</code> 负责等值定位，<code>created_at</code> 接住时间范围和倒序读取，<code>id</code> 解决同一时间多笔订单时翻页不稳定的问题。列表只取二十条，少量回表通常可以接受；我不会为了追求覆盖索引，把金额、状态等字段全部塞进去。</p>\n<p>如果接口还在使用 <code>LIMIT 100000, 20</code>，仅仅加索引只解决了一半。我会改成基于上一页末尾记录的游标翻页：</p>\n<pre><code class=\"language-sql\">AND (created_at &lt; ?\n  OR (created_at = ? AND id &lt; ?))\nORDER BY created_at DESC, id DESC\nLIMIT 21\n</code></pre>\n<p>上线前不会只看 <code>EXPLAIN</code> 里是否出现目标索引，而会用普通用户、超级用户和大时间区间分别验证扫描行数、接口 P99、数据库 IO 和建索引期间的写入延迟。</p>\n<blockquote>\n<p>如果单个用户几年的历史订单仍然很多，我会再做按时间归档或冷热分离。<strong>总表一千万行本身，不是立刻分库分表的理由。</strong></p>\n</blockquote>\n","problemAnalysisHtml":"<p>这道题真正考察的不是能不能背出 B+Tree，而是能不能从线上接口反推出完整访问路径。<strong>“上千万行”只是背景，不是根因。</strong></p>\n<ul>\n<li><code>tenant_id</code>、<code>user_id</code> 是等值条件，先缩到单个用户；</li>\n<li><code>created_at</code> 承担时间过滤和倒序读取；</li>\n<li><code>id</code> 提供稳定排序，避免翻页重复或漏单。</li>\n</ul>\n<p><code>status</code> 是否进入联合索引要看它是否必传。可选状态夹在 <code>user_id</code> 与 <code>created_at</code> 中间，反而会破坏不带状态查询的范围与排序能力。</p>\n","pitfallsHtml":"<ul>\n<li><strong>看到千万级就分库分表。</strong> 正确索引通常仍能很好支撑。</li>\n<li><strong>把 WHERE 每列都建单列索引。</strong> 完整访问路径比字段清单重要。</li>\n<li><strong>制造超宽覆盖索引。</strong> 会占缓存、磁盘并放大订单写入。</li>\n<li><strong>只测第一页。</strong> 深分页、超级用户和大时间范围才是风险点。</li>\n</ul>\n"}