{"id":"article-collections-queue-deque-priorityqueue","sourceRef":"collections/queue-deque-priorityqueue","category":"Java集合","difficulty":"基础","durationMinutes":2,"type":"mechanism","prompt":"要实现任务排队、最近访问记录和按优先级调度，你会分别选择什么队列结构？","answer":["后台任务排队首先要解决背压，我会用有界 ArrayBlockingQueue 或有容量的 LinkedBlockingQueue，队列满时明确阻塞、降级还是拒绝，绝不会用无界队列把流量尖峰变成 OOM。 最近访问记录需要两端加入和淘汰，单线程或加锁环境下用 ArrayDeque；如果还要按 key 去重，通常是 HashMap + 双向链表，直接采用成熟的 LRU 缓存实现更稳妥。 PriorityQueue 适合按优先级取最小/最大元素，但它不是线程安全的，也不保证同优先级稳定顺序。我会把提交序号作为第二排序键；并发场景用 PriorityBlockingQueue，同时增加老化或配额，避免低优先级任务永远饿死。定时任务则优先 DelayQueue 或调度线程池，不拿普通优先队列轮询时间。"],"keyPoints":[],"relatedArticles":["collections/queue-deque-priorityqueue","collections/arraylist-linkedlist","collections/comparable-comparator"],"followUps":[{"id":"article-collections-queue-deque-priorityqueue-follow-up-1","prompt":"优先级队列里低优先级任务一直饿死，你会怎么改调度策略？","durationMinutes":1,"answer":["单纯按固定优先级排序会让持续到来的高优任务占满队头。我会增加等待时间权重或分级队列配额，例如每处理若干高优任务必须取一个普通任务，并给紧急任务设置独立容量。 同时监控各优先级等待时间而不只看总吞吐。调度策略的目标是业务 SLA，不是让比较器写得越复杂越好。"],"answerHtml":"<p>单纯按固定优先级排序会让持续到来的高优任务占满队头。我会增加等待时间权重或分级队列配额，例如每处理若干高优任务必须取一个普通任务，并给紧急任务设置独立容量。</p>\n<p>同时监控各优先级等待时间而不只看总吞吐。调度策略的目标是业务 SLA，不是让比较器写得越复杂越好。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[{"id":"article-collections-queue-deque-priorityqueue-follow-up-1-1","prompt":"任务入队后优先级发生变化，直接修改对象字段可以吗？","durationMinutes":1,"answer":["不可以指望堆自动重排。PriorityQueue 不知道元素内部字段变了，队头可能继续错误。应移除后重新入队，或保存不可变调度键并通过新事件更新；并发场景使用有明确并发协议的调度器。"],"answerHtml":"<p>不可以指望堆自动重排。<code>PriorityQueue</code> 不知道元素内部字段变了，队头可能继续错误。应移除后重新入队，或保存不可变调度键并通过新事件更新；并发场景使用有明确并发协议的调度器。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}]},{"id":"article-collections-queue-deque-priorityqueue-follow-up-2","prompt":"最近访问记录既要去重又要限制一百条，只用 ArrayDeque 怎么做？","durationMinutes":1,"answer":["Deque 擅长两端操作，但查找并删除中间重复项是线性的。数据只有一百条时可以接受，先删旧值再放队头；若调用很频繁或容量大，我会组合 Map 与双向链表，直接定位节点。 这就是 LRU 的核心结构。实现前也会确认是否已有成熟缓存库，避免自己维护并发和节点一致性。"],"answerHtml":"<p>Deque 擅长两端操作，但查找并删除中间重复项是线性的。数据只有一百条时可以接受，先删旧值再放队头；若调用很频繁或容量大，我会组合 Map 与双向链表，直接定位节点。</p>\n<p>这就是 LRU 的核心结构。实现前也会确认是否已有成熟缓存库，避免自己维护并发和节点一致性。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}],"shortAnswer":"任务排队、最近访问和优先级调度的约束不同，我会分别选有界阻塞队列、ArrayDeque 和带明确并发控制的优先队列。","answerHtml":"<p>后台任务排队首先要解决背压，我会用有界 <code>ArrayBlockingQueue</code> 或有容量的 <code>LinkedBlockingQueue</code>，队列满时明确阻塞、降级还是拒绝，绝不会用无界队列把流量尖峰变成 OOM。</p>\n<p>最近访问记录需要两端加入和淘汰，单线程或加锁环境下用 <code>ArrayDeque</code>；如果还要按 key 去重，通常是 <code>HashMap + 双向链表</code>，直接采用成熟的 LRU 缓存实现更稳妥。</p>\n<p><code>PriorityQueue</code> 适合按优先级取最小/最大元素，但它不是线程安全的，也不保证同优先级稳定顺序。我会把提交序号作为第二排序键；并发场景用 <code>PriorityBlockingQueue</code>，同时增加老化或配额，避免低优先级任务永远饿死。定时任务则优先 <code>DelayQueue</code> 或调度线程池，不拿普通优先队列轮询时间。</p>\n"}