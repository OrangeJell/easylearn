{"id":"article-elasticsearch-write-search-process","sourceRef":"elasticsearch/write-search-process","category":"ES","difficulty":"进阶","durationMinutes":2,"type":"mechanism","prompt":"一条商品数据写入 ES 后为什么不能立刻搜到？写入和搜索链路分别发生了什么？","answer":["协调节点根据 routing 找到主分片，主分片校验并写入内存 buffer 和 translog，再复制到副本，满足确认条件后返回。此时按 id 的实时 GET 可以从内部状态拿到新文档，但普通 search 依赖已打开的 segment reader。 refresh 会把内存中的新 segment 发布给搜索，默认周期通常约一秒，所以 ES 是近实时，不是每次写完都立即可搜。flush 则和 translog、持久化及 segment 提交相关，不能把 refresh 和 flush 混为一谈。 如果业务只在少量关键写后必须搜索到，可以使用 refresh=wait for 等待下一次 refresh；不会每条写都 refresh=true，那会制造大量小 segment、增加 merge 和查询成本。批量导入时反而会临时放大 refresh interval，完成后再恢复。 设计接口时，写后立即展示可直接使用写入返回的数据或按 ID GET，不必强迫搜索链路变成强实时。"],"keyPoints":[],"relatedArticles":["elasticsearch/write-search-process","elasticsearch/inverted-index","elasticsearch/shard-replica","elasticsearch/deep-pagination"],"followUps":[{"id":"article-elasticsearch-write-search-process-follow-up-1","prompt":"写接口返回成功后立刻搜索不到，为什么不用 refresh=true 解决所有写请求？","durationMinutes":1,"answer":["refresh=true 会让每次写等待刷新并制造很多小 segment，降低批量写吞吐、增加 merge 压力。只有极少数必须写后立刻搜索的交互才考虑 wait for，普通写接受近实时。 若按 ID 读取，可以直接 GET 实时获取，不必强迫搜索刷新。要根据读路径选方案。"],"answerHtml":"<p><code>refresh=true</code> 会让每次写等待刷新并制造很多小 segment，降低批量写吞吐、增加 merge 压力。只有极少数必须写后立刻搜索的交互才考虑 <code>wait_for</code>，普通写接受近实时。</p>\n<p>若按 ID 读取，可以直接 GET 实时获取，不必强迫搜索刷新。要根据读路径选方案。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[{"id":"article-elasticsearch-write-search-process-follow-up-1-1","prompt":"使用 refresh=wait_for 时流量高峰会有什么表现？","durationMinutes":1,"answer":["请求会等下一次 refresh，延迟受 refresh interval 影响；等待监听器过多时也可能触发额外刷新或压力。应限制到关键场景，并监控 refresh 时间、segment 与写入 P99，不把它加到所有 bulk。"],"answerHtml":"<p>请求会等下一次 refresh，延迟受 refresh interval 影响；等待监听器过多时也可能触发额外刷新或压力。应限制到关键场景，并监控 refresh 时间、segment 与写入 P99，不把它加到所有 bulk。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}]},{"id":"article-elasticsearch-write-search-process-follow-up-2","prompt":"Bulk 部分成功、部分失败，重试时怎样避免覆盖更新版本？","durationMinutes":1,"answer":["逐条解析 bulk response，只重试可重试的失败项，保留原业务版本和文档 ID。写入使用 external version 或带序列号条件，旧事件不能覆盖新文档。 整批无脑重发既放大流量，也可能让乱序更新回退。不可重试的 Mapping 错误进入隔离并告警。"],"answerHtml":"<p>逐条解析 bulk response，只重试可重试的失败项，保留原业务版本和文档 ID。写入使用 external version 或带序列号条件，旧事件不能覆盖新文档。</p>\n<p>整批无脑重发既放大流量，也可能让乱序更新回退。不可重试的 Mapping 错误进入隔离并告警。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}],"shortAnswer":"写入成功后不能立刻搜索，通常是因为文档已进 translog 和内存缓冲，但还没 refresh 成新的可搜索 segment。","answerHtml":"<p>协调节点根据 routing 找到主分片，主分片校验并写入内存 buffer 和 translog，再复制到副本，满足确认条件后返回。此时按 <code>_id</code> 的实时 GET 可以从内部状态拿到新文档，但普通 search 依赖已打开的 segment reader。</p>\n<p>refresh 会把内存中的新 segment 发布给搜索，默认周期通常约一秒，所以 ES 是近实时，不是每次写完都立即可搜。flush 则和 translog、持久化及 segment 提交相关，不能把 refresh 和 flush 混为一谈。</p>\n<p>如果业务只在少量关键写后必须搜索到，可以使用 <code>refresh=wait_for</code> 等待下一次 refresh；不会每条写都 <code>refresh=true</code>，那会制造大量小 segment、增加 merge 和查询成本。批量导入时反而会临时放大 refresh interval，完成后再恢复。</p>\n<p>设计接口时，写后立即展示可直接使用写入返回的数据或按 ID GET，不必强迫搜索链路变成强实时。</p>\n"}