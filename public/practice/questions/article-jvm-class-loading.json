{"id":"article-jvm-class-loading","sourceRef":"jvm/class-loading","category":"JVM","difficulty":"进阶","durationMinutes":2,"type":"project","prompt":"项目里一个类从写完代码到真正可以 new 出对象，中间经历了什么？哪些阶段容易出问题？","answer":["第一次主动使用类时，类加载器找到字节码并创建 Class；验证阶段检查格式和类型安全；准备阶段给静态字段分配空间并设默认值；解析把符号引用变成直接引用；初始化才执行静态字段赋值和 。对象 new 还会继续分配内存、设零值、执行构造器。 这套流程对排障有直接帮助。ClassNotFoundException 是主动加载没找到类，NoClassDefFoundError 可能是依赖缺失，也可能这个类之前初始化失败；静态块访问配置或远程资源失败后，后续使用只会继续报类无法初始化。 我会用 Xlog:class+load=info 或 JFR 看类由哪个加载器、从哪个 jar 加载，检查依赖树和第一条初始化异常。静态初始化只做确定、轻量的事情，网络调用和复杂业务放到可重试、可观测的启动组件里。"],"keyPoints":[],"relatedArticles":["jvm/class-loading","jvm/runtime-data-area","jvm/classloader-delegation"],"followUps":[{"id":"article-jvm-class-loading-follow-up-1","prompt":"类初始化时抛了异常，后续请求为什么一直得到 NoClassDefFoundError，而不是再次初始化？","durationMinutes":1,"answer":["同一个类加载器里，类初始化失败后会被标记为错误状态，后续主动使用不会重新执行静态初始化，而是抛出 NoClassDefFoundError。最初那次 ExceptionInInitializerError 的 cause 才是根因。 我会保留第一次失败日志，检查静态代码块是否访问配置、网络或依赖顺序。恢复通常需要修正原因并重启或更换类加载器，不能靠接口重试。"],"answerHtml":"<p>同一个类加载器里，类初始化失败后会被标记为错误状态，后续主动使用不会重新执行静态初始化，而是抛出 <code>NoClassDefFoundError</code>。最初那次 <code>ExceptionInInitializerError</code> 的 cause 才是根因。</p>\n<p>我会保留第一次失败日志，检查静态代码块是否访问配置、网络或依赖顺序。恢复通常需要修正原因并重启或更换类加载器，不能靠接口重试。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[{"id":"article-jvm-class-loading-follow-up-1-1","prompt":"把远程配置读取放进 static 初始化有什么生产风险？","durationMinutes":1,"answer":["它把类能否加载和外部网络绑定，一次超时就可能让类永久进入错误状态，还阻塞所有首次使用线程。远程资源应在可重试、有超时和降级的生命周期组件中初始化，静态初始化只做确定、快速的本地工作。"],"answerHtml":"<p>它把类能否加载和外部网络绑定，一次超时就可能让类永久进入错误状态，还阻塞所有首次使用线程。远程资源应在可重试、有超时和降级的生命周期组件中初始化，静态初始化只做确定、快速的本地工作。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}]}],"shortAnswer":"类从字节码到可用要经历加载、验证、准备、解析和初始化；项目事故多发生在依赖不一致、静态初始化和类加载器边界。","answerHtml":"<p>第一次主动使用类时，类加载器找到字节码并创建 <code>Class</code>；验证阶段检查格式和类型安全；准备阶段给静态字段分配空间并设默认值；解析把符号引用变成直接引用；初始化才执行静态字段赋值和 <code>&lt;clinit&gt;</code>。对象 <code>new</code> 还会继续分配内存、设零值、执行构造器。</p>\n<p>这套流程对排障有直接帮助。<code>ClassNotFoundException</code> 是主动加载没找到类，<code>NoClassDefFoundError</code> 可能是依赖缺失，也可能这个类之前初始化失败；静态块访问配置或远程资源失败后，后续使用只会继续报类无法初始化。</p>\n<p>我会用 <code>-Xlog:class+load=info</code> 或 JFR 看类由哪个加载器、从哪个 jar 加载，检查依赖树和第一条初始化异常。静态初始化只做确定、轻量的事情，网络调用和复杂业务放到可重试、可观测的启动组件里。</p>\n"}