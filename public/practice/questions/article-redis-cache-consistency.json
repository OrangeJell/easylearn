{"id":"article-redis-cache-consistency","sourceRef":"redis/cache-consistency","category":"Redis","difficulty":"进阶","durationMinutes":1,"type":"project","prompt":"商品价格更新后，用户偶尔还能看到旧缓存，你们如何控制缓存与数据库的不一致窗口？","answer":["我通常使用 cache aside：读请求 miss 后查库并写缓存；价格更新在数据库事务提交成功后删除缓存，而不是直接更新缓存。下一次读取会回源拿新值，避免同时维护两份复杂状态。 仍有一个竞态：旧读请求先查到旧价格，更新事务提交并删缓存后，旧读又把旧值写回。会通过较短 TTL、带版本的缓存值，或订阅 binlog/业务事件再次失效来收敛；高价值价格读取可以比较版本，拒绝覆盖更新版本。 删除缓存失败不能只记日志。我会把变更事件放可靠消息/outbox，消费者按商品 ID 幂等删除并重试，监控消息延迟和失败数。 业务还要定义允许窗口：商品列表允许几秒旧值，结算金额必须重新从权威服务校验。缓存一致性不是所有页面都追求“瞬时强一致”，而是把错误价格影响控制在明确边界。"],"keyPoints":[],"relatedArticles":["redis/cache-consistency","redis/data-structures-use-cases","architecture/cache-consistency","redis/hot-key-big-key"],"followUps":[{"id":"article-redis-cache-consistency-follow-up-1","prompt":"更新数据库成功后删除缓存失败，你会在请求里一直重试到成功吗？","durationMinutes":1,"answer":["不会无限占住请求。先把数据库提交作为权威结果，同时通过事务消息、outbox 或可靠重试任务记录失效事件；请求内可以做一次短重试，之后交异步并告警。 缓存仍有 TTL 作为最终兜底。关键是失效任务不能只存在进程内，否则服务重启后这个旧值会留到过期。"],"answerHtml":"<p>不会无限占住请求。先把数据库提交作为权威结果，同时通过事务消息、outbox 或可靠重试任务记录失效事件；请求内可以做一次短重试，之后交异步并告警。</p>\n<p>缓存仍有 TTL 作为最终兜底。关键是失效任务不能只存在进程内，否则服务重启后这个旧值会留到过期。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[{"id":"article-redis-cache-consistency-follow-up-1-1","prompt":"异步失效消息乱序，旧更新的删除晚到会有问题吗？","durationMinutes":1,"answer":["如果消息动作只是删除，晚到通常只会造成一次额外回源，不会把旧值写回；若消费者重建并写缓存，就必须带数据版本，只允许更高版本覆盖。设计时我更偏向消息只失效，由读请求回填当前数据库值。"],"answerHtml":"<p>如果消息动作只是删除，晚到通常只会造成一次额外回源，不会把旧值写回；若消费者重建并写缓存，就必须带数据版本，只允许更高版本覆盖。设计时我更偏向消息只失效，由读请求回填当前数据库值。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}]},{"id":"article-redis-cache-consistency-follow-up-2","prompt":"读请求查库后准备回填缓存，恰好另一个请求更新了数据库并删除缓存，旧值会不会被写回来？","durationMinutes":1,"answer":["会出现这个竞态。能接受短窗口时依靠较短 TTL；要求更严时，回填值带数据库版本，并用 Lua 只在缓存版本不更新时写入，或用逻辑过期与单线程刷新。 双删加固定 sleep 很难覆盖所有查询耗时。我会用版本和监控把窗口变成可证明的协议。"],"answerHtml":"<p>会出现这个竞态。能接受短窗口时依靠较短 TTL；要求更严时，回填值带数据库版本，并用 Lua 只在缓存版本不更新时写入，或用逻辑过期与单线程刷新。</p>\n<p>双删加固定 sleep 很难覆盖所有查询耗时。我会用版本和监控把窗口变成可证明的协议。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}],"shortAnswer":"商品价格以数据库为准，更新采用先提交数据库再删除缓存，并用消息或延迟重试把删除失败的不一致窗口收敛。","answerHtml":"<p>我通常使用 cache-aside：读请求 miss 后查库并写缓存；价格更新在数据库事务提交成功后删除缓存，而不是直接更新缓存。下一次读取会回源拿新值，避免同时维护两份复杂状态。</p>\n<p>仍有一个竞态：旧读请求先查到旧价格，更新事务提交并删缓存后，旧读又把旧值写回。会通过较短 TTL、带版本的缓存值，或订阅 binlog/业务事件再次失效来收敛；高价值价格读取可以比较版本，拒绝覆盖更新版本。</p>\n<p>删除缓存失败不能只记日志。我会把变更事件放可靠消息/outbox，消费者按商品 ID 幂等删除并重试，监控消息延迟和失败数。</p>\n<p>业务还要定义允许窗口：商品列表允许几秒旧值，结算金额必须重新从权威服务校验。缓存一致性不是所有页面都追求“瞬时强一致”，而是把错误价格影响控制在明确边界。</p>\n","problemAnalysisHtml":"<p>一致性的核心不是“先删还是后删”口令，而是找出并发读写的时序、失败后的补偿通道，以及最终哪个环节必须再次校验权威数据。</p>\n"}