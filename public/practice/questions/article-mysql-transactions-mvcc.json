{"id":"article-mysql-transactions-mvcc","sourceRef":"mysql/transactions-mvcc","category":"MySQL","difficulty":"进阶","durationMinutes":2,"type":"project","prompt":"订单状态更新和用户查询并发发生时，你们怎么选择隔离级别？MVCC 在里面起什么作用？","answer":["大多数订单系统我会从 READ COMMITTED 或 REPEATABLE READ 中选。列表查询接受同一事务内再次查询看到新提交数据时，RC 更直观；对账或一次事务内需要稳定快照时用 RR。选择不是背默认值，而是把“同一请求能否看到别人刚提交的状态”写清楚。 MVCC 让普通 SELECT 通过 Read View 和版本链读取快照，不和更新行直接互斥，但它不会自动防止两个请求同时把 PAID 改成不同状态。更新会写成带前置状态或版本的条件语句： 受影响行为 0 就说明状态已变化，业务决定返回幂等成功、冲突还是重试。确实需要“读后立刻改且中间不能变”时才用 SELECT ... FOR UPDATE，并把事务保持很短。 还会关注长事务：它会让旧版本无法清理，Undo 增长并拖慢查询。隔离级别不能替代正确的状态机和幂等。"],"keyPoints":[],"relatedArticles":["mysql/transactions-mvcc","mysql/btree-index","mysql/locks-deadlock","mysql/redo-undo-binlog"],"followUps":[{"id":"article-mysql-transactions-mvcc-follow-up-1","prompt":"两个请求都读到订单是 PAID，一个退款、一个发货，MVCC 能避免状态走错吗？","durationMinutes":1,"answer":["不能。两边的快照读都可能合法看到 PAID，MVCC 只是让读不阻塞写。真正约束状态迁移的是条件更新，例如退款要求当前仍为 PAID，发货要求当前为 PAID 且未退款，并带版本号。 更新影响 0 行后重新读取当前状态，决定幂等成功还是冲突。不能把隔离级别当作订单状态机。"],"answerHtml":"<p>不能。两边的快照读都可能合法看到 <code>PAID</code>，MVCC 只是让读不阻塞写。真正约束状态迁移的是条件更新，例如退款要求当前仍为 <code>PAID</code>，发货要求当前为 <code>PAID</code> 且未退款，并带版本号。</p>\n<p>更新影响 0 行后重新读取当前状态，决定幂等成功还是冲突。不能把隔离级别当作订单状态机。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[{"id":"article-mysql-transactions-mvcc-follow-up-1-1","prompt":"把隔离级别升到 SERIALIZABLE 能不能省掉状态条件？","durationMinutes":1,"answer":["不能省。更高隔离会增加锁和冲突，但业务允许哪些状态迁移仍需要显式表达；多服务和消息重试也不在一个数据库事务里。用 SERIALIZABLE 替代状态条件，成本更高且边界仍不完整。"],"answerHtml":"<p>不能省。更高隔离会增加锁和冲突，但业务允许哪些状态迁移仍需要显式表达；多服务和消息重试也不在一个数据库事务里。用 SERIALIZABLE 替代状态条件，成本更高且边界仍不完整。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}]},{"id":"article-mysql-transactions-mvcc-follow-up-2","prompt":"线上 Undo 持续增长，你怎么证明是长事务导致的？","durationMinutes":1,"answer":["我会查活跃事务的开始时间、隔离级别和当前 SQL，找到长时间不提交的读写事务，再看 history list length 与其时间是否对应。常见来源是分页导出、事务里远程调用或连接归还前未结束事务。 先终止确认安全的异常任务，再把长查询改成分批且不持有超长快照，缩短事务边界；同时监控 purge 是否逐步追上。"],"answerHtml":"<p>我会查活跃事务的开始时间、隔离级别和当前 SQL，找到长时间不提交的读写事务，再看 history list length 与其时间是否对应。常见来源是分页导出、事务里远程调用或连接归还前未结束事务。</p>\n<p>先终止确认安全的异常任务，再把长查询改成分批且不持有超长快照，缩短事务边界；同时监控 purge 是否逐步追上。</p>\n","keyPoints":[],"relatedArticles":[],"followUps":[]}],"shortAnswer":"订单查询通常用 MVCC 做非阻塞快照读，状态更新用条件更新或显式锁保证并发正确；隔离级别按业务一致性选择。","answerHtml":"<p>大多数订单系统我会从 <code>READ COMMITTED</code> 或 <code>REPEATABLE READ</code> 中选。列表查询接受同一事务内再次查询看到新提交数据时，RC 更直观；对账或一次事务内需要稳定快照时用 RR。选择不是背默认值，而是把“同一请求能否看到别人刚提交的状态”写清楚。</p>\n<p>MVCC 让普通 <code>SELECT</code> 通过 Read View 和版本链读取快照，不和更新行直接互斥，但它不会自动防止两个请求同时把 <code>PAID</code> 改成不同状态。更新会写成带前置状态或版本的条件语句：</p>\n<pre><code class=\"language-sql\">UPDATE orders\nSET status = ?, version = version + 1\nWHERE id = ? AND status = ? AND version = ?;\n</code></pre>\n<p>受影响行为 0 就说明状态已变化，业务决定返回幂等成功、冲突还是重试。确实需要“读后立刻改且中间不能变”时才用 <code>SELECT ... FOR UPDATE</code>，并把事务保持很短。</p>\n<p>还会关注长事务：它会让旧版本无法清理，Undo 增长并拖慢查询。隔离级别不能替代正确的状态机和幂等。</p>\n","pitfallsHtml":"<ul>\n<li><strong>认为 MVCC 能解决丢更新。</strong> 快照读不等于写入冲突检测。</li>\n<li><strong>所有查询都加 FOR UPDATE。</strong> 会把本可并发的订单查询串行化。</li>\n<li><strong>事务里调用远程服务。</strong> 锁和旧版本会被无谓地长时间保留。</li>\n</ul>\n"}