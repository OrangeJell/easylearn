const articles={
 string:{category:'Java 基础面试题',title:'String 为什么被 final 修饰且不可变？',sections:[['面试考察点',`<ul><li><b>语言基础：</b>是否理解 <code>final</code> 修饰类、引用和字段时的区别。</li><li><b>设计思想：</b>能否从安全、缓存、线程安全和性能角度解释不可变性。</li><li><b>源码理解：</b>知道不同 JDK 版本中 String 的内部存储结构存在差异。</li></ul>`],['核心答案',`<div class="answer"><b>一句话回答</b><br>String 类被 final 修饰是为了禁止继承和篡改其行为；String 对象不可变，是因为内部数据不对外暴露，且所有修改操作都会创建新对象。</div><p>需要区分两个概念：<code>final class String</code> 表示 String 不能被继承；字符串不可变则来自类的整体封装设计，并不只是因为某个字段加了 final。</p>`],['为什么设计成不可变对象？',`<h3>1. 字符串常量池可以安全复用</h3><p>多个引用可能指向常量池中的同一个字符串。如果字符串可以被修改，一个引用的操作会影响所有共享者，常量池也就失去了意义。</p><h3>2. 适合作为 HashMap 的键</h3><p>String 会缓存哈希值。对象内容不可变意味着哈希值稳定，放入 HashMap 后不会因为内容变化而找不到原来的桶。</p><h3>3. 天然线程安全</h3><p>不可变对象没有写操作，多个线程可以安全共享同一个 String 实例，不需要额外加锁。</p><h3>4. 提升安全性</h3><p>类名、文件路径、网络地址和数据库连接参数通常使用字符串传递。不可变可以防止参数在校验后被偷偷修改。</p>`],['final 到底保证了什么？',`<p>final 引用保证的是引用不能重新指向另一个对象，并不自动保证对象内部状态不可变。</p><pre>final List&lt;String&gt; list = new ArrayList&lt;&gt;();\nlist.add("Java");  // 合法：对象内部仍然可以修改\n// list = new ArrayList&lt;&gt;();  // 非法：引用不能重新赋值</pre><p>因此 String 的不可变性来自：类不能继承、内部字段私有、没有暴露修改方法，并且修改操作返回新对象。</p>`],['String、StringBuilder 与 StringBuffer',`<table><tr><th>类型</th><th>是否可变</th><th>线程安全</th><th>典型场景</th></tr><tr><td>String</td><td>不可变</td><td>是</td><td>少量拼接、常量、Map 键</td></tr><tr><td>StringBuilder</td><td>可变</td><td>否</td><td>单线程大量拼接</td></tr><tr><td>StringBuffer</td><td>可变</td><td>是</td><td>多线程共享拼接对象</td></tr></table>`],['高频追问',`<p><b>问：String 真的是绝对不可变吗？</b><br>正常 Java API 语义下不可变。反射、Unsafe 等特殊手段可能突破封装，但这不属于常规业务使用。</p><p><b>问：字符串拼接一定产生新对象吗？</b><br>编译期常量可能被直接折叠；运行期拼接通常会借助 StringBuilder，最终生成新的 String。</p>`],['总结',`<p>回答这道题时，先区分“final 类”和“不可变对象”，再从常量池、哈希稳定、线程安全、安全性四个方面解释设计收益，最后补充 StringBuilder 的适用场景。</p>`]]},
 hashmap:{category:'Java 集合面试题',title:'HashMap 的底层原理是什么？',sections:[['核心答案',`<div class="answer"><b>一句话回答</b><br>JDK 8 的 HashMap 由数组、链表和红黑树组成，通过扰动后的 hash 定位桶，在冲突严重时将链表树化。</div>`],['数据结构',`<p>数组负责快速定位，链表与红黑树解决哈希冲突。默认负载因子为 0.75，在容量与冲突概率之间折中。</p>`],['扩容机制',`<p>元素数量超过阈值后，容量通常扩大为原来的两倍。JDK 8 利用原 hash 的高位判断元素留在原位置还是迁移到“原位置 + 旧容量”。</p>`],['高频追问',`<p>HashMap 不是线程安全容器，并发写入应根据场景选择 ConcurrentHashMap 或外部同步。</p>`]]},
 pool:{category:'Java 并发面试题',title:'Java 线程池有几种创建方式？',sections:[['核心答案',`<div class="answer"><b>一句话回答</b><br>生产环境优先手动构造 ThreadPoolExecutor，明确线程数量、队列容量和拒绝策略。</div>`],['执行流程',`<p>核心线程 → 工作队列 → 非核心线程 → 拒绝策略。这个顺序决定了队列容量会直接影响扩容时机。</p>`],['七个参数',`<p><code>corePoolSize</code>、<code>maximumPoolSize</code>、存活时间、时间单位、工作队列、线程工厂和拒绝策略共同定义线程池边界。</p>`],['高频追问',`<p>CPU 密集型线程数通常接近 CPU 核数；IO 密集型可以适当提高，但最终应以压测和监控数据为准。</p>`]]},
 jvm:{category:'JVM 面试题',title:'JVM 运行时数据区有哪些？',sections:[['核心答案',`<div class="answer"><b>一句话回答</b><br>主要包括程序计数器、虚拟机栈、本地方法栈、堆和方法区。</div>`],['线程私有区域',`<p>程序计数器、虚拟机栈和本地方法栈随线程创建和销毁。</p>`],['线程共享区域',`<p>堆与方法区由多个线程共享，是垃圾回收重点关注的区域。</p>`]]}
};
const groups=[['Java 基础面试题',[['string','String 为什么不可变？'],['string','== 和 equals 的区别？'],['string','接口和抽象类的区别？']]],['Java 集合面试题',[['hashmap','HashMap 的底层原理？'],['hashmap','ArrayList 如何扩容？'],['hashmap','ConcurrentHashMap 原理？']]],['Java 并发面试题',[['pool','线程池有几种创建方式？'],['pool','volatile 能保证原子性吗？'],['pool','synchronized 如何实现？']]],['JVM 面试题',[['jvm','JVM 运行时数据区？'],['jvm','类加载过程是什么？'],['jvm','常见 GC 收集器？']]],['Spring 面试题',[['string','IOC 和 AOP 是什么？'],['string','Bean 生命周期？'],['string','事务失效场景？']]],['MySQL 面试题',[['hashmap','B+Tree 索引原理？'],['hashmap','MVCC 如何实现？'],['hashmap','索引失效场景？']]]];
let current='string';function renderCatalog(filter=''){document.getElementById('catalog').innerHTML=groups.map((g,gi)=>{const items=g[1].filter(x=>x[1].toLowerCase().includes(filter.toLowerCase())||g[0].includes(filter));if(!items.length)return'';return `<section class="group ${gi===0||items.some(x=>x[0]===current)?'open':''}"><button class="group-head">${g[0]}<span>⌄</span></button><div class="group-list">${items.map(x=>`<button class="topic ${x[0]===current?'active':''}" data-id="${x[0]}">${x[1]}</button>`).join('')}</div></section>`}).join('');document.querySelectorAll('.group-head').forEach(x=>x.onclick=()=>x.parentElement.classList.toggle('open'));document.querySelectorAll('.topic').forEach(x=>x.onclick=()=>renderArticle(x.dataset.id))}
function renderArticle(id){current=id;const a=articles[id]||articles.string;document.getElementById('category').textContent=a.category;document.getElementById('crumb-title').textContent=a.title;document.getElementById('title').textContent=a.title;document.getElementById('body').innerHTML=a.sections.map((s,i)=>`<section><h2 id="s${i}">${s[0]}</h2>${s[1]}</section>`).join('');document.getElementById('outline').innerHTML=a.sections.map((s,i)=>`<a href="#s${i}" class="${i===0?'active':''}">${s[0]}</a>`).join('');renderCatalog(document.getElementById('search').value);window.scrollTo({top:0,behavior:'smooth'})}document.getElementById('search').oninput=e=>renderCatalog(e.target.value);renderArticle('string');
const polishStyle=document.createElement('link');polishStyle.rel='stylesheet';polishStyle.href='polish.css';document.head.appendChild(polishStyle);
const topbar=document.querySelector('.top'),articlePanel=document.querySelector('.article'),themeButton=document.querySelector('.theme');window.addEventListener('scroll',()=>topbar.classList.toggle('scrolled',scrollY>12),{passive:true});themeButton.addEventListener('click',()=>{document.body.classList.toggle('dark');themeButton.textContent=document.body.classList.contains('dark')?'☀':'◐'});new MutationObserver(()=>{articlePanel.classList.remove('switching');void articlePanel.offsetWidth;articlePanel.classList.add('switching');setTimeout(()=>articlePanel.classList.remove('switching'),420)}).observe(document.getElementById('body'),{childList:true});const updateOutline=()=>{const heads=[...document.querySelectorAll('.article-body h2')];if(!heads.length)return;let active=heads[0].id;heads.forEach(h=>{if(h.getBoundingClientRect().top<150)active=h.id});document.querySelectorAll('.outline a').forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+active))};window.addEventListener('scroll',updateOutline,{passive:true});document.addEventListener('click',e=>{const t=e.target.closest('.topic');if(!t)return;const r=document.createElement('i');r.style.cssText='position:absolute;inset:0;background:rgba(231,102,54,.09);pointer-events:none;animation:fadeRipple .45s forwards';t.style.position='relative';t.style.overflow='hidden';t.appendChild(r);setTimeout(()=>r.remove(),460)});const motion=document.createElement('style');motion.textContent='@keyframes fadeRipple{from{opacity:1}to{opacity:0}}';document.head.appendChild(motion);
